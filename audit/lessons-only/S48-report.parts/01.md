**Evidence:** Source explicitly maps missing cost to error-slice evaluation.  
**Pedagogical/technical impact:** Learners are taught a remediation token unrelated to the cause of uncertainty.  
**Attack:** Remove only `reindex_cost_pen`; error slices cannot recover that value.  
**Defense:** The branch may be intended as a generic evaluation review step.  
**Verdict:** CONFIRMED_FINDING P2.  
**Improved version:** Route missing cost to `ESTIMATE_REINDEX_COST`/`RESTORE_COST_EVIDENCE`; keep error slices for measured retrieval-regression diagnosis.  
**Reasoning:** Recovery action should causally address the missing/breached evidence.

## S48-F11 — P1 — Two early figures teach later constructs rather than the current embedding/evaluation construct.

**Location:** T1-A and T1-B figures  
**Category:** visual-alignment/accessibility  
**Evidence:** T1-A uses `S48-chunk-provenance` (document→section→chunk hash, a T2-A concept). T1-B uses `S48-evidence-allowlist` (ACL/citation graph, T2-B/T3-B).  
**Pedagogical/technical impact:** Visuals introduce future concepts instead of reducing load for similarity and holdout evaluation.  
**Attack:** Compare each figure's nodes/layers with its current subtopic outcome.  
**Defense:** The figures preview the larger RAG chain.  
**Verdict:** CONFIRMED_FINDING because consecutive early figures are locally misaligned.  
**Improved version:** T1-A: metric/normalization/vector-contract visual. T1-B: baseline/candidate holdout table. Move provenance and allowlist figures to T2-A/T2-B/T3-B.  
**Reasoning:** Representations should expose the deep structure of the current task.

## S48-F12 — P2 — The lesson presents semantic-unit chunking as the productive rule and fixed slicing as categorically inferior, without framing chunk size/strategy as an empirical retrieval trade-off.

**Location:** opening theory + T2-A prose  
**Category:** T2-A/chunking-absolute  
**Evidence:** Text says productive chunking follows sections/clauses/titles, 'no rebanadas ciegas'; current retrieval systems expose configurable/static chunking and fixed token windows remain a legitimate baseline.  
**Pedagogical/technical impact:** Learners may skip benchmarking chunk strategy against recall, citation resolution, context cost and document structure.  
**Attack:** Consider well-structured vs unstructured docs and token-window baselines; no single strategy dominates every corpus.  
**Defense:** Breaking a sentence at arbitrary character counts is indeed a bad default for citable text.  
**Verdict:** CONFIRMED_FINDING P2 on categorical framing, not on recommending semantic boundaries.  
**Improved version:** Teach semantic boundaries as a strong candidate strategy, compare against a fixed-token baseline, and choose via retrieval/citation eval.  
**Reasoning:** Chunking is a design variable to evaluate, not an axiom.

## S48-F13 — P1 — The prose explicitly warns that dedup hash must include `doc_id`, section and version; the code hashes only text and omits `source_version`.

**Location:** T2-A prose vs `chunk_by_section`  
**Category:** T2-A/theory-code-contradiction  
**Evidence:** `digest = sha256(text)` and output has id/text/hash/doc_id/section only. The callout nevertheless says hash + source_version preserve provenance.  
**Pedagogical/technical impact:** Identical text from different docs/versions/permissions gets the same identity—the exact anti-pattern the paragraph warns against.  
**Attack:** RED: same text under docA/v1 and docB/v9 generates identical 12-hex hash.  
**Defense:** The hash could be intended as content fingerprint while provenance remains separate metadata.  
**Verdict:** CONFIRMED_FINDING because the source explicitly defines the hash as provenance-aware dedup identity.  
**Improved version:** Separate `content_hash = sha256(text)` from `chunk_id/dedup_key = sha256(doc_id|section|source_version|content_hash)` and persist `source_version`.  
**Reasoning:** Content equality and provenance identity are different relations.

## S48-F14 — P2 — A SHA-256 digest truncated to 12 hex characters is presented as the stable dedup identity without qualification.

**Location:** T2-A code/I Do  
**Category:** T2-A/hash-strength  
**Evidence:** Code uses `.hexdigest()[:12]`.  
**Pedagogical/technical impact:** A display-friendly 48-bit prefix can be mistaken for a collision-resistant identity in larger corpora.  
**Attack:** Scale the lesson from two chunks to production-scale ingestion; collision risk and identity ambiguity grow.  
**Defense:** For the tiny synthetic fixture the chance of accidental collision is negligible.  
**Verdict:** CONFIRMED_FINDING P2.  
**Improved version:** Use full SHA-256 for identity; optionally display the first 12 characters in UI while storing/comparing the full digest.  
**Reasoning:** A shortened display label should not silently become the integrity key.

## S48-F15 — P1 — Practice does not repair the provenance-aware hash defect; it largely checks uniqueness of already-supplied hashes and prints provenance/version strings.

**Location:** T2-A I Do/We Do  
**Category:** T2-A/practice-evidence  
**Evidence:** I Do computes text-only hash and prints `source` separately; exercises can use arbitrary hash values such as `a`/`b` instead of recomputing provenance-bound identities.  
**Pedagogical/technical impact:** Learners can pass T2-A without implementing the lesson's most security-relevant dedup invariant.  
**Attack:** Supply two arbitrary distinct hashes even though their underlying provenance/content relation is wrong.  
**Defense:** Exercises focus on diagnosing duplicates and missing metadata.  
**Verdict:** CONFIRMED_FINDING.  
**Improved version:** Make exercises construct the dedup key from raw chunk metadata and include a same-text/different-ACL/version adversarial pair.  
**Reasoning:** Practice must exercise the causal operation, not merely inspect its claimed output.

## S48-F16 — P1 — Deletion cache invalidation and provenance are claimed but not executed.

**Location:** T2-B theory/code/I Do/We Do  
**Category:** T2-B/deletion-provenance  
**Evidence:** Theory says tombstone invalidates index/cache. Code only skips `deleted` and prints a tombstone ID; I Do prints a provenance string; exercises accept `cache_invalidated=True` and `provenance.startswith('doc-')`.  
**Pedagogical/technical impact:** A stale cache/vector index can still be conceptually 'proven safe' by flags with no invalidation artifact.  
**Attack:** Set arbitrary `provenance='doc-fake-anything'` and `cache_invalidated=True`; predicate passes.  
**Defense:** The stdlib fixture intentionally avoids implementing a vector store/cache.  
**Verdict:** CONFIRMED_FINDING because the lesson states these as demonstrated evidence.  
**Improved version:** Use a tiny local index/cache dict: retrieve once, apply tombstone, invalidate keyed cache/index entry, re-retrieve and assert zero; bind provenance to doc/version/chunk manifest.  
**Reasoning:** Deletion is a state transition whose evidence is changed retrieval behavior.

## S48-F17 — P2 — The lesson says ACL is pre-ranking, but the executable demo never composes authorization filtering with a ranking function.

**Location:** T2-B ACL demonstration  
**Category:** T2-B/pre-rank-integration  
**Evidence:** `retrieve_allowed` returns IDs after ACL/deleted filtering; no score/rank stage follows in the same demonstrable pipeline.  
**Pedagogical/technical impact:** Learners see the two pieces separately but do not verify that denied chunks cannot influence ranking/reranking candidates.  
**Attack:** Ask for the candidate list fed into the ranker and prove denied IDs are absent; current demo has no ranker input.  
**Defense:** The function itself can reasonably be called before ranking.  
**Verdict:** CONFIRMED_FINDING P2; conceptual direction is correct, integration evidence is weak.  
**Improved version:** Compose `authorized_candidates = filter_acl(...); ranked = rank(query, authorized_candidates)` and assert denied/deleted IDs never enter score computation.  
**Reasoning:** Ordering of security controls is best taught as an executable data-flow edge.

## S48-F18 — P1 — Raw dense and lexical scores are linearly combined without normalization/calibration even though their scales need not be comparable.

**Location:** T3-A hybrid retrieval code  
**Category:** T3-A/hybrid-score-calibration  
**Evidence:** `w*dense + (1-w)*lexical` directly combines raw values. RED: multiplying lexical scores by 100 changes the winner while preserving the same lexical ordering.  
**Pedagogical/technical impact:** The chosen top document can be an artifact of score units rather than calibrated relevance.  
**Attack:** Rescale one retriever without changing its ranking; fused top changes.  
**Defense:** The prose says weights are calibrated and synthetic scores happen to share a small range.  
**Verdict:** CONFIRMED_FINDING because calibration is asserted, not shown.  
**Improved version:** Either normalize each score family before weighted linear fusion or teach RRF as a scale-agnostic baseline, then evaluate the choice on the holdout.  
**Reasoning:** Current Elastic guidance explicitly distinguishes RRF from normalized linear fusion for heterogeneous score scales.

## S48-F19 — P1 — Reranking is promised but not actually implemented.

**Location:** T3-A outcome/theory/I Do/We Do  
**Category:** T3-A/reranking  
**Evidence:** Outcome/title mention reranking; code performs only first-stage lexical+vector fusion and sorting. Exercises recalculate weighted top only.  
**Pedagogical/technical impact:** One of the roadmap's explicit T3 constructs is assessed as vocabulary rather than an operation.  
**Attack:** Find a second-stage reranker/cross-encoder or scoring function in the executable path; none exists.  
**Defense:** The section may intend hybrid fusion itself as the reranking step.  
**Verdict:** CONFIRMED_FINDING: fusion and second-stage reranking are distinct operations in current search systems.  
**Improved version:** Add a small deterministic reranker over the ACL-filtered top-N candidates or clearly rename the topic to fusion only and defer reranking.  
**Reasoning:** Promised outcomes need executable practice.

## S48-F20 — P1 — ACL is absent from the hybrid/rerank function and exercises despite prose saying rerank operates only on ACL-filtered candidates.

**Location:** T3-A hybrid executable path  
**Category:** T3-A/security-integration  
**Evidence:** Inputs are lexical scores, vector scores, weights and expected_top; no allowed-ID set/ACL appears.  
**Pedagogical/technical impact:** A denied document can win the hybrid score in the exercise model without the gate detecting it.  
**Attack:** Add a high-scoring denied ID; current hybrid function has no authorization information.  
**Defense:** T2-B taught ACL separately.  
**Verdict:** CONFIRMED_FINDING because S48's core safety property must compose across retrieval stages.  
**Improved version:** Pass only an authorized candidate map into fusion and test an adversarial denied high-score document.  
**Reasoning:** Security invariants must survive composition, not reset at each subtopic.

## S48-F21 — P1 — The prose promises a five-query holdout and Recall@3 before declaring hybrid improvement, but practice mostly validates one expected top.

**Location:** T3-A Recall@k / holdout practice  
**Category:** T3-A/evaluation-alignment  
**Evidence:** Theory demo computes Recall@2 for one gold set; E1/E2/E3 schemas revolve around lexical/vector/weights/expected_top.  
**Pedagogical/technical impact:** Learners can 'calibrate' the hybrid ranker to one example and never demonstrate aggregate retrieval improvement.  
**Attack:** Overfit the weights to the supplied SLA query; no multi-query eval catches it.  
**Defense:** T1-B introduced holdout promotion separately.  
**Verdict:** CONFIRMED_FINDING because T3-A explicitly claims hybrid recall evaluation.  
**Improved version:** Run baseline and hybrid over a small versioned multi-query qrel/gold fixture; report Recall@k/MRR/NDCG and error slices before promotion.  
**Reasoning:** One expected top is a unit test, not retrieval evaluation.

## S48-F22 — P2 — Weights and k are not domain-validated.

**Location:** hybrid weights + recall_at_k  
**Category:** T3-A/input-validation  
**Evidence:** Linear fusion accepts weights below 0 or above 1; `recall_at_k` accepts nonpositive/oversized k without policy validation.  
