**Pedagogical/technical impact:** Malformed configuration can produce a plausible ranking score.  
**Attack:** Use `w=2.0`; function produces scores with negative contribution from lexical.  
**Defense:** Fixtures use valid values.  
**Verdict:** CONFIRMED_FINDING P2.  
**Improved version:** Validate configured weights/normalizer and `1 <= k <= candidate_count` (or explicitly define clipping behavior).  
**Reasoning:** Ranking configuration is part of the reproducible contract.

## S48-F23 — P0 — The core 'grounded/cited' gate does not verify that cited text actually supports the claim.

**Location:** T3-B + T4-A + section evidence gate  
**Category:** core-gate/grounding/false-green  
**Evidence:** T3-B checks `claims <= cited_claims`; T4-A checks nonempty allowed evidence IDs and an injection flag. Neither function receives the cited text/claim-support relation. RED: claim 'El SLA es 48 horas' + allowed id `d1#sla` passes even if that fragment says something else.  
**Pedagogical/technical impact:** The section's defining gate can answer an unsupported material claim while reporting PASS/grounded.  
**Attack:** Keep an allowed evidence ID but change claim text to a contradiction/fabrication; both metadata gates still pass.  
**Defense:** T4-A prose correctly admits response-level evidence IDs are only a cheap first schema check.  
**Verdict:** CONFIRMED_FINDING P0 because the final/section-level evidence language still treats this metadata gate as support.  
**Improved version:** Represent claim-level attribution `{claim_id, claim_text, evidence_ids}`; resolve each ID to authorized active text/version and run a deterministic fact check/evaluator or labeled support fixture. Only emit a material claim when every claim-support edge passes.  
**Reasoning:** Modern RAG evaluation distinguishes retrieval, groundedness and response completeness; citation presence is not semantic entailment.

## S48-F24 — P1 — Authorization/resolution of all citations is collapsed into one caller-supplied boolean.

**Location:** T3-B `citation_acl`  
**Category:** T3-B/citation-authorization  
**Evidence:** `context_ok(..., citation_acl: bool, ...)` has no evidence IDs, ACL records, tombstone/version state or resolver.  
**Pedagogical/technical impact:** A mix of permitted and forbidden/stale citations can be certified with `citation_acl=True`.  
**Attack:** Supply a response with several evidence IDs including a deleted/denied one; the function cannot inspect them.  
**Defense:** T2-B already models ACL.  
**Verdict:** CONFIRMED_FINDING because T3-B promises 'cita autorizada y resoluble'.  
**Improved version:** Resolve each evidence ID against the active authorized corpus and require every cited chunk to pass ACL, deletion/version and provenance checks.  
**Reasoning:** Authorization is per resource/principal, not a global adjective.

## S48-F25 — P1 — Indirect prompt-injection resistance is self-attested by a boolean instead of observed behavior.

**Location:** T4-A `injected_instruction_ignored`  
**Category:** T4-A/prompt-injection-evidence  
**Evidence:** The poisoned example never passes hostile retrieved text through a model; caller supplies False/True.  
**Pedagogical/technical impact:** Learners can claim an injection control without demonstrating that the model ignored the hostile instruction.  
**Attack:** Set the flag True with no model call or policy test.  
**Defense:** S50 owns full red-team methodology.  
**Verdict:** CONFIRMED_FINDING for minimum evidence; demanding exhaustive red-team coverage here is REJECTED.  
**Improved version:** Use a deterministic/approved local model fixture or recorded model response: retrieve a chunk containing hostile instruction, verify output stays within allowed task/schema and does not follow the injected directive, log the test; defer broader suites to S50.  
**Reasoning:** A security flag is a result field, not the test itself.

## S48-F26 — P2 — The structured-output field changes from `claim` to `answer` across examples without explaining a schema migration.

**Location:** T4-A theory vs I Do/We Do/You Do  
**Category:** T4-A/schema-consistency  
**Evidence:** Theory `grounded()` requires `{claim,evidence_ids}`; later exercises/use-case use `{answer,evidence_ids}`; You Do describes `{status, claim?, evidence_ids?}`.  
**Pedagogical/technical impact:** Learners cannot tell which schema is the contract being validated.  
**Attack:** Try to reuse the theory validator on the exercise output; it fails solely on key name.  
**Defense:** Different examples may legitimately use different schemas.  
**Verdict:** CONFIRMED_FINDING P2 because the changes are within one subtopic/project and are unexplained.  
**Improved version:** Define one versioned response schema and reuse it, or explicitly teach `ResponseV1`→`ResponseV2` migration.  
**Reasoning:** Structured output is only useful when the structure itself is stable/explicitly versioned.

## S48-F27 — P1 — Answer quality/groundedness is self-attested through scalar/boolean inputs rather than computed from response+context/eval data.

**Location:** T4-B faithfulness/support  
**Category:** T4-B/evaluation-evidence  
**Evidence:** `route(support, recall, faith, cost,...)` consumes supplied numbers; We Do later uses `support=True/False`.  
**Pedagogical/technical impact:** The system can answer by typing favorable metrics rather than demonstrating that the current answer is supported.  
**Attack:** Set support=.8/True and faith=.91 for an arbitrary ungrounded answer.  
**Defense:** The function could represent a downstream evaluator result.  
**Verdict:** CONFIRMED_FINDING because no evaluator result ID/dataset/trace is bound.  
**Improved version:** Bind each metric to a versioned eval result/trace. For per-answer support, calculate or label from `{query,response,context}`; for system retrieval metrics, bind to the current eval dataset/index revision.  
**Reasoning:** RAG process and system evaluators consume different evidence and should remain traceable.

## S48-F28 — P1 — Cost is not consistently defined or measured.

**Location:** T4-B theory/code/I Do/We Do  
**Category:** T4-B/cost-units  
**Evidence:** `route` uses `cost` with cap 0.1; We Do names `cost_pen`; the example separately prints `cost_tokens 1200` without deriving currency from token usage.  
**Pedagogical/technical impact:** A learner cannot reproduce the cost gate or know whether the threshold is PEN, tokens or another unit.  
**Attack:** Ask how 1200 tokens become 0.08/0.1 cost; no model pricing/derivation exists.  
**Defense:** The numbers are only synthetic budget fixtures.  
**Verdict:** CONFIRMED_FINDING.  
**Improved version:** Choose explicit fields (`input_tokens`, `output_tokens`, `price_version`, `cost_pen`) and derive cost; or use a pure token-budget gate without currency.  
**Reasoning:** Units are part of every quantitative contract.

## S48-F29 — P1 — `support` changes from a numeric score with threshold to a boolean without explaining what measurement changed.

**Location:** T4-B theory/code vs We Do  
**Category:** T4-B/support-semantics  
**Evidence:** Theory example: support 0.8 vs 0.2; `route` compares to thr=.5. We Do uses `support=True/False` and no threshold.  
**Pedagogical/technical impact:** Learners cannot transfer the abstention policy because the construct changes type halfway through.  
**Attack:** Try to map a support score of .49/.51 into the boolean exercises; calibration information disappears.  
**Defense:** Boolean can represent an already-thresholded evaluator.  
**Verdict:** CONFIRMED_FINDING because that thresholding step is neither represented nor named.  
**Improved version:** Keep `support_score` + `min_support` through the lab, or explicitly introduce `support_pass = support_score >= min_support` with evaluator version.  
**Reasoning:** Type changes must preserve the causal interpretation.

## S48-F30 — P1 — Evaluation/cost domains are not validated.

**Location:** T4-B route/We Do gates  
**Category:** T4-B/input-validation  
**Evidence:** RED: recall=2.0, faith=2.0 and negative cost produces `answer`; negative thresholds can also make nonsensical evidence pass.  
**Pedagogical/technical impact:** Malformed telemetry can authorize an answer.  
**Attack:** Mutate scores beyond [0,1] and cost below zero.  
**Defense:** Evaluators may guarantee ranges upstream.  
**Verdict:** CONFIRMED_FINDING because the gate does not declare/validate that precondition.  
**Improved version:** Validate finite metric domains, nonnegative budgets/cost, evaluator/version metadata and threshold domains before answer/abstain.  
**Reasoning:** Fail-closed includes invalid evidence, not just low evidence.

## S48-F31 — P1 — The 24 exercises heavily reuse one grammar: repair inverted predicate → valid/invalid/missing table → route token.

**Location:** all 24 We Do exercises  
**Category:** pedagogy/practice-pattern  
**Evidence:** Across all eight subtopic families E1/E2/E3 repeat the same dict/predicate scaffolding with different field names.  
**Pedagogical/technical impact:** Students can learn the wrapper instead of retrieval manifests, ACL traces, qrels, citation graphs, model outputs and eval records.  
**Attack:** Solve later families by recognizing the code skeleton before reading the domain story.  
**Defense:** Stable structure reduces extraneous load.  
**Verdict:** CONFIRMED_FINDING because uniformity dominates the entire practice set.  
**Improved version:** Interleave vector validation, qrel tables, chunk manifests, ACL candidate lists, hybrid ranking tables, citation graphs, structured-output records and eval traces.  
**Reasoning:** Worked-example guidance warns against surface-pattern memorization and supports variability.

## S48-F32 — P1 — E3 is labeled transfer but usually keeps the same CASO-PUN fixture, schema, predicate and output vocabulary.

**Location:** all E3 exercises  
**Category:** pedagogy/transfer  
**Evidence:** Roadmap requires E3 to change context and demand independent transfer; S48 mostly removes hints while preserving representation.  
**Pedagogical/technical impact:** Learners practice completion, not recognition of RAG invariants in novel artifacts.  
**Attack:** Compare E2 and E3 per family: same fields/fixtures and near-identical conditional logic.  
**Defense:** E3 asks for the final decision route with less direct guidance.  
**Verdict:** CONFIRMED_FINDING.  
**Improved version:** Make E3 consume a changed evidence surface/context—qrels file, manifest diff, retrieval trace, citation JSON, poisoned-document run, cost log—without providing the final predicate.  
**Reasoning:** Transfer requires stable deep structure under changed surface form.

## S48-F33 — P1 — Missing-first routing can hide an already-observable breach in every family.

**Location:** all 8 E3 decision functions  
**Category:** state-machine/fail-closed  
**Evidence:** RED compound fixtures: bad metric/version + missing top→REVIEW; recall regression + missing cost→EVALUATE; duplicate/bad metadata + missing version→RESTORE; denied/deleted + missing cache flag→VERIFY; bad hybrid + missing expected_top→REVIEW; uncited/ACL false + missing max tokens→REQUEST; illegal evidence + missing injection flag→VALIDATE; low recall/faith/high cost + missing support→TUNE.  
**Pedagogical/technical impact:** Removing one field downgrades a known unsafe/failing state into a generic uncertainty route.  
**Attack:** Start from each adverse fixture and delete one unrelated required field.  
**Defense:** Unknown evidence should not be guessed.  
**Verdict:** CONFIRMED_FINDING: missingness and known breach are orthogonal.  
**Improved version:** Compute all observable breaches and missing fields independently; known breach blocks first while missing evidence remains attached for remediation.  
**Reasoning:** Fail-closed must preserve what is already known.

## S48-F34 — P0 — The project can become READY by flipping four booleans while `retrieve()` and `answer()` remain unimplemented stubs.

**Location:** You Do starter `readiness`  
**Category:** YouDo/readiness/false-green  
**Evidence:** Starter defines both core functions as `raise NotImplementedError`; `readiness()` only checks whether four names map to True. RED: `{name: True for name in REQUIRED}` returns `('READY', [])` without invoking either function.  
