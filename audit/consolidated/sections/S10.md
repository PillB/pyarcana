# S10 — consolidated issues

**22 findings** · P0 0 · P1 12 · P2 10 · unspecified 0
Elements audited: 42 · Rejected hypotheses: 8 · Red tests: 0

## P1 (12)

### S10-F02 — Due to this problem, a reasonable learner can implement two different precedence orders depending on which paragraph they copy.
- **Category:** CORE_CONTRACT_CONTRADICTION
- **Location:** jobRelevance vs learningOutcomes/T4-A
- **Evidence:** jobRelevance says command-line option overrides file and file overrides environment, while the outcome and T4-A define flags > env > file > defaults.
- **Impact:** Due to this problem, a reasonable learner can implement two different precedence orders depending on which paragraph they copy.
- **Proposed fix:** Use one canonical order everywhere; retain flags > env > file > defaults because T4 and the outcome already teach it.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F03 — Due to this problem, a reasonable learner can believe an import-safe module may execute arbitrary prints as long as the CLI function itself…
- **Category:** EXAMPLE_CONTRADICTS_PROSE
- **Location:** S10-T1-A main_guard.py
- **Evidence:** The example claims the import path is usable without CLI side effects, but executes print('import_safe', ...) at top level, so importing the module prints output.
- **Impact:** Due to this problem, a reasonable learner can believe an import-safe module may execute arbitrary prints as long as the CLI function itself is guarded.
- **Proposed fix:** Move every demonstrative print under main()/the __main__ guard; leave only definitions at import time unless initialization is explicitly intended.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F04 — Due to this problem, a reasonable learner can run python -m familiarity_core from an uninstalled src-layout checkout and conclude the packag…
- **Category:** TECHNICAL_CONTRADICTION
- **Location:** S10-T1-A vs S10-T2-A
- **Evidence:** T1-A says python -m familiarity_core runs the package without fighting sys.path; T2-A correctly says src layout prevents importing the package from the repository until it is installed. PyPA states src layout requires project installation.
- **Impact:** Due to this problem, a reasonable learner can run python -m familiarity_core from an uninstalled src-layout checkout and conclude the package or __main__.py is broken when the real problem is discoverability.
- **Proposed fix:** State that python -m familiarity_core works after the package is discoverable, normally after python -m pip install -e . in this course.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F08 — Due to this problem, a reasonable learner can publish metadata claiming support for Python 3.11 even though the course never validated the p…
- **Category:** CROSS_SECTION_RUNTIME_CONTRADICTION
- **Location:** S10-T2-A pyproject_min.py
- **Evidence:** The package declares requires-python = >=3.11 although S01 explicitly requires Python 3.12+ and says the course uses Python 3.12.
- **Impact:** Due to this problem, a reasonable learner can publish metadata claiming support for Python 3.11 even though the course never validated the package against that runtime.
- **Proposed fix:** Use requires-python = >=3.12 unless the project actually tests and supports 3.11.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F09 — Due to this problem, a reasonable learner can perform an editable install, find no familiarity executable, and have no lesson-local artifact…
- **Category:** GATE_ARTIFACT_INCOMPLETE
- **Location:** opening + S10-T2-A
- **Evidence:** The opening correctly says [project.scripts] is what creates the installed command, but the canonical pyproject fragment omits [project.scripts]. Installing that demonstrated metadata cannot create the promised familiarity command.
- **Impact:** Due to this problem, a reasonable learner can perform an editable install, find no familiarity executable, and have no lesson-local artifact showing what entry point is missing.
- **Proposed fix:** Add [project.scripts] familiarity = 'familiarity_core.cli:main' and show __main__.py delegating to the same main function.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F13 — Due to this problem, a reasonable learner can repeat the S08/S09 misconception that printing/returning an integer inside a helper proves the…
- **Category:** PROCESS_EXIT_EVIDENCE_FAILURE
- **Location:** S10-T3-A argparse_subs.py
- **Evidence:** try_parse([]) catches argparse's SystemExit(2), returns integer 2 and the surrounding demonstration keeps running. The example prints code 2 but does not make the Python process exit 2; it also never demonstrates runtime/business code 1.
- **Impact:** Due to this problem, a reasonable learner can repeat the S08/S09 misconception that printing/returning an integer inside a helper proves the operating-system process status.
- **Proposed fix:** Demonstrate the actual entrypoint boundary with raise SystemExit(main(...)) or sys.exit(main()), and separately show a real runtime-error path mapping to 1.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F14 — Due to this problem, a reasonable learner can write a main function expected to return 2, then see a test abort with SystemExit instead.
- **Category:** ARGPARSE_CONTROL_FLOW_CONTRADICTION
- **Location:** S10-T3-A paragraph 3
- **Evidence:** The prose says main(argv) -> int lets tests simulate invalid usage and receive code 2, but default ArgumentParser.parse_args raises SystemExit(2) on usage errors. The code avoids this only by inventing a separate try_parse wrapper, not the promised main architecture.
- **Impact:** Due to this problem, a reasonable learner can write a main function expected to return 2, then see a test abort with SystemExit instead.
- **Proposed fix:** Teach that argparse owns usage errors and raises SystemExit(2) by default; tests assert that behavior, while main returns 0/1 for normal/runtime paths, or deliberately customize parser error handling and explain the tradeoff.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F15 — Due to this problem, a reasonable learner can copy the example into a JSON-producing CLI and contaminate stdout with diagnostic text while b…
- **Category:** STDIO_DEMO_CONTRADICTION
- **Location:** S10-T3-B stdio_split.py
- **Evidence:** The paragraph promises data on stdout and diagnostics on stderr, but the canonical demo uses print for the data, the literal label '--- stderr ---', and the log contents. Capturing the example shows stderr is empty and every line goes to stdout.
- **Impact:** Due to this problem, a reasonable learner can copy the example into a JSON-producing CLI and contaminate stdout with diagnostic text while believing it is on stderr.
- **Proposed fix:** Use print(data, file=sys.stdout) for data and print/logging StreamHandler(sys.stderr) for diagnostics; demonstrate captured stdout and stderr separately.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F16 — Due to this problem, a reasonable learner cannot implement the promised pipe contract from the lesson before practice.
- **Category:** OUTCOME_NOT_TAUGHT
- **Location:** S10-T3-B
- **Evidence:** The theory promises support for path or '-' as stdin and discusses pipes twice, but the code never reads sys.stdin, never branches on '-', and never opens an input path.
- **Impact:** Due to this problem, a reasonable learner cannot implement the promised pipe contract from the lesson before practice.
- **Proposed fix:** Add a minimal read_input(path) example: if path == '-', read sys.stdin; otherwise read the named Path.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F18 — Due to this problem, a reasonable learner can understand dictionary merge order yet still be unable to implement any of the three real confi…
- **Category:** CONFIG_OUTCOME_SIMULATED_NOT_IMPLEMENTED
- **Location:** S10-T4-A/T4-B
- **Evidence:** The lesson's core outcome is flags > env > file > defaults, but the example receives four already-constructed dictionaries. It never reads an OS environment variable, never reads a config file, and never connects argparse Namespace values to flags. T4-B then calls .env 'the environment-variable file' even though Python stdlib does not automatically load .env files.
- **Impact:** Due to this problem, a reasonable learner can understand dictionary merge order yet still be unable to implement any of the three real configuration sources, or assume creating .env automatically populates os.environ.
- **Proposed fix:** Show one concrete source per layer: argparse flags, os.environ, a documented config file format, and defaults; state that .env is only a file convention and requires explicit loading/parsing.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F20 — Due to this problem, a reasonable learner can ship a CLI that logs a configuration failure but exits 0, causing schedulers and CI to mark th…
- **Category:** FAIL_FAST_CONTRADICTION
- **Location:** S10-T4-B validate_config.py
- **Evidence:** The text says invalid startup config must fail-fast, but the demo catches RuntimeError, prints the message and completes normally. This repeats the exact S08/S09 process-status defect at the point S10 was supposed to repair it.
- **Impact:** Due to this problem, a reasonable learner can ship a CLI that logs a configuration failure but exits 0, causing schedulers and CI to mark the run successful.
- **Proposed fix:** At the application boundary print a sanitized diagnostic to stderr and return/exit 1; do not swallow the fatal exception in the canonical fail-fast demonstration.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F22 — Due to this problem, a reasonable learner reaches practice without having seen the complete packaging mechanism that the lesson's gate requi…
- **Category:** ESSENTIAL_TEACHING_OUTSIDE_LESSON
- **Location:** S10-T2-A + section criterion
- **Evidence:** The section criterion is an installable package with useful CLI, but theory only prints a TOML string and a layout path. It never creates the actual artifact, runs python -m pip install -e ., invokes familiarity --help, or verifies python -m familiarity_core. The I Do explicitly says the real install lives in You Do.
- **Impact:** Due to this problem, a reasonable learner reaches practice without having seen the complete packaging mechanism that the lesson's gate requires.
- **Proposed fix:** Add one compact reference artifact in theory: real pyproject.toml, src/familiarity_core/cli.py, __main__.py, editable-install command and one successful --help invocation.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

## P2 (10)

### S10-F01 — Due to this problem, a reasonable learner can be unsure whether CP-N1-B/C denotes a package, project increment, assessment gate or file.
- **Category:** PERSISTENT_WIDOW_TERM
- **Location:** tagline/opening/reference
- **Evidence:** CP-N1-B/C and gate remain internal curriculum labels without a stable learner-facing expansion.
- **Impact:** Due to this problem, a reasonable learner can be unsure whether CP-N1-B/C denotes a package, project increment, assessment gate or file.
- **Proposed fix:** Expand each identifier once in learner language before reusing the abbreviation.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F05 — Due to this problem, a reasonable learner can believe __all__ enforces privacy or stability at runtime.
- **Category:** PUBLIC_API_OVERCLAIM
- **Location:** S10-T1-A/T1-B
- **Evidence:** __all__ is presented as the public API list the team can import with confidence. Python primarily uses __all__ to control from package import *; it does not prevent explicit imports of omitted/private names.
- **Impact:** Due to this problem, a reasonable learner can believe __all__ enforces privacy or stability at runtime.
- **Proposed fix:** Say __all__ documents/controls wildcard export; public API stability remains a project contract backed by documentation, reexports and naming conventions.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F06 — Due to this problem, a reasonable learner can cargo-cult lazy imports or utility modules without diagnosing the dependency direction that ca…
- **Category:** IMPORT_MODEL_OVERGENERALIZATION
- **Location:** S10-T1-A circular-import paragraph
- **Evidence:** Circular imports are reduced to 'Python se enreda' and a shared util is prescribed as first resource. The actual failure mode is access to partially initialized modules/names; some cycles execute and some fail depending on when names are needed.
- **Impact:** Due to this problem, a reasonable learner can cargo-cult lazy imports or utility modules without diagnosing the dependency direction that caused the cycle.
- **Proposed fix:** Add one sentence explaining partially initialized modules and prefer refactoring dependency direction before lazy-import workarounds.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F07 — Due to this problem, a reasonable learner can encounter visual explanations before the concept they are meant to explain and associate the w…
- **Category:** FIGURE_PLACEMENT
- **Location:** S10-T1-B and S10-T2-A figures
- **Evidence:** S10-package-layout is attached to Dependencias cíclicas y API pública, while S10-config-precedence is attached to Layout src, pyproject.toml y builds, before configuration precedence is taught in T4-A.
- **Impact:** Due to this problem, a reasonable learner can encounter visual explanations before the concept they are meant to explain and associate the wrong diagram with the wrong subtopic.
- **Proposed fix:** Move package-layout to T2-A and config-precedence to T4-A without rewriting either figure.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F10 — Due to this problem, a reasonable learner can edit pyproject entry points or dependencies and expect the installed metadata/wrapper to updat…
- **Category:** EDITABLE_INSTALL_OVERCLAIM
- **Location:** S10-T2-A
- **Evidence:** Editable install is described as reflecting changes without reinstalling, without limiting that claim to source-code changes. PyPA explicitly describes editable reflection for source code; metadata, dependency and script changes can require reinstall/rebuild.
- **Impact:** Due to this problem, a reasonable learner can edit pyproject entry points or dependencies and expect the installed metadata/wrapper to update automatically.
- **Proposed fix:** Say Python source edits are reflected; reinstall after changing packaging metadata, dependencies or console-script declarations.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F11 — Due to this problem, a reasonable learner can conclude the project has zero third-party dependencies of any kind and misunderstand why the b…
- **Category:** DEPENDENCY_CATEGORY_CONFLATION
- **Location:** opening/T2-A callout
- **Evidence:** The section says the program uses only the standard library while its build-system requires setuptools>=61, a third-party build dependency. Runtime dependencies and build dependencies are not distinguished.
- **Impact:** Due to this problem, a reasonable learner can conclude the project has zero third-party dependencies of any kind and misunderstand why the build environment downloads setuptools.
- **Proposed fix:** Distinguish runtime dependencies from build-system dependencies; runtime may be stdlib-only while packaging still uses a build backend.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F12 — Due to this problem, a reasonable learner can treat 0.1.0 as a stable SemVer contract or jump to 1.0.0 merely because any pre-1.0 API change…
- **Category:** SEMVER_ZERO_INCONSISTENCY
- **Location:** S10-T1-B/T2-B
- **Evidence:** The lesson teaches breaking -> MAJOR as a general rule while using version 0.1.0 and only briefly saying 0.x is more flexible. SemVer 2.0 states 0.y.z is initial development and the public API should not be considered stable.
- **Impact:** Due to this problem, a reasonable learner can treat 0.1.0 as a stable SemVer contract or jump to 1.0.0 merely because any pre-1.0 API changed.
- **Proposed fix:** Separate the course's pre-1.0 policy from SemVer 1.x rules; define exactly how 0.y.z changes are versioned before declaring 1.0.0.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F17 — Due to this problem, a Windows learner can think stdin piping is unavailable or that their shell is wrong when cat is not the documented com…
- **Category:** CROSS_PLATFORM_GAP
- **Location:** S10-T3-B pipe example
- **Evidence:** The only shell example uses cat data.json, while S01 explicitly supports Windows/PowerShell as a first-class path.
- **Impact:** Due to this problem, a Windows learner can think stdin piping is unavailable or that their shell is wrong when cat is not the documented command for their environment.
- **Proposed fix:** Label Unix/macOS and PowerShell examples separately, or use a platform-neutral conceptual pipe plus both concrete commands.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F19 — Due to this problem, a reasonable learner can lose an explicit clear/disable value from a configuration source and believe it was absent.
- **Category:** MISSING_NULL_COLLAPSE
- **Location:** S10-T4-A merge_config
- **Evidence:** Prose says None in flags means 'not passed', but code filters None from file_cfg and env_cfg too. That silently treats explicit None in every layer as missing.
- **Impact:** Due to this problem, a reasonable learner can lose an explicit clear/disable value from a configuration source and believe it was absent.
- **Proposed fix:** Apply the 'None means not passed' rule only to the layer whose parser uses that sentinel, or represent absence with omitted keys/a dedicated sentinel.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*

### S10-F21 — Due to this problem, a reasonable learner can let jobs='4' override jobs=1 and later hit type-dependent behavior while believing the precede…
- **Category:** ENV_TYPE_MODEL_MISSING
- **Location:** S10-T4-A
- **Evidence:** env_cfg is shown as an ordinary typed dict even though Python os.environ keys and values are strings. Numeric/boolean settings require explicit parsing before merge/validation.
- **Impact:** Due to this problem, a reasonable learner can let jobs='4' override jobs=1 and later hit type-dependent behavior while believing the precedence merge preserved the configuration type.
- **Proposed fix:** State that environment values arrive as strings and parse each supported key into its contract type before merging.
- **Sources (1):** audit/lessons-only/sections/S10.json
- *(title derived — source record had no title field)*
