# S12 — consolidated issues

**23 findings** · P0 0 · P1 13 · P2 10 · unspecified 0
Elements audited: 46 · Rejected hypotheses: 6 · Red tests: 6

## P1 (13)

### S12-F02 — The learner can classify every 4xx as malformed request while simultaneously memorizing 429 as a server error, producing incompatible status…
- **Category:** FALSE_HTTP_STATUS_MENTAL_MODEL
- **Location:** opening p2 + S12-T1-A p2
- **Evidence:** Opening says 'los 400 significan que el pedido estaba mal formulado' and then treats 429 as a server-side transient example. T1-A later correctly makes 429 a special retryable 4xx under course policy.
- **Impact:** The learner can classify every 4xx as malformed request while simultaneously memorizing 429 as a server error, producing incompatible status-to-action logic.
- **Proposed fix:** Teach status classes as broad categories, then classify action by specific status: 400 malformed/bad request, 401 auth, 404 missing, 429 rate limit, 503 unavailable.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F03 — The learner can reason about a fake status object yet still cannot construct a request, pass headers/timeout, receive bytes, decode the body…
- **Category:** OUTCOME_NOT_TAUGHT
- **Location:** S12-T1-A
- **Evidence:** Outcome promises consuming synchronous HTTP APIs, headers, timeout and response handling; theory code only constructs MockResponse objects and a local dictionary. urllib/urlopen is mentioned but never demonstrated.
- **Impact:** The learner can reason about a fake status object yet still cannot construct a request, pass headers/timeout, receive bytes, decode the body, or map network exceptions in a real synchronous adapter.
- **Proposed fix:** Add one minimal stdlib adapter around urllib.request with a transport seam/fake so the mechanism is visible without requiring live internet.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F04 — The learner reaches practice knowing the words retry/backoff but with no executable model of when an attempt repeats, how long it waits, or…
- **Category:** OUTCOME_NOT_TAUGHT
- **Location:** S12-T1-B paginate_rate.py
- **Evidence:** T1-B promises retry/backoff, Retry-After and max_retries; the canonical code implements pagination and increments a delay counter only. No should_retry, attempts, backoff schedule or Retry-After handling exists.
- **Impact:** The learner reaches practice knowing the words retry/backoff but with no executable model of when an attempt repeats, how long it waits, or when it stops.
- **Proposed fix:** Add a deterministic bounded retry loop with explicit retryable statuses, attempt counter, Retry-After/backoff delay and terminal failure path.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F05 — A learner can wrap a non-idempotent POST/write in the same timeout retry policy and duplicate side effects when the first attempt succeeded…
- **Category:** FORGOTTEN_PREREQUISITE_IDEMPOTENCY
- **Location:** S12-T1-A/T1-B
- **Evidence:** The section introduces GET/POST and then describes retrying timeouts/statuses generically even though S09 left idempotency MISCONCEPTION_RISK. It never limits automatic retry to safe/idempotent operations or explains how a POST becomes safely retryable.
- **Impact:** A learner can wrap a non-idempotent POST/write in the same timeout retry policy and duplicate side effects when the first attempt succeeded but the response was lost.
- **Proposed fix:** State that the canonical retry demo is a GET/read; do not automatically retry non-idempotent writes unless the operation contract/idempotency key makes repetition safe.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F07 — The learner can infer that putting a token-like value into source is the normal way to populate an environment variable, exactly the mechani…
- **Category:** SECRET_HANDLING_EXAMPLE_CONTRADICTION
- **Location:** S12-T2-A auth_cache_prov.py
- **Evidence:** Prose says never hardcode or use a demo token pasted into code; canonical code immediately executes os.environ['API_TOKEN'] = 'demo-token-not-real'.
- **Impact:** The learner can infer that putting a token-like value into source is the normal way to populate an environment variable, exactly the mechanism the paragraph warns against.
- **Proposed fix:** Inject a synthetic environment mapping into get_token for the deterministic demo, while showing that real os.environ is populated outside source code.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F08 — If two identities can see different representations at the same URL, a learner can reuse one principal's cached response for another princip…
- **Category:** AUTHENTICATED_CACHE_ISOLATION_RISK
- **Location:** S12-T2-A cached_get
- **Evidence:** The authenticated-adapter subtopic caches GET responses using sha256(url) only. Authentication identity/scope/tenant is absent from the key and cache policy.
- **Impact:** If two identities can see different representations at the same URL, a learner can reuse one principal's cached response for another principal.
- **Proposed fix:** Cache only explicitly public/shared resources or include a non-secret authorization scope/tenant identifier in the cache key; never include the raw bearer token.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F09 — A green static fixture test can be mistaken for evidence that the provider has not drifted.
- **Category:** CONTRACT_TEST_EVIDENCE_OVERCLAIM
- **Location:** S12-T2-B p1 + assert_contract
- **Evidence:** Prose says a provider field rename makes the fixture-based contract test fail before production. assert_contract only inspects a fixed local payload; it has no connection to the provider's current schema.
- **Impact:** A green static fixture test can be mistaken for evidence that the provider has not drifted.
- **Proposed fix:** Distinguish local adapter regression tests from provider/sandbox contract checks; static fixtures catch code regressions only after the expected contract/fixture is updated from trusted provider evidence.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F10 — The learner cannot implement the actual causal boundary failure -> fallback -> provenance because the demo models a manual mode switch inste…
- **Category:** FALLBACK_MECHANISM_NOT_DEMONSTRATED
- **Location:** S12-T2-B contract_fallback.py
- **Evidence:** Prose says 5xx/network failure triggers precomputed fallback. Code merely switches on a caller-supplied online boolean; no request failure/status causes the branch and no failure reason is carried.
- **Impact:** The learner cannot implement the actual causal boundary failure -> fallback -> provenance because the demo models a manual mode switch instead.
- **Proposed fix:** Use a fake online adapter that raises/returns 503, catch/classify that failure at the boundary, select the offline dataset, and record fallback_reason.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F11 — The first real external boundary bypasses the named domain model and reinforces the pre-S11 habit of allowing unvalidated dictionaries to ci…
- **Category:** FORGOTTEN_PREREQUISITE_DOMAIN_INTEGRATION
- **Location:** entire S12 theory
- **Evidence:** S11 is a required prerequisite, but canonical S12 theory never references ClientRecord, ResolvedEntity, Transaction or RelationshipEvidence. External JSON, cache bodies, SQL rows and evidence continue as anonymous dicts/tuples.
- **Impact:** The first real external boundary bypasses the named domain model and reinforces the pre-S11 habit of allowing unvalidated dictionaries to circulate through the system.
- **Proposed fix:** Show the complete boundary: external DTO/dict -> explicit validation/parsing -> canonical S11 domain constructor -> repository mapping.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F12 — The learner can collapse client identity and resolved-entity identity, destroying the layer separation that CP-N1-C relies on.
- **Category:** CROSS_SECTION_DOMAIN_MODEL_CONTRADICTION
- **Location:** S12-T3-A
- **Evidence:** Paragraph says clients/transactions/evidence names are aligned with S11. DDL creates clients(id,name) and evidence(entity_id), then inserts evidence.entity_id='C001' and joins it directly to clients.id. S11 distinguishes ClientRecord client_id C... from ResolvedEntity entity_id E... and RelationshipEvidence left_id/right_id.
- **Impact:** The learner can collapse client identity and resolved-entity identity, destroying the layer separation that CP-N1-C relies on.
- **Proposed fix:** Either map evidence explicitly to client_id and rename it honestly, or persist the S11 entity/evidence model with separate client/entity keys and relationships.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F13 — The learner reaches the SQL gate without a theory-local worked model for the full CRUD vocabulary or one of the three domain tables the para…
- **Category:** SQL_OUTCOME_ARTIFACT_INCOMPLETE
- **Location:** S12-T3-A
- **Evidence:** Roadmap/outcome says schema, CRUD and joins and prose names clients, transactions and evidence. The theory DDL creates only clients/evidence and demonstrates CREATE/INSERT/SELECT; no transactions table and no UPDATE/DELETE path is shown.
- **Impact:** The learner reaches the SQL gate without a theory-local worked model for the full CRUD vocabulary or one of the three domain tables the paragraph claims exists.
- **Proposed fix:** Either narrow the outcome to create/insert/select/join or add a minimal transactions table plus one UPDATE and clearly scoped DELETE/soft-delete example.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F14 — The learner can mistake naming convention for enforced referential integrity and publish incomplete case joins without a database error.
- **Category:** REFERENTIAL_INTEGRITY_GAP
- **Location:** S12-T3-A/T3-B
- **Evidence:** evidence.entity_id is only a TEXT column. No FOREIGN KEY is declared or enabled; the callout explicitly says 'FKs lógicas primero'. Orphan evidence is therefore accepted and an inner JOIN silently drops it.
- **Impact:** The learner can mistake naming convention for enforced referential integrity and publish incomplete case joins without a database error.
- **Proposed fix:** Declare the actual foreign key for the chosen model, enable PRAGMA foreign_keys=ON per connection, and add an orphan-insert negative example; otherwise label the schema intentionally non-enforcing and reconcile orphans explicitly.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F18 — The section's central privacy boundary remains a sentence; the learner has no executable guard preventing document_id/name/account fields fr…
- **Category:** SAFETY_OUTCOME_NOT_IMPLEMENTED
- **Location:** S12-T4-A
- **Evidence:** Prose promises an allowlist ALLOWED={'address','city','country'} and says allowed_for_public_geocoder returns False when document_id appears. No such function or egress-check code exists in the canonical theory block.
- **Impact:** The section's central privacy boundary remains a sentence; the learner has no executable guard preventing document_id/name/account fields from being sent to a provider adapter.
- **Proposed fix:** Implement sanitize_geocoder_payload/allowed_for_public_geocoder before the geocoder call and demonstrate an allowed payload plus a blocked document_id payload.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

## P2 (10)

### S12-F01 — A reasonable learner can recognize labels yet not know whether they denote a capstone increment, case, policy level, queue, or data-boundary…
- **Category:** PERSISTENT_WIDOW_TERM
- **Location:** opening/reference/T1-A/T1-B
- **Evidence:** CP-N1-C, CASO-LIM-012, N1, DLQ, egress and related internal/ops labels appear before one stable learner-facing expansion; DLQ appears in a figure alt without definition.
- **Impact:** A reasonable learner can recognize labels yet not know whether they denote a capstone increment, case, policy level, queue, or data-boundary concept.
- **Proposed fix:** Expand CP-N1-C and CASO-LIM-012 once, define N1 as the course-local retry policy, spell out dead-letter queue before DLQ, and keep egress paired with the Spanish meaning salida de datos.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F06 — The visual summary teaches a broader retry policy than the text and can override the narrower rule the learner is supposed to implement.
- **Category:** FIGURE_POLICY_CONTRADICTION
- **Location:** figure S12-retry-or-not
- **Evidence:** Figure alt says '5xx reintenta acotado, 4xx de negocio va a DLQ' while the local N1 prose says only 429/503/timeouts retry and explicitly maps 500 to fail_server without blind retry.
- **Impact:** The visual summary teaches a broader retry policy than the text and can override the narrower rule the learner is supposed to implement.
- **Proposed fix:** Make the figure use the exact course table: timeout/429/503 -> bounded retry; 400 -> fix request; 404 -> missing; 500 -> fail_server under N1.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F15 — The only indexing example teaches that indexes are added by ritual rather than because a query plan needs them, while omitting the child/joi…
- **Category:** INDEX_DESIGN_MISCONCEPTION
- **Location:** S12-T3-B params_tx.py
- **Evidence:** document_id is declared UNIQUE and then CREATE INDEX idx_doc ON clients(document_id) creates another index over the same column; the prose also mentions entity_id indexing but no entity_id index is demonstrated.
- **Impact:** The only indexing example teaches that indexes are added by ritual rather than because a query plan needs them, while omitting the child/join key that would be a more defensible candidate.
- **Proposed fix:** Remove the redundant document_id index, add an entity/join-key index only if the schema/query requires it, and show EXPLAIN QUERY PLAN or at least the query that motivates the index.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F16 — A malformed provider payload can crash the pipeline at the validation function rather than enter the documented invalid-coordinate path.
- **Category:** COORDINATE_VALIDATION_CONTRACT_GAP
- **Location:** S12-T4-B valid_coord
- **Evidence:** Prose explicitly says strings are invalid coordinates and should be rejected. valid_coord('bad', -77) raises TypeError instead of returning False or an explicit domain error; 91 and NaN happen to produce False.
- **Impact:** A malformed provider payload can crash the pipeline at the validation function rather than enter the documented invalid-coordinate path.
- **Proposed fix:** Validate numeric type/finite value first and return a documented result or raise one deliberate validation error.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F17 — The learner can describe the result as a WGS84 geodesic or route distance rather than a spherical straight-line surface approximation from l…
- **Category:** GEOSPATIAL_MODEL_PRECISION
- **Location:** S12-T4-B p2
- **Evidence:** Text says Haversine estimates km 'entre dos puntos WGS84 con radio R=6371 km'. The implemented formula is a spherical Haversine approximation using a mean radius; WGS84 itself is an ellipsoid.
- **Impact:** The learner can describe the result as a WGS84 geodesic or route distance rather than a spherical straight-line surface approximation from lat/lon coordinates.
- **Proposed fix:** Say 'para coordenadas lat/lon expresadas en WGS84, usamos aquí una aproximación esférica Haversine con R=6371 km; no es distancia de ruta ni geodésica elipsoidal exacta'.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F19 — The learner can record a plausible-looking timestamp unrelated to the actual acquisition event and call the manifest provenance.
- **Category:** PROVENANCE_TIMESTAMP_SIMULATION_GAP
- **Location:** S12-T2-A auth_cache_prov.py
- **Evidence:** The code imports time and computes cache age using the actual clock, but fetched_at is hard-coded to 2026-07-20T12:00:00Z while prose describes it as metadata of each fetch.
- **Impact:** The learner can record a plausible-looking timestamp unrelated to the actual acquisition event and call the manifest provenance.
- **Proposed fix:** Inject a clock for deterministic tests and set fetched_at from that clock at the actual fetch/cache decision boundary.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F20 — An auditor sees that fallback happened but cannot determine why or which frozen source supplied the coordinates.
- **Category:** FALLBACK_PROVENANCE_INCOMPLETE
- **Location:** S12-T2-B prose/callout vs code
- **Evidence:** Callout says fallback leaves mode=offline and the reason in provenance. The returned dict contains mode only; there is no provenance object, failure reason, source version or offline dataset identity.
- **Impact:** An auditor sees that fallback happened but cannot determine why or which frozen source supplied the coordinates.
- **Proposed fix:** Return/record fallback_reason, source_id/version, generated_at and mode alongside the value.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F21 — A beginner reasonably expects PRIMARY KEY to guarantee a non-null identity and may persist rows with no ID while believing the constraint bl…
- **Category:** SQLITE_PRIMARY_KEY_PORTABILITY_TRAP
- **Location:** S12-T3-A sqlite_join.py
- **Evidence:** Schema uses id TEXT PRIMARY KEY without NOT NULL. In ordinary SQLite rowid tables this historical quirk can permit NULL primary-key values; an execution check inserted (NULL,'x') successfully.
- **Impact:** A beginner reasonably expects PRIMARY KEY to guarantee a non-null identity and may persist rows with no ID while believing the constraint blocks them.
- **Proposed fix:** Declare TEXT PRIMARY KEY NOT NULL (or an appropriate STRICT/WITHOUT ROWID design) and demonstrate the negative insert if identity presence matters.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F22 — The learner can cache geocoder results indefinitely or without normalized keys/provider terms because no domain-specific cache contract is s…
- **Category:** GEO_CACHE_POLICY_NOT_DEMONSTRATED
- **Location:** S12-T4-B p3
- **Evidence:** Outcome/prose requires caching geocodes under provider TTL/policy. T4-B has no cache code or provider-policy artifact; it only points back to the generic URL cache from T2-A, which itself has auth-scope issues.
- **Impact:** The learner can cache geocoder results indefinitely or without normalized keys/provider terms because no domain-specific cache contract is shown.
- **Proposed fix:** Add a tiny geocode cache keyed by normalized authorized query + provider/version with explicit TTL/policy and no sensitive raw address when policy forbids storage.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*

### S12-F23 — The learner can think response.json() and JSON decoding failure are independent mechanisms rather than two views of the same response body.
- **Category:** RESPONSE_PARSING_DEMO_SPLIT_MODEL
- **Location:** S12-T1-A mock_http_status.py
- **Evidence:** MockResponse.json() simply returns the prebuilt _payload and never parses self.text. parse_json_body separately parses raw text. The same response object therefore does not model the causal path response bytes/text -> JSON decode -> payload/error.
- **Impact:** The learner can think response.json() and JSON decoding failure are independent mechanisms rather than two views of the same response body.
- **Proposed fix:** Make the mock response store text and let json() call json.loads(text), so invalid body raises the same decode error the adapter must classify.
- **Sources (1):** audit/lessons-only/sections/S12.json
- *(title derived — source record had no title field)*
