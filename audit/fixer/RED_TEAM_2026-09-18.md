# Red-team review of the curriculum, badges, levels and capstones — 2026-09-18

Produced by the red-team layer above the skills-to-content synthesis (solarize cycle, run
`wf_a34a8bf0-6ff`). Three lenses read the whole map against the badges, level gates and capstone
requirements: a learner walking S01→S52 with only what the course has taught, an employer-style
auditor attacking every badge claim, and a curriculum architect grouping gaps by cause. Where a
question could not be settled from the repository it was sent back to research and answered.

**All six reviews over two rounds returned BROKEN.** 47 distinct P0/P1 findings.
Several are one decision each, not one fix each — the architect lens exists to show that.
Items marked ⚖ need the repo owner (AGENTS.md's Ask-first list, D11, or a product-scope call).

This is the first statement of each finding. A third round was running when this was written;
its refinements, and the final revised synthesis, will be appended.

## Level 1 and the cumulative capstone (Q3)

### [P0] `level1-gap-by-gap-rebuilds-rejected-q3-route` ⚖

*Scope:* level

Synthesis ranks 3, 4, 5, 6 and 20 are one decision, Q3. So are the verifier's move of the dict block into S02 and the onramp's traceback-reading D3 block in S02. Taken together, they adopt Q3's route 3 one gap at a time, and the repository records route 3 as rejected. The cause sits upstream of the content: V3 itself asks the S2 and S4 gates for multi-field, multi-record work, but schedules functions at S5 and dictionaries at S6. None of these is a primer case, because the learner writes and returns records. D11 therefore orders the routes as rewrite, then move; teaching earlier is the other branch of D6 question 2. The 13 gate positions hold under every route except Q3 route 2.

**Treatment.** Put one decision to the owner, laid out as D11's ladder, not five separate gap treatments. (a) Land the route-independent part now, as one change: D9 guard removal (rank 4), D10 try/except removal (rank 5, LBYL with isascii()+isdecimal()), and rank 6's move of the separate test module to S10. (b) Then choose one route. REWRITE THE DEPENDENCY (Q3 route 1): CP-N1-A becomes V3:169's stdin/stdout script, where a record is one line split into a fixed tuple, the S03 def block is the only def the learner writes, and there is no dict. Cost: the brief's interface and the 33 dict uses go. Or BUILD THE MISSING TEACHING as a spiral (D6 q2): a dict-as-record supporting block in S02 (literal, [clave], .get, KeyError, one guided exercise, one check) next to the existing S03 def block, with S05 and S06 opening by recalling both. Cost: the S02 load that Q3 rejected, and that reversal must be stated. MOVE THE GATE (route 2) only if both fail, because it shifts V3's fixed S4/S8 gates and CP-N1-B's scope. No primer: the learner authors the records. Record the chosen route in decisions.md so that rank 3's block and rank 20's lambda block are not built under the other route.

<details><summary>Evidence</summary>

V3:147 (S2 gate) 'capturar un registro sintético de cliente con nombres, dos apellidos, contacto y dirección, sin perder el valor original'. V3:169 (S4 gate) 'procesa múltiples registros por stdin/stdout'. V3:175 (S5) 'T1 Funciones: definición, llamada y retorno'. V3:187 (S6) 'T2 Mapeos/conjuntos: diccionarios'. OPEN_QUESTIONS.md:122-125 recommends route 2 and says 'route 3 was correctly rejected as tripling S02'. Before S05/S06 the synthesis adds a dict block (rank 3; the verifier moves it to S02 because of s02:1110 'armar dict `raw` / `clean` / `errors`'), a traceback D3 block in S02 (onramp), LBYL predicate teaching in S02/S03 (rank 5), and a loop-from-blank exercise in S04. It relies on the existing def block, s03:141 'Una regla con nombre: `def`, llamada y `return`'. CONCEPT_QUEUE has 12 Q3_ROUTE_PENDING rows (#23-26, #33-34, #40, #42-43, #47, #51, #57). AGENTS.md:97 puts 'Restructuring a practice layer or moving a project between sections' under Ask first. Ranks 4 and 6 are coupled: if S09 keeps test_audit_log.py importing audit_log.py after the guard is removed, the import runs the demo (verifier 2).

</details>

### [P1] `first-break-s02-youdo-compound-leap` ⚖

*Scope:* capstone

The learner's path first breaks at S02's You Do, the first CP-N1-A increment, not at S04. The learner goes from S01's 4-line print script to a graded file that needs three functions at once:
- def with return;
- a returned dict with 13 keys;
- try/except;
- annotated tuple returns;
- `from __future__ import annotations`;
- the main guard.
Synthesis ranks 3, 4 and 5 fix dict, the guard and try one at a time. Fixed that way, the S02 increment would still be the steepest step in Level 1.

**Treatment.** Under D11, rewrite the dependencies first, then move.
- S02's increment becomes a top-level script: it parses one hard-coded record into raw and clean variables, rejects empty text and non-decimal age by checking first (`t.strip() == ""`, `t.isascii() and t.isdecimal()`), and prints an f-string summary. No def, dict, try or guard.
- `safe_int` and `parse_client` move to S03's increment, after the S03 def block, with the dict block per synthesis rank 3.
This is the Q3 route, so it is recorded for the owner. Not a primer: the learner writes all of it.

<details><summary>Evidence</summary>

S01's graded script is hello_env.py, which ends at s01:2396 `print("CP-N1-A skeleton OK")`.

The S02 starter:
- s02:2239 `from __future__ import annotations`.
- s02:2242 `def safe_int(campo: str, valor: str) -> tuple[bool, int | None, str | None]:`.
- s02:2252 `# 3) try int(texto) / except ValueError → mensaje con valor!r`.
- s02:2236 'diseña el dict, luego llena cada clave'.
- The parse_client docstring lists 13 required keys: six *_raw, six clean fields and errors.
- s02:2228 requires the main guard.

Only 7 of 24 S02 We Do exercises contain any def (verifier recount). Given the fading measured above, none has the learner write one from a blank line.

</details>

## Badges, credentials and the award engine

### [P0] `badge-dag-locks-phase-credentials-under-proposed-retirements` ⚖

*Scope:* badge

Synthesis rank 11 rewrites 26 badges one at a time and never reads prerequisite_badges. Applied as written, it makes three of the four phase-and-final credentials permanently unreachable, and gives the others prerequisites from later levels. This is the same pattern OPEN_QUESTIONS Q1 already settled as a bug.

**Treatment.** FIX THE GATING MACHINERY before rewriting any claim. Add an invariant with a test: a phase credential's prerequisite_badges may name only badges whose required_sections all fall at or before the phase's gate section, and retiring a badge removes it from every prerequisite list in the same change. Then the owner decides the graph's shape (AGENTS.md:96). Recommended shape: competency badges stand alone, and each phase credential requires only progress_phaseN_walked and its three capstones. The trade-off, stated: a phase credential stops certifying competency badges, so a learner holds those separately. The alternative, credentials that require competency badges, can close a phase only if every such badge sits inside that phase's band.

<details><summary>Evidence</summary>

engine.ts:174-186 returns `state: STATE_LOCKED` ('You need to earn these badges first') when any prerequisite is missing. OPEN_QUESTIONS.md:7-12: 'no retroactive or out-of-order award path exists anywhere in the engine'. badge_catalog.json:5340: the prerequisites of integrated_python_ai_capstone_advanced_applied include reliable_async_python_development and applied_deep_learning_practice, both of which rank 11 proposes to retire. Transitive closure computed by script over the catalog: retiring reliable_async_python_development alone locks 8 badges (production_python_hardening_practice, architecture_decision_practice, container_platform_engineering_practice, ai_governance_code_review_practice, integrated_production_python_practice, integrated_python_ai_capstone_advanced_applied, integrated_python_ai_capstone_integrated_mastery, evidence_grounded_ai_systems_capstone). Retiring llmops_production_delivery ('or retire') locks integrated_mastery and evidence_grounded. Rank 11's repoints also create forward edges. The L1 credential's prerequisites (badge_catalog.json:4849: independent_data_preparation, applied_analytical_reasoning, reliable_automation_development) move to 'S14-S18', 'S18, S19, S33, S34' and 'S09 and S23'. The L2 credential's prerequisites include production_python_delivery_foundations ('becoming L4') and applied_rag_llm_service_development ('S41 and S48'). The L3 credential's include applied_mlops ('S46 and S47') and hardening ('S38, S42, S45'). My count: 9 forward edges plus 2 permanent locks.

</details>

### [P0] `badge-catalog-reissues-active-without-retest` ⚖

*Scope:* badge

Every non-progress badge in the catalog carries a written policy that upgrades holders to 'active' 'without requiring existing holders to re-test', which is the retroactive award D11 forbids. Neither the synthesis nor its verifiers saw it. The same policy closes curriculum gaps with a supplementary self-directed exercise rather than with teaching.

**Treatment.** FIX THE GATING MACHINERY, with owner approval (AGENTS.md:96). Strike the re-issue clause from all 26 gap_closure_policy entries, and state that any change of status requires re-assessment. A supplementary exercise is not a D11 primer, because it names no lesson. Either BUILD THE MISSING TEACHING and assess it (leakage is the live case; see level3-grades-data-roles-before-teaching-them), or remove the skill from the claim. Nothing issued under the old clause carries forward.

<details><summary>Evidence</summary>

badge_catalog.json:1158, and in 26 badges in total by script count: 'The badge is issued at 'pilot' status until Phase 4 closes the curriculum gap; once closed, the badge is re-issued at 'active' status without requiring existing holders to re-test (the supplementary exercise evidence is preserved as equivalent).' industry_alignment/badge_rubrics/responsible_machine_learning_evaluation.json, gap_closure_exercises.leakage_prevention: 'Identify a public ML tutorial notebook (Kaggle, GitHub, Medium). Audit it for data leakage… Write a 500-word memo', estimated_hours 6. decisions.md:281-282: 'Never award a capstone or a badge retroactively, and never mark one as earned because the evidence for it appears somewhere else in the course.'

</details>

### [P0] `no-server-graded-performance-evidence-for-any-claim` ⚖

*Scope:* course

No badge or credential claim survives the evidence test. The only work the platform grades on the server is multiple-choice section exams. Every You Do, capstone and exercise signal is either self-reported or asserted by the client. Even so, 15 badge claims promise 'pruebas ocultas' and the 5 credentials promise 'evidencia a prueba de manipulación'. For none of the 26 non-progress badges can I name a check that would catch a learner who cannot do the thing claimed. That includes python_data_foundations, the only badge whose sections match its claim.

**Treatment.** Fix the gating machinery and rewrite the claims. This needs an owner decision, because AGENTS.md puts 'changing what a credential attests' on its Ask-first list. (1) Now: set all 26 non-progress badges to a status that cannot be issued, or rewrite each claim to what is actually checked (for example 'aprobó con ≥85% los exámenes de opción múltiple de S01–S05'). Delete 'pruebas ocultas' and 'evidencia a prueba de manipulación' from the catalog and from credential-policy/page.tsx:32 and :47. (2) Build a per-learner submission record for You Do and capstone work: an artifact reference plus either server-run hidden tests or a recorded rubric review with the reviewer's id. That record produces `Sxx-YOUDO` evidence with a score and a tier. Only after that, wire the engine. No primer applies: this is missing machinery, not missing teaching.

<details><summary>Evidence</summary>

The only server-graded item type is multiple choice: prisma/schema.prisma:81-82 QuestionBank `options String // JSON array of strings`, `correctIndex Int`. exam/start/route.ts draws 'One question per distinct concept present in the bank'. A You Do counts as done when the learner clicks a button: SectionView.tsx:811 'Proyecto enviado a mi GitHub', and progress-store.ts:61-66 simply flips the state. Exercise correctness is whatever the client says: exercise/attempt/route.ts:13 `correct: z.boolean(),`, and no client code calls this route. None of the 29 API route files accepts a capstone submission. `grep -c 'pruebas ocultas'` on badge_catalog.json returns 15, e.g. :1017 'El aprendiz demostró independientemente Python Data Foundations mediante tarea práctica, pruebas ocultas y puertas críticas no compensables'. credential-policy/page.tsx:32 promises the same. Nothing in src implements hidden tests. The engine that would award badges has no caller: the only reference to EligibilityEngine in src is src/lib/eligibility/index.ts:32. The activity ids it reads (`${section}-YOUDO`, engine.ts:611) have no producer. The required `BADGE:<id>:integrator` project has only a rubric shell, whose deliverable is 'Bounded exercise artifact ... per the badge's exercise prompt', and no prompt exists (industry_alignment/badge_rubrics/python_data_foundations.json, integrator_spec). The capstone `:synthesis` and `:defense` projects have neither a prompt nor a route that records them. In addition, 20 of 21 competency badges gate on sections that teach something else (the verifier's re-derivation).

</details>

### [P1] `badge-layer-unwired-so-claim-rewrites-come-second` ⚖

*Scope:* course

The synthesis treats the badge catalog as live ('The harm arrives when a badge is awarded'). It is not: no app code reads the catalog, no UI issues a credential, and the only issuing route always refuses. So the first decision is product scope, not 26 claim rewrites. Meanwhile the Dashboard promises learners a verification flow that does not exist, and the Capstones page shows badge ids that exist nowhere in the catalog.

**Treatment.** FIX THE GATING MACHINERY, in this order. (1) The owner decides whether competency badges and credentials are part of the product (research request below). (2) Until then, reword the Dashboard's badge text and the CapstonesPage chip to what exists: progress markers, and a capstone reference that names no badge. (3) If they are part of the product: wire the engine with a slug-to-Sxx mapping, route issuance through it (verifier: gate the id fix on this), and define the integrator and synthesis projects. Only then apply rank 11's claim rewrites, under badge-dag-locks-phase-credentials-under-proposed-retirements. Trade-off: the Dashboard shows less, and what it shows is true.

<details><summary>Evidence</summary>

A grep of src outside src/lib/eligibility finds badge_catalog and eligibility only in a comment in section-id-migrations.ts. The engine is imported only by tests: tests/e2e_max/badge_eligibility.spec.ts says 'The production TypeScript engine in `src/lib/eligibility/` is imported directly'. No src file calls /api/credentials/issue, which always returns 403 (WORK_QUEUE.md:50-52). Dashboard.tsx:817 reads 'un supervisor de tu cohorte puede verificar un badge. La verificación es lo que convierte una vista previa en una insignia verificada', but src/app/api/supervisor and src/app/api/admin/supervisor contain no badge logic. CapstonesPage.tsx:258 renders `cap.badgeId` from catalog.ts, whose ids ('capstone_foundations', 'capstone_independent', 'capstone_advanced_applied', 'capstone_integrated_mastery', 'evidence_grounded_ai_systems') match no badge in badge_catalog.json. catalog.ts gives CP-N4-C 'evidence_grounded_ai_systems' and CP-FINAL 'capstone_integrated_mastery'; badge_catalog.json and route.ts:92-97 map them the other way round.

</details>

### [P1] `credential-route-attests-unchecked-and-stores-no-evidence` ⚖

*Scope:* capstone

Beyond the id mismatch the verifiers found, the issuance route signs a statement it has no basis for, lets the client choose the specification version it attests to, and stores nothing an employer could inspect. It also uses a single 13-gate check for all five credentials, so Foundations can never be earned at the end of Level 1.

**Treatment.** Fix the gating machinery, in this order. First fix the engine defects in award-idempotent-grandfathers-any-recorded-award, progress-markers-award-without-claimed-evidence and engine-averages-only-present-evidence. Then issue through the engine, with a LearnerProgress built on the server from the learner's own rows. Fix specificationVersion to the catalog version on the server. Write Credential and CredentialEvidence rows that point at the learner's own attempt and submission ids. Scope each credential to its own sections (Foundations: S01-S13). Either correct the verify comment or publish an asymmetric key; the owner chooses. Keep the 403 until all of this lands: fixing only the ids opens the unchecked path.

<details><summary>Evidence</summary>

route.ts:83 `const { badgeId, specificationVersion } = body`. The client's value is then signed into the statement at :141 '...under PyArcana Assessment Specification ${specificationVersion}, including critical evidence, hidden validation, and independent review.' `evidenceReferences` (:15) is never read. The record goes to `db.notification.create` (:153), although schema.prisma defines `model Credential` and `model CredentialEvidence` ('exam_attempt | exercise_attempt | capstone_submission | rubric_score'); nothing writes either. verify/route.ts returns no evidence. Its comment says a verifier can 're-validate the credential against the issuer's published public key' (:161), but signing is symmetric HMAC (issue route.ts:55 `createHmac('sha256', getSigningKey())`), so no public key exists. credential-policy/page.tsx:57 promises 'Un reclutador puede inspeccionar un resumen conciso de evidencia'. route.ts:112 requires S52 for every credential in capstoneMap (:92-97), Foundations/CP-N1-C included. CredentialRevocation is never written.

</details>

### [P1] `award-idempotent-grandfathers-any-recorded-award`

*Scope:* badge

engine.awardIdempotent returns AWARDED for any badge the caller lists as already awarded, even when evaluation fails and even when the badge is retired. The credential policy says awards made under earlier criteria 'conservan su validez'. Together these form a standing path to the retroactive awards D11 forbids. It is latent today only because the engine has no caller.

**Treatment.** Fix the gating machinery. awardIdempotent must re-evaluate and report superseded or withdrawn when the stored award's catalog version or the badge's status no longer holds. awarded_badges must come only from server-side BadgeAward rows (the schema already has one with a status). Owner decision on the policy text: under D11, nothing awarded under catalog 1.0.0 is carried forward, because its required_sections point at other content. Amend credential-policy and badge-notice to match. Add a test that a retired or failing badge is never reported as awarded.

<details><summary>Evidence</summary>

engine.ts:516-523 `const already = progress.awarded_badges.find(...)` … `// Force awarded state since it's already in the record.` `report.state = STATE_AWARDED` `report.eligible = true`. I ran the real engine and catalog under tsx from the scratchpad. A learner with no evidence but a pre-recorded applied_deep_learning_practice award came back `awarded true newly false`, although evaluation's own failing requirement was `prerequisite_badges`. With reliable_async_python_development set to 'retired', a pre-recorded award came back `awarded`, while evaluate() returned `locked`. Gate 2 also trusts the caller-supplied awarded_badges list (engine.ts:157-161). badge-specs.ts:9 and index.ts:26 say 'the engine refuses to evaluate against a catalog whose version doesn't match', but evaluate() has no version check. credential-policy/page.tsx:80-82 'las insignias emitidas bajo criterios anteriores conservan su validez'; badge-notice/page.tsx says the same.

</details>

### [P1] `progress-markers-award-without-claimed-evidence`

*Scope:* badge

The five progress markers are awarded on different evidence from what their claims state. The engine returns before it checks required_projects, never checks self-checks, and accepts guided-tier evidence with a score of 0.

**Treatment.** Rewrite the claims to what is gated, or gate what is claimed: run finalizeProgress after Gate 4 so phase markers require their capstone submissions, require tier ≥4 as the catalog's own unacceptable-evidence list implies, and correct the phase3 and journey text. Change both implementations together and add a test that journey without capstones is refused.

<details><summary>Evidence</summary>

engine.ts:254-257 `// --- For progress badges, that's the entire gate chain ---` `if (isProgress(spec)) { return this.finalizeProgress(...)`. This runs before Gate 4 (:259). The Python reference does the same (test_eligibility_engine.py:409-413). Probe: progress_journey_completed was awarded with 0 of its 13 required CP projects, from We Do-tier evidence, score 0, legacy_only true. Its description says 'all 13 capstones' (badge_catalog.json:1009). Claims against gates: phase0 (:36) says 'including the You Do project and self-check for each section', but required_activities are YOUDO and EXAM only. phase3 (:557) says 'S40 through S52', but required_sections stop at S51. journey (:734) says 'El aprendiz completó las secciones asignadas de esta fase del curso' about a 52-section marker. The catalog lists 'We Do exercise completed (guided, not independent)' as unacceptable evidence, yet evidenceTierMinimum returns TIER_WE_DO for progress badges (engine.ts:88).

</details>

### [P1] `engine-averages-only-present-evidence`

*Scope:* badge

Component scores average only the sections that have evidence. No badge lists SELFCHECK in required_activities, and the five integrated badges also omit EXAM. So a single self-check and a single exam can satisfy a component that stands for seven or eight sections.

**Treatment.** Fix the gating machinery. A required section with no evidence makes that component input null; it must not be skipped. Add SELFCHECK and EXAM to required_activities wherever the matching component exists, or drop the component. Add a test in which one exam of seven fails.

<details><summary>Evidence</summary>

engine.ts:596-602 (aggregateSelfCheck) and :624-630 (aggregateExams) both skip missing sections with `if (!e || e.score_pct === null) continue`. From the catalog dump: none of the 26 competency or credential badges lists SELFCHECK in required_activities, and integrated_* list YOUDO only. Probe: integrated_data_analyst_practice was awarded with 1 of 7 exams and 1 self-check: `component:self_check=100 ... component:section_exams=100`.

</details>

### [P1] `badge-prerequisite-graph-not-section-ordered` ⚖

*Scope:* badge

Prerequisites point forward in the course, so every phase credential is out of reach at the end of its own phase. The synthesis's proposed retirements would also lock whole chains, including three credentials.

**Treatment.** Fix the gating machinery. Regenerate prerequisite_badges from the repointed required_sections and add a test that no prerequisite requires a section later than the dependent badge's last section. Any retirement proposed in gap-rank 11 must land together with the prerequisite rewrite, never on its own. The owner approves.

<details><summary>Evidence</summary>

Computed from the prerequisite closure in the catalog. integrated_python_ai_capstone_foundations (S01-S13) needs S18 and S24, via independent_data_preparation and reliable_automation_development, and also lists progress_phase0_walked (badge_catalog.json:4850). _independent (up to S26) needs S37, via applied_sql_query_development. _advanced_applied (up to S39) needs S43, via applied_mlops_pipeline_delivery. applied_analytical_reasoning (S09, S10) needs S18. Dependents: retiring reliable_async_python_development locks 8 badges, among them integrated_python_ai_capstone_advanced_applied, _integrated_mastery and evidence_grounded_ai_systems_capstone. Retiring applied_deep_learning_practice locks those three credentials. The mechanism: Gate 1 returns STATE_LOCKED for a retired badge (engine.ts:129-148), and Gate 2 requires every prerequisite to be AWARDED.

</details>

### [P1] `legacy-progress-reaches-verified-credential`

*Scope:* badge

Legacy localStorage completion reaches a verified credential through Gate 2, against the catalog's own rule, and that localStorage record is self-reported. The one test named for this passes for a different reason. Legacy exam carry-forward is not tied to what the exam actually assessed.

**Treatment.** Fix the gating machinery. Remove progress_* from every competency and credential prerequisite list. Enforce legacy_only by rejecting it for any non-progress badge, and add a test with legacy rows at tier 4 and score 90 that still expects refusal. Add a content version to ExamAttempt, and under D11 carry no score forward across the V3 rename.

<details><summary>Evidence</summary>

badge_catalog.json:185 'Legacy `python-ds-progress` localStorage keys are accepted as evidence of section completion'; :187 'Legacy progress does NOT grant any competency badge or capstone credential.' But progress_phase0_walked is a prerequisite of integrated_python_ai_capstone_foundations (:4850). section-id-migrations.ts:6-8 '...badge_catalog.json accepts those keys as evidence of section completion'. The keys are written by toggle buttons (SectionView.tsx:296-305) and by a quiz scored in the browser (≥70 calls onDone, :847-848). legacy_only (types.ts:114) is read nowhere. test_eligibility_engine.py:1162-1188 'legacy progress doesn't fabricate evidence' tags its legacy rows TIER_THEORY, so it exercises the tier check, not legacy handling. badge_catalog.json:4789 lets legacy exam scores ≥85 carry forward 'provided the exam questions have not been rotated', but ExamAttempt stores no content version (schema.prisma:91-106), and the rename migration moves attempts by slug. The 2026-07-13 seed (commit 44a7b3bb) put NumPy questions ('¿Por qué NumPy es más rápido...') under the `numpy` slug, which now maps to `collections` (S06). HYPOTHESIS: whether any live database holds such attempts was not examined.

</details>

### [P1] `capstone-cards-show-orphaned-swapped-badge-ids`

*Scope:* capstone

Every capstone card shows a badge id that does not exist in the badge catalog, and the two Level-4 ids are swapped relative to the credential route. This is visible to learners on the capstones page.

**Treatment.** Fix the gating machinery. Set each capstone's badgeId to the catalog's credential id (N1 → integrated_python_ai_capstone_foundations, …, CP-N4-C → _integrated_mastery, CP-FINAL → evidence_grounded_ai_systems_capstone). Add a test that every CAPSTONES badgeId exists in the catalog and agrees with route.ts's capstoneMap. Show the credential's display name rather than the snake_case id.

<details><summary>Evidence</summary>

catalog.ts:61,:65,:69 'capstone_foundations'; :97 and :101 'capstone_integrated_mastery'; :105 CP-N4-C 'evidence_grounded_ai_systems'; :112 CP-FINAL 'capstone_integrated_mastery'. I checked all five ids: none is a badge_id in badge_catalog.json. The route maps the other way: route.ts:96 'integrated_python_ai_capstone_integrated_mastery' → 'CP-N4-C' and :97 'evidence_grounded_ai_systems_capstone' → 'CP-FINAL'. The ids are rendered at CapstonesPage.tsx:258 and :422 as `{cap.badgeId}`.

</details>

### [P1] `badge-explainer-describes-nonexistent-system` ⚖

*Scope:* course

The badge pages on the live site promise a local eligibility preview, verification by a supervisor and a 70% threshold. None of these exists, and the catalog floors are 85.

**Treatment.** Rewrite, because AGENTS.md's Never list covers presenting placeholders as features. Until the grader in no-server-graded-performance-evidence-for-any-claim exists, say plainly that the course issues no badges yet and that the 'Hecho' marks are self-reported. Remove the 70% statement and the preview and supervisor statements.

<details><summary>Evidence</summary>

Dashboard.tsx:770 'Alcanzaste el umbral de la rúbrica (70% o más).' Against that: types.ts:74-81 PROVISIONAL_FLOORS (self_check 85, you_do 80, exam 85, overall 85) and badge_catalog.json:4789 'Legacy scores in the 70-84% range do NOT satisfy the badge floor'. Dashboard.tsx:803 'el badge es una vista previa local guardada solo en este navegador' and badge-notice/page.tsx:10 'puedes ver una vista previa de tu elegibilidad para insignias', but no component calls the engine. Dashboard.tsx:817 'un supervisor de tu cohorte puede verificar un badge', but no API route other than credentials/issue and credentials/verify mentions badges. Dashboard.tsx:747 says 'Un badge es un pequeño marcador de que completaste un ejercicio o sección de forma independiente', while section completion is self-reported.

</details>

### [P1] `gap-closure-exercises-evidence-from-outside-course`

*Scope:* badge

The only supplementary work defined for the four gap-affected critical competencies (13 badges) has the learner either learn untaught tools elsewhere or audit someone else's code. The exercise ids differ from the ones the engine reads, a single exercise serves up to five badges, and selector_resilience has no exercise at all.

**Treatment.** Build the missing teaching or rewrite. Evidence each critical competency in the learner's own capstone: leakage in CP-N3-B after gap-rank 8's rewrite, SQL in the S29 You Do, type safety in a mypy step the course actually teaches. Retire gap_closure_* as credential evidence and keep them, if at all, as optional practice. Fix the id contract and cover it with a test.

<details><summary>Evidence</summary>

industry_alignment/badge_rubrics/applied_sql_query_development.json, sql_competency: 'Take a slow SQL query (your own, or from a public Postgres/MySQL tutorial). Run EXPLAIN ANALYZE.' The course teaches SQL on sqlite. leakage_prevention: 'Identify a public ML tutorial notebook (Kaggle, GitHub, Medium). Audit it for data leakage', which is evidence about someone else's pipeline for a 'prevention' competency. The rubrics use ids 'gap_closure_*', while the engine looks for `BADGE:${badge_id}:supplementary:${comp_id}` (engine.ts:390). gap_closure_leakage_prevention appears in 5 badges' rubrics. No rubric defines selector_resilience, which reliable_automation_development, integrated_automation_engineering_practice and evidence_grounded_ai_systems_capstone all require. 13 badges carry a gap-affected critical competency (computed with types.ts:87-96).

</details>

### [P1] `critical-reproducibility-gate-demands-untaught-ci-and-seeds` ⚖

*Scope:* badge

25 badges gate on reproducibility_determinism as a non-compensatory critical competency that must score 100%. Its rubric requires CI, coverage reporting and fixed seeds for NumPy, PyTorch and scikit-learn. That includes python_data_foundations (S01-S05) and the Foundations credential (S01-S13). By construction, a learner who masters the band fails the gate. A reviewer who waives it awards criteria the course never taught. Both break D11. The machine-readable rubric a grader would load has no criterion text at all.

**Treatment.** Rewrite the dependency: scale the critical rubric to each badge's band. For L1: a pinned requirements.txt, a README that reproduces the result in a clean venv, `random.seed` wherever randomness occurs, and the given asserts passing (all taught in S01-S04). CI and coverage only from S44; scikit-learn seeds only from S33; drop PyTorch. Generate the JSON criteria from the markdown. Add a test that fails on any placeholder criterion, and on any criterion naming a tool first taught after the badge's last required section. The owner decides whether L1 badges keep a non-compensatory critical gate at all (research request 2).

<details><summary>Evidence</summary>

industry_alignment/badge_requirements/python_data_foundations.md:268-270 '**Deterministic seeds** — all randomness (NumPy, PyTorch, scikit-learn, hash seeds) is seeded'. :274-276 '**Testing discipline** — at least one unit test per transformation function; tests run in CI; coverage reported'. The same text appears in 25 badge_requirements files, among them integrated_python_ai_capstone_foundations.md:319 and :325. Of the 31 badges, only the 5 progress markers and applied_sql_query_development lack this critical competency. In S01-S05, grep finds 0 hits for coverage/cobertura, 0 for seed(/semilla and 0 for torch. CI appears only as a concept (s01:594 'La **integración continua (CI)** es la comprobación automática…'). Coverage first appears at s27:657 'coverage_demo.py', CI workflows are S44, and no section executes PyTorch (verifier). Criteria 1 and 3 are within the band: s01:43 teaches a reproducible requirements.txt, and s01:40 teaches opening a Pull Request. In the JSON rubrics, 284 criteria across 26 files read literally 'See badge_requirements/<id>.md for full text.' (for example badge_rubrics/python_data_foundations.json:120).

</details>

### [P1] `engine-ignores-evidence-owner-verification-and-age` ⚖

*Scope:* badge

The engine cannot enforce D11's 'the learner's own work', even after an adapter is written. It never reads learner_id, server_verified, submitted_at, verification_mode or any expiry. The supplementary gate only checks that a score exists, so a supplementary exercise scored 0 satisfies it. Capstone credentials accept You Do-tier evidence, although types.ts declares a defense tier for them. This is new relative to the round-1 engine findings, which covered legacy_only, Gate 4 and averaging.

**Treatment.** Fix the gating machinery before any adapter exists. Every evidence row carries learner_id, a source id (attempt or submission), a grader id and graded_at. The engine rejects evidence whose owner differs from the learner, evidence not verified on the server where verification_mode requires it, evidence older than the content version or past its expiry, and a supplementary score below its floor (85, per the catalog's acceptable_evidence). Either use TIER_MIN_FOR_CAPSTONE or delete it. Write one refusal test per rule. This goes in RC-1's order before rows 14-16.

<details><summary>Evidence</summary>

A grep of src, excluding types.ts, finds server_verified 0, submitted_at 0, learner_id 0 and TIER_MIN_FOR_CAPSTONE 0; verification_mode is read only by the parser (badge-specs.ts:75). The fields exist in types.ts:110, :112 and :132, and types.ts:68 has `TIER_MIN_FOR_CAPSTONE = TIER_DEFENSE`, yet engine.ts:84-89 returns TIER_YOU_DO for capstones. The catalog promises `"server_verification_required": true` (:1141) and `"expiration_period_days": 1095` (:1162). engine.ts:391 `if (progress.project_results[supp_id] === undefined) {`. I ran the real engine and catalog under tsx on the dynamic edition, with learner_id 'someone-else', no server_verified flag and supplementary scores of 0. applied_sql_query_development came back awarded, responsible_machine_learning_evaluation came back awarded, and evidence_grounded_ai_systems_capstone, with all 4 gap supplementaries at 0, came back awarded. The Python reference reads none of these fields either; its fixtures set server_verified=True (:836-844), but no gate checks it.

</details>

### [P1] `two-badge-catalogs-same-version-divergent-engines` ⚖

*Scope:* course

There are two badge catalogs, both labelled version 1.0.0, and they disagree. The TypeScript engine reads one; the Python 'executable specification' reads the other. The two engines return different verdicts for the final credential. A credential would attest to 'especificación PyArcana 1.0.0' (or 1.1.0), a label that names two different documents. Each claim-evidence contract has a 'specification_hash' that is not a hash, and the tests that guard the contracts are tautological.

**Treatment.** Fix the gating machinery. The owner names one catalog as canonical; I recommend src/lib/eligibility, because the engine reads it. The other becomes a generated copy, or is retired after a preservation review. The Python reference loads the same file. Add a parity test that runs both engines on one fixture set, including the no-supplementary case, and fails on any difference in verdict. Replace specification_hash with a real sha256 of the canonical bytes, computed by a script and checked by a test. Bump the catalog version on every change, and have credentials/issue take specificationVersion from it (RC-1 row 12). Replace the substring tests with behavioural ones.

<details><summary>Evidence</summary>

badge-specs.ts:22 `import badgeCatalog from './badge_catalog.json'` (src/lib/eligibility). test_eligibility_engine.py:44 `CATALOG_PATH = ROOT / "industry_alignment" / "badge_catalog.json"`. engine.ts:15-16 says the Python reference 'mirrors this engine exactly; both must produce identical outputs'. Both files carry `"version": "1.0.0"` and generated_at 2026-07-28T22:08:04Z. A field diff by script: public_claim differs in 26 badges, non_claims in 31, credential_class in 31 (absent from the industry_alignment copy), version in 5, verification_mode in 5, and required_sections/activities in 1 (progress_phase3_walked is S40-S51 in src but S40-S52 in industry_alignment, whose claim at :560 reads 'S40 through S52'). The previous rounds' quotes ('pruebas ocultas', 15 hits) come from the src copy, which the engine reads; the industry_alignment copy has 0 hits. The engines also gate supplementaries differently: Python :526 `if spec.status == "pilot":` versus TS `if (spec.gap_affected_competencies.length > 0)`. Probe with full evidence and no supplementary: the Python engine returns evidence_grounded_ai_systems_capstone 'awarded'; the TS engine returns 'assessment_ready'. Claims cite 'especificación PyArcana 1.1.0' at src :3799, :4211, :4416 and :5537, although the catalog version is 1.0.0. All 31 contracts have a specification_hash of the form "sha256:python_data_foundations:1.0.0" (python_data_foundations.json:37), and all 31 have market_task_mappings []. test_claim_evidence_contracts.py:57-64 checks only that fields exist and that class D says "server_authoritative", a value outside the engine's type `'local_only' | 'server_verified'` (types.ts:151) that nothing reads. test_final_capstone_supplementary_gap.py checks substrings of engine.ts, such as "anyCriticalFailed = true", not behaviour.

</details>

### [P1] `badge-skill-vocabulary-comes-from-outside-v3` ⚖

*Scope:* badge

Rank 17 lists nine untaught claims, 'bi_tools, async, deep learning, fine-tuning, graph-RAG, Kubernetes, OWASP, OpenTelemetry, mentoring', and treats them one row at a time, but they have two different causes. Some are skills V3 schedules and the course only simulates: async (V3:542), observability traces (S38-T3) and LLM security (S50-T3). Those are build cases under RC-7. The rest appear nowhere in V3's syllabus: BI tools, deep learning, fine-tuning, graph-RAG, Kubernetes, GPU and mentoring. They come from the superseded learning_roadmap.md and from industry_alignment's role taxonomy, whose gap matrix proposes inserting them into sections V3 never gave them. Once RC-3 lands, this second group is struck from the claims by rule, not taught, unless the owner amends V3 and budgets the hours. That is one decision, not nine. It also leaves a third authority, the role taxonomy, that course_requirements.json's hierarchy does not rank.

**Treatment.** FIX THE GATING MACHINERY with one rule, approved together with RC-3: a credential's skill_nodes and public_claim may name only skills that a V3 topic line schedules. Market skills outside V3 go to a gap register, never into a claim. Apply it in two groups. Strike bi_tools, deep_learning, kubernetes and mentoring from skill_nodes and claims. Retire applied_deep_learning_practice only together with the prerequisite rewrite (badge-dag-locks-phase-credentials-under-proposed-retirements). Skills V3 schedules but the course simulates (async, Docker/Compose, CI, FastAPI, scikit-learn) stay in the claims and are built under RC-7. The gap matrix's insertions (Kubernetes in S45, BI in S09/S25) become V3 amendment proposals, each carrying its hours, and research request 1 decides them, not the catalog. Add a test that every skill_node maps to a V3 topic id. Trade-off, stated: the badges lose keywords employers search for (BI, DL, K8s). The honest alternative is to amend V3 and teach them, which costs whole sections of hours that V3:777 has not budgeted.

<details><summary>Evidence</summary>

A grep of learning_roadmap_52_V3.md finds 0 matches for 'Power BI', 'Tableau', 'Looker', 'PyTorch', 'TensorFlow', 'fine-tun', 'LoRA', 'GraphRAG', 'Kubernetes', 'OpenTelemetry', 'mentor', 'GPU' and 'MLflow'. 'deep learning' appears only as the resource link at V3:811 '- [Full Stack Deep Learning](…)'. learning_roadmap.md has them: fine-tun at :74 and :104, GraphRAG at :75 and :104, Kubernetes at :3627, :3653 and :3691, mentor at :4598, PyTorch at :4235. My script count over skill_nodes in src/lib/eligibility/badge_catalog.json finds bi_tools in 7 badges, deep_learning in 12, kubernetes in 3 and mentoring in 3, including the phase credentials. industry_alignment/curriculum_gap_matrix.md:425 reads '**Exact insertion point:** S43 (containers) — add explicit docker exercises… S45 (iac) — add kubernetes deployment exercises (manifest, rollout, rollback).' and :613 reads '`GAP-P3-002` — bi_tools taught in only S09 + S25 — required at DA/DS foundation+', citing role_skill_taxonomy.json. course_requirements.json:4-11 ranks 'External sources' last and names no role taxonomy. The V3-scheduled group: V3:542 '- T1 Concurrencia: threads/processes/async'; V3:599 Dockerfile; V3:601 Compose.

</details>

## Exams and completion on the platform

### [P0] `exam-submit-grades-client-chosen-questions`

*Scope:* course

Section exams are the only evidence the server grades, and they can be forged. exam/submit grades whatever question ids the client sends: from any section, repeated at will, and any number from 1 to 100. It scores over the answers sent, not the questions drawn, so one correct answer gives 100%. Every surviving path under RC-1 rests on these scores: research 4's knowledge-badge option ('calificados en el servidor'), the credential route's count of attempts ≥70, the PDF report, and the cohort dashboards.

**Treatment.** Fix the gating machinery. Submit loads the attempt's variantSeed, rejects any questionId not drawn for that attempt, rejects duplicates, and scores over the number of questions drawn, with unanswered questions counted wrong. Return the answer key only after the last allowed attempt. Add behavioural tests: a one-answer submission scores 1/8, and a foreign id is rejected with 400. Store a grading version on ExamAttempt. Under D11, rows graded before the fix carry no weight as evidence. Until this lands, no claim, including RC-1 option (a), may say the exams are 'calificados en el servidor'.

<details><summary>Evidence</summary>

submit/route.ts:50 `const questionIds = answers.map((a) => a.questionId)` and :52 `where: { id: { in: questionIds } },`. There is no sectionId filter, and the route never reads attempt.variantSeed, although exam/start stores the drawn set there. exam-scoring.ts:113 `const totalQuestions = detailedAnswers.length`; :127 `passed: clamped >= PASS_THRESHOLD`. The schema accepts 1-100 answers and does not require unique ids. I probed under tsx with the real examSubmitSchema and gradeExamAnswers: one answer out of an 8-question attempt gave {"score":100,"passed":true,"totalQuestions":1}; one question from another section alone gave 100; the same id sent 8 times gave 100. The route also returns the answer key after each attempt (`detailedAnswers`, :89, with correctIndex and explanation), so attempt 1's keys can be replayed on attempt 2. tests/adversarial/exam-scoring.test.ts has 14 cases, and none compares the submitted set with the drawn set. WORK_QUEUE and the earlier rounds do not record this (grep for variantSeed and questionId: 0 hits).

</details>

### [P1] `path-gates-unenforced-self-report-completion` ⚖

*Scope:* course

The platform enforces none of the gates the learner is told about.
- Nothing locks a section.
- The self-check can be passed by reading the first attempt.
- Section completion is self-reported, and 8 self-reported sections produce a 'Certificado de Finalización'.
- The badge engine is never called from the app.
- Exam attempts hard-stop at three, with no remedial route.
The relayed P1 on server-side progress is resolved in the working tree. Its fix canonicalises ids into the slug space, which the badge and credential layers do not use.

**Treatment.** FIX THE GATING MACHINERY; the owner decides the policy.
(a) Either implement V3:91 (a section lock, critical items, and a remedial route that reopens attempts after review), or change the copy to 'recomendado' and remove the unlock promise.
(b) Draw self-check retries from other variants.
(c) Rename the PDF 'Registro de progreso' and print the real hour total.
(d) Build one mapper from slug progress to `Sxx-ACTIVITY` evidence, keyed by section.index, and share it between the engine and the credential route.
(e) Show no earned badge state until issuance goes through the engine (synthesis rank 1). Until then, self-reported buttons count as no evidence.

<details><summary>Evidence</summary>

Section lock and self-check:
- SectionView.tsx:871 'Si sacas 70% o más, desbloqueas la siguiente sección.' and Dashboard.tsx:541 promise an unlock. A grep of src for desbloque, unlocked, isUnlocked or locked finds only those strings, which confirms synthesis rank 18's hypothesis.
- handleRetry (SectionView.tsx:852-853) only clears the answers. The questions are the fixed selfCheck list, and :889 highlights the correct option after a submission.

Completion and certificate:
- A section counts as completed at `([, steps]) => steps.length >= 5` (PdfReport.tsx:240), counting the buttons 'Marcar teoría como leída', 'Práctica completada' and 'Proyecto enviado a mi GitHub'.
- `const canGetCertificate = sectionsCompleted >= 8` (:250).
- The certificate is headed `<h1>Certificado de Finalización</h1>` (:603) and prints `<div class="stat-value">1040h</div>` (:618), against a course whose sections sum to 491 h.

Badges and exercises:
- EligibilityEngine is instantiated only in src/lib/eligibility and in tests (tests/e2e_max, tests/adversarial).
- No code writes BadgeAward.
- The engine scores `${section}-YOUDO` activity ids (engine.ts:611, :670), and nothing in src builds them from the slug-keyed progress.
- /api/exercise/attempt has no UI caller.

Exam cap: exam/start/route.ts:41 `if (existingAttempts.length >= 3) {`, :44 'Has alcanzado el máximo de 3 intentos para esta sección'. No code resets it, against V3:91 'Tras tres fallos se requiere ruta remedial y revisión antes de desbloquear la siguiente sección'.

Relayed P1:
- progress/route.ts:39-43 renames each row and lets the current slug win.
- :71 and :122 canonicalise writes with `z.string().transform(renameSectionId)`.
- Migration 20260918030000 moves eight tables.
- I ran tests/adversarial/test_section_id_db_migration.py here: 11 of 11 pass.
- Meanwhile the credential route still filters on 'S04'…'S52' (the 403 already recorded in WORK_QUEUE).

</details>

### [P1] `signed-in-learners-can-never-complete-a-section`

*Scope:* course

For signed-in learners no section ever reaches completion, so the server-backed report, the certificate and the admin and cohort views refuse learners who did the work. For signed-out learners, passing a self-check a second time undoes completion.

**Treatment.** Fix the gating machinery. When exam/submit scores at or above PASS_THRESHOLD, record the quiz step or section completion on the server, and make completion a set operation, not a toggle. Land this together with completion-certificate-unverifiable-and-overclaims, because fixing it unlocks the certificate.

<details><summary>Evidence</summary>

SectionView.tsx:308 `{session?.user ? (` renders ExamView, which receives only sectionId, sectionTitle and onAuthRequired. Otherwise it renders QuizTab, whose onDone toggles 'quiz' and section completion (:317-321). ExamView writes no progress (grep: only ProgressRing), and exam/submit writes no Progress row. When QuizTab is shown the learner is signed out, so its syncToServer POST (progress-store.ts:177-184) gets a 401. Server Progress therefore never holds subStep 'quiz'. PdfReport counts `steps.length >= 5` (PdfReport.tsx:121, :176, :372), and admin-analytics.ts:112 requires `SUB_STEPS.every(...) || steps.size >= 5`, so both show 0 sections for every signed-in learner. Local completedSections is set only from QuizTab (:320), and the server never returns it (progress-sanitize.ts:180-182). Both toggles flip state (progress-store.ts:54-58, :61-66), and QuizTab calls onDone on every pass (≥70, :847-848), so a second pass removes 'quiz' and switches completion off. I verified this by reading the code; I did not run it in a browser.

</details>

### [P1] `completion-certificate-unverifiable-and-overclaims` ⚖

*Scope:* course

The LMS offers a 'Certificado de Finalización' after 8 of 52 self-reported sections. Its ID is recorded nowhere, it prints a fixed 1040 h and 13 integrator projects, and the accompanying progress report labels sections with topics they do not teach.

**Treatment.** Rewrite. Rename the document to 'Reporte de progreso'. Remove the ID, or issue one the server records and /verify can resolve. Compute hours and projects from the learner's own records. Generate section labels from each section's `index` and `title`, with a test that every label matches its title. The owner decides whether any partial-completion document is issued at all.

<details><summary>Evidence</summary>

PdfReport.tsx:179 `if (sectionsCompleted < 8)`. :479 `const certId = `PYDS-${Date.now().toString(36).toUpperCase()}`` is generated on the client, while /verify accepts only `^verify_[a-f0-9]{16}$`. :618 prints '1040h', against index.ts totalHours 491. :622-623 print '13' and 'Proyectos integradores' whatever the learner submitted. The report's SECTION_NAMES (PdfReport.tsx:53-86) include '13. RPA', '14. Security', '20. RAG', '21. FastAPI' and '46. GPU', each printed next to the learner's best exam score. The real titles are 'Automatización robusta de Excel' (S20), 'Documentos, plantillas y reportes trazables' (S21) and 'Ingeniería de datos y orquestación de producción' (S46). To its credit, :609 says 'registro de progreso, no una certificación profesional'. Today signed-in-learners-can-never-complete-a-section masks all of this, because sectionsCompleted is always 0.

</details>

### [P1] `level-promotion-announced-but-not-enforced` ⚖

*Scope:* level

Every level promises promotion only after three capstones, a level regression and a CF checkpoint. The platform records none of the three, so every learner is effectively promoted and the promise is false.

**Treatment.** Until the machinery exists, rewrite the promotion text to describe what actually happens, e.g. 'El siguiente nivel ya está disponible; los capstones son evidencia para tu portafolio y la plataforma no los verifica todavía'. Otherwise, build per-learner capstone submission and review records and gate the next level on them. The owner decides, and should weigh the completion-rate trade-off already stated in gap-rank 18.

<details><summary>Evidence</summary>

V3:44 'La promoción exige aprobar las 13 secciones, los tres gates de capstone, la regresión del nivel y el checkpoint transversal CF correspondiente'. Learners read s13:61: '**Promoción de nivel.** Los tres capstones del Nivel 1 aprobados, la regresión de S01 a S13 en verde y el paquete CF-1 —privacidad, demostración y runbook— entregado.' No API route or Prisma model records a capstone approval, a level regression or a CF receipt: none of the 29 route files handles capstones, and 'CF-1' appears only in section content. course-state/capstone_ledger.json records the course product's own 13 'formally_passed' capstones, not any learner's.

</details>

### [P1] `completion-and-tamper-gates-are-tautological` ⚖

*Scope:* course

The gates that report course and credential health turn unverified claims into green checks, which is exactly what AGENTS.md warns tautological tests do.

**Treatment.** Fix the gating machinery. course_complete_gate should derive capstone state by running each package's tests, and fail on any `assert True  # placeholder`. Replace the tamper test's substring check with a behavioural test: a seeded database, attempts stored under slugs, and an expected refusal when evidence is missing. Rewrite the legacy test as described in legacy-progress-reaches-verified-credential. Under D11, reset the 13 'formally_passed' entries until real tests pass (owner review).

<details><summary>Evidence</summary>

scripts/course_complete_gate.py:22-24 accepts `c.get("state") in good` straight from course-state/capstone_ledger.json, which lists all 13 capstones as 'formally_passed' with 'execution_status': 'pass'. Meanwhile 66 of 77 tests across the 11 packages CP-N1-A to CP-N4-B are `assert True  # placeholder — replace with real check` (my count), e.g. CP-N1-B/tests/test_demo.py:14, 19, 24, 29 and 34, which cover all five brief criteria. tests/adversarial/test_credential_tamper_resistance.py:35-36 passes as long as the route's text contains 'exam', and a route that always returns 403 satisfies that. test_eligibility_engine.py:1162-1188 is the case described in legacy-progress-reaches-verified-credential.

</details>

### [P1] `exam-attempt-cap-refuses-learners-who-can` ⚖

*Scope:* badge

A learner who has the knowledge can be refused permanently. The server allows three attempts per section for life and counts attempts that were abandoned before submission. Nothing lets a learner resume an attempt, and the UI offers attempts the server will refuse. Meanwhile every competency badge needs Sxx-EXAM ≥85, but the exam reports a pass at 70. So a learner who scores 75, 80 and 84 is told they passed each time, yet can never earn any badge that lists that exam. The catalog promises retakes after a cool-down, in 90-day windows.

**Treatment.** Fix the gating machinery; the owner sets the policy. Count only completed attempts, or resume the open one. Either implement the catalog's windowed retake or amend the catalog to three for life and say so on the exam page. Show the badge floor on the result screen. Decide which attempt counts toward a badge (research request 1). Land this with exam-submit-grades-client-chosen-questions and RC-2's remedial route (V3:91).

<details><summary>Evidence</summary>

exam/start/route.ts:35-41 counts every attempt, whether or not it was completed: `if (existingAttempts.length >= 3) {`. ExamView.tsx:374 `const completedAttempts = previousAttempts.filter((a) => a.completedAt)`, and the button at :451 is labelled `${t('exam.startN', lang)} ${completedAttempts.length + 1}`. handleStartExam (:442) always POSTs /api/exam/start and there is no resume path, so three abandoned tabs lock the section while the UI still offers 'intento 1'. Each unsubmitted attempt keeps score 0 and lowers the PDF's average. exam-scoring.ts:8 `PASS_THRESHOLD = 70`, against a component floor of 85. Catalog :1131 'the learner may retake the failed component(s) after a cool-down period', :1132-1134 `"cool_down_days": 7`, `"max_attempts_per_window": 3`, `"window_days": 90`. engine.ts:325 tells the learner 'You can re-attempt this after a short cool-down.' Nothing implements a window or a reset.

</details>

## Capstones and their contracts

### [P0] `capstone-contract-split-across-brief-section-and-package` ⚖

*Scope:* capstone

Each capstone exists as three artifacts, written independently, that disagree on what is graded: the public brief and rubric that learners open, the section You Do increment that the course actually grades, and the course-state package (demo, starter, tests). Synthesis ranks 2, 9, 10 and 25, and four of the verifiers' missed items, all trace to this split. Every brief primer the synthesis wrote patches one symptom of it.

**Treatment.** REWRITE THE DEPENDENCY at the source. The owner names one canonical artifact. I recommend the section You Do increments, because V3 defines every gate by its section's 'Incremento/gate' and the You Do is what is graded. Generate each brief's acceptance criteria and P0 list, and the rubric's tests_required, from that artifact. Add a consistency test that fails when a brief criterion has no matching You Do requirement. Most of ranks 2, 9 and 10 then become brief rewrites, not new teaching. Two cases still need content: CP-N3-A's three data roles (see level3-grades-data-roles-before-teaching-them) and CP-N1-C's correction mechanism (a primer naming S30-T3-B and S39, per verifier 2). Trade-off: the briefs lose criteria that read as more professional than what the sections teach, which is the honest direction under D11.

<details><summary>Evidence</summary>

CP-N1-A: the rubric's tests_required include 'amount=0 → warn' and 'email mal → error' (public/capstones/CP-N1-A_RUBRIC.json:40-46), and the brief's interface returns 'n_ok, n_warn, n_error, error_rate' (BRIEF:40). The S04 You Do instead asks for 'Mínimo: edad, region, monto_ingreso con accept|reject|review' (s04:1782) and returns 'n_total', 'n_accept', 'n_reject', 'n_review' (s04:1791). CP-N3-A: the rubric has 'separación train/dev/test respetada' (RUBRIC:47) and the brief a P0 'Sin separación train/dev/test' (BRIEF:31), while S30's split names only train, test and cross_split (s30:356). CP-N2-A: the brief's P0 is 'Sin memo ejecutivo', while S17 grades 'Memo en español profesional: límites del dataset, no-claims, y delta de leakage' (s17:1614). The S17 You Do is titled '(cierre del nivel)' (s17:1596), but Level 2 closes at S26. CP-N1-C: the P0 'Sin mecanismo de corrección' (BRIEF:31) is not among the S13 You Do requirements (verifier 2). The package starter course-state/capstones/CP-N1-C/STARTER/cp_n1_c.py raises NotImplementedError. 66 of the 77 package tests in CP-N1-A…CP-N4-B are `assert True  # placeholder` (recounted).

</details>

### [P0] `late-gates-prebuilt-authorship-declines` ⚖

*Scope:* level

How much the learner must write at each capstone gate falls level by level, the reverse of gradual release.
- Level 1 gates start from stubs.
- The CP-N1-C and CP-N3-B closes are defect repairs whose comments state the fix.
- The CP-N2-C, CP-N3-C, CP-N4-A (S43) and CP-N4-B closes ship starters that already implement the graded automation, and in S26 the level-regression result is typed in.
- Meanwhile 11 of 13 Level-4 You Dos repeat the same four template requirements.
The course keeps practising the gate idiom it had already secured by S40, while the starter supplies the section's own skill. So Level 3-4 capstones rest on skills the learner never exercises alone.

**Treatment.** REWRITE THE DEPENDENCY: the capstone starters; do not move the gates.
- Each Level 2-4 closing starter keeps its fixtures and harness, but blanks the functions that carry the section's target skill: S26's regression runner, S39's build_bundle stages, the gate and predicate functions in S43, S44 and S47, and S27's tests.
- The planted-defect comments in S13 and S34 stop stating the fix.
- Level-regression fields are computed by re-running the earlier capstones' tests, never typed in.
- The four template requirements move into one shared Level-4 delivery checklist, and each You Do lists only its section-specific items.
Capstone interfaces change, so under D11 record the change and raise it for owner review. Interacts with synthesis ranks 12 and 15 and with research request 3.

<details><summary>Evidence</summary>

I ran all 52 You Do starters under .venv-content in scratch directories:
- S02-S08 and S11-S17 stop at NotImplementedError or a failing assert.
- From S18 on, 32 of 35 starters run to completion as given. The exceptions are S21 (reportlab missing), S32 and S34.

Where the answer is already there:
- s13:2022 '# DEFECT: siempre 1.0 — debe ser 1.0 solo si same doc+block; 0.5 same doc only; else 0.0'.
- s34:2345 `return "review"  # DEFECT: en banda debe ser abstain`; s34:2367 `"accuracy_only": True,  # DEFECT: debe ser False`.
- s26:1734 `"n2_regression": "pass",  # re-run real: CP-N2-A/B/C + privacy + CF-2`.
- s39:2377 `def build_bundle(...)` is the complete bundle, and s39:2501 says '# Aceptación: corre camino normal y force_failure; inspecciona manifest, packets,'.
- s43:2376-2379 already returns CONTINUE, QUARANTINE_IMAGE and TRIAGE_SCAN_FINDING, which is exactly the requirement at s43:2345 'Automatiza un caso normal (CONTINUE/PASS), uno de breach … y uno incierto (`TRIAGE_SCAN_FINDING` …)'. S44 does the same for its requirement at s44:2042.
- s47:1977 `breach_action = "STOP_CANARY" if not breach["canary"] else "ROLLBACK_TO_LAST_GOOD"`, leaving only s47:1989 '# Completa: un segundo breach…'.
- The S27 starter already contains the passing tests (s27:1689 `def test_threshold_branches():`) that s27:1646 grades: 'Demo reproducible: `python -m pytest tests/ -q` o el módulo de asserts del starter'.

The cumulative level regression (V3:99) is pre-filled: s13:1969 '…privacy_sheet y level1_regression_notes ya están listos'.

The four template requirements ('Usa exclusivamente fixtures sintéticos identificados por', 'Automatiza un caso normal', 'comandos locales reproducibles…', 'Registra riesgo residual, responsable, criterio de rollback') appear in S40, S41, S43-S45 and S47-S52. In S43, S44, S47, S48, S50 and S51 they make up 4 of 8 requirements.

</details>

### [P1] `capstone-ledger-green-on-placeholder-tests` ⚖

*Scope:* capstone

The repository's own course-completion gate reports all 13 capstones and 5 checkpoints as passed, based on a hand-set ledger whose required 'tests' evidence is placeholders. This confirms synthesis rank 25, which stayed a hypothesis, and names the consumer that makes the ledger matter.

**Treatment.** FIX THE GATING MACHINERY. Derive each ledger state from running that package's tests, with a check that rejects any test whose body is only `assert True`. Reset the 11 affected capstones to content_authored, and let the course-complete gate fail until real tests exist. Trade-off, stated: a green check turns red, and red is correct (AGENTS.md: a tautological test is worse than none).

<details><summary>Evidence</summary>

scripts/course_complete_gate.py:15-23 reads course-state/capstone_ledger.json and accepts 'formally_passed'. Line :44 is `"capstones_formally_passed_13": cap_ok`. The ledger summary reads 'formally_passed': 13, 'checkpoints_passed': 5. The CP-N1-B entry reads "evidence_required": "scripts, CSV/JSON contracts, quarantine, tests, lineage", "execution_status": "pass". 66 of the 77 package tests in CP-N1-A…CP-N4-B are `assert True  # placeholder` (recounted by grep). CLAUDE.md lists `npm run test:course-complete` among its verification commands.

</details>

### [P1] `cp-n1-c-scaffold-v3-promises-is-not-built` ⚖

*Scope:* capstone

Rank 10 presents the CP-N1-C dashboard as an open choice between rewriting the deliverable and a primer. V3 already made that choice: a provided static scaffold, naming S19 as the lesson that teaches full design. That is D11's primer route written into the contract. What is missing is the scaffold itself.

**Treatment.** BUILD THE MISSING TEACHING ARTIFACT: a provided static HTML scaffold, including the map and tooltip layer that S13's objective names. Pair it with a primer that says what the learner calls, that they fill in only the case data, and that S19 teaches accessible design. Beyond approving the helper, no owner decision is needed. The correction-mechanism P0 gets verifier 2's primer, naming S30-T3-B and S39, or leaves the brief under capstone-contract-split-across-brief-section-and-package.

<details><summary>Evidence</summary>

V3:268 'La UI usa un scaffold estático o plantilla proporcionada; el diseño visual completo se aprende en S19 y ER probabilístico en S30.' course-state/capstones/CP-N1-C/STARTER/cp_n1_c.py: `raise NotImplementedError("Completa este capstone según BRIEF.md")`. s13:359: the gate asks for neither a design system nor Streamlit. V3:39, the Level 1 exit: 'un dashboard de evidencia con revisión humana'. Verifier 2: an HTML-table helper does not cover S13's 'mapa/tooltips' objective.

</details>

### [P1] `cp-n3-a-critical-failure-grades-untaught-dev-split`

*Scope:* capstone

CP-N3-A (gate S30) lists 'Sin separación train/dev/test' as a critical failure. S30 teaches a two-way, entity-disjoint train/test split, and no section before S50 mentions a dev set. So the first graded use of the three data roles is here, not at S33 where gap-rank 8 puts the teaching.

**Treatment.** Rewrite the capstone criterion to S30's contract: 'entidades disjuntas entre train y test; el umbral se elige solo con train; pares cross_split fuera de las métricas'. The alternative is to move the three-roles D3 block that gap-rank 8 places before S33-T3-B forward to S30-T4-A, because CP-N3-A is the first gate that grades it. Not a primer: the learner has to build the split to avoid a critical failure.

<details><summary>Evidence</summary>

public/capstones/CP-N3-A_BRIEF.md:27 '- separación train/dev/test respetada' and :31 '- Sin separación train/dev/test'; the same appears in CP-N3-A_RUBRIC.json:29 and :47 and in catalog.ts:86 criticalFailures, which CapstonesPage.tsx:350-351 renders. s30:33 'Partir pares por entidad sin leakage: train/test disjuntos y pares cross_split fuera de métricas primarias'; s30:356 covers only train, test and cross_split. A grep across the sections for 'train/dev' hits only s50:97. The brief's remedy (line 39) is 'Si no hay separación train/dev/test, divídela.' The package test for this criterion (course-state/capstones/CP-N3-A/tests/test_demo.py, docstring at :42) is `assert True  # placeholder`.

</details>

### [P1] `executed-anchors-need-a-lab-verification-tier-and-cp-n2-c` ⚖

*Scope:* level

RC-7 option (a) names an executed anchor for every Level 3-4 capstone and none for CP-N2-C, although V3 marks S23 and S24 'Entorno: local' and S23 says outright that its graded exercises simulate the browser so they can run without one. So the cause of simulation-by-habit is not only habit. The only gate that verifies lesson code runs each snippet for 8 seconds, in one interpreter, with no services, and skips any missing module. CI installs only the four pinned packages. Under today's machinery, a real Playwright, pytest, FastAPI or Docker anchor would either be skipped as 'missing_dependency', leaving unverified content (AGENTS.md forbids presenting that as a feature), or fail on timeout. The anchors cannot land before the verification tier does.

**Treatment.** FIX THE GATING MACHINERY before building any anchor. (1) One pinned requirements file per level: Level 2 adds openpyxl, jinja2, python-docx, reportlab, matplotlib and playwright; Level 3 adds pytest; Level 4 adds fastapi, pydantic and uvicorn. The runtime audit then refuses to report on a section that imports outside its level's pins, instead of skipping those snippets. (2) A lab tier in CI, separate from the 8-second snippet tier. It starts the services each anchor needs: Chromium, which tests.yml:187 already installs for E2E; a Docker job for S43; uvicorn for S41. It runs each anchor end to end and keeps the transcript. (3) Then BUILD THE MISSING TEACHING under RC-7(a), adding an anchor for CP-N2-C: Playwright against the 'sitio de práctica local con HTML y CSV sintéticos' that S23 already describes (s23:58), keeping the dict model as the E1 rung. Trade-off, stated: this costs CI time and maintenance, and learners must install a browser at S23 and a container runtime at S43, both environments V3 already declares. Without (1) and (2), RC-7(a) would produce unverified code, which is worse than today's honest simulations.

<details><summary>Evidence</summary>

s23:59 reads '**Equivalencias.** Los ejercicios calificados modelan el árbol de la página y la sesión con diccionarios, para que corran sin navegador.' The course's only Playwright import is the optional sketch at s23:63-69 ('# Solo local (requiere: pip install playwright && playwright install)'). V3:373 (S23) reads '**Entorno:** local'. The anchor list in RC-7(a) covers S33/S34, S38, S39, S41, S43, S44, S47 and S48, and nothing in S22-S26. scripts/python_content_runtime_audit.py:35 sets `TIMEOUT_SEC = int(os.environ.get("PYARCANA_SNIPPET_TIMEOUT", "8"))`, and :450-456 map 'ModuleNotFoundError' to `"status": "skip", "reason": "missing_dependency"`. .github/workflows/tests.yml:291 runs `python3 -m pip install -r requirements-content.txt`, which pins only numpy, pandas, scikit-learn and scipy. In course-state/python_runtime_audit_report.json, dependency_visibility.missing includes 'fastapi', 'pydantic' and 'pytest', so S27's pytest and S41's FastAPI could not be verified even if they were written. My scan of COURSE_SECTIONS code blocks finds openpyxl imported in 43 S20 blocks, jinja2, docx and reportlab in S21, and matplotlib in S19, none of them pinned. .venv-content has openpyxl, jinja2, docx, matplotlib and playwright installed without pins, so the local green run and CI verify different things.

</details>

### [P1] `capstone-increments-never-compose-until-cp-final` ⚖

*Scope:* course

The course calls its capstones cumulative, but no increment uses the code or output of the one before it. Each You Do brings its own data and its own functions. The first gate that makes the learner integrate earlier work is CP-FINAL at S52. It asks for 12 capstones feeding one product through 12 versioned interfaces, and no earlier gate had the learner build any of those interfaces. It also asks for a pytest regression across S1-S52. The level-closing regressions at S13 and S26 come already filled in. Along the way, S10 has the learner repeat S08's ETL (the extract-transform-load pipeline) with the finished code already in the starter, which spends time on a skill already secured while the answer is visible.

**Treatment.** No primer applies: the learner does the integrating, so this is Make-level. The owner decides, and research request 1 informs the choice.

(a) BUILD THE MISSING TEACHING.
- Each level closer (S13, S26, S39, S51) gets an integration increment. The learner puts the level's three increments behind the brief's interface and runs a real regression, which replaces the pre-filled notes and the typed 'pass'.
- Each later starter imports the previous increment from a course reference module ('la tuya o la de referencia'), so a learner whose earlier increment failed can still go on.
- CP-N1-A keeps one schema from S01's CSV onward. Under RC-5 route 1, S04 reads the `raw_line` form from stdin, which V3:169 already asks for.

(b) REWRITE CP-FINAL. It integrates course-provided reference subsystems behind the brief's contracts.
- Strike 'Los 12 capstones previos alimentan este producto' and 'regresión S1–S52'.
- Narrow the brief's 'Sí afirmar' at :66 to what was actually integrated.
- The course-state package already holds an integration scaffold (CP-FINAL/STARTER/scaffold.py imports `integration.platform`). HYPOTHESIS: learners do not see it, since CapstonesPage serves only the brief and the rubric (previous verifier).

Under either option, recompute S52's hours (RC-10).

Trade-offs:
- (a) adds four blocks, and unless reference modules ship, one early failure blocks every later increment.
- (b) keeps the course shorter, but the final claim shrinks.

Test: a scan that fails when a You Do context or requirement says it builds on 'SNN' while its starter neither imports nor reads that section's artifact.

<details><summary>Evidence</summary>

Notation: sNN:line means src/lib/course/sections/sNN-*.ts.

Import scan. I dumped COURSE_SECTIONS with tsx to my scratchpad and scanned every You Do starter's imports. The only learner-written module any starter imports is S10's own familiarity_core, which S10's bootstrap creates. From S14 on, reads of files or databases appear only in S20 (its own workbook), S28/S29 (sqlite `:memory:`) and S39 (an append-mode log).

CP-N1-A uses five different record schemas:
- S01: s01:2374 `# client_id,full_name,country,signup_date,monthly_amount`.
- S02: `parse_client(nombres, apellido_paterno, apellido_materno, contacto, direccion, edad)`.
- S03: `validate_edad` / `validate_region` / `validate_monto`.
- S04: s04:1809 `{"edad": 30, "region": "Lima", "monto_ingreso": 0, "raw_line": "30|Lima|0"}`.
- The brief: id/name/email/amount.
The text promises continuity: s01:2336 'S02–S04 añadirán el script de admisión sin rehacer esta base'; s03:2263 'Parte del parser de S02'; s04:1754 'Sobre el parser de S02 y las reglas de S03'. Yet neither the S03 nor the S04 starter calls parse_client, and no S02-S04 starter reads data/clients_synthetic.csv.

CP-N1-B: the S07 starter defines normalize_nombre, normalize_email and normalize_phone again instead of extending S05's. The S08 starter calls none of the S05-S07 functions. s08:1933 'Integrar normalizadores (S05–S07) y modelo en memoria (S06) donde aplique' makes that optional.

S10: the requirement at s10:2126 says ingest '(reutiliza lo que ya construiste en S08)', but the starter ships a complete `def run_ingest` (s10:2205) with its own id,name,monto schema.

Level 2 and the level closers:
- s18:523 'Partimos del dataset limpio y el memo de límites de **S17 (CP-N2-A)**', yet the S18 You Do generates its own data with `rng = np.random.default_rng(18)`.
- S26 orchestrates 'ingest→validate→analyze→ai_assist→report→approve→draft_email' with advance, can_draft, run_all and package_e2e over step names, and calls no S20-S25 code. s26:1734 hard-codes `"n2_regression": "pass"`.
- s13:1969 says the regression notes 'ya están listos'.

CP-FINAL:
- s52:57 'Recibes los artefactos congelados de S01–S51'; s52:1947 '# Los 12 capstones previos alimentan este producto'; s52:2004 `regression_cmd = "python -m pytest tests/ -q"  # ajusta a tu suite local S1–S52`.
- CP-FINAL_BRIEF.md:7 'Integrar los **doce** capstones previos … mediante **interfaces versionadas**', and :66 lets the learner claim 'integré doce capstones'.
- None of the 12 interface result types the briefs name (IntakeResult, EtlManifest, ReviewPacket, EdaReport, ReportBundle, RpaAudit, ClusterSet, GraphCase, TriageDecision, ApiResponse, DeployRecord, IntegrationBundle) occurs anywhere in the 52 active section files: the grep count is 0 for each.
- The final leap is also unpriced: s52:19 `estimatedHours: 29`, while s52:1935 plans 'Hitos 80 h'.

</details>

## Practice design and the learning ramp

### [P0] `simulation-by-habit-at-levels-3-4` ⚖

*Scope:* course

One cause lies behind synthesis ranks 12, 13, 14, 21 and 22, the onramp's E2/E3 assess/decide template, and the CLAIMED_NOT_TAUGHT badge rows. Levels 3 and 4 were written as predicates over dictionaries of flags. That includes sections V3 labels local, demos the data model already marks local-python, and tools that are in the standard library. So this is one decision about the execution contract, not a set of teach-in-place items.

**Treatment.** First, one owner decision on the execution contract per level. Then BUILD THE MISSING TEACHING as one executed anchor per capstone, not one per gap. CP-N3-B: a scikit-learn Pipeline plus GroupKFold block in S33/S34 (already pinned). CP-N3-C: a ThreadPoolExecutor plus asyncio.wait_for demo in S38, and PSI computed from data in S39 (standard library). CP-N4-A: FastAPI plus Pydantic in S41 (add pins), and a docker build/run with a health check in S43, with commands and output stated. CP-N4-B: one workflow file in S44. CP-N4-C: a chunker plus a lexical scorer in S48 (standard library). Everything else is REWRITTEN to 'audit / decide / simulate' wording in outcomes, P0s and badge claims (rank 22), so that no claim outruns an anchor. E2/E3 exercises in S32-S35 and S39-S52 then apply the anchor tool where one exists (V3:87). Trade-off: a local-only marker means some demos cannot run in the Pyodide playground. The cheaper alternative is to re-scope everything: fully honest, but then the Level 4 exit promises less.

<details><summary>Evidence</summary>

S38 marks its demos `environment: "local-python"` (s38:442, :480, :509; 8 of 8 by grep, and the same holds in S41, S43 and S44), yet s38:234 reads '# En async real: asyncio.wait_for cancela la tarea; aquí solo clasificamos.' asyncio and concurrent.futures are standard library, and so is everything needed for S39 drift, S30 Levenshtein, the S46 Kahn trace and an S48 lexical scorer. V3:118 defines local-python as 'Git, venv, archivos, Excel, OCR, browser RPA, servicios y contenedores'. V3:599 'T1 Imágenes: Dockerfile, layers y cache'; V3:601 'T3 Compose: API/worker/DB/cache'. requirements-content.txt pins only numpy, pandas, scikit-learn and scipy. scripts/python_content_runtime_audit.py:36-60 lists matplotlib, fastapi, pydantic and torch as OPTIONAL_MODULES, which are skipped on ModuleNotFoundError. s43:779 is a dict of flags; s47:1965 and s48:1942 are literals. scikit-learn is pinned but imported by no section (verified by both layers).

</details>

### [P0] `practice-rungs-answer-adjacent-no-unaided-make`

*Scope:* course

Across the course, the practice rungs almost never ask the learner to write code without help before the graded You Do. The E2 'independent' rung is usually a one-line repair of a planted defect. The fix is often written out in the instruction or a hint, both hints show from the start, and the solution is one click away with no attempt required. The ramp is also a sawtooth rather than a fade. Practice demand collapses to one-line edits across S14-S30, then jumps in S31-S34.

**Treatment.** BUILD THE MISSING TEACHING, then FIX THE GATING MACHINERY.
(1) In each subtopic, E2 becomes a completion problem: the function signature and tests are given, the body is blank, or it is a faded Parsons problem. E3 becomes a transfer task on a blank page. Keep planted-defect repair as E1 or as a separate debugging exercise; D6 lets such an exercise carry no subtopic id.
(2) Hints name the idea, never the line of code.
(3) Hint 2 renders only after hint 1 is opened. 'Ver solución' appears only after the learner has opened the hints or confirmed an attempt.
(4) Add a ratchet test that fails when an E2's changed-line count is 1 or less, or when its changed lines appear verbatim in its instruction or hints.
The scope depends on research request 1.

<details><summary>Evidence</summary>

Method (a heuristic; the counts are mine): I dumped COURSE_SECTIONS through tsx and diffed each of the 1,248 We Do starter/solution pairs with difflib, counting changed lines that are not comments or blank.

Results: the median changed-line count is 2 for E1, 1 for E2 and 3 for E3. 212 of 416 E2 exercises change one line or fewer, against 130 of 416 for E1. 859 of 1,248 starters carry a planted-defect marker (regex `DEFECT|Bug a corregir|FALLO|defecto intencional`). At least 174 exercises state the whole change in their instruction or hints; this is a lower bound, and in S14-S26 the figure is 81 of 312.

Share of exercises that need 5 or more changed lines, by band: S01-S04 41%, S05-S08 19%, S09-S13 42%, S14-S17 6%, S18-S21 6%, S22-S26 13%, S27-S30 11%, S31-S34 53%, S35-S39 36%, S40-S43 35%, S44-S47 31%, S48-S52 39%.

Two examples where the answer is given:
- s17:704, S17-T1-A-E3, whose kind is transfer, has instruction '2. Haz `cli.merge(tx, on='cliente_id', how='inner')` sin deduplicar.' That is the exercise's only changed line.
- s16:1135, a hint of S16-T2-B-E3, reads "print('card_ok' if df['id'].nunique() == len(df) else 'card_bad')." That is the solution.

UI:
- SectionView.tsx:579-588 renders every hint at once, although types.ts:95 says '/** V3: two progressive hints; UI shows both when present */'.
- SectionView.tsx:613-625: the 'Ver solución' toggle requires no attempt.
- SectionView.tsx:600-606: the starter is a read-only CodeBlock, and the card has no runner.
- /api/exercise/attempt has no caller in src/components (grep).

Contract: V3:87 'E1 muestra proceso; E2 retira parte del apoyo; E3 cambia el contexto y exige transferencia'.

The synthesis's onramp saw this only for the S04 loop header and for planted-defect E1 exercises.

</details>

### [P0] `phase1-data-core-one-line-practice-then-compose-gate`

*Scope:* level

In the Level-2 data core (S14-S21, which feeds CP-N2-A at S17 and CP-N2-B at S21), practice never goes past a one-line edit. The graded You Dos of S14, S15, S16 and S17 then require multi-step pandas functions written from a stub. The first multi-step pandas function the learner writes is the capstone increment itself.

**Treatment.** BUILD THE MISSING TEACHING; not a primer, because the learner writes these functions (Make-level).
- In each of S15, S16 and S17, add one E3 per topic that has the learner write a 4-6 line pandas function into a blank body.
- Before the S17 You Do, add a faded worked example in S17-T1/T2: merge → anti-join → as-of sum → delta, with each later step left for the learner.
- Keep the one-line repairs as E1.

<details><summary>Evidence</summary>

Measured with the same difflib method on S14-S21:
- The median changed-line count is 1 for E1, for E2 and for E3.
- Only 12 of 192 exercises need 5 or more changed lines.
- The E3 examples are single-line fixes whose code is dictated: s17:704 and s16:1135 above, and S15-T3-A-E3, whose instruction says 'Añade `errors='coerce'`'.

I ran every You Do starter under .venv-content. The S14, S15, S16 and S17 starters all stop at NotImplementedError. For S17, s17:1629 reads '# Contrato: implementa la función (no dejes NotImplemented)', and `portfolio_summary` must compute a left merge with an indicator, an anti-join count, an as-of cutoff sum, a leakage delta and a reconciliation flag, per its docstring.

The synthesis rates every S14-S17 skill TAUGHT_IN_ORDER, because it judged whether each skill is taught, not whether the learner ever writes it.

</details>

### [P1] `theory-playground-legacy-topics-out-of-order`

*Scope:* course

The Theory tab's 'Pruébalo tú mismo' editor is the only Python the learner can run on the page, and in at least 33 of 52 sections it runs another section's topic. Its demos come from the retired roadmap and preview topics out of order. At S10 it runs scikit-learn, which contradicts the synthesis's statement that no active section imports sklearn.

**Treatment.** REWRITE THE DEPENDENCY.
- Replace each off-topic playground with the section's own I Do demo marked `browser-pyodide` (48 exist), or remove the playground where none fits.
- Under preservation and D6 question 2, move the legacy demos to the sections that teach them rather than deleting them: sklearn to S33, the asyncio simulation to S38, hashing to S42, semver to S10.
- Rewrite S52's demo so it models evidence-backed claims.
- Key the map by section index, or add a test that each playground's title shares a learning-outcome keyword with its section. That test should also fail when a slug rename orphans a demo.

<details><summary>Evidence</summary>

The component comment reads 'appears in every section's theory tab' (SectionView.tsx:461-462 `{/* Interactive playground — appears in every section's theory tab */}`).

The demo map is keyed by slug. Comparing each section's title with its demo title:
- S10 'Módulos, packaging y CLI' shows 'Practica scikit-learn' (SectionView.tsx:1292). The code imports `from sklearn.linear_model import LogisticRegression` (:1294) and runs `cv_scores = cross_val_score(model, X, y, cv=5, scoring='accuracy')` (:1317). CodePlayground.tsx:132 `sklearn: 'scikit-learn'` loads the package automatically.
- S11 (OOP) shows 'Practica testing con asserts' and imports numpy, which is taught at S14.
- S14 (NumPy) shows 'Practica seguridad: hashing y cifrado'.
- S27 (pytest) shows 'Practica concurrencia con asyncio'.
- S41 (FastAPI) shows 'Practica QLoRA concepts (simulado)'.
- S44 (CI/CD) shows 'Practica CLIP y Whisper (simulado)'.
- S52's expected output teaches fabricated CV impact: SectionView.tsx:4121 '1. ✓ Construi pipeline de churn con XGBoost, reduciendo churn 15% y salvando S/2M anuales'.

On-topic: S01-S09, S30, S35, S38, S42, S45, S49. Borderline: S26, S33, S39, S52.

Neither WORK_QUEUE nor CONCEPT_QUEUE records this; grep 'playground' finds only the S01 entrypoint rows. Only the per-section wiki notes it (audit/fixer/wiki/S52-20260908.md F04).

The map survived the S01-S13 rename only because it was re-keyed (commit a492d8ea). Rename batches B-D (WORK_QUEUE row 22) must re-key it too, or the playground silently disappears.

</details>

### [P1] `topic-evaluations-are-the-v3-rung-and-their-gate-reads-a-constant`

*Scope:* course

RC-6 question 2 offers 'E3 in every subtopic (…386 subtopics)' or 'the 208 topic evaluations V3 already requires (196 missing)' as if the two differed in scope. V3 defines each topic evaluation as two authentic tasks, one per subtopic. Both options therefore mean 416 generative tasks, and doing both doubles the cost. The synthesis also missed two facts that hide the shortfall. First, the invariant gate that should report the 196 missing evaluations asserts a JSON constant, not the content. Second, 208 'topic evaluations' already exist as generated drafts in course-state, and all 416 of their tasks share one deliverable template and use pre-V3 slugs. V3:777 prices this authentic-evaluation layer into every section's hours. It is a component the roadmap promises and the course never built, and its gate is green.

**Treatment.** (1) FIX THE GATING MACHINERY. Compute the invariant vector from COURSE_SECTIONS as floors (D6): at least 4 mounted TEs per section, at least 1 task per subtopic, at least 3 exercises per subtopic. The 196-evaluation shortfall then shows red, and a ratchet brings it down section by section. Prove the check fails on today's tree and passes for S02. (2) BUILD THE MISSING TEACHING. Hand-write the 196 missing TEs, one task per subtopic, to the S02/S30 standard. Each task is that subtopic's generative rung: a given signature, fixtures and tests the learner runs, and a blank body. Use the generated drafts only as a checklist of topic summaries, and never mount them. (3) E3 stays 'transfer with a changed context' (V3:87). Its repair is RC-6's leak removal, not a second rewrite of 386 exercises. The 0-3 TE rubric is self-assessment until RC-1's grader exists, and the page says so. Build by level, starting with the topics that feed gate sections. Trade-off, stated: 416 tasks is the largest authoring cost in this map, but it sits inside V3's per-section hours, not on top of them. The unaided attempt also comes at the end of each topic rather than right after each subtopic; research request 2 decides whether that is enough.

<details><summary>Evidence</summary>

V3:33 '…termina con una evaluación auténtica que cubre ambos subtemas'. V3:89 'Cada evaluación de tema contiene dos tareas auténticas —una por subtema— y una rúbrica 0–3'. V3:91 'Las dos tareas forman un único artefacto integrado y se califican conjuntamente una sola vez sobre 12'. scripts/v3_regression.spec.ts:48 '// Canonical invariant vector from V3 prompt' and :59 `expect(iv.topic_evaluations).toBe(208)` read the declared course-state/course_requirements.json:23 `"topic_evaluations": 208`, not the content. In the COURSE_SECTIONS dump, topicEvaluations are mounted only in S01, S02 and S30 (4 each, 12 in total). course-state/topic_evaluations/s01_te.json…s52_te.json were generated 2026-07-20 by scripts/generate_topic_evaluations.py and hold 208 TEs and 416 tasks. With the topic summary and ids substituted out, my script finds every deliverable is the same string: 'Artefacto ejecutable o documentado (script/notebook/README) sobre datos **sintéticos** que demuestra: <X>. Incluye 3–6 oraciones de justificación…'. s21_te.json has `"section_id": "fastapi"` for 'Documentos, plantillas y reportes trazables', and the generator's SECTION_IDS map 3→'data-structures' … 13→'rpa-automation'. The hand-written ones set the standard to follow: S02-T1-TE-2 'safe_int con vacío, espacios y letras', and S30-T4-TE-1 (s30:2104). SectionView.tsx:773-800 only displays them; there is no submission. V3:777 counts 'cuatro evaluaciones' inside each section's 16-24 h.

</details>

### [P1] `l2-data-stack-dropped-after-s20`

*Scope:* level

Level 2 spends seven sections building NumPy, pandas and matplotlib. From S21 on, no code in any section uses them again. The Level 3 data and ML sections are written in the standard library over lists and dicts: S28 (data tests), S32 (feature engineering), S33 (supervised ML), S34 (metrics) and S36 (clustering, PCA). So is Level 4's S46 data engineering. At the L2→L3 boundary the learner goes from DataFrames back to hand-built lists, and pandas goes unused for 32 sections. The browser runtime does not force this choice. If RC-7's scikit-learn anchors are built as proposed, the learner has to pick up pandas and NumPy arrays again 13 sections after last using them. That turns the anchor itself into a leap.

**Treatment.** REWRITE THE DEPENDENCY of RC-7's Level-3 anchors, and order this change after RC-8.
- The S32-S34 anchor starts from a pandas DataFrame produced by the learner's own S16/S17 cleaning code, or its reference, and ends in a scikit-learn Pipeline.
- RC-8's per-case fixtures are DataFrames.
- S28's data tests assert on DataFrames.
- S46 reads and writes files through pandas.
- The standard-library demos stay as the I Do that shows the mechanism. Each Level-3 data or ML topic then gets one E3 that redoes the same step with pandas, NumPy or scikit-learn: a transfer rung, which is what V3:87 asks of E3.

Trade-off: standard-library code keeps the mechanism visible and needs no install; the libraries match what the badges claim and what an employer tests. Research request 2 decides whether the course goes library-first or from-scratch-first. Either answer brings pandas back into Level 3.

<details><summary>Evidence</summary>

Scan of the theory, I Do, We Do and You Do code in the tsx dump:
- `pd.` appears in S15-S20 only.
- `np.` appears in S14, S18 and S19 only, plus one theory `import numpy as np` at s33:264.
- The last You Do that imports pandas is S19 (s19:1670 `import pandas as pd`). S20 imports it only in I Do and We Do (s20:246, :547).
- Across the source files S21-S52, the word 'pandas' appears once, in prose (s37:94). grep -c pandas gives 0 for s28, s29, s32, s33, s34, s36 and s46.

The sections say so themselves: s36:68 '**Stack.** Biblioteca estándar (`statistics`, listas). sklearn se cita como referencia profesional, sin exigirlo en los ejercicios.'; s46:44 'Modelas los contratos con la biblioteca estándar'. In S36, PCA is done 'con pesos fijos `w0,w1` documentados a mano' (s36:592).

The runtime allows the libraries: CodePlayground.tsx:129-132 auto-loads numpy, pandas, matplotlib and scikit-learn.

The synthesis map lists pandas_numpy among the Phase-0 credential's skill nodes.

</details>

### [P1] `tools-taught-once-then-demanded-cold` ⚖

*Scope:* course

Beyond pandas, the tools that late gates rely on are practised in a single section and then not again until a gate demands them unaided.

pytest:
- The S27 starter already contains the graded tests, and installing pytest is optional.
- No code after S28 defines a test function.
- Yet S29 and S31 require test suites written from nothing, S44 a lint/types/tests matrix, and CP-FINAL a pytest regression across S1-S52.
- So the first test suite a learner writes from a blank file is S29's.

logging: S09 is the only active section that imports it. S13 and S51 grade log hygiene and observability, and neither uses the module.

sqlite3: appears in S12, S28 and S29 only.

**Treatment.** BUILD THE MISSING TEACHING as retrieval, not as new subsections:
(1) Make pytest mandatory at S27, with its version pin. From S28 on, every You Do keeps its tests in a tests/ folder that pytest discovers, so the S1-S52 regression CP-FINAL names exists by construction.
(2) The S29 starter ships one failing Arrange-Act-Assert test and leaves the rest blank, so the first suite written from nothing has one step of scaffolding.
(3) S13, S38 and S51 require the S09 logger and masking filter instead of a dict of log lines.

Then FIX THE GATING MACHINERY: add a ratchet that fails when a You Do requirement names a tool (pytest, logging, sqlite3, pandas) and no practice code in the N sections before it uses that tool. N is the owner's choice.

This complements WORK_QUEUE row 9, which covers what S27 says about pytest, not what later gates demand. Trade-off: starter changes in about 25 sections. The gain is that late gates stop grading skills the learner last used 20 or more sections earlier.

<details><summary>Evidence</summary>

Practice-code scan of the dump:
- `logging.` appears in S09 only.
- `import logging` appears only in s09 among the active files; the other hits are in the orphaned s11-advanced-topics.ts. s38:2166 lists logging only as a resource link.
- `def test_` count is 0 in every active file S29-S52; S28 has 6.
- sqlite3 appears in S12, S28 and S29; `csv.` in S08 and S10; `with open(` in S08, S10, S21 and S39.

pytest:
- s27:1634 'Construye un pequeño conjunto de pruebas con pytest (o, si aún no instalas pytest, un módulo con `assert` equivalentes)'.
- The S27 starter says 'Aquí el mismo contrato corre como módulo con assert + print' and already defines `test_threshold_branches` (s27:1689, previous round).
- s29:2250 'Entregables: schema.sql, seed.py, repository.py, test_store.py (suite que falla hasta implementar)'. The S29 starter (74 lines) holds no test code.
- s31:2068 'Tests mínimos: no self-loop basura, provenance presente, construcción idempotente, ego-k no excede radio'. The S31 starter has no test functions.
- s44:2038 'Incluye matriz lint/types/tests con artifacts.'
- s52:1929 'Incluye tests/evals/red team/performance'; s52:2004 is the pytest regression command.

Logging demands: s09:2243 'Ningún log de demo contiene PII completa'; s13:1959 'Runbook con regresión level-1 (S01–S13) y respuesta a PII en log'.

</details>

## Authority, numbering and scope

### [P0] `level3-grades-data-roles-before-teaching-them`

*Scope:* level

Two of Level 3's three capstones grade data-role separation as a P0, the course never teaches train, validation and test as one unit, and the middle capstone's reference starters commit the leak. Synthesis rank 8 places the teaching block at S33-T3-B, one gate after the first capstone that grades it. Ranks 15, 16 and 19 depend on per-case fixtures that will exist only once the split lands.

**Treatment.** BUILD THE MISSING TEACHING once, at the first gate that grades it. Amend CONCEPT_QUEUE #194 from two roles to three: an S30-T4-A supporting block where train sets the weights, dev (validation) picks the threshold, and test is read once. Use the brief's own word 'dev', and give holdout one glossary meaning. S32-S34 then open by recalling this block, and rank 8's S33 block becomes recall plus folds rather than a first teaching. REWRITE the S33, S34, S39, S47 and S50 fixtures as one per-case family carried forward, so that rank 19's paired-bootstrap primer, rank 15's drift statistic and the S39 model card read computed numbers. Order: this lands before ranks 15, 16 and 19. Trade-off: S30 gets heavier, and ER threshold tuning is an unusual first context for validation (research request below).

<details><summary>Evidence</summary>

CP-N3-A (gate S30) has the P0 'Sin separación train/dev/test' (BRIEF:31) and the rubric line 'separación train/dev/test respetada' (RUBRIC:47). CP-N3-C (gate S39) has the P0 'Fuga de datos' (BRIEF:32). S30 teaches two roles: s30:356 'Un par es `train` solo si **ambos** extremos ⊆ train; `test` solo si **ambos** están fuera de train'. CONCEPT_QUEUE #194 queues the same two roles ('Train pairs set weights and thresholds… Test pairs never touch calibration'). Rank 8: 'Add a block with no subtopicId before S33-T3-B'. The CP-N3-B starters select and report on the same points (s33:2471-2475; s34:2349-2360, confirmed by both verifiers). Rank 19's verifier: gate fixtures hold aggregates only, for example s47:1965 `log_run({"depth": 4}, 0.81, 0.805, 42, 0.01)`. V3:121 'Desde S32: prevención de leakage' comes two sections after CP-N3-A grades it.

</details>

### [P1] `stale-pre-v3-numbering-is-one-regeneration` ⚖

*Scope:* course

Synthesis ranks 2, 11 and 23, the gate-driven insertions in S19-T4-B, S33-T4-A and S45-T3-A, and the roadmap_verdict are one cause: artifacts keyed to learning_roadmap.md's numbering. The fix is one generator plus one test, not edits artifact by artifact. The synthesis misses two victims of this cause that learners see: the PDF progress report, and content that gates inserted into the wrong sections.

**Treatment.** One owner decision: V3 becomes primary, as the synthesis proposes, and course_requirements.json:7 and :51 are amended. Then REWRITE THE DEPENDENCY with a single generator that reads the sections' `title:` fields and writes the brief Prerrequisitos and Remediación lines, the PdfReport SECTION_NAMES labels, the badge required_sections and skill_nodes, and the industry_alignment graph builder. Add one test that fails when any of these names a section with a topic other than its V3 title. Gate-driven insertions are rewritten in place under D6 question 1, not moved (the verifiers' amendment to ranks 7 and 23). Relabel the PdfReport first, because the PDF is what a learner hands to an employer.

<details><summary>Evidence</summary>

PdfReport.tsx:40-92 now receives canonical ids (commit 29bf0240) but labels them with the old topics: '"evidence-dashboard": '13. RPA'', 'security: '14. Security'' (S14 is NumPy), '"computer-vision": '23. CV'' (S23 is Playwright), 'multimodal: '44. Multi-Modal'' (S44 is CI/CD), '"gpu-computing": '46. GPU'' (S46 is data engineering). By my reading, 29 of the 52 labels name a topic the section does not teach. Commit 96ea4408 'feat(gate-12): teach SQL + leakage prevention in S19' credits 'sql_competency: S19 (new)'. Commit 0c2329f6 added PyTorch prose to S33, Docker/K8s prose to S45, and A/B-testing theory to s09-sklearn.ts, which is now orphaned. course_requirements.json:7 reads 'learning_roadmap.md (52 sections, primary)'.

</details>

### [P1] `primer-must-name-a-lesson-that-exists-and-recalls-it`

*Scope:* course

Counted across the whole course, the synthesis's primers do not empty any level of meaning. Level 1 carries about six, and five of them point inside Level 1 (S09-S11, and S13 to S19/S30); Levels 2-4 carry one or two each. The real failure is different: four of the synthesis's primers name a lesson that does not teach the topic. A D11 primer is valid only when the named lesson exists and takes the promise up again, and S09 already shows a promise that is never taken up.

**Treatment.** FIX THE GATING MACHINERY before writing any primer. Add a gate shaped like test_forward_dependencies.py: every 'en Sxx' or 'por ahora' promise must name a section whose source mentions both the originating section and the topic. A primer treatment in this map becomes admissible only when it passes that gate; rank 24's S09 opening is the first instance. At this density no level stops meaning anything. The limit that matters is that a primer never stands in for a P0 the learner authors (D11's line between Make and Use).

<details><summary>Evidence</summary>

Primer targets refuted or amended by the verifiers: rank 15 names 'S46 and S51', neither of which computes distribution drift (s46:275 is schema drift; s51:272 is the literal 'drift 0.04'). Rank 17's Use-level route names 'the block beside T1-A', which exists only under the other route. Rank 20's `@` primer names 'S14-T2', which teaches broadcasting. Rank 9 names 'S18 explains quartiles', while WORK_QUEUE row 5 teaches quartiles in S16. S02 names S09 on seven lines (s02:59, 61, 188, 213, 359, 451, 453), and S09 has zero references to S02-S04 (verifier grep).

</details>

### [P1] `hour-budget-unpriced-across-proposed-blocks` ⚖

*Scope:* level

Every section is estimated at 9 or 10 hours (S52 at 29), while V3 plans 16-24 hours per section. The synthesis and its verifiers propose about 25 new supporting blocks and several capstone rewrites, and cost none of them. The 13-section invariant holds, because every addition is a D6 block with no subtopicId. The advertised course length does not hold, and Level 3 absorbs the most.

**Treatment.** Each block ships with an hour estimate, and estimatedHours is recomputed per section, so the displayed number follows content as D6 requires. The owner picks one course length (V3's 1,040 h or a re-estimate), and V3:777's pilot measurement replaces both. Level 3 needs an order inside the level: data roles (S30), then the sklearn anchors (S33/S34), then the S39 drift and model-card rewrite. Trade-off: the 491 h on the landing page grows.

<details><summary>Evidence</summary>

index.ts:64 `totalHours: 491`. Per-level sums of the estimatedHours fields: L1 118, L2 118, L3 118, L4 137. V3:777 'Cada sección parte de 16–24 horas según dificultad'; V3:775 plans 960 + 80 h. My tally of proposed additions from the synthesis and verifiers: L1 about 4 (dict S02, traceback S02, lambda S05/S06, optional GROUP BY S12). L2 about 4 (describe/value_counts S15/S16, quartiles S16, groupby S16, plotting helper S18). L3 about 10 (data roles S30, Levenshtein S30, GROUP BY bridge S29, pytest S27, F1 S30, sklearn blocks S32-S34, CV S33, linear regression S33 or S18, concurrency S38, drift S39). L4 about 7 (FastAPI S41, threat model S42, docker S43, workflow S44, Kahn trace S46, chunker plus lexical scorer S48, judge TPR/TNR S50).

</details>

### [P1] `v3-level-exits-overclaim-titles-and-verbs` ⚖

*Scope:* level

If V3 becomes the primary authority, as the roadmap verdict recommends, its level exits carry job-title language the repo forbids and verbs for things the course never has the learner do. The learner-facing catalog.ts states different, weaker level promises.

**Treatment.** Rewrite. Add this to the roadmap verdict's list of V3 amendments for the owner: replace V3:39-42 and the level headers at :125, :270, :415 and :560 with catalog.ts's LEVELS names and exitCapability, and state each verb as the course practises it ('modelar el despliegue', 'simular un navegador') until the teaching in gap-ranks 12-13 lands. Once V3 is the authority, add it to the paths test_level_language.py scans.

<details><summary>Evidence</summary>

learning_roadmap_52_V3.md:41 'Experto capaz de diseñar ER, grafos y ML responsable, medir errores y operar pipelines robustos'; :42 'Máster capaz de desplegar, gobernar, observar y defender un sistema...'; :125 'Nivel 1 — S1–S13 · Novato a principiante avanzado'; :560 'Nivel 4 — S40–S52 · Experto a máster'. tests/adversarial/test_level_language.py:6 forbids 'experto · máster · master · senior'. catalog.ts:35 'No constituye nivel “senior”, “staff” ni “experto” organizacional'; :38 'No otorga título de “arquitecto”, “master”'. The verbs against the teaching: 'desplegar' (S43-S45 check dict flags; no docker build or run), 'operar pipelines robustos' (no concurrency is ever executed), 'ML responsable, medir errores' (CP-N3-B picks the threshold and reports on the same points), L2 'automatizar ... web' (s23:59 simulates the page with dicts), L1 'un dashboard de evidencia' (V3:39) against catalog.ts:28 'con apoyo'.

</details>

### [P1] `v3-primary-settles-five-owner-decisions-and-fixes-their-order` ⚖

*Scope:* course

The synthesis gives the owner eight OWNER_DECISION root causes (RC-1, RC-2, RC-3, RC-4, RC-5, RC-6, RC-7, RC-10) as if they were independent. They are not. Once RC-3 makes V3 primary, V3's own text already answers five of them: RC-4 (which artifact is canonical), RC-6's first two questions, RC-7's execution contract, RC-2's gating and RC-10's hours method. Each of those becomes 'accept V3's line or amend it'. Four choices stay open: V3 primary together with its amendment list; the RC-5 route (the one place V3 contradicts itself on sequencing); RC-1's credential class (V3 defines no badges); and RC-6's hint gating. The synthesis also sequences these wrongly. It builds content that depends on RC-4 before RC-4 is decided: RC-8 at S30, and the brief primers at ranks 43-46. And it asks for RC-1's class before the content decisions that settle whether independent or executed evidence will ever exist.

**Treatment.** FIX THE GATING MACHINERY at the level of decisions: one owner packet, in this order. (1) Now, with no dependency: RC-1's text withdrawals, and the D9/D10 changes that do not depend on the route. (2) RC-3, V3 primary, presented together with its amendment list. Add what the synthesis's list lacks: V3:53 'memo ejecutivo' → 'memo de límites' to match :314; the lines route 3 would move (V3:162 'cierre CP-N1-A' and course_requirements.json:41); and the environment fields of S23, S38, S41, S43 and S44 if RC-7(b) is chosen. (3) Then these are settled by V3 unless the owner amends the line: RC-4(a), where the gate line outranks the V3 table, the briefs and the rubrics; RC-6 q1-q2, where E2 means reduced support and the per-subtopic authentic task lives inside the topic evaluation; RC-7, execute where V3 says local; RC-2, V3:91, or amend it with the Kulik et al. 1990 trade-off stated; RC-10, re-estimate from content starting at 16-24 h per section. (4) The open choices come last: the RC-5 route, RC-6 q3 (hint gating), then RC-1's class, because options a/b/c differ once RC-6 and RC-7 settle whether an independent rung and executed anchors will exist. Trade-off, stated: making V3 binding binds its costs too. That means at least 832 h, 416 hand-written topic-evaluation tasks, containers on the learner's machine from S43, and a lock-and-remediation gate associated with lower completion. learning_roadmap_52_V3.md is a protected path (AGENTS.md), so every amendment needs the owner's approval.

<details><summary>Evidence</summary>

RC-4: V3 defines each gate by its section's gate line. V3:314 reads '**Incremento/gate:** **Executive Data Quality & EDA Portfolio** con dataset limpio, notebook/script reproducible, reconciliación, preguntas de negocio y memo de límites.' V3's own summary table disagrees at V3:53: '| CP-N2-A | S17 | Executive Data Quality & EDA Portfolio | notebook reproducible, reconciliación, métricas y memo ejecutivo |'. The brief follows :53 and S17 follows :314. RC-6 q1-q2: V3:33 'E2 es You Do con andamiaje decreciente y E3 es transferencia independiente… termina con una evaluación auténtica que cubre ambos subtemas'; V3:89 'Cada evaluación de tema contiene dos tareas auténticas —una por subtema—'. RC-7: V3's environment fields, V3:373 (S23) '**Entorno:** local', V3:597 (S43) 'local con contenedores', V3:608 (S44) 'CI/cloud controlado'. RC-2: V3:91 'Tras tres fallos se requiere ruta remedial y revisión antes de desbloquear la siguiente sección.' RC-10: V3:777 'Cada sección parte de 16–24 horas según dificultad: ocho bloques de subtema, cuatro evaluaciones, examen/remediación y proyecto… no se falsifican horas para conservar un número comercial.' The estimatedHours sums I computed from COURSE_SECTIONS (dumped with tsx) are 118/118/118/137 by level, i.e. 9-10 h per section, below V3's own floor of 16 × 13 = 208 h per level. So the code already departs from the budget, and the proposed blocks do not break it. CP-N1-C: V3:268 already chose the scaffold. RC-5 is the exception: V3:147 and :169 set multi-field and multi-record gates at S2 and S4, while V3:175 schedules functions at S5 and V3:187 dicts at S6. Ordering defects in the synthesis: rank 6 builds at S30 'because CP-N3-A is the first gate that grades it', but only the brief grades it (see data-roles-third-role-already-taught-at-s34-t3-b), so the placement depends on RC-4. Rank 45 offers 'a primer naming S30-T3-B and S39, or drop it from the brief', which also depends on RC-4. Rank 2 asks for the class-B choice before RC-6 and RC-7 are decided.

</details>

### [P1] `data-roles-third-role-already-taught-at-s34-t3-b` ⚖

*Scope:* level

RC-8 would build a train/dev/test block at S30-T4-A and amend CONCEPT_QUEUE #194 to three roles 'because CP-N3-A is the first gate that grades it'. Only CP-N3-A's brief and rubric grade that. V3's S30 gate, the S30 You Do and S30's own topic evaluation all ask for an entity-disjoint train/test split with cross_split excluded. The third role, fitting on one set and measuring on a set not used for the fit, is already taught at S34-T3-B, where V3 places 'evaluación fuera de muestra'. So the real defect is narrower. The S34 You Do chooses its threshold and reports on the same points, in the very section whose theory forbids that for the calibrator. A three-role block in the entity-resolution section teaches a model-selection protocol before any model is selected, and it replaces a spiral that already works: two roles (S30), then train/valid cross-validation (S33, #166), then an out-of-sample third set (S34-T3-B).

**Treatment.** REWRITE THE DEPENDENCY at CP-N3-A and BUILD IN PLACE at S34. Nothing new at S30. (a) Under RC-4(a), rewrite CP-N3-A's brief and rubric P0 to the section's contract: 'Sin split por entidad: train y test disjuntos, umbrales elegidos solo con train, pares cross_split fuera de las métricas'. Record the change for owner review (D11). (b) Apply CONCEPT_QUEUE #194 as queued, with two roles. Do not amend it to three. Keep the 'ajustar' fix for #193 and the computed-folds fix for #166. (c) At S34-T4-A, add one supporting block with no subtopicId that extends S34-T3-B's rule from the calibrator to the threshold: choose the threshold on out-of-fold validation scores from S33, then read test once. Rewrite the S34 You Do starter and rubric to that contract. This is the first point where the third role does any work, because it is the first time a fitted model's scores are used to select something. (d) RC-8's per-case fixture family for S33, S34, S39, S47 and S50 is unchanged. Trade-off, stated: learners meet 'dev' four sections later than RC-8 intended, and the CP-N3-A brief loses a criterion that sounds more rigorous. In exchange, S30 stays an entity-resolution lesson, nothing is taught twice, and no protocol arrives before there is something to select. If research request 3 says first contact must show all three roles, move (c) to S30 and accept the entity-resolution setting the synthesis already named as unusual for teaching validation.

<details><summary>Evidence</summary>

V3:457 (S30): '- T4 Evaluación: labeled pairs y splits por entidad; precision/recall, pairwise/cluster metrics y error slices.' V3:56: 'CP-N3-A | S30 | Testable Entity Resolution Engine | bloqueo, comparadores, evaluación etiquetada y cola de revisión'. The S30 You Do objective (COURSE_SECTIONS) reads 'Evaluar con gold sintético, split por entidad sin leakage (cross_split excluido), P/R/F1 pairwise…'. Its five requirements name no role beyond 'política de cross_split', and the You Do contains 'dev' 0 times and 'holdout' 0 times. S30's mounted topic evaluation, s30:2104 'S30-T4-TE-1', asks for a 'Lista de pares con etiquetas train/test/cross_split; overlap de entidades train∩test = 0 en métricas primarias'. 'train/dev/test' appears only in public/capstones/CP-N3-A_BRIEF.md:27, :31, :39 and CP-N3-A_RUBRIC.json:29, :47. The S33 You Do objective reads 'Run log completo y group CV con disyunción train/valid por entidad'. s34:303-307 (S34-T3-B 'Calibradores y evaluación fuera de muestra'): 'se ajustan en un set de calibración **distinto** del train del modelo base'; '(3) evaluación de Brier/reliability en un split no usado para fit. `calibrator_set=train_in_sample` activa `REJECT_IN_SAMPLE_CAL`'; 'Esa frase evita el training-serving skew de las probabilidades cuando el thr de T4 se elige sobre scores «calibrados».' V3:500: '- T3 Calibración: reliability curves y Brier; calibradores y evaluación fuera de muestra.' Both verifiers upheld the leak: the S34 starter chooses the threshold, then reports on the same SCORES/LABELS (s34:2349-2360). CONCEPT_QUEUE.md:1640-1642 (#194) already queues the two-role idea at S30-T4-A, with a 'two-box train/test figure'.

</details>

## Questions the red team sent back to research, and the answers

- **Answer** (High that the code addresses every point in the review comment: I read each code path the reviewer named, and the two tests added for this fix pass. Medium that it works in production: I did not run the migration against a real deployed database or check the live site.): Scope note first. The request you sent is the Codex P1 comment on src/lib/section-id-migrations.ts:16 ("Migrate server-side progress with the slug rename"). The workflow's computed task asked something unrelated: a learning-science question about hint and solution gating. The harness says your request wins where the two conflict, so I worked on the review comment and did not research hint gating. I changed nothing in the repo; I only read files and ran tests.

Verdict: the comment is already addressed. Commit 29bf0240 ("fix(progress): carry server-side progress across the section-id rename") fixes it, and that commit is on origin/audit/consolidated-issues-20260910. Here is each point the reviewer raised:

1. **Server rows are migrated.** prisma/migrations/20260918030000_rename_section_ids_batch_a/migration.sql moves every table that stores sectionId. Because the documented setup uses db:push, which never runs migration files, package.json:13 chains it: "db:push": "prisma db push && node scripts/apply_section_id_renames.mjs".
2. **`/api/progress` returns the new ids.** In src/app/api/progress/route.ts, GET passes every row through renameSectionId. Where a learner has rows under both
  - *Implication:* No new code change is needed for this P1. On the PR, reply to the Codex thread pointing at commit 29bf0240 and resolve it. The migration file itself documents two limits that are still open (migration.sql lines 28-30): (a) the Firestore mirror in src/lib/firebase/sync.ts still holds old slugs, which is harmless for now because the app never reads it; (b) a PDF generated from a tab opened before the deploy uses that old bundle's SECTION_NAMES. Both are stated limits of the fix, not gaps in it.
  - *Owner decision:* Two decisions. (1) Do you accept the two documented limits, the stale Firestore mirror and PDFs from tabs opened before the deploy? If the mirror will ever be read, it needs its own migration. (2) This workflow run also carried an unrelated research question: does giving novices open hints and one-click solutions on all 1,248 exercises reduce learning compared with gated or progressive reveal? I did not answer it because it was not part of your request. If you want it researched, ask for it directly.
  - *Sources:* /Users/pabloillescas/Documents/GitHub/pyarcana/src/lib/section-id-migrations.ts; /Users/pabloillescas/Documents/GitHub/pyarcana/src/app/api/progress/route.ts; /Users/pabloillescas/Documents/GitHub/pyarcana/src/components/course/PdfReport.tsx (lines 40, 106, 370); /Users/pabloillescas/Documents/GitHub/pyarcana/prisma/migrations/20260918030000_rename_section_ids_batch_a/migration.sql (lines 28-30); /Users/pabloillescas/Documents/GitHub/pyarcana/scripts/apply_section_id_renames.mjs; /Users/pabloillescas/Documents/GitHub/pyarcana/package.json (line 13)

- **Answer** (Medium-high that claims worded as "delivery" or "engineering practice" are read as authored and run: exam design, competency frameworks, peer curricula and the badges' own rubrics all agree. Low on any figure: no source directly measures whether employers accept audit-only evidence for container, CI/CD or MLOps credentials. Some quotes come from vendor surveys (CoderPad, Coursera). The SFIA and Zoomcamp wording was extracted by a summariser, and the Zoomcamp criteria are undated.): SHORT ANSWER. For credentials with these names and claims, authoring and running is expected. Audit evidence is not enough. The badges claim "demostró independientemente MLOps Pipeline Delivery / Container Platform Engineering mediante tarea práctica". The capstones describe the holder as "Ingeniero de plataforma que despliega un servicio acotado" (CP-N4-A) and "Ingeniero MLOps que gobierna modelos" (CP-N4-B), at src/lib/capstones/catalog.ts:97 and :101. Every source I found that assesses these skills reads claims like these as: the holder wrote a Dockerfile, a Compose stack, a CI workflow and a model registry, and ran them. Auditing artefacts is a real skill that hiring values more and more, but it is a different and narrower claim. The evidence is mixed at one point only: modern hiring assessments favour brownfield work (fix existing code) over greenfield writing. That argues for a third repair rather than for audit-only: give the learner a flawed but real artefact, and require them to fix it AND run it.

WHAT THE REPOSITORY ALREADY SAYS (this is the strongest evidence, and it is internal):
- The market evidence the badges cite rejects audit-only work. industry_alignment/industry
  - *Implication:* UNDER THIS ANSWER, the curriculum should do the following.
1. Stop the overclaim now. Do not award applied_mlops_pipeline_delivery (status "active") or container_platform_engineering_practice (status "pilot"), and do not award CP-N4-A/B, on the current evidence (D11). Do not rewrite the You Do tasks as audits while keeping the names "Delivery", "Engineering Practice" or "Ingeniero de plataforma que despliega": that is the overclaim in its worst form.
2. Teach one real, locally run artefact in place, as a D3 subsection for each section. Prefer brownfield: hand the learner a real but flawed artefact that they must fix and then run.
   - S43: build a multi-stage image with Docker or Podman, run it as non-root, and bring the stack up with `compose up` with health checks. Record the digest from
  - *Owner decision:* 1. Keep the "delivery" and "engineering practice" claims and fund real, locally run builds in S43, S44 and S47 (my recommendation)? Or narrow to an "assess" claim under a new badge id or version?
2. Interim status: applied_mlops_pipeline_delivery is marked "active" today, and its own rubric requires the learner "built the solution". Should it be paused until the claim is decided?
3. Is self-attested execution, backed by static checks on the server and an oral defence, acceptable evidence? Or must a sandboxed runner observe the build?
4. Is the hardware and tooling barrier (Docker or Podman, a 
  - *Sources:* Repo: src/lib/course/sections/s43-llmops.ts:119-120, 495, 864, 2341-2345, 2373-2379, 2400, 2405-2408; Repo: src/lib/course/sections/s44-multimodal.ts:2038-2039, 2055, 2062, 2081-2086; Repo: src/lib/course/sections/s47-opensource.ts:1937-1938, 1947, 1951, 1977, 1981; Repo: src/lib/capstones/catalog.ts:95-102 (CP-N4-A/B role statements); Repo: src/lib/eligibility/claim_evidence_contracts/applied_mlops_pipeline_delivery.json and container_platform_engineering_practice.json (capability_statement, status active/pilot); Repo: industry_alignment/badge_rubrics/applied_mlops_pipeline_delivery.json:76,87,93; container_platform_engineering_practice.json:87,93,180

- **Answer** (Medium.

Why not higher:
- The studies are short (1 hour to 8 weeks), small or quasi-experimental, and none compares a flagged repair with a dictated fix against writing code in a self-paced course lasting months.
- Several sources were read at abstract level only (Lytle 2019, van Merriënboer 1990, Tepgeç 2025, Yang 2025, Frerejean 2019).
- My PyArcana counts come from heuristics (diff plus regex) spot-checked on only 9 items. Treat the exact numbers as hypothesis-grade; the direction is robust.
- One correction: the WebFetch summary of the PRIMM paper reported an effect size of 0.81. The extracted text says r=.13, and I used the text.

Why not lower: the direction is consistent across PRIMM, Use-Modify-Create, completion/4C/ID, Parsons and faded Parsons. Early read, modify and complete tasks are fine. Unaided generation must close each task class. Copying a dictated fix is not practice at any stage.

Not examined:
- Whether the You Do projects are generative.
- The full 1,733-line CONCEPT_QUEUE (I grepped it; it covers concept dependencies, not the shape of the practice ladder, so this does not duplicate it).
- Every E3 individually.
- Paywalled full texts.): SHORT ANSWER. A one-line repair of a flagged, planted defect cannot be the rung where the learner codes unaided ("Make"), at any stage. The evidence also does not say that rung has to be E2. It does not say every E2 must generate code, and it does not support a minimum-change-size rule. Repair, modify and completion tasks are well-supported early practice. The research agrees on two points: (1) every task class ends in an unaided task where the learner generates the code, before the course relies on that concept; (2) repair only counts as practice when the learner has to find the defect or decide the fix. PyArcana fails both points, and the failure is mostly not specific to E2.

1. THE QUESTION MISLABELS E2
- The contract says E2 is not the independent rung. learning_roadmap_52_V3.md:33: "E1 es We Do guiado, E2 es You Do con andamiaje decreciente y E3 es transferencia independiente." Line 87: "E1 muestra proceso; E2 retira parte del apoyo; E3 cambia el contexto y exige transferencia."
- The code contradicts the contract. All 416 E2 steps carry kind 'independent' (measured; the union is at src/lib/types.ts:102).
- The UI prints 'Independiente' (src/components/course/SectionView.tsx:
  - *Implication:* Restate the finding practice-rungs-answer-adjacent-no-unaided-make. The unit of repair is the subtopic ladder, not the set of 416 E2s.

1. Leaked or dictated fixes, plus hints and solutions open with no attempt, are defects at every rung and every level, from S01. Fix them with text edits and a gate in SectionView.
2. Up to about 386 of 416 subtopics have no rung where the learner generates code. That rung belongs in E3, as the V3 contract says, or in a mounted topic evaluation.

The S01-S13 E2s can stay repair-shaped only if their dictation and leaks are removed and a generative E3 exists beside them. Do not adopt a minimum-change-size ratchet from S01: it misclassifies S24-T4-A-E2 (one line, generative) and S08-T2-A-E2 (four lines, dictated). Ratchet instead on (a) no changed solution li
  - *Owner decision:* 1. What does E2 mean? V3 says the reduced-support rung ("andamiaje decreciente", learning_roadmap_52_V3.md:33); the code and UI say 'independent'/'Independiente' (types.ts:102, SectionView.tsx:596). I recommend following V3.

2. Where does the unaided Make rung live? The options:
   - In every subtopic's E3: the V3 reading, fits 4C/ID, largest cost.
   - In every topic evaluation: 208 items, but only 12 are mounted.
   - In the section You Do only: not supported by 4C/ID or by D6.

3. Should "Ver solución" require an attempt, and should hints be staged? SectionView is not a protected path. The
  - *Sources:* learning_roadmap_52_V3.md:33 and :87 (E2 = andamiaje decreciente; E3 = transferencia independiente); src/lib/types.ts:102 (kind union); measured 416/416 E2 kind='independent'; src/components/course/SectionView.tsx:541, :544, :580, :596, :625 (tab title, callout, all hints rendered at once, 'Independiente' label, ungated 'Ver solución'); audit/fixer/decisions.md:173-178 (D6 explicit-instruction sequence incl. independent practice); src/lib/course/sections/s16-wxpython-gui.ts:951, :962, :974 (S16-T2-A-E2 answer in hints and starter comment); src/lib/course/sections/s25-streamlit-dashboards.ts:740, :747 (S25-T1-A-E2 dictated fix)

- **Answer** (Medium-high on the comparator facts. Each comes from the programme's or the standard's own current page, fetched in September 2026. Some details are inference and are labelled as such: that CS50's final project is checked automatically, and that freeCodeCamp's server does not re-run the tests. Whether TMC uses hidden tests is simply unverified. Medium on how employers read multiple-choice badges. The evidence is one peer-reviewed field study on a freelance platform, two platform retirements with stated or relayed reasons, one meta-analysis about job-knowledge tests rather than course quizzes, and two self-interested vendor surveys. Nothing is specific to Peru or Latin America, and no study tests an employer's reading of a badge explicitly labelled as multiple choice.

Not examined:
- Blocked with 403, surfaced and not retried: the Class Central report on Coursera peer grading, the EDUCAUSE Review October 2025 article on microcredential rigor, and Upwork's primary announcement.
- Not checked: Coursera's grading of each individual quiz and lab, edX's identity checks for verified certificates, the final published text of ICE 1100 (I read the public-review draft), Credential Engine's CTDL assessment profiles, the OB3 conformance-suite algorithm list, and the exam flow on the static site in detail.
- No repository gate was run. This was read-only work.): SHORT ANSWER. The comparators do not all back skill claims with hidden tests or a recorded human review, so the benchmark does not require the "suspend all 26" branch as written. What every one of them does require is that a credential's wording matches the evidence behind it. PyArcana's wording does not, and that stays P0 whichever branch the owner picks. The treatment should be split by class:
- Class B: rewrite down now to a clearly labelled knowledge badge backed by the server-graded multiple-choice exams.
- Classes C and D: suspend. There is no instrument to rewrite them down to.
- Build a grader only for the verified tiers.
One question is a real judgement call for the owner, set out in section 4.

On copyright: my rules allow one short verbatim quote from outside sources, and I used it for LinkedIn. Every other outside source is paraphrased, with a section or page pointer so it can be checked. Repository quotes are verbatim.

1. WHAT PYARCANA ACTUALLY RECORDS (read-only, HEAD 6417dbe2). This confirms the red team's premise.
- Exams are graded on the server: src/app/api/exam/submit/route.ts checks each answer against QuestionBank.correctIndex.
- Exercise results come from the
  - *Implication:* The false claims are P0 under either branch, and fixing them is text work that needs no grader. No code path issues a class B or C badge today, and class D issuance checks only multiple-choice exams. Rewriting class B into a knowledge badge that states its assessment type, supervision and identity check is defensible against Open Badges 3.0, the EU micro-credentials recommendation and ICE 1100. Classes C and D have nothing to rewrite down to and should be suspended until the server stores a submission, a grader result and a review or defense. When that grader is built, it should copy the comparators' pattern: code run on the server against tests, plus an explain-your-solution backstop. Hidden tests are optional. Earlier badge holders are never upgraded retroactively (D11).
  - *Owner decision:* 1. Approve changing what the credentials claim. AGENTS.md lists this under "Ask first", and it covers badge_catalog.json, the credential policy page, the capability statement at credentials/issue/route.ts:141 and the badge text at Dashboard.tsx:765 and :770.
2. Choose one:
   - (a) issue class B as publicly verifiable knowledge badges, labelled as unsupervised multiple choice;
   - (b) show them only locally, with no public verification;
   - (c) suspend all 26 until a grader exists.
3. Confirm that classes C and D are suspended rather than reworded.
4. Decide whether the future grader include
  - *Sources:* /Users/pabloillescas/Documents/GitHub/pyarcana/src/lib/eligibility/badge_catalog.json (lines 1017, 1085, 1093, 4806, 5043, 5289, 5537, 5785); /Users/pabloillescas/Documents/GitHub/pyarcana/src/lib/eligibility/engine.ts (lines 294-308, 633-652, 683); /Users/pabloillescas/Documents/GitHub/pyarcana/src/app/api/credentials/issue/route.ts (lines 117-118, 141, 153); /Users/pabloillescas/Documents/GitHub/pyarcana/src/app/api/exercise/attempt/route.ts (line 13); /Users/pabloillescas/Documents/GitHub/pyarcana/src/app/api/exam/submit/route.ts; /Users/pabloillescas/Documents/GitHub/pyarcana/src/app/credential-policy/page.tsx (lines 31-33, 46-48)

- **Answer** (Moderate on the design direction. Integrate more than once as a growing whole task, and never let an increment depend on an unverified learner artifact with no fallback. This rests on 4C/ID theory backed by a meta-analysis (d = 0.56), the Wickens part-task meta-analysis, the completion-strategy finding of lower dropout, and MOOC dropout patterns. It also matches the one comparator that teaches integration, Full Stack Open. Low on the narrower question of whether learners should carry their own code or restart from a reference starter. I found no controlled comparison: the comparators are course designs, not experiments, and the dropout and completion evidence is indirect (van Merriënboer 1990 had n = 57; Rõõm 2021 is observational). Four key full texts were blocked, so those findings come from abstracts.): SHORT ANSWER. As posed, neither option is right. The research does speak clearly to two things, and the choice of repair should follow them. (1) Integration has to be practised more than once, as a whole task that grows in complexity. It should not be taught only at the end, so "integration only at CP-FINAL" is the design to avoid. (2) An early failure must not block the next increment. What makes both possible is a fixed contract (one schema) per capstone. Each later starter imports the previous capstone's module through that contract: the learner's own module if it passes that capstone's contract tests, and the course reference module if it does not. This is treatment (a) in structure, with integration increments at S13/S26/S39/S51. No study I could find directly compares "carry your own code forward" with "restart from a starter". The part of the question about whose code the learner carries forward is therefore not settled by evidence, and I say so below.

WHAT THE REPOSITORY SHOWS (read-only, quoted)
- The red team's premise holds. None of the 13 declared integration interfaces (for example `src/lib/capstones/catalog.ts:65` 'etl.run(batch) -> EtlManifest', `:112` 'platform.int
  - *Implication:* capstone-increments-never-compose-until-cp-final should take treatment (a), with two amendments. First, use a mechanical fallback: the learner's module if it passes the capstone's contract tests, otherwise the course reference module. Second, CP-FINAL keeps 'integré doce capstones' but declares per subsystem whether it is the learner's own or the course reference, and its S1–S52 regression narrows to regression over the 12 contracts plus the end-to-end test. Treatment (a) costs less than the red team assumed, because reference implementations of all 12 subsystem interfaces already exist and are tested in course-state/capstones/CP-FINAL/integration/. The main work is rewriting the 12 gate You Dos (S04, S08, S13, …, S51) so each emits its catalog interface, and publishing the references. Tre
  - *Owner decision:* Three owner decisions, none of which the evidence settles. (1) Default direction: own code by default with the reference as fallback (Full Stack Open), or reference by default with own code opt-in. My recommendation is to let the contract test decide, which removes the choice from the learner. (2) Reveal timing: publishing reference modules for gate capstones exposes the solutions for badge evidence. Full Stack Open handles this by revealing model solutions after a part is submitted and by plagiarism checks. Whether PyArcana can gate the reveal depends on whether You Do submissions are recorde
  - *Sources:* Repo: src/lib/capstones/catalog.ts:61,65,111,112 (declared interfaces; CP-FINAL summary claim); Repo: src/lib/course/sections/s52-career-strategy.ts:57,1936,1940,1947,2004,2006 (CP-FINAL contract, requirements, starter); Repo: src/lib/course/sections/s01-setup.ts:2399 (carry-forward intent); Repo: src/lib/course/sections/s04-iteration-summaries.ts:1781; s13-evidence-dashboard.ts:1941 (re-scaffolded starters); Repo: course-state/capstones/CP-FINAL/integration/*.py (12 reference subsystems, contracts.py, contract_tests.py, e2e_test.py; etl.py:40); tests/adversarial/test_cp_final_integration.py; Repo: public/capstones/CP-FINAL_BRIEF.md:57,62,66 (learner brief references integration/ files; contribution declaration; allowed claim)
