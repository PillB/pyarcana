# Declared teaching environment for the PyArcana curriculum.
#
# Every `output:` block in src/lib/course/sections/ states what the learner will
# see when they run the snippet. That is only a meaningful promise against a
# named environment, so this file is the environment the content is verified
# against, and the one CI installs before running the runtime audit.
#
# Verified with: python3 scripts/python_content_runtime_audit.py
# Last full green run: 2026-08-22, 3265 pass / 0 fail / 92 skip across 52 sections.
#
# ── Why these are pinned, not floating ───────────────────────────────────────
# The course teaches pandas 3 dtype contracts and prints them. Inferred text is
# `str`; the schema contract for identifiers is nullable `string`; `object` is
# the leftover for mixed Python types. numpy 2.2.6 is the version pip resolves
# with pandas 3.0.5 + scikit-learn 1.6.1 + scipy 1.13.1.

numpy==2.2.6
pandas==3.0.5
scikit-learn==1.6.1
scipy==1.13.1
