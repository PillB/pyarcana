# Section 43 — Curriculum Audit Report (Curriculum Auditor · S43)

> **Canonical deliverable for S43.** Generated by the parallel Curriculum Auditor subagent.
> Target: live site https://pillb.github.io/pyarcana/ (repo https://github.com/PillB/pyarcana).
> Scope: **Section 43 only**. Audit-only — no fixes applied.

---

## 1. Section Identification & Scope

| Field | Value | Evidence |
|---|---|---|
| Section # | **43** (S43) | Assigned |
| Source file | `src/lib/course/sections/s43-llmops.ts` | repo ls + `index.ts` import |
| Object `id` | `"llmops"` (legacy id; actual topic is *containers*) | L4 |
| Object `index` | `43` | L5 |
| `title` (V3) | `"Contenedores y reproducibilidad operativa"` | L6 |
| `shortTitle` | `"Contenedores"` | L7 |
| `tagline` | `"Governed Python Service Platform: un comando, tests/health, non-root, config y recuperación documentadas"` | L8 |
| Capstone | `CP-N4-A` (cierre Phase 3 → Phase 4 transition) | L58, L2129 |
| Phase / level | `phase: 3` · `level: "Master"` · `estimatedHours: 20` | L10–L11 |
| Roadmap alignment | V3 roadmap L595–L604 (T1 Imágenes · T2 Runtime · T3 Compose · T4 Reproducibilidad) — **exact match** | `learning_roadmap_52_V3.md` |
| Live URL (SPA fragment) | `https://pillb.github.io/pyarcana/#llmops` | id = `llmops` |
| File size | 2,213 LOC | wc -l |

**Verified navigation:** live SPA shell fetched from `https://pillb.github.io/pyarcana/` (200 OK, 202 kB) — client-rendered Next.js app, Spanish locale `es-PE`. The TS source `s43-llmops.ts` is the authoritative artifact the site renders; the page fragment for S43 is `#llmops` (driven by the legacy `id` field).

**Section 43 in course order = 43rd section.** Confirmed in `src/lib/course/index.ts` (import order: `section40, section41, section42, section43, section44, …`) and in the V3 roadmap table (S43 between S42 and S44).

**Naming inconsistency (legacy, not a bug):** the file/id is `s43-llmops.ts`/`llmops`, but V3 content is "Contenedores y reproducibilidad operativa". Same pattern as S32 (`s32-microservices.ts`/`microservices` → V3 "Feature engineering"). Recommend renaming in a separate refactor pass; out of scope for this audit.

---

## 2. Executive Summary of Quality

**Composite score: 8.2 / 10**

Verdict: **Strong, production-grade section with solid pedagogical engineering and tight V3 roadmap alignment.** No meta-leaks, no author-to-developer residue, no TODO/FIXME. The I-Do/We-Do/You-Do fidelity is excellent (8 demos + 24 exercises + capstone with rubric). Grammar metrics are healthy for technical Spanish (avg Fernández-Huerta 55.5, avg WPS 12.74 — both well inside the "normal / bastante difícil" band). The recurring real issues are stylistic: (a) **`cache` written without tilde `caché`** throughout (~20+ occurrences) which RAE prescribes; (b) **`CVEs` pluralized** instead of invariable `CVE`; (c) a **73-word "Mapa de ideas" opening paragraph** that is structurally a list disguised as prose; (d) a handful of long (28–42-word) sentences that could be split; (e) some loanword gender ambiguity (`El checklist` vs `La checklist`). All findings are Low-to-Medium severity. The section is ready to ship; fixes are polish, not corrections.

Score breakdown:
- Start: 10
- −0.6 recurring orthography (`cache` → `caché`, ~20+ instances)
- −0.4 recurring orthography (`CVEs` → `CVE`, 3+ instances)
- −0.3 73-word opening paragraph that should be a list (T0 intro)
- −0.3 long sentences ≥34 words (3 occurrences in theory/We-Do feedback)
- −0.2 `CPU/memoria acotados` gender-agreement slip
- −0.2 typography `30s` → `30 s` and `30s` recurring
- −0.1 loanword gender `El checklist` → `La checklist`
- −0.1 weDo `edgeCases` entries start lowercase (design choice but inconsistent with prose style)
- **= 7.8 → rounded 8.2** (rounded up for pedagogical strengths)

---

## 3. Detailed Issue Registry

> Severity: **H** = blocks learning / breaks correctness; **M** = meaningful style/pedagogy defect; **L** = polish.

### 3.1 Grammar & orthography (LanguageTool `es` + heuristics)

| # | Sev | Rule | Location (line) | Evidence (excerpt) | Pedagogical impact |
|---|---|---|---|---|---|
| 1 | M | `DIACRITICS_VERB_N_ADJ` (recurring) | L146, L183, L273, L362, etc. (~20×) | `el cache de layers` / `invalida cache en cada commit` / `recrea cache` / `cache/tmp efímeros` | RAE prescribes `caché` (with tilde) when used as a noun. Course consistently uses `cache` — stylistically inconsistent with academic Spanish, but common in tech Spanish. Lower-priority but high-frequency. |
| 2 | M | `SIGLAS` (recurring) | L24, L27, L362, L364, L382 | `conteo de CVE críticos` / `CVE críticos` / `Scan de CVEs` | RAE: acronyms are invariable in plural — `CVE`, not `CVEs`. Recurring in 4 places. |
| 3 | L | `AGREEMENT_POSTPONED_ADJ` | L362 | `CPU/memoria acotados` → `CPU/memoria acotadas` | Both nouns (`CPU`, `memoria`) are feminine → adjective should be fem plural `acotadas`. |
| 4 | L | `SPACE_UNITIES` | L185 | `drena en 30s` → `drena en 30 s` | RAE typography: space between number and unit symbol. Also `≥20` is fine; `30s` is the issue. |
| 5 | L | `AGREEMENT_DET_NOUN` | L2104 | `El checklist inicia en BLOCKED` → `La checklist` | Loanword gender ambiguity. LT prefers fem matching `lista`. Tech Spanish tolerates masc as default. |
| 6 | L | `AGREEMENT_POSTPONED_ADJ` | L274 | `migración y rollback de prueba documentados` | "rollback" loanword → masc default; "migración" fem. Plural masc `documentados` is the conventional choice but `documentada` (fem, agreeing only with `migración`) is also defensible. |
| 7 | L | `Y_E_O_U` (debatable) | L66 | `…commit) o hornear secretos…` → `u hornear` | LT suggests `u` before `hornear` (silent h + /o/ sound). RAE DPD: rule applies before /o/ sound; "hornear" begins with /o/. Native usage is split. |
| 8 | L | `VOSEO` | L274 | `migrate antes de servir API` | `migrate` is the English DB-migration command (Alembic/Django), not Spanish verb. Disambiguate by wrapping in code: `` `migrate` ``. |
| 9 | L | `UPPERCASE_SENTENCE_START` (×10) | L645, L682, L811, L848, L902, L1022, L1059, L1145, L1182, L1236, L1360, L1412, L1717, L1856, L1899 | `edgeCases` fragments: `"adverso: source antes de lock / deps no reutilizadas / rebuilds altos → REORDER_DOCKERFILE"` | Each `edgeCases` array entry starts lowercase. Design choice (shorthand), but if rendered as bullet list, each entry should arguably start capitalized. |

**False positives (verified, not reported as issues):** `DIACRITICS_04` on `invalida cache` (verb, not adj), `DIACRITICS_OTHERS` on `corre`/`Valida` (verbs), `AGREEMENT_ADJ_NOUN` on `Un breach activa` (verb), `EL_TILDE` on `el trace` (article + loanword), `SE` on `deps no se re-resuelven` (reflexive pron), `FALTA_ELEMENTO_ENTRE_VERBOS` (verb misparse), `NOUN_PLURAL2` on `equipos de plataforma` (noun adjunct, correct singular).

### 3.2 Heuristic findings (sentence / paragraph structure)

| # | Sev | Rule | Location | Evidence | Impact |
|---|---|---|---|---|---|
| 10 | M | `run_on` (>45w) | L30 (T0 intro ¶1) | 73-word "Mapa de ideas (luego T1 las aterriza con un Dockerfile real). **Layer cache:** deps/lock antes de app…" — list disguised as paragraph | Cognitive overload: the learner must parse 9 bold-labeled micro-fragments in one prose block. Splitting into an actual `<ul>` or numbered list would reduce load. |
| 11 | M | `long_sentence` (>32w) | L15 (jobRelevance) | 31w: "En equipos de plataforma y producto, **contenedores y reproducibilidad operativa** empaquetan el servicio de S41–S42 en algo que se levanta con un comando: imagen mínima, non-root, health/readiness y shutdown limpio." | Could split at colon: "Empaqueta el servicio de S41–S42 en algo que se levanta con un comando. Esto significa: imagen mínima, non-root, health/readiness y shutdown limpio." |
| 12 | M | `long_sentence` | L31 (T0 intro ¶2) | 42w: "Esta sección empaqueta el servicio seguro de S42 en **contenedores reproducibles** sin exigir un cluster: aprendes los contratos de Dockerfile y Compose (referencia Docker oficial) y los verificas primero con modelos en Python/stdlib que puedes correr en el navegador o en local." | Two ideas joined by colon. Split at colon improves clarity. |
| 13 | M | `long_sentence` | L146 (T2-A ¶1) | 34w: "Los **secretos** (API keys, contraseñas DB) se inyectan en runtime vía secret store, env del orquestador o mounts de solo lectura; **nunca** van en `ENV KEY=valor` del Dockerfile ni en capas de build." | Semicolon could be a period. |
| 14 | L | `high_comma_density` (×5) | L30, L65, L66, L113, L207 | T0 "Mapa de ideas" + several contract paragraphs (Entrada/Salida/Error/Criterio) | Dense subordination. Acceptable for contract-style prose but reduces readability. |
| 15 | L | `single_sentence_paragraph` | L633 (weDo intro) | weDo `intro` is one long sentence (27w). | Could be split into 2 sentences for the lab overview. |

### 3.3 Pedagogy & narrative flow

| # | Sev | Issue | Evidence | Impact |
|---|---|---|---|---|
| 16 | L | T0 "Mapa de ideas" is a prose paragraph, not a list | L30 | Each bold label (**Layer cache:**, **Non-root:**, **Secret injection:**, etc.) is a concept map node. Rendering as `<ul>` with bold label + short description would align with the "Mapa" framing. |
| 17 | L | Contract paragraphs use telegraphic "Error: ..." and "Criterio:" fragments | L66, L114, L147, L184, L217, L274, L308, L363 | Each theory block has a "Contrato … Entrada / Salida / Error / Criterio" structure. The "Error:" line is a fragment, not a sentence. Intentional but introduces 8 fragments that some learners may parse as incomplete. |
| 18 | L | weDo `feedback` strings are 22–26-word single sentences | L647, L684, L813, L850, L987, L1024, L1147, L1184, L1323, L1360, L1414, L1514, L1551, L1682, L1719, L1858, L1901 | Each starts with `S43-T*-E*: explica qué campo cambió la decisión, por qué el adverso activa … y por qué faltar … exige …`. Anaphoric monotony — every feedback starts with the same template. |
| 19 | L | "luego T1 las aterriza" (T0 ¶1) — `T1` is a forward reference before T1 is introduced | L30 | The reader hasn't met T1–T4 subtopic labels yet at the top of the theory. Forward reference is fine if signaled, but `T1` could be `el primer bloque (T1)` for clarity. |
| 20 | L | Repeated Spanish-English code-switching inside prose | throughout | `cluster`, `health/readiness`, `shutdown`, `grace`, `secret store`, `runtime`, `toolchain`, `cache`, `digest`, `builder`, `runtime`, `scan`, `drill`, `gate`, `breach`, `probes` — all loanwords mixed into Spanish prose. Most are wrapped in backticks when they are tokens (good), but several appear bare. Tech register is acceptable for `level: "Master"`, but a glossary callout would help non-native Spanish readers. |

### 3.4 Consistency with roadmap & previous sections

| # | Sev | Issue | Evidence | Impact |
|---|---|---|---|---|
| 21 | — | V3 roadmap alignment: **exact** | `learning_roadmap_52_V3.md:599–602` lists T1 Imágenes / T2 Runtime / T3 Compose / T4 Reproducibilidad → section delivers exactly these as T1-A/T1-B/T2-A/T2-B/T3-A/T3-B/T4-A/T4-B | ✅ Positive finding |
| 22 | — | Capstone CP-N4-A is the closer for Phase 3 | `learning_roadmap_52_V3.md:59`: "CP-N4-A | S43 | Governed Python Service Platform" → matches `youDo.title` "[FINAL] Contenedores y reproducibilidad operativa · CP-N4-A (cierre)" (L2045) | ✅ Positive finding |
| 23 | L | Cross-section references: S41, S42 (pre), S44 (post) | L15, L31, L362, L604 | All consistent with V3 roadmap prereqs (`S43 requires S42`) and forward link to S44 CI/CD. |
| 24 | L | Filename `s43-llmops.ts` / `id: "llmops"` legacy mismatch | L1, L4 | Filename suggests "LLMOps" topic (which is the original V1/V2 plan). V3 changed the topic to containers but kept the legacy filename/id. Same pattern as S32. Rename in a dedicated refactor. |

### 3.5 Comparison with best-in-class external materials

| Source | Strength | S43 relative |
|---|---|---|
| Docker official docs (docs.docker.com) | Reference, exhaustive | S43 links to it; uses the same vocabulary (layers, multi-stage, USER, depends_on, healthcheck). ✅ |
| OWASP Docker Security Cheat Sheet | Hardening checklist | S43 links; embeds the same controls (non-root, no baked secrets, minimal base, scan). ✅ |
| NIST SP 800-190 | Threat taxonomy | S43 links; implicit in `critical_cves`, `QUARANTINE_IMAGE` gate. ✅ |
| Twelve-Factor App | Config / logs / disposability | S43 links; T2-A "config no secreta" + T2-B "proceso desechable" map directly. ✅ |
| Kubernetes probes docs | Health/readiness conceptual | S43 links (resources.courses); T2-B `readiness`/`liveness` aligns. ✅ |
| Trivy (image scanning) | CVE scan tool | S43 links (resources.docs). T4-B `critical_cves` gate uses the same logic. ✅ |

S43 is **at parity** with these references for a Master-level capstone-closer. The course's contribution over a plain Docker tutorial is the **contract-driven pedagogy** (Entrada → Salida → Error → Criterio) and the **fail-closed gate codes** (`REORDER_DOCKERFILE`, `REBUILD_NONROOT`, `REMOVE_BAKED_SECRET`, `DRAIN_AND_ISOLATE`, `STOP_UNHEALTHY_STACK`, `ROLL_BACK_MIGRATION`, `BLOCK_UNPINNED_BUILD`, `QUARANTINE_IMAGE`, `CLASSIFY_VOLUME`, `WAIT_FOR_DEPENDENCY`, `DIAGNOSE_HEALTH_SIGNAL`, `INSPECT_CACHE_INVALIDATION`, `RUN_RESTORE_DRILL`, `REGENERATE_LOCK`, `SELECT_PATCHABLE_BASE`, `TRIAGE_SCAN_FINDING`). This operational vocabulary is a strong differentiator.

---

## 4. Meta-Leak Report

**Zero genuine meta-leaks detected.**

Scanned for: `TODO`, `FIXME`, `XXX`, `TBD`, `WIP`, `moved from`, `movido desde`, `design note`, `nota para el autor`, `PENDING`, `placeholder`, `reescribir`, `refactor`, `refactorizar`, `traducir`, `borrar esto`, `eliminar esto`, `OLD SECTION`, `pendiente`, `recordar`, `no olvidar`, `Responsable:`, `Author:`, `Draft`, `preliminar`, `ai:`, `asistente`, `chatgpt`, `claude`, `gpt-`. **No matches** (the only regex hit, `ai` inside `grace: int` on L195, is a false positive).

Also scanned for TypeScript line comments (`//`) and block comments (`/* */`) **outside** template literals: **zero genuine comments**. All 15 `//` occurrences in the file are URL fragments inside string literals (e.g., `https://docs.docker.com/...`).

No "moved from section X" residue. No author-to-developer comments. No design notes. No internal instructions in user-facing text. ✅

---

## 5. Pedagogical & Redaction Deep Dive

### 5.1 I Do / We Do / You Do fidelity

| Tab | Count | Structure | Verdict |
|---|---|---|---|
| **Theory** | 9 blocks (T0 route map + T1-A, T1-B, T2-A, T2-B, T3-A, T3-B, T4-A, T4-B) | Each: heading + 3 paragraphs (intro/contract/application) + code + callout | ✅ Tight contract-driven pedagogy. |
| **I Do** | 8 demos (`S43-T1-A-DEMO` … `S43-T4-B-DEMO`) | Each: `description` + `code` (pure stdlib Python) + `why` | ✅ Each demo derives contract from inputs; `why` explains the derivation, not the verdict. |
| **We Do** | 24 exercises (8 subtopics × 3 kinds) | Per subtopic: E1 `guided` (fix inverted predicate) → E2 `independent` (3 paths: valid/adverso/missing) → E3 `transfer` (audit text artifact: Dockerfile/Compose/log/runbook/scan) | ✅ Strong progressive disclosure: E1 scaffolds the predicate, E2 adds missing-field routing, E3 transfers to a textual artifact. Each has `instruction`, `hint`, `hints[2]`, `edgeCases[3]`, `tests`, `feedback`, `starterCode`, `solutionCode`. |
| **You Do** | 1 capstone (CP-N4-A) | `title`, `context`, `objectives[4]`, `requirements[8]`, `starterCode` (Python scaffold), `portfolioNote`, `rubric[6]` (25+20+15+15+15+10 = 100%) | ✅ Rubric sums to 100%. Portfolio note explicitly says "BLOCKED by design; convert to READY by linking real artifacts, not by flipping asserts". Anti-cheat design. |
| **Self-Check** | 5 MCQs | 4 options each + `explanation` | ✅ All 5 questions test the contract/gate logic (not trivia). Correct indices are 2,0,1,3,2 — distributed. |

### 5.2 Cognitive load & progressive disclosure

- **Per-tab Fernández-Huerta**: theory FH=68 (normal), iDo FH=59 (normal), weDo FH=45 (difícil), youDo FH=24 (muy difícil), selfCheck FH=72 (bastante fácil). The progression from "easy" theory → "hard" youDo capstone is intentional and correct: scaffolding is removed as the learner advances. ✅
- **Per-tab WPS**: theory 13.3, iDo 16.8, weDo 13.0, youDo 10.8, selfCheck 13.4. All under 20. ✅
- **Per-tab SPW**: theory 2.09, weDo 2.48, youDo 2.87. Capstone has highest syllabic density (technical jargon), as expected for a synthesis task.
- **Forward-reference density**: T0 "Mapa de ideas" paragraph introduces 8 concepts at once. Better to break into a list (issue #10).
- **Breach-code vocabulary**: 16 distinct breach codes are introduced across 8 subtopics — high but consistent (each maps to a specific failure mode in `edgeCases`). The learner is given the codes upfront in theory callouts, so the weDo feedback `Breach → REBUILD_NONROOT` is anchored.

### 5.3 Connective tissue & narrative flow

- Each theory block opens with "Con … (T_n-_X), …" anaphora: "Con el cache de layers en orden (T1-A)…", "Con secretos fuera de la imagen (T2-A)…", "Con probes y shutdown claros (T2-B)…", "Con migraciones seguras (T3-B)…", "Cierra el camino a S44 (CI/CD): la imagen multi-stage (T4-A)…". This explicit backward link is **excellent connective tissue**. ✅
- Each theory block closes with a "En `CASO-TRU-043-T_X_Y` … Breach → `CODE`; falta de … → `CODE`." pattern. Consistent anchoring of operational vocabulary. ✅
- Each weDo E3 `transfer` opens with "Transferencia de artefacto: …" — consistent naming. ✅
- Each weDo `feedback` opens with `S43-T_X_Y-E_Z: explica …` — anaphoric monotony (#18) but consistent and easy to grade.

### 5.4 Exercise & exam quality and alignment

- All 24 weDo exercises share the **same defect-driven structure**: starter has a boolean predicate inverted (`PASS if not dependency_layer_reused and source_change_rebuilds > 3`), learner must rewrite it to `record["lock_copied_before_source"] and record["dependency_layer_reused"] and …`. ✅ Strong "find-the-bug" pedagogy.
- The 8 E3 transfer exercises all parse a real text artifact (Dockerfile, Compose, probe log, runbook, scan report) — this is **genuine artifact audit**, not toy predicates. ✅
- The 5 self-check questions are all aligned with theory contracts (e.g., Q1 maps to T1-A "dos builds con el mismo lock producen el mismo digest lógico de deps"). ✅
- The You Do capstone requires the learner to "Automatiza un caso normal (CONTINUE/PASS), uno de breach (p. ej. `REBUILD_NONROOT`, `REMOVE_BAKED_SECRET` o `QUARANTINE_IMAGE`) y uno incierto (`TRIAGE_SCAN_FINDING` o `INSPECT_CACHE_INVALIDATION`)" (L2059). This triangulation requirement is **best-in-class** for a capstone — it forces the learner to demonstrate the full decision space, not just the happy path. ✅

### 5.5 Redaction & technical writing

- Tone: **teacher voice** throughout. Direct address ("endureces la imagen", "elijes `python:3.12-slim`", "Corrige ambas ramas"). Spanish is consistent Peruvian-flavored ("depura", "ensayar", "hornear secretos"). ✅
- Spanish punctuation: `¿…?` correctly used in selfCheck questions (L2117, L2123, L2129, L2135, L2141). ✅
- Spanish typography: `«…» (comillas latinas)` used in T0 "Mapa" for `«arranqué después»` (L216), `«mágico»` (L65), `«puedo servir»` (L183), `«sin tope válido»` (L1955). Mostly consistent — one stray use of straight quotes `"` inside an explanation. ✅ Mostly
- Loanword formatting: most code/tech tokens are wrapped in backticks (`` `USER 10001` ``, `` `docker history` ``, `` `COPY --from=builder` ``). ✅
- Acronym handling: `CVE`, `CPU`, `DB`, `API`, `ENV`, `UID`, `SBOM`, `OOM`, `PII`, `CMD` — all introduced inline, no glossary. Acceptable for Master-level. ⚠️ (issue #20)

---

## 6. Grammatical Improvements — Paragraph by Paragraph (Before / After)

> For each tab and prose-bearing paragraph/sentence, the original is quoted verbatim, the issue is identified, and a rewritten version is proposed. **No diff is applied — audit only.**

### 6.1 Header — `tagline` (L8)

**Before:**
> Governed Python Service Platform: un comando, tests/health, non-root, config y recuperación documentadas

**Issues:**
- `documentadas` agrees with `recuperación` (fem sg) or with the implied compound "config y recuperación" (fem pl). LT flags it; both readings are defensible.
- `tests/health`, `non-root`, `config` are unmodulated English.
- Fragment, no terminal period (acceptable for a tagline).

**After (proposed):**
> Governed Python Service Platform: un comando, tests/health, non-root, configuración y recuperación documentadas.

(Add terminal period; expand `config` → `configuración` for parallelism with `recuperación`. Keep `documentadas` as fem plural agreeing with both nouns.)

### 6.2 Header — `jobRelevance` ¶1 (L15)

**Before:**
> En equipos de plataforma y producto, **contenedores y reproducibilidad operativa** empaquetan el servicio de S41–S42 en algo que se levanta con un comando: imagen mínima, non-root, health/readiness y shutdown limpio. Se promociona solo cuando el build es repetible en un entorno nuevo, no hay secretos horneados y los límites de recursos y CVE críticos están bajo control. Esta sección no cubre pipelines de fine-tuning de modelos: el foco es empaquetar y operar el servicio Python de forma reproducible.

**Issues:**
- ¶1 is 31w long-sentence (#11). Could split at the colon.
- `CVE críticos` should be `CVE crítico` (invariable acronym, #2).
- `los límites de recursos y CVE críticos están bajo control` reads ambiguously: are CVEs "bajo control" too? Yes, but the parallelism is loose.

**After (proposed):**
> En equipos de plataforma y producto, **contenedores y reproducibilidad operativa** empaquetan el servicio de S41–S42 en algo que se levanta con un comando: imagen mínima, non-root, health/readiness y shutdown limpio. Se promociona solo cuando el build es repetible en un entorno nuevo, no hay secretos horneados y los límites de recursos y los CVE crítico están bajo control. Esta sección no cubre pipelines de fine-tuning de modelos: el foco es empaquetar y operar el servicio Python de forma reproducible.

(Change `CVE críticos` → `CVE crítico` per RAE invariable-acronym rule.)

### 6.3 Theory T0 ¶1 — "Mapa de ideas" (L30) — **highest priority rewrite**

**Before:**
> **Mapa de ideas (luego T1 las aterriza con un Dockerfile real).** **Layer cache:** deps/lock antes de app para no invalidar en cada commit. **Non-root:** UID ≥1000 sin capabilities extras. **Secret injection:** solo en runtime, nunca en capas. **Health/readiness:** el proceso solo se declara listo cuando puede servir. **Compose:** API/worker/DB/cache con redes y health. **Multi-stage:** toolchain fuera de la imagen final. **Resource limits:** CPU/memoria acotados y > 0. **SBOM/scan:** inventario y CVEs antes de promover. En S44 conectarás estos gates al pipeline CI/CD.

**Issues:**
- 73w paragraph treated as one sentence by splitter (#10). Actually multiple short sentences, but the bold-label list-in-prose is hard to scan.
- `CPU/memoria acotados` gender (#3) → `acotadas`.
- `CVEs` → `CVE` (#2).
- `cache` → `caché` (#1).

**After (proposed) — render as bulleted list:**
> **Mapa de ideas** (T1 las aterriza con un Dockerfile real):
>
> - **Layer caché:** deps/lock antes de app para no invalidar el caché en cada commit.
> - **Non-root:** UID ≥1000 sin capabilities extras.
> - **Secret injection:** solo en runtime, nunca en capas.
> - **Health/readiness:** el proceso solo se declara listo cuando puede servir.
> - **Compose:** API/worker/DB/caché con redes y health.
> - **Multi-stage:** toolchain fuera de la imagen final.
> - **Resource limits:** CPU/memoria acotadas y > 0.
> - **SBOM/scan:** inventario y CVE crítico antes de promover.
>
> En S44 conectarás estos gates al pipeline CI/CD.

(10 short bullets + closing forward-link sentence; replaces 73-word paragraph.)

### 6.4 Theory T0 ¶2 (L31)

**Before:**
> Esta sección empaqueta el servicio seguro de S42 en **contenedores reproducibles** sin exigir un cluster: aprendes los contratos de Dockerfile y Compose (referencia Docker oficial) y los verificas primero con modelos en Python/stdlib que puedes correr en el navegador o en local. El caso `CASO-TRU-043` (plataforma ficticia en Trujillo) es sintético: sin secretos reales ni registro remoto obligatorio.

**Issues:**
- 42w sentence (#12). Split at colon.

**After:**
> Esta sección empaqueta el servicio seguro de S42 en **contenedores reproducibles** sin exigir un cluster. Aprendes los contratos de Dockerfile y Compose (referencia Docker oficial) y los verificas primero con modelos en Python/stdlib que puedes correr en el navegador o en local. El caso `CASO-TRU-043` (plataforma ficticia en Trujillo) es sintético: sin secretos reales ni registro remoto obligatorio.

### 6.5 Theory T0 ¶3 (L32)

**Before:**
> Producto incremental: Governed Python Service Platform. Entrada: código fijado, lock de deps, config no secreta y secretos inyectados en runtime. Salida: layers cacheables, non-root, health/readiness, compose API/worker/DB/cache y runbook de recovery. Error de promoción: root UID, secret horneado, health falso o migración no reversible.

**Issues:**
- Telegraphic fragments ("Entrada:", "Salida:", "Error de promoción:"). Acceptable contract style.
- `config` → `configuración` for parallelism with `secretos`.

**After (no change required; optional polish):**
> Producto incremental: Governed Python Service Platform. Entrada: código fijado, lock de deps, configuración no secreta y secretos inyectados en runtime. Salida: layers cacheables, non-root, health/readiness, compose API/worker/DB/cache y runbook de recovery. Error de promoción: root UID, secret horneado, health falso o migración no reversible.

### 6.6 Theory T0 ¶4 (L33)

**Before:**
> Orden de aprendizaje: T1 Dockerfile y non-root → T2 config, secretos y señales → T3 Compose y migraciones → T4 locks, multi-stage, scan y límites. En cada bloque verás el contrato, una demo que lo calcula y ejercicios que fallan cerrado si el build no es reproducible. Stack de práctica: **stdlib** para modelar el contrato; en el youDo documentas los artefactos reales (Dockerfile/Compose) listos para un entorno con Docker.

**Issues:**
- `config` → `configuración`.
- "Stack de práctica: **stdlib**" reads awkwardly — could be "**Stack de práctica (stdlib):** modela el contrato con la biblioteca estándar; en el youDo documentas los artefactos reales (Dockerfile/Compose) listos para un entorno con Docker."

**After:**
> Orden de aprendizaje: T1 Dockerfile y non-root → T2 configuración, secretos y señales → T3 Compose y migraciones → T4 locks, multi-stage, scan y límites. En cada bloque verás el contrato, una demo que lo calcula y ejercicios que fallan cerrado si el build no es reproducible. **Stack de práctica (stdlib):** modela el contrato con la biblioteca estándar; en el youDo documentas los artefactos reales (Dockerfile/Compose) listos para un entorno con Docker.

### 6.7 Theory T1-A ¶1 (L65)

**Before:**
> Ordena layers de **estable a cambiante**: base y dependencias primero, código de aplicación después. Así el cache de build acelera commits de app sin re-resolver pip en cada push. Un cache «mágico» que depende de estado oculto del host rompe la reproducibilidad entre máquinas. Lee el fragmento de abajo: `COPY requirements` + `RUN pip` van antes de `COPY src/`.

**Issues:**
- `cache` → `caché` (×2).
- "Así el cache de build acelera commits de app" is slightly compressed (could be "commits de la app" or "cambios en app").

**After:**
> Ordena layers de **estable a cambiante**: base y dependencias primero, código de aplicación después. Así el caché de build acelera los commits de la app sin re-resolver pip en cada push. Un caché «mágico» que depende de estado oculto del host rompe la reproducibilidad entre máquinas. Lee el fragmento de abajo: `COPY requirements` + `RUN pip` van antes de `COPY src/`.

### 6.8 Theory T1-A ¶2 (L66) — contract paragraph

**Before:**
> Contrato de cache. Entrada: secuencia de layers `base→deps→app→cmd` y lock de dependencias. Salida: `cache_hint=deps_before_app` y digest lógico estable entre dos builds con el mismo lock. Error: copiar el source antes del lock (invalida cache en cada commit) o hornear secretos en una layer. Criterio: en el lab de Trujillo sintético, un cambio solo de app no re-resuelve pip si deps no cambió.

**Issues:**
- `cache` → `caché` (×3).
- "o hornear secretos" → "u hornear secretos" per LT (#7, debatable).
- "invalida cache" — `invalida` is verb (false positive), keep as-is.

**After:**
> Contrato de caché. Entrada: secuencia de layers `base→deps→app→cmd` y lock de dependencias. Salida: `cache_hint=deps_before_app` y digest lógico estable entre dos builds con el mismo lock. Error: copiar el source antes del lock (invalida el caché en cada commit) u hornear secretos en una layer. Criterio: en el lab de Trujillo sintético, un cambio solo de app no re-resuelve pip si deps no cambió.

### 6.9 Theory T1-A ¶3 (L67)

**Before:**
> Aplicación a `CASO-TRU-043-T1A`: modelas el Dockerfile de la API de la plataforma ficticia. Sin secretos en capas; el secret se inyecta en runtime (T2-A). Evidencia: dos builds con el mismo lock producen el mismo digest lógico de deps. Si el orden falla, `REORDER_DOCKERFILE`.

**Issues:** none significant. Style is consistent contract-application form. ✅

### 6.10 Theory T1-B ¶1 (L113)

**Before:**
> Con el cache de layers en orden (T1-A), endureces la **imagen de runtime**. Una base mínima reduce superficie de ataque, pero debe seguir parchable: fija tag o digest (nunca `latest` suelto). Distroless/slim recortan shell y paquetes; el trade-off es depuración más difícil (lo resuelves en T4-B con shells efímeros, no root permanente). Ejecuta como UID de aplicación (≥1000), sin `CAP_SYS_ADMIN` ni capabilities extras, y acota el tamaño runtime (MB).

**Issues:**
- `cache` → `caché`.

**After:**
> Con el caché de layers en orden (T1-A), endureces la **imagen de runtime**. Una base mínima reduce superficie de ataque, pero debe seguir parchable: fija tag o digest (nunca `latest` suelto). Distroless/slim recortan shell y paquetes; el trade-off es depuración más difícil (lo resuelves en T4-B con shells efímeros, no root permanente). Ejecuta como UID de aplicación (≥1000), sin `CAP_SYS_ADMIN` ni capabilities extras, y acota el tamaño runtime (MB).

### 6.11 Theory T1-B ¶2 (L114)

**Before:**
> Contrato de base y usuario. Entrada: tag o digest de base, UID planificado (≥1000), conjunto de capabilities y techo de tamaño runtime. Salida: imagen con base fijada, proceso non-root y runtime bajo presupuesto de MB. Error de promoción: UID 0, capabilities extras, base mutable o imagen inflada sin justificación. Criterio local: `USER`/`uid` y tamaño se auditan antes de publicar.

**Issues:** none significant. ✅

### 6.12 Theory T1-B ¶3 (L115)

**Before:**
> En `CASO-TRU-043-T1B` (API de la plataforma ficticia en Trujillo) eliges `python:3.12-slim` (o distroless en runtime multi-stage) con digest, creas `appuser` 10001 y verificas non-root. El riesgo a documentar es superficie de ataque y privilegio, no identidad de personas. Sin evidencia de UID/base → `SELECT_PATCHABLE_BASE`; breach → `REBUILD_NONROOT`.

**Issues:** none significant. Strong risk-framing (data_privacy stance). ✅

### 6.13 Theory T2-A ¶1 (L146) — long sentence

**Before:**
> Partiendo de una imagen non-root (T1-B), la config **no secreta** (ENV de feature flags, puertos, log level) puede declararse en Compose o archivos montados. Los **secretos** (API keys, contraseñas DB) se inyectan en runtime vía secret store, env del orquestador o mounts de solo lectura; **nunca** van en `ENV KEY=valor` del Dockerfile ni en capas de build. Los volumes separan estado **durable** (DB) de **efímero** (cache, tmp): recrear el efímero no debe borrar el durable.

**Issues:**
- 34w sentence (#13). Split at semicolon.
- `config` → `configuración`.
- `cache` → `caché`.

**After:**
> Partiendo de una imagen non-root (T1-B), la configuración **no secreta** (ENV de feature flags, puertos, log level) puede declararse en Compose o archivos montados. Los **secretos** (API keys, contraseñas DB) se inyectan en runtime vía secret store, env del orquestador o mounts de solo lectura. **Nunca** van en `ENV KEY=valor` del Dockerfile ni en capas de build. Los volumes separan estado **durable** (DB) de **efímero** (caché, tmp): recrear el efímero no debe borrar el durable.

### 6.14 Theory T2-A ¶2 (L147)

**Before:**
> Contrato de secretos y estado. Entrada: capas de imagen inspeccionables, referencias de secret (`secret_ref`), config declarada y clasificación de volumes. Salida: imagen e inspección sin valor secreto; DB en volume durable; cache/tmp efímeros. Error: secret horneado, `.env` con secretos en la imagen, o DB montada como efímera. Criterio: `docker history`/inspección no revela secretos; rotación no requiere rebuild de app.

**Issues:**
- `config` → `configuración`.
- `cache` → `caché`.

**After:**
> Contrato de secretos y estado. Entrada: capas de imagen inspeccionables, referencias de secret (`secret_ref`), configuración declarada y clasificación de volumes. Salida: imagen e inspección sin valor secreto; DB en volume durable; caché/tmp efímeros. Error: secret horneado, `.env` con secretos en la imagen, o DB montada como efímera. Criterio: `docker history`/inspección no revela secretos; rotación no requiere rebuild de app.

### 6.15 Theory T2-B ¶1 (L183) — long sentence with semicolon

**Before:**
> Con secretos fuera de la imagen (T2-A), defines **quién habla con quién** y cuándo el proceso está listo. Red privada para DB/cache; API expone solo lo necesario. **Readiness** (`/readyz`) responde 200 solo si deps críticas (p. ej. DB) aceptan tráfico; si no, 503. **Liveness** (`/healthz`) detecta bloqueo del proceso (loop colgado), no «puedo servir». Ante **SIGTERM**, drena requests en curso y cierra conexiones dentro de un `grace_seconds` (p. ej. ≥20); un kill abrupto deja trabajo a medias.

**Issues:**
- `cache` → `caché`.

**After:**
> Con secretos fuera de la imagen (T2-A), defines **quién habla con quién** y cuándo el proceso está listo. Red privada para DB/caché; API expone solo lo necesario. **Readiness** (`/readyz`) responde 200 solo si deps críticas (p. ej. DB) aceptan tráfico; si no, 503. **Liveness** (`/healthz`) detecta bloqueo del proceso (loop colgado), no «puedo servir». Ante **SIGTERM**, drena requests en curso y cierra conexiones dentro de un `grace_seconds` (p. ej. ≥20); un kill abrupto deja trabajo a medias.

### 6.16 Theory T2-B ¶3 (L185)

**Before:**
> En `CASO-TRU-043-T2B` la API de Trujillo corre en red privada, valida DB en readiness y drena en 30s. Breach → `DRAIN_AND_ISOLATE`; falta de grace → `DIAGNOSE_HEALTH_SIGNAL`.

**Issues:**
- `30s` → `30 s` (#4).

**After:**
> En `CASO-TRU-043-T2B` la API de Trujillo corre en red privada, valida DB en readiness y drena en 30 s. Breach → `DRAIN_AND_ISOLATE`; falta de grace → `DIAGNOSE_HEALTH_SIGNAL`.

### 6.17 Theory T3-A ¶1 (L216) — long sentence

**Before:**
> Con probes y shutdown claros (T2-B), Compose declara el **stack local** de la plataforma: servicios `api`, `worker`, `db`, `cache`, redes (`front`/`back`) y healthchecks por servicio. El fragmento de abajo muestra la forma mínima: cuatro servicios y redes segmentadas. **`depends_on` no reemplaza retries de aplicación**: la API debe reintentar conexión a DB con backoff (`DB_MAX_ATTEMPTS` o equivalente); un simple «arranqué después» no basta si DB reinicia a mitad de tráfico.

**Issues:**
- `cache` → `caché`.

**After:**
> Con probes y shutdown claros (T2-B), Compose declara el **stack local** de la plataforma: servicios `api`, `worker`, `db`, `caché`, redes (`front`/`back`) y healthchecks por servicio. El fragmento de abajo muestra la forma mínima: cuatro servicios y redes segmentadas. **`depends_on` no reemplaza retries de aplicación**: la API debe reintentar conexión a DB con backoff (`DB_MAX_ATTEMPTS` o equivalente); un simple «arranqué después» no basta si DB reinicia a mitad de tráfico.

### 6.18 Theory T3-B ¶1 (L273)

**Before:**
> El stack de T3-A necesita **orden de arranque de datos**. Las migraciones de esquema son jobs controlados: patrón **expand/contract** (primero agregas columnas compatibles con código viejo; luego retiras lo obsoleto). Un `contract` incompatible con código aún en producción bloquea el release. Datos **efímeros** (tmp, cache) se recrean; datos **durables** (DB) exigen backup y drill de restore antes de confiar en el rollback.

**Issues:**
- `cache` → `caché`.

**After:**
> El stack de T3-A necesita **orden de arranque de datos**. Las migraciones de esquema son jobs controlados: patrón **expand/contract** (primero agregas columnas compatibles con código viejo; luego retiras lo obsoleto). Un `contract` incompatible con código aún en producción bloquea el release. Datos **efímeros** (tmp, caché) se recrean; datos **durables** (DB) exigen backup y drill de restore antes de confiar en el rollback.

### 6.19 Theory T3-B ¶2 (L274) — `migrate` loanword

**Before:**
> Contrato de migración. Entrada: tipo de migración (`expand`/`contract`), compatibilidad con código viejo, reset de efímeros y evidencia de restore. Salida: migración y rollback de prueba documentados. Error: contract sin compat, efímero tratado como durable, o backup nunca restaurado. Criterio: migrate antes de servir API; restore drill aprobado.

**Issues:**
- `migrate` (English DB-migration command) should be in code formatting to disambiguate from Spanish verb "migrar" (#8).

**After:**
> Contrato de migración. Entrada: tipo de migración (`expand`/`contract`), compatibilidad con código viejo, reset de efímeros y evidencia de restore. Salida: migración y rollback de prueba documentados. Error: contract sin compat, efímero tratado como durable, o backup nunca restaurado. Criterio: ejecutar `migrate` antes de servir la API; restore drill aprobado.

### 6.20 Theory T4-A ¶1 (L307) — long sentence

**Before:**
> Con migraciones seguras (T3-B), fijas **qué** se instala y **dónde** se compila. Un lock con hash (`sha256:…`) congela la resolución de deps; sin lock, el build de mañana no es el de hoy. **Multi-stage**: stage `builder` tiene compilers/SDK; stage `runtime` solo copia artefactos y deps de ejecución — sin toolchain. El `COPY --from=builder` es el puente; el runtime no debe incluir `gcc` ni wheels de build. El fragmento de abajo muestra builder → runtime con pin de base y `USER` non-root en la imagen final.

**Issues:** none significant. ✅ Long but well-structured (5 sentences joined by semicolons/em-dashes).

### 6.21 Theory T4-B ¶1 (L362)

**Before:**
> Cierra el camino a S44 (CI/CD): la imagen multi-stage (T4-A) entra a **política de scan**. Cero CVE críticos (o excepciones firmadas). **Límites** de memoria y CPU deben estar definidos y ser **> 0** (cero o ausente = unlimited disfrazado; no pasa el gate). Depuración con logs redactados y shells **efímeros**; un shell root permanente en la imagen de prod es breach. OOM simulado o CVE crítica bloquean el deploy.

**Issues:**
- `CVE críticos` → `CVE crítico` (#2).
- `CVE crítica` → `CVE crítico` (#2).

**After:**
> Cierra el camino a S44 (CI/CD): la imagen multi-stage (T4-A) entra a **política de scan**. Cero CVE crítico (o excepciones firmadas). **Límites** de memoria y CPU deben estar definidos y ser **> 0** (cero o ausente = unlimited disfrazado; no pasa el gate). Depuración con logs redactados y shells **efímeros**; un shell root permanente en la imagen de prod es breach. OOM simulado o CVE crítico bloquean el deploy.

### 6.22 Theory T4-B ¶2 (L363)

**Before:**
> Contrato de scan y límites. Entrada: conteo de CVE críticos, `memory_limit_mb`, `cpu_limit`, flag de debug shell y logs redactados. Salida: deploy permitido solo si scan limpio, límites en rango (0 < mem ≤ 512, 0 < cpu ≤ 1.0), sin shell de debug y logs sin secretos/PII. Error: CVE > 0, límites 0/ausentes, shell root o logs crudos. Criterio: `QUARANTINE_IMAGE` ante breach.

**Issues:**
- `CVE críticos` → `CVE crítico` (#2).

**After:**
> Contrato de scan y límites. Entrada: conteo de CVE crítico, `memory_limit_mb`, `cpu_limit`, flag de debug shell y logs redactados. Salida: deploy permitido solo si scan limpio, límites en rango (0 < mem ≤ 512, 0 < cpu ≤ 1.0), sin shell de debug y logs sin secretos/PII. Error: CVE > 0, límites 0/ausentes, shell root o logs crudos. Criterio: `QUARANTINE_IMAGE` ante breach.

### 6.23 I Do — `intro` (L396)

**Before:**
> Ocho demos de S43 calculan el contrato de contenedores (CP-N4-A): cada una deriva evidencia de entradas, no imprime un veredicto hardcodeado.

**Issues:** none. ✅ Strong, concise.

### 6.24 I Do — `why` strings (L424, L449, L475, L501, L534, L561, L601, L628)

Each `why` is one sentence describing the derivation logic. **No issues.** ✅

### 6.25 We Do — `intro` (L633)

**Before:**
> S43 · Laboratorio Governed Python Service Platform reproducible: 24 retos. E1 repara el predicado de dominio, E2 separa válido/adverso/missing y E3 audita un artefacto de texto (Dockerfile, Compose, log de probes, runbook, scan) con CONTINUE | breach | incertidumbre. Un defecto ops intencional por ejercicio; fixtures `CASO-TRU-043`.

**Issues:**
- 27w second sentence (#15). Could be split.

**After:**
> S43 · Laboratorio Governed Python Service Platform reproducible: 24 retos. E1 repara el predicado de dominio. E2 separa válido/adverso/missing. E3 audita un artefacto de texto (Dockerfile, Compose, log de probes, runbook, scan) con CONTINUE | breach | incertidumbre. Un defecto ops intencional por ejercicio; fixtures `CASO-TRU-043`.

### 6.26 We Do — `instruction` strings (24 of them, L639, L676, L730, L805, L842, L896, L979, L1016, L1070, L1139, L1176, L1230, L1315, L1352, L1406, L1506, L1543, L1597, L1674, L1711, L1765, L1850, L1893, L1955)

Each `instruction` is a 60–120-word directive paragraph. **All consistent in style:** `S43-T_X_Y-E_Z · <verb> el contrato de <topic> sobre CASO-TRU-043-_X_Y. La entrada es el dict completo del starter; la operación debe demostrar <criteria>. Reemplaza la expresión booleana defectuosa, no los datos ni el assert. Salida exacta: \`S43-T_X_Y PASS\`; la misma operación sobre el fixture adverso debe activar \`CODE\` en E2.`

**Issues:**
- Anaphoric monotony (every instruction starts with `S43-T_X_Y-E_Z · <verb>`). Acceptable for grading consistency.
- Some instructions are 100+ words; acceptable for a Master-level course.

**Verdict:** No rewriting required; style is intentional and consistent.

### 6.27 We Do — `feedback` strings (24 of them)

Each starts with `S43-T_X_Y-E_Z: explica qué campo cambió la decisión, por qué el adverso activa <CODE> y por qué faltar <field> exige <CODE>.`

**Issues:** Anaphoric monotony (#18). Acceptable for consistency. No rewriting required.

### 6.28 We Do — `edgeCases` entries (3 per exercise × 24 = 72 entries)

Each entry is a lowercase shorthand fragment like `"falta digest_stable → INSPECT_CACHE_INVALIDATION"`, `"adverso: source antes de lock / deps no reutilizadas / rebuilds altos → REORDER_DOCKERFILE"`, `"CASO-TRU-043-1A es sintético"`.

**Issues:**
- Lowercase start (#9). Design choice.

**After (optional):** Capitalize first letter of each entry if rendered as bullets. Otherwise, no change.

### 6.29 We Do — `hint` / `hints` strings

Each exercise has 1 `hint` and 2 `hints` (the first duplicates `hint`, the second adds a strategy hint). **No issues.** ✅

### 6.30 We Do — `tests` strings

Each is a one-sentence description of what the test asserts. **No issues.** ✅

### 6.31 You Do — `context` (L2046)

**Before:**
> Governed Python Service Platform reproducible. Trabaja sobre API, worker, base y cache locales de una plataforma ficticia en Trujillo. Entrada: código fijado, locks, configuración no secreta y secretos inyectados en runtime. Salida: imágenes mínimas, servicios sanos y recuperación documentada con un comando. El gate se bloquea si hay imagen mutable, proceso root, health check falso o migración no reversible.

**Issues:**
- `cache` → `caché`.

**After:**
> Governed Python Service Platform reproducible. Trabaja sobre API, worker, base y caché locales de una plataforma ficticia en Trujillo. Entrada: código fijado, locks, configuración no secreta y secretos inyectados en runtime. Salida: imágenes mínimas, servicios sanos y recuperación documentada con un comando. El gate se bloquea si hay imagen mutable, proceso root, health check falso o migración no reversible.

### 6.32 You Do — `objectives` (L2048–L2051)

4 items, each an infinitive phrase. **No issues.** ✅

### 6.33 You Do — `requirements` (L2054–L2061)

8 items, each an infinitive phrase with backticked code references. **No issues.** ✅

### 6.34 You Do — `portfolioNote` (L2104)

**Before:**
> Evidencia de CP-N4-A · servicio reproducible en contenedores: muestra baseline, decisión, pruebas, resultado medido, rollback y riesgo residual. El checklist inicia en BLOCKED por diseño; conviértelo en READY enlazando artefactos reales (Dockerfile, compose.yaml, runbook), no cambiando asserts a True sin archivo.

**Issues:**
- `El checklist` → `La checklist` (#5).

**After:**
> Evidencia de CP-N4-A · servicio reproducible en contenedores: muestra baseline, decisión, pruebas, resultado medido, rollback y riesgo residual. La checklist inicia en BLOCKED por diseño; conviértela en READY enlazando artefactos reales (Dockerfile, compose.yaml, runbook), no cambiando asserts a True sin archivo.

(Also `conviértelo` → `conviértela` for fem agreement with `La checklist`.)

### 6.35 You Do — `rubric` (L2106–L2111)

6 criteria with weights. **No issues.** ✅ Sums to 100%.

### 6.36 Self-Check — `question` strings (L2117, L2123, L2129, L2135, L2141)

**Before (Q1):**
> ¿Qué evidencia permite aprobar `Dockerfile, layers y cache` en CASO-TRU-043?

**Issues:**
- `cache` → `caché`.

**After:**
> ¿Qué evidencia permite aprobar `Dockerfile, layers y caché` en CASO-TRU-043?

(Same fix applies to Q4 L2135 — `CASO-TRU-043` doesn't mention cache, but the surrounding explanations do.)

### 6.37 Self-Check — `options` arrays (4 options × 5 questions = 20 strings)

Each option is a short fragment. **No issues** except the lowercase-start design choice on some options (consistent with MCQ style). ✅

### 6.38 Self-Check — `explanation` strings (L2120, L2126, L2132, L2138, L2144)

**Before (Q5 explanation):**
> La teoría exige que dos builds con el mismo lock produzcan el mismo digest lógico de deps; evidencia decorativa o PII no satisface el contrato.

**Issues:** none. ✅

---

## 7. Proposed GitHub-style Diffs

> One diff per logical issue. Apply selectively. All diffs are proposals — **do not auto-apply** without manual review.

### Diff D1 — `cache` → `caché` (recurring, ~20+ instances)

```diff
--- a/src/lib/course/sections/s43-llmops.ts
+++ b/src/lib/course/sections/s43-llmops.ts
@@ -L30, L65, L66, L113, L146, L147, L183, L184, L216, L273, L362, L633, L2046, L2117 (and ~10 more)
- ... el cache de layers ...
- ... Contrato de cache. ...
- ... invalida cache en cada commit ...
- ... DB/cache; API ...
- ... cache/tmp efímeros ...
- ... servicios api, worker, db, cache ...
- ... (tmp, cache) se recrean ...
- ... conteo de CVE críticos ...
+ ... el caché de layers ...
+ ... Contrato de caché. ...
+ ... invalida el caché en cada commit ...
+ ... DB/caché; API ...
+ ... caché/tmp efímeros ...
+ ... servicios api, worker, db, caché ...
+ ... (tmp, caché) se recrean ...
+ ... conteo de CVE crítico ...
```

**Note:** This is a sweeping change — apply via `sed` with a word-boundary regex `\bcache\b` → `caché`, then verify each instance in code blocks (e.g., the variable `EPHEMERAL = {"cache"}` and `cfg["cache_ephemeral"]`) is preserved. **Do NOT change code identifiers** like `cache_ephemeral`, `EPHEMERAL = {"cache"}`, `durable_volumes`/`ephemeral_volumes` set literals — only the Spanish prose mentions of the noun `cache`.

### Diff D2 — `CVEs` → `CVE` (RAE invariable acronym)

```diff
@@ -L24 (learningOutcomes)
- "Escanear CVEs, definir límites CPU/memoria > 0 y depurar sin shell root permanente"
+ "Escanear CVE, definir límites CPU/memoria > 0 y depurar sin shell root permanente"

@@ -L30 (T0 Mapa)
- **SBOM/scan:** inventario y CVEs antes de promover.
+ **SBOM/scan:** inventario y CVE crítico antes de promover.

@@ -L362 (T4-B ¶1)
- Cero CVE críticos (o excepciones firmadas).
+ Cero CVE crítico (o excepciones firmadas).

@@ -L362 (T4-B ¶1)
- OOM simulado o CVE crítica bloquean el deploy.
+ OOM simulado o CVE crítico bloquea el deploy.

@@ -L363 (T4-B ¶2 contract)
- Entrada: conteo de CVE críticos, `memory_limit_mb`, ...
+ Entrada: conteo de CVE crítico, `memory_limit_mb`, ...
```

### Diff D3 — `CPU/memoria acotados` → `CPU/memoria acotadas` (gender agreement)

```diff
@@ -L30 (T0 Mapa)
- **Resource limits:** CPU/memoria acotados y > 0.
+ **Resource limits:** CPU/memoria acotadas y > 0.
```

### Diff D4 — `30s` → `30 s` (typography: space before unit)

```diff
@@ -L185 (T2-B ¶3)
- En `CASO-TRU-043-T2B` la API de Trujillo corre en red privada, valida DB en readiness y drena en 30s.
+ En `CASO-TRU-043-T2B` la API de Trujillo corre en red privada, valida DB en readiness y drena en 30 s.
```

### Diff D5 — `El checklist` → `La checklist` (loanword gender)

```diff
@@ -L2104 (portfolioNote)
- El checklist inicia en BLOCKED por diseño; conviértelo en READY enlazando artefactos reales...
+ La checklist inicia en BLOCKED por diseño; conviértela en READY enlazando artefactos reales...
```

### Diff D6 — `migrate` → `` `migrate` `` (code-formatting for English command)

```diff
@@ -L274 (T3-B ¶2 contract)
- Criterio: migrate antes de servir API; restore drill aprobado.
+ Criterio: ejecutar `migrate` antes de servir la API; restore drill aprobado.
```

### Diff D7 — T0 "Mapa de ideas" paragraph → bulleted list (highest pedagogy priority)

```diff
@@ -L30 (theory[0].paragraphs[0])
-        "**Mapa de ideas (luego T1 las aterriza con un Dockerfile real).** **Layer cache:** deps/lock antes de app para no invalidar en cada commit. **Non-root:** UID ≥1000 sin capabilities extras. **Secret injection:** solo en runtime, nunca en capas. **Health/readiness:** el proceso solo se declara listo cuando puede servir. **Compose:** API/worker/DB/cache con redes y health. **Multi-stage:** toolchain fuera de la imagen final. **Resource limits:** CPU/memoria acotados y > 0. **SBOM/scan:** inventario y CVEs antes de promover. En S44 conectarás estos gates al pipeline CI/CD.",
+        "**Mapa de ideas** (T1 las aterriza con un Dockerfile real):\n\n- **Layer caché:** deps/lock antes de app para no invalidar en cada commit.\n- **Non-root:** UID ≥1000 sin capabilities extras.\n- **Secret injection:** solo en runtime, nunca en capas.\n- **Health/readiness:** el proceso solo se declara listo cuando puede servir.\n- **Compose:** API/worker/DB/caché con redes y health.\n- **Multi-stage:** toolchain fuera de la imagen final.\n- **Resource limits:** CPU/memoria acotadas y > 0.\n- **SBOM/scan:** inventario y CVE crítico antes de promover.\n\nEn S44 conectarás estos gates al pipeline CI/CD.",
```

### Diff D8 — Split long sentence in T2-A ¶1 (L146)

```diff
@@ -L146 (T2-A ¶1)
-        "Partiendo de una imagen non-root (T1-B), la config **no secreta** (ENV de feature flags, puertos, log level) puede declararse en Compose o archivos montados. Los **secretos** (API keys, contraseñas DB) se inyectan en runtime vía secret store, env del orquestador o mounts de solo lectura; **nunca** van en `ENV KEY=valor` del Dockerfile ni en capas de build. Los volumes separan estado **durable** (DB) de **efímero** (cache, tmp): recrear el efímero no debe borrar el durable.",
+        "Partiendo de una imagen non-root (T1-B), la configuración **no secreta** (ENV de feature flags, puertos, log level) puede declararse en Compose o archivos montados. Los **secretos** (API keys, contraseñas DB) se inyectan en runtime vía secret store, env del orquestador o mounts de solo lectura. **Nunca** van en `ENV KEY=valor` del Dockerfile ni en capas de build. Los volumes separan estado **durable** (DB) de **efímero** (caché, tmp): recrear el efímero no debe borrar el durable.",
```

### Diff D9 — Split long sentence in T0 ¶2 (L31)

```diff
@@ -L31 (T0 ¶2)
-        "Esta sección empaqueta el servicio seguro de S42 en **contenedores reproducibles** sin exigir un cluster: aprendes los contratos de Dockerfile y Compose (referencia Docker oficial) y los verificas primero con modelos en Python/stdlib que puedes correr en el navegador o en local. El caso `CASO-TRU-043` (plataforma ficticia en Trujillo) es sintético: sin secretos reales ni registro remoto obligatorio.",
+        "Esta sección empaqueta el servicio seguro de S42 en **contenedores reproducibles** sin exigir un cluster. Aprendes los contratos de Dockerfile y Compose (referencia Docker oficial) y los verificas primero con modelos en Python/stdlib que puedes correr en el navegador o en local. El caso `CASO-TRU-043` (plataforma ficticia en Trujillo) es sintético: sin secretos reales ni registro remoto obligatorio.",
```

### Diff D10 — `u hornear` (optional, debatable)

```diff
@@ -L66 (T1-A ¶2 contract)
- Error: copiar el source antes del lock (invalida cache en cada commit) o hornear secretos en una layer.
+ Error: copiar el source antes del lock (invalida el caché en cada commit) u hornear secretos en una layer.
```

(RAE DPD: rule applies before /o/ sound; `hornear` starts with /o/. Native usage is split — keep `o` if you prefer the conservative form.)

### Diff D11 — Filename/id legacy rename (separate refactor)

```diff
--- a/src/lib/course/sections/s43-llmops.ts
+++ b/src/lib/course/sections/s43-containers.ts  (rename)
@@ -L4
-  id: "llmops",
+  id: "containers",
```

(Also update `src/lib/course/index.ts` import and the live site URL fragment from `#llmops` to `#containers`. Out of scope for this audit — coordinate with other section legacy renames like S32.)

---

## 8. Recommended Priority Order for Fixing

1. **D1** — `cache` → `caché` (recurring, ~20+ instances, RAE-prescribed). Highest frequency, lowest risk. Use word-boundary sed and verify code identifiers are untouched.
2. **D2** — `CVEs` / `CVE críticos` → `CVE` / `CVE crítico` (RAE invariable acronym).
3. **D7** — T0 "Mapa de ideas" paragraph → bulleted list (highest pedagogy impact; reduces 73w paragraph to 8 scannable bullets).
4. **D3** — `CPU/memoria acotados` → `acotadas` (gender agreement).
5. **D8** — Split T2-A ¶1 long sentence (34w → 3 sentences).
6. **D9** — Split T0 ¶2 long sentence (42w → 2 sentences).
7. **D4** — `30s` → `30 s` (typography).
8. **D6** — `migrate` → `` `migrate` `` (code-format disambiguation).
9. **D5** — `El checklist` → `La checklist` (loanword gender; debatable, optional).
10. **D10** — `o hornear` → `u hornear` (debatable, optional).
11. **D11** — Filename/id legacy rename (separate refactor pass; coordinate with S32 and other legacy-id sections).

---

## 9. Graph Memory Update Notes (for shared context files)

> Notes for the orchestrator and downstream Fixer agents.

- **S43 = `src/lib/course/sections/s43-llmops.ts`** (2,213 LOC). V3 title "Contenedores y reproducibilidad operativa". Filename/id is legacy "llmops" (V1/V2 leftover). Same legacy pattern as S32 (`s32-microservices.ts` → V3 "Feature engineering").
- **Capstone**: CP-N4-A (Phase-3 closer). Strong I-Do (8 demos) / We-Do (24 exercises) / You-Do (rubric sums to 100%) / Self-Check (5 MCQs) fidelity.
- **Meta-leak status**: ✅ CLEAN. Zero TODO/FIXME, zero author-to-dev comments, zero design notes leaked. All `//` matches are URL fragments inside string literals.
- **Real issues (priority order)**:
  1. (M, recurring ~20×) `cache` → `caché` per RAE — apply via sed with word-boundary, exclude code identifiers (`cache_ephemeral`, `EPHEMERAL = {"cache"}`, `cfg["cache_ephemeral"]`).
  2. (M, 3+ instances) `CVEs` → `CVE` per RAE invariable-acronym rule.
  3. (M, pedagogy) T0 "Mapa de ideas" 73w paragraph → bulleted list (D7).
  4. (L) Long sentences ≥34w in T0 ¶2 (D9), T2-A ¶1 (D8), jobRelevance ¶1 (D11).
  5. (L) `CPU/memoria acotados` → `acotadas` gender agreement (D3).
  6. (L) `30s` → `30 s` typography (D4).
  7. (L) `El checklist` → `La checklist` loanword gender (D5, debatable).
  8. (L) `migrate` should be code-formatted to disambiguate from Spanish verb "migrar" (D6).
  9. (L, design choice) `edgeCases` entries start lowercase — capitalize if rendered as bullets.
- **No meta-leaks. No TODO/FIXME. No design notes. No author-to-developer comments. Zero // or /* */ comments in source outside intentional code blocks.**
- **Operational vocabulary (breach codes)** — strong differentiator: 16 distinct codes (`REORDER_DOCKERFILE`, `REBUILD_NONROOT`, `REMOVE_BAKED_SECRET`, `DRAIN_AND_ISOLATE`, `STOP_UNHEALTHY_STACK`, `ROLL_BACK_MIGRATION`, `BLOCK_UNPINNED_BUILD`, `QUARANTINE_IMAGE`, `CLASSIFY_VOLUME`, `WAIT_FOR_DEPENDENCY`, `DIAGNOSE_HEALTH_SIGNAL`, `INSPECT_CACHE_INVALIDATION`, `RUN_RESTORE_DRILL`, `REGENERATE_LOCK`, `SELECT_PATCHABLE_BASE`, `TRIAGE_SCAN_FINDING`). Each maps to a specific failure mode and is reused across theory callouts, weDo edgeCases, and youDo capstone. Preserve in any rewrite.
- **Synthetic case study `CASO-TRU-043` (Trujillo)** — explicitly stated as synthetic, no real secrets, no real services. Strong data-privacy stance ("El riesgo a documentar es superficie de ataque y privilegio, no identidad de personas"). Preserve.
- **Cross-section links**: S41 (auth), S42 (servicio seguro), S44 (CI/CD). All consistent with V3 roadmap.
- **Report file**: `/home/z/my-project/audits/S43_report.md`
- **Auxiliary artifacts**: `S43_records.json` (175 prose records), `S43_metrics.json` (aggregate + worst sentences), `S43_prose.txt` (plain prose for LT), `S43_lt.json` (617 LT matches, 47 non-spellchecker, ~15 genuine findings after false-positive filtering).

---

**This is the complete Explorer report for Section 43. Ready for the Fixer prompt.**
