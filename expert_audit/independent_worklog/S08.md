# Independent worklog — S08

- **Owner:** independent Section 08 owner
- **Canonical section:** `index: 8` · compatibility `id: pandas` · **Archivos, CSV, JSON y contratos de ingesta**
- **Branch:** `agent/independent-s08`
- **Scope:** Section 8 only
- **Prior Fixer evidence:** explicitly excluded

## Surfaces completed

- 1 section map and all 8 theory subtopics
- 8 I Do demos
- 24 We Do exercises, including starter, solution, expected output, feedback, and retrospective
- 1 You Do portfolio increment and rubric
- 11 public self-check questions
- 24 authenticated questions: 8 concepts × 3 variants
- S08-owned playground and PDF mappings
- public Pages baseline and local production export

## Resolutions

- Replaced the stale Pandas/groupby playground with an executable stdlib CSV → clean/quarantine → manifest lab.
- Corrected the PDF identity from `8. Pandas` to `8. Archivos & ETL`.
- Rebalanced the authenticated bank from 24/24 at position 1 to 6/6/6/6 overall.
- Balanced each deterministic attempt to 2/2/2/2 and moved every concept’s answer across three distinct positions.
- Replaced implausible distractors and aligned `col_count`, datetime serialization, and minimum provenance wording.
- Removed the learner-facing `S08 V3` / platform-id assessment leak.
- Reframed the section opener around evidence of loss detection rather than comparison with a groupby demo.
- Corrected `reprocessar` and named `UnicodeDecodeError` precisely.
- Moved seven unchanged exercise IDs next to their instructions so the newbie-packet parser exposes all 24 canonical IDs exactly once and in order.
- Added a sixth focused adversarial regression for the learner-manifest contract.

## Validation

- Focused S08 tests: 6/6 pass.
- S08 newbie manifest: exact canonical order, 24/24 unique.
- Exact fleet packet test: S08 subtest passes; this isolated fork still reports 50 unrelated section subtest failures because their repair branches are not present.
- Python artifacts: 65/65 pass; fail=0, skip=0, P0=0, P1=0.
- TypeScript and ESLint: pass.
- V3 counts, structure, and invariants: pass.
- Exam audit: 1,248 questions / 416 concepts; P0=0, P1=0.
- Production static export: pass; 3/3 pages generated.
- Local exported HTTP response: 200 with expected title.
- Static bundle: corrected playground/PDF labels present; old playground title absent.
- Fleet-generated JSON side effects: restored and excluded from staging.

## Residual

The `pandas` compatibility id/hash, bank key, and filename remain until a coordinated route/progress-key migration. Public verification must be repeated after the parent integrates and deploys the batch. Bare section-code references and historical audit snapshots remain fleet-level editorial/closing tasks.

Section 8 has been fully fixed and validated under strict anti-aberration rules. Ready for the next section.
