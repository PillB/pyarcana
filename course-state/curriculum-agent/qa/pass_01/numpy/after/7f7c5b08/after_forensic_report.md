# S06 post-fix rendered forensics

- Outer pass: `01`
- Section: `numpy` — S06, “Colecciones y estructuras de datos”
- Exact tested source SHA: `7f7c5b08`
- Capture: `capture_001`
- Optimized build: passed
- Result: `2 passed (26.3s)`

## Complete visual coverage

All five tabs were recaptured as lossless, hashed viewport tiles at desktop `1440×1000` and mobile `390×844`. Desktop coverage sums exactly to theory `11,407`, Yo hago `11,970`, Hacemos juntos `27,689`, Tú haces `3,728`, and Autocheck `4,036` CSS pixels. Mobile sums exactly to `17,840`, `20,325`, `41,095`, `5,757`, and `4,912` respectively.

Every tab records page width equal to viewport width, zero learner-visible horizontal overflow, zero fixed-control obstruction, zero console errors, and zero page errors. Card boundaries, code surfaces, hint panels, project rubric, and assessment choices remain contained. No lesson content or assessment was rewritten.

## Glossary boundary repair

Rendered controls now expose context-safe micro-definitions:

- `Dict`: a key-value collection in which a key locates its value; no unexplained O(1) claim.
- `Tuple`: an ordered sequence that is not modified after creation; no unsupported “faster” claim.
- `Shape`: the form/structure of records or collections, without assuming a future NumPy array API.
- `Merge`: combining collections or records by a common key/rule, without leaking future DataFrame join vocabulary.

The definitions remain visible through the existing accessible term-hint controls and fit the S06 mental model.

## Audit-harness repair

The learner prompt now provides the exact packet-derived exercise ID catalog, including canonical `S06-YOU-DO`. The sealer independently recomputes that catalog and rejects missing, duplicate, invented, or misspelled IDs. RED evidence included one omitted You Do and repeated `S06-YOUDO` spellings; fresh reruns sealed only after returning all 25 canonical IDs.

The realistic prompt additionally requires empty `observed_output` and `CANNOT_VERIFY` when its manifest provides no execution receipt. Both selected realistic learners complied, so their evidence is admissible as `BLOCKED_ENVIRONMENT`, not as completed execution.

## Visual-aid decision

`VISUAL_AID_NOT_REQUIRED`. The existing ordinary collection-choice questions, memory-arrow predictions, nested examples, and before/after output traces produced accurate copy/identity models across both epistemic and realistic semantic identities. A copy graph remains optional P3 enrichment only.

## Gate decision

Local optimized rendering and semantic epistemic review are GREEN. S06 realistic execution remains `CANNOT_VERIFY` until an isolated student runtime can produce per-attempt execution receipts. Live Pages validation remains blocked until the review-required PR is merged and deployed.
