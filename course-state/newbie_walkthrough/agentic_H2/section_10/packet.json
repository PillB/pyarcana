{
  "attempt_id": "agentic_H2",
  "section_index": 10,
  "landing": {
    "title": "PyArcana",
    "subtitle": "De cero a Data Scientist — transforma datos en decisiones que cambian negocios",
    "description": "Curso autónomo en español peruano, basado en el método I Do / We Do / You Do. 52 secciones en 4 fases que te llevan desde instalar Python hasta arquitectar plataformas de IA a escala. Incluye adquisición de datos, análisis con pandas, ML production-ready, RPA con IA, seguridad, ingeniería de datos, LLMs, y automatización multi-modal. Bienvenido a PyArcana.",
    "hero_badge": "PyArcana · 52 secciones · Español peruano",
    "brand_tagline": "El arte de aprender Python",
    "curriculum_summary": "52 secciones · método I Do / We Do / You Do · proyectos de portafolio",
    "public_edition_notice": "Edición pública / Public edition: el progreso se guarda solo en este navegador. GitHub Pages no ofrece cuentas, pagos, feedback privado ni panel de administración.",
    "footer_copy": [
      "{tr('footer.method')} · interfaz es-PE, es-ES y English · lecciones en español peruano / lessons in Peruvian Spanish"
    ],
    "method_cards": [
      {
        "title": "Yo hago — Demostración",
        "desc": "Te muestro paso a paso cómo se resuelve un problema real, explicando el porqué de cada línea."
      },
      {
        "title": "Hacemos juntos — Práctica guiada",
        "desc": "Escribes código con mi guía. Te doy starter code, hints y solución para que compares."
      },
      {
        "title": "Tú haces — Proyecto de portafolio",
        "desc": "Construyes un mini-proyecto que va directo a tu GitHub. Esto es lo que muestras en entrevistas."
      },
      {
        "title": "Quiz con feedback inmediato",
        "desc": "Active recall para fijar la memoria. Necesitas 70% para desbloquear la siguiente sección."
      }
    ],
    "why_cards": [
      {
        "title": "Casos situados en Perú",
        "desc": "Practicas con escenarios sintéticos de trabajo y datos; no usamos información personal real ni prometemos resultados laborales."
      },
      {
        "title": "Proyectos con evidencia verificable",
        "desc": "Cada entrega pide código, pruebas, decisiones y límites para que puedas explicar qué construiste y cómo lo validaste."
      },
      {
        "title": "Ruta trazable y progresiva",
        "desc": "Los conceptos avanzan desde fundamentos hasta sistemas de datos e IA, con documentación oficial enlazada en cada sección."
      },
      {
        "title": "Aprendizaje autoguiado",
        "desc": "Avanzas a tu ritmo y puedes pedir revisión externa cuando la necesites. En la edición pública, el progreso se guarda solo en tu navegador."
      }
    ],
    "brand": "PyArcana",
    "language_truth": {
      "interface_and_lessons_claim": "{tr('footer.method')} · interfaz es-PE, es-ES y English · lecciones en español peruano / lessons in Peruvian Spanish",
      "public_edition_claim": "Edición pública / Public edition: el progreso se guarda solo en este navegador. GitHub Pages no ofrece cuentas, pagos, feedback privado ni panel de administración."
    },
    "source_sha": "78058f8237924509"
  },
  "prior_sections": [
    {
      "file": "src/lib/course/sections/s01-setup.ts",
      "id": "setup",
      "index": 1,
      "title": "Entorno reproducible y trabajo seguro",
      "shortTitle": "Entorno reproducible",
      "tagline": "Python, editor, entorno aislado (venv) y control de versiones (Git) listos desde el día 1",
      "jobRelevance": "El 90% de los problemas en equipos de data science no son de código: son de **entorno** (la máquina, el Python y los paquetes con los que corres el proyecto). Un analista que sabe crear un **entorno virtual** (carpeta aislada de Python + paquetes, con la herramienta `venv`), usar **Git** (historial de cambios del código) y un editor como VS Code ahorra horas al equipo. En empresas peruanas como Interbank, BBVA o Caja Arequipa, el primer día suelen pedirte **clonar un repo** (repositorio: la carpeta del proyecto con su historial Git en GitHub u otro remoto), **activar el entorno virtual** y correr un notebook. Si te trabas ahí, no pasas la semana de prueba. Esta sección te enseña cada una de esas palabras antes de usarla a fondo.",
      "learningOutcomes": [
        "Seleccionar el intérprete Python correcto (el programa que ejecuta tu código) y usar el REPL (modo interactivo) para inspección rápida",
        "Interpretar códigos de salida (0 = éxito, no-cero = error) y distinguir PATH (dónde el sistema busca programas) del directorio de trabajo",
        "Instalar Python 3.12+ y verificarlo desde la terminal (PowerShell, bash o zsh)",
        "Crear y activar un entorno virtual con venv (carpeta `.venv` por proyecto, aislada del Python global)",
        "Configurar VS Code con extensiones Python esenciales (Pylance, Ruff, Jupyter)",
        "Inicializar un repositorio Git (repo: carpeta con historial), hacer commit/push y abrir un Pull Request (propuesta de cambios) en GitHub",
        "Escribir un requirements.txt (lista de paquetes y versiones) reproducible y un .gitignore (qué no subir a Git)"
      ],
      "theory": [
        {
          "subtopicId": "S01-T1-A",
          "heading": "El intérprete Python y el REPL · El intérprete en la terminal (comandos de verificación)",
          "paragraphs": [
            "Cuando instalas Python, lo que realmente instalas es un **intérprete**: un programa que lee tu código y lo ejecuta. En la terminal, ese programa suele llamarse `python` o `python3`. La primera habilidad profesional no es escribir un algoritmo: es **verificar qué intérprete estás usando**. En Windows a veces el comando `python` no está en el PATH (la lista de carpetas donde el sistema busca programas). En macOS/Linux es común tener `python3` como comando principal. Por eso siempre empiezas con `python --version` (o `python3 --version`) y anotas la respuesta. Para este curso apuntamos a **Python 3.12 o superior**.",
            "El **REPL** (Read–Eval–Print Loop) es el modo interactivo del intérprete. Lo abres escribiendo solo `python` (o `python3`) y Enter. Verás el prompt `>>>`. Ahí puedes escribir una expresión, presionar Enter, y Python la evalúa al instante: `2 + 2` devuelve `4`, `type(\"hola\")` devuelve `<class 'str'>`. Es ideal para probar una idea en 10 segundos sin crear un archivo. Para salir: `quit()` o `exit()`, o el carácter de fin de archivo (Ctrl-D en macOS/Linux, Ctrl-Z y Enter en Windows). Salir del REPL **no cierra** tu terminal: vuelves al prompt de la shell (`$` o `PS>`).",
            "Hay una diferencia crítica entre **sesión REPL** y **script `.py`**. En el REPL cada línea se ejecuta al presionar Enter. En un script, escribes el programa completo en un archivo y lo lanzas con `python hello.py`. El script es lo que subes a GitHub y lo que corre en producción o en un pipeline. El REPL es tu laboratorio de bolsillo. Cuando un colega dice \"ábrelo en el intérprete y mira el tipo\", te está pidiendo el REPL. Cuando dice \"corre el entrypoint\", te está pidiendo un archivo. Confundir ambos genera la sensación de que \"a mí me funciona\" en la laptop y falla en el servidor.",
            "Tu primer script usa tres piezas mínimas: (1) **`print(...)`** escribe texto a la salida estándar (lo ves en la terminal). (2) **`def nombre():`** define una función — un bloque indentado que puedes reutilizar; por convención el entrypoint se llama `main`. (3) el guardián **`if __name__ == \"__main__\":`** solo corre `main()` cuando ejecutas el archivo con `python archivo.py` (no cuando alguien lo importa como módulo). Para la versión de Python dentro del script: `import sys` y `sys.version.split()[0]`. Un **f-string** formatea texto con variables: `print(f\"Hola {nombre}\")` — la `f` delante de las comillas permite `{expresiones}` dentro.",
            "**Anotaciones de tipo opcionales (pistas):** `def main() -> None:` dice “esta función no devuelve un valor útil”. `-> None` y `: str` en parámetros son **anotaciones** (type hints): documentan el contrato y ayudan a Ruff/editores; en S01 Python **no** las exige en runtime. Si el starter trae `-> None`, puedes dejarlo o copiarlo; no cambia la lógica del script.",
            "Además del script, sigue verificando el intérprete desde la shell antes de crear venvs o instalar paquetes."
          ],
          "code": null,
          "code_output": null
        },
        {
          "subtopicId": "S01-T1-B",
          "heading": "Terminal, rutas y códigos de salida · cwd, PATH y códigos de salida en la shell",
          "paragraphs": [
            "La **terminal** (bash, zsh o PowerShell) es el lugar donde lanzas procesos: `python`, `git`, `mkdir`. Cada comando que escribes es un **proceso** hijo de la shell. Cuando termina, devuelve un **código de salida** (exit status): por convención, **0 significa éxito** y **cualquier valor distinto de 0 significa fallo**. En bash/zsh lo lees con `echo $?`. En PowerShell, con `echo $LASTEXITCODE`. CI, scripts y pipelines usan ese número para decidir si continúan o se detienen. Un mensaje de error en pantalla y un código 0 (o al revés) son cosas distintas: siempre mira el código cuando automatizas.",
            "No confundas el **directorio de trabajo actual** (cwd: dónde “estás” con `cd` y `pwd` / `Get-Location`) con el **PATH** (lista de carpetas donde el sistema busca ejecutables como `python`). Puedes estar en `~/proyectos/python-ds-journey` y aun así fallar `python` si ese ejecutable no está en el PATH. Al revés: puedes tener Python en el PATH y fallar al abrir un archivo si tu cwd no es la carpeta del proyecto. `mkdir` y `cd` mueven o crean rutas relativas al cwd; no “instalan” Python en el PATH.",
            "En Python, `sys.exit(n)` termina el proceso con código `n`. Es la forma limpia de señalizar éxito o error a la shell y a herramientas externas. Ejemplo: un script de validación sale con `0` si los argumentos son correctos y con `1` si faltan. En data ops verás el mismo contrato: un job ETL “failed” casi siempre es exit code distinto de cero, no solo un print rojo. Practica leer `$?` / `$LASTEXITCODE` después de cada comando crítico antes de culpar a la librería.",
            "**Argumentos de línea de comandos:** `sys.argv` es una lista de strings. `sys.argv[0]` es el nombre del script; los argumentos del usuario empiezan en `sys.argv[1]`. **`len(sys.argv)`** cuenta cuántos elementos hay (incluye el nombre del script). Ejemplo: `python check_arg.py ok` → `sys.argv == [\"check_arg.py\", \"ok\"]` y `len(sys.argv) == 2`. **Rebanado (slice):** `sys.argv[1:]` es la sublista desde el índice 1 hasta el final (todos los args del usuario, sin el nombre del script). La forma general es `lista[inicio:fin]` (fin excluido); omitir `fin` llega al final. Para errores al usuario usa `print(\"uso: ...\", file=sys.stderr)` y luego `sys.exit(1)`.",
            "**Qué intérprete es este proceso:** `sys.executable` es la ruta absoluta del binario Python que está corriendo tu script (ej. `.../.venv/bin/python`). Si `import requests` falla, compara `sys.executable` con el `pip` que usaste: la regla de oro es instalar con `python -m pip install ...` usando **el mismo** ejecutable. Así evitas el clásico \"lo instalé pero ModuleNotFoundError\".",
            "Desde la shell, confirma cwd, PATH conceptual y códigos de salida con los mismos números 0/1 que usa `sys.exit`."
          ],
          "code": "import sys\n\ndef main() -> None:\n    # sys.argv[0] = script; usuario desde [1]\n    if len(sys.argv) != 2:\n        print(\"uso: python check_arg.py <arg>\", file=sys.stderr)\n        sys.exit(1)\n    print(\"OK:\" + sys.argv[1])\n    print(\"executable:\", sys.executable)\n    sys.exit(0)\n\nif __name__ == \"__main__\":\n    main()",
          "code_output": "OK:hola\nexecutable: /ruta/a/python"
        },
        {
          "subtopicId": "S01-T2-A",
          "heading": "Entornos virtuales con venv",
          "paragraphs": [
            "Un **entorno virtual** es una carpeta autocontenida con su propio intérprete Python y su propio directorio de paquetes. La herramienta estándar de la biblioteca es el módulo **`venv`**: `python -m venv .venv`. La regla de oro no cambia: **un proyecto = un entorno = un conjunto de dependencias**. Así evitas que el pandas 2.x de un cliente rompa el notebook legacy de otro. Instalar paquetes “en el Python global” del sistema es la vía rápida al caos en tres meses.",
            "**Nombre de la carpeta:** la documentación oficial de Python recomienda **`.venv`** (con punto): queda semi-oculto en listados Unix y se distingue de archivos `.env` de secretos. El nombre `venv` (sin punto) también es válido y lo verás en muchos tutoriales; en este curso el canónico es **`.venv`**, y tratamos `venv` como alias aceptado si ya lo usas. Ambos deben ir en `.gitignore` — nunca subas el entorno a GitHub (pesa decenas o cientos de MB y se regenera).",
            "**Activación** engancha la shell al Python del entorno: en macOS/Linux, `source .venv/bin/activate`; en Windows PowerShell, `.venv\\Scripts\\Activate.ps1`. El prompt suele mostrar `(.venv)`. **`deactivate`** vuelve al Python anterior. Si rompes el entorno, no “reparas a mano” el site-packages: borras la carpeta `.venv` y la recreas con el mismo comando. `conda` sigue siendo alternativa en investigación o GPU, pero **no es el default** de este curso. Herramientas más nuevas como `uv` son tendencia; aquí dominas `venv` porque es lo que encontrarás en la mayoría de onboarding corporativos."
          ],
          "code": null,
          "code_output": null
        },
        {
          "subtopicId": "S01-T2-B",
          "heading": "pip, freeze y requirements.txt",
          "paragraphs": [
            "Con el `.venv` activado, instalas dependencias de **terceros** con **`python -m pip`**. El prefijo `python -m` ata pip al mismo intérprete que acabas de verificar: evita el clásico “pip instaló en un Python y `import` usa otro”. Comandos del día a día: `python -m pip install paquete`, pin con `paquete==1.2.3`, listar con `python -m pip list`, y exportar un snapshot con `python -m pip freeze > requirements.txt`. Para restaurar en otra máquina o en un entorno limpio: `python -m pip install -r requirements.txt`. La biblioteca estándar (`sys`, `datetime`, `pathlib`, `csv`, `json`…) **no** va a requirements: se importa sin `pip install`.",
            "**`pip freeze`** escribe *todo* lo instalado en el entorno activo, incluidas **dependencias transitivas** (si instalas `requests`, también aparecen `urllib3`, `certifi`, etc.). Eso es un **snapshot reproducible** muy útil para clonar el entorno de un analista. No es un lockfile moderno con hashes y resolución completa (Poetry, PDM, `uv lock` van más allá). En S01 usamos freeze + `==` porque es el mínimo que encontrarás en onboarding corporativo y en muchos repos de data. Entiende el límite: freeze captura *lo que hay hoy* en *tu* venv; no resuelve conflictos multiplataforma ni firma paquetes.",
            "Flujo profesional: (1) activar `.venv`, (2) instalar lo necesario, (3) `python -m pip freeze > requirements.txt`, (4) commitear el archivo (no la carpeta `.venv/`). Si un colega clona y hace `install -r`, debería obtener las mismas versiones pinneadas. Si ves `ModuleNotFoundError`, primero pregunta: ¿estoy en el venv correcto? ¿usé `python -m pip`? ¿el paquete es stdlib o de terceros? Diagnosticar en ese orden ahorra horas de “a mí me funciona”."
          ],
          "code": null,
          "code_output": null
        },
        {
          "subtopicId": "S01-T3-A",
          "heading": "Git: commits y lectura de diffs",
          "paragraphs": [
            "Git es un **sistema de control de versiones**: registra *quién cambió qué y por qué*, no solo “guardar en la nube”. Flujo local mínimo: `git init` (una vez por repo), editas archivos, `git status` (¿qué cambió?), `git add` (staging), `git commit -m \"...\"` (punto de retorno con mensaje). Cada commit es un snapshot recuperable. GitHub/GitLab son **remotos** donde publicas esos commits; el historial útil empieza en tu máquina con mensajes claros.",
            "La convención **Conventional Commits** usa un prefijo + descripción en **imperativo** y en minúsculas tras el prefijo: `feat:`, `fix:`, `docs:`, `refactor:`, `test:`, `chore:`. Ejemplos: `feat: agregar script hello_env`, `docs: documentar instalación del venv`, `fix: corregir ruta en README`. Evita “cambios”, “wip”, “arreglé algo”: en seis meses nadie (ni tú) sabrá qué mirar. Equipos de datos en bancos y fintech en Perú suelen pedir este estilo en code review.",
            "Leer un **diff** es tan importante como escribir el commit. `git diff` muestra cambios *sin* stage; `git diff --staged` lo ya agregado; `git show` el último commit (o un hash). Líneas con `+` se añadieron; con `-` se quitaron. Un archivo nuevo aparece como todo `+`. Antes de `commit`, lee el diff: es tu última revisión de calidad y la misma habilidad que usarás al revisar un Pull Request de un colega."
          ],
          "code": "git init\n# identidad solo la primera vez en la máquina (global):\n# git config --global user.name \"Tu Nombre\"\n# git config --global user.email \"tu@correo.com\"\n\necho \"# Mi proyecto\" > README.md\ngit add README.md\ngit status\ngit commit -m \"docs: agregar README inicial\"\n\necho \"\" >> README.md\necho \"Setup con venv.\" >> README.md\ngit add README.md\ngit commit -m \"docs: documentar setup con venv\"\n\ngit log --oneline\ngit show HEAD   # mira +/- del último commit",
          "code_output": "docs: documentar setup con venv\ndocs: agregar README inicial\n# git show: líneas + con el texto nuevo del README"
        },
        {
          "subtopicId": "S01-T3-B",
          "heading": "Ramas, Pull Requests y recuperación segura",
          "paragraphs": [
            "Trabajar siempre en `main` es el atajo del principiante y el riesgo del equipo. El flujo profesional: crea una **rama de feature** con `git switch -c feat/nombre-corto`, haz commits ahí, publica con `git push -u origin feat/nombre-corto`, y abre un **Pull Request (PR)** en GitHub para revisar antes de integrar a `main`. El PR no es burocracia: es el lugar donde un colega lee el diff, comenta y aprueba. Nombres útiles: `feat/hello-env`, `docs/readme-install`, `fix/gitignore-env`.",
            "Un **conflicto** aparece cuando dos ramas editaron las mismas líneas. Git marca el archivo; tú eliges el contenido final, `git add` y un commit de merge o de resolución. En S01 no necesitas ser experto en merges complejos: sí necesitas no paniquear y no “arreglar” con historial destructivo. La regla de oro de este curso: **no hagas `git push --force` a `main`**. Reescribe historial solo en ramas tuyas no compartidas y con permiso del equipo; en onboarding, ni eso.",
            "Recuperación **no destructiva** del día a día: `git restore archivo` descarta cambios *sin commitear* en el working tree (vuelve a la última versión commiteada o staged, según el caso). `git stash` guarda cambios temporales y deja el árbol limpio; `git stash pop` los recupera. Prefiere restore/stash a `reset --hard` como primer reflejo: hard borra trabajo no commiteado de forma fácil de lamentar. Aprende primero a no perder trabajo; después, a reescribir con cuidado."
          ],
          "code": null,
          "code_output": null
        },
        {
          "subtopicId": "S01-T4-A",
          "heading": "VS Code y Ruff como calidad mínima",
          "paragraphs": [
            "El editor recomendado en este curso es **VS Code** con la extensión de **Python** (Pylance para tipos e IntelliSense) y **Ruff** (linter/formateador ultra rápido escrito en Rust). No sustituyen pensar: atrapan errores baratos antes del code review — imports sin usar, errores de sintaxis obvios, imports desordenados. En equipos de datos, un linter en el repo es el primer “CI humano” que corre en tu laptop.",
            "La configuración mínima vive en **`pyproject.toml`** en la raíz del proyecto, sección `[tool.ruff]` y opcionalmente `[tool.ruff.lint]`. Valores sensatos para empezar: `line-length = 88`, `target-version` acorde a tu Python, y `select = [\"E\", \"F\", \"I\"]` (pycodestyle errores, pyflakes, isort). Instala el CLI en el venv: `python -m pip install ruff`. Ejecuta: `python -m ruff check ruta/` o un archivo. `ruff format` formatea; en S01 el foco es **`ruff check`**.",
            "Flujo: escribes código → `ruff check` reporta (ej. **F401** import sin usar) → corriges o, en casos justificados avanzados, documentas un `noqa` (en S01 prefiere corregir). No habilites `select = [\"ALL\"]` el primer día: el ruido abruma y nadie arregla 200 reglas a la vez. Un mínimo que el equipo respeta vale más que un máximo que todos ignoran."
          ],
          "code": null,
          "code_output": null
        },
        {
          "subtopicId": "S01-T4-B",
          "heading": "Archivos de calidad: ignore, secretos y README",
          "paragraphs": [
            "**.gitignore** le dice a Git qué no trackear. Mínimo Python/data: `.venv/`, `venv/`, `__pycache__/`, `*.pyc`, `.env`, `.ipynb_checkpoints/`, y a menudo `data/raw/` o dumps grandes si tu política es no versionar datos pesados. Incluye **ambos** nombres de entorno (`.venv/` y `venv/`) porque el ecosistema usa los dos. Plantilla base: https://github.com/github/gitignore (Python.gitignore). Ojo: si un archivo **ya** está trackeado, agregarlo al ignore no lo saca del historial: necesitas `git rm --cached archivo` y un commit.",
            "**.env** guarda secretos y variables locales (API keys, contraseñas). **Nunca** va al repo. **`.env.example`** sí: lista las *claves* con valores vacíos o ficticios para que un colega sepa qué copiar a su `.env` privado. Ejemplo: `DATABASE_URL=` y `API_TOKEN=` sin valores reales. Si subiste un secreto por error, rotarlo (cambiar la clave en el proveedor) es más importante que solo borrar el archivo del commit siguiente: el historial puede conservarlo.",
            "**README.md** es el onboarding del clon limpio: título, qué hace el repo, mención al **esqueleto CP-N1-A** (Client Intake & Data Quality), instalación (`python -m venv .venv`, activate, `python -m pip install -r requirements.txt`), uso (`python scripts/hello_env.py`), y una nota de seguridad (no commitear `.env`, datos sintéticos). Para S01 agrega `data/clients_synthetic.csv` (PII falsa) y `data/data_dictionary.md` describiendo columnas. Un repo sin README ni diccionario de datos no se puede auditar ni reutilizar en S02–S04."
          ],
          "code": "# Fragmento mínimo de .gitignore\n# .venv/\n# venv/\n# __pycache__/\n# *.pyc\n# .env\n# .ipynb_checkpoints/\n\n# Verificar que .env queda ignorado (tras git init y .gitignore commiteable)\ngit check-ignore -v .env\n# .gitignore:N:.env    .env\n\n# .env.example SÍ se trackea (claves sin secretos)\n# SECRET_KEY=\n# API_URL=https://example.com",
          "code_output": ".gitignore:5:.env    .env"
        }
      ],
      "iDo": {
        "steps": [
          {
            "demoId": "S01-T1-A-DEMO",
            "subtopicId": "S01-T1-A",
            "environment": "local-python",
            "description": "Verificar el intérprete y abrir una sesión REPL mínima",
            "why": "Si no sabes qué intérprete responde, todo lo demás (venv, pip, scripts) puede apuntar a otro Python y generar errores fantasma. El REPL confirma que el intérprete evalúa código; `quit()` demuestra que sales del REPL sin cerrar la terminal. Usar `python -m pip` evita instalar paquetes en un Python distinto al que ejecutas.",
            "code": null,
            "language": null,
            "output": null
          },
          {
            "demoId": "S01-T1-B-DEMO",
            "subtopicId": "S01-T1-B",
            "environment": "local-python",
            "description": "Observar códigos de salida 0 y no-cero con sys.exit",
            "why": "El código de salida es el contrato entre tu script y todo lo que lo invoca (shell, CI, orquestadores). 0 = sigamos; no-cero = detente o reintenta. Separar cwd (dónde estoy) de PATH (qué ejecutables existen) evita el clásico \"en mi máquina funciona\" cuando solo cambió la carpeta o el PATH del job.",
            "code": "# Éxito: el proceso Python termina con 0\npython3 -c \"import sys; print('ok'); sys.exit(0)\"\necho \"exit_ok=$?\"\n# PowerShell:\n# python -c \"import sys; print('ok'); sys.exit(0)\"; echo \"exit_ok=$LASTEXITCODE\"\n\n# Fallo controlado: termina con 1 (la shell sigue viva)\npython3 -c \"import sys; print('fallo controlado'); sys.exit(1)\"\necho \"exit_fail=$?\"\n\n# Comando inexistente → también no-cero (mensaje distinto, mismo tipo de señal a CI)\nfalse\necho \"exit_false=$?\"\n\n# Rutas: cwd vs carpeta\npwd\nmkdir -p /tmp/s01_t1b_demo && cd /tmp/s01_t1b_demo\npwd\n# Estar en /tmp/s01_t1b_demo NO \"arregla\" un Python ausente del PATH",
            "language": "bash",
            "output": "ok\nexit_ok=0\nfallo controlado\nexit_fail=1\nexit_false=1\n/Users/...\n/tmp/s01_t1b_demo"
          },
          {
            "demoId": "S01-T2-A-DEMO",
            "subtopicId": "S01-T2-A",
            "environment": "local-python",
            "description": "Crear y activar entorno virtual con .venv",
            "why": "El entorno virtual aísla las dependencias por proyecto. `python -m venv` usa el mismo intérprete que acabas de verificar. Si rompes paquetes, borras `.venv/` y la recreas — no reinstalas el sistema. Activar no es opcional en el flujo diario: sin activar, `pip` puede caer en el Python global.",
            "code": null,
            "language": null,
            "output": null
          },
          {
            "demoId": "S01-T2-B-DEMO",
            "subtopicId": "S01-T2-B",
            "environment": "local-python",
            "description": "Instalar con python -m pip, freeze y reinstall -r",
            "why": "python -m pip ata el instalador al intérprete del venv. freeze genera el contrato reproducible; install -r lo aplica en un entorno limpio. Eso es lo que un colega hará el día 1 al clonar tu repo.",
            "code": "# .venv activado (prompt (.venv))\npython -m pip install requests==2.32.3\n\n# Snapshot (incluye transitivas: urllib3, certifi, ...)\npython -m pip freeze > requirements.txt\nhead -5 requirements.txt\n\n# Replicar en entorno limpio (demo rápida):\ndeactivate\npython3 -m venv .venv_clean\nsource .venv_clean/bin/activate   # Windows: .venv_clean\\Scripts\\Activate.ps1\npython -m pip install -r requirements.txt\npython -c \"import requests; print(requests.__version__)\"\n# 2.32.3\n\n# Stdlib no necesita pip:\npython -c \"import sys; print(sys.version.split()[0])\"\n\n# En el proyecto real del curso también instalarás pandas/numpy/etc.\n# y dejarás un solo requirements.txt pinneado en la raíz.",
            "language": "bash",
            "output": "requests==2.32.3\n...\n2.32.3\n3.12.3"
          },
          {
            "demoId": "S01-T3-A-DEMO",
            "subtopicId": "S01-T3-A",
            "environment": "local-python",
            "description": "Primer commit Conventional Commits y lectura de git show",
            "why": "Un commit con prefijo docs:/feat: es legible en el historial y en el PR. git show es la misma habilidad de lectura de diff que usarás al revisar código ajeno.",
            "code": "git init\n# git config --global user.name \"Tu Nombre\"   # solo 1ª vez\n# git config --global user.email \"tu@correo.com\"\n\necho \"# python-ds-journey\" > README.md\ngit add README.md\ngit commit -m \"docs: agregar README inicial\"\n\necho \"\" >> README.md\necho \"Esqueleto CP-N1-A.\" >> README.md\ngit add README.md\ngit commit -m \"docs: mencionar esqueleto CP-N1-A\"\n\ngit log --oneline\ngit show HEAD --stat\n# Lee las líneas +/− : qué se agregó o quitó en el último commit",
            "language": "bash",
            "output": "docs: mencionar esqueleto CP-N1-A\ndocs: agregar README inicial\n README.md | 2 ++\n 1 file changed, 2 insertions(+)"
          },
          {
            "demoId": "S01-T3-B-DEMO",
            "subtopicId": "S01-T3-B",
            "environment": "local-python",
            "description": "Rama feature, push -u y plan de PR (sin force-push)",
            "why": "La rama + PR es el flujo de equipo. restore/stash recuperan sin reescribir historial compartido. force-push a main está prohibido en este curso.",
            "code": "# Asegura estar en main con working tree limpio\ngit switch main\n\ngit switch -c feat/hello-env\n# ... edita o crea scripts/hello_env.py ...\ngit add scripts/hello_env.py\ngit commit -m \"feat: agregar smoke hello_env\"\n\n# Publicar rama (requiere remote origin ya configurado)\ngit push -u origin feat/hello-env\n\n# Abrir PR en GitHub (UI) o con GitHub CLI:\n# gh pr create --title \"feat: smoke hello_env\" --body \"- Agrega scripts/hello_env.py\\n- Smoke del entorno\"\n\n# Recuperación local (demo):\n# echo \"temp\" >> README.md\n# git restore README.md          # descarta cambio no commiteado\n# git stash push -m \"wip\"        # guarda a medias\n# NUNCA: git push --force origin main",
            "language": "bash",
            "output": "branch 'feat/hello-env' set up to track 'origin/feat/hello-env'\n# PR abierto en la UI de GitHub"
          },
          {
            "demoId": "S01-T4-A-DEMO",
            "subtopicId": "S01-T4-A",
            "environment": "local-python",
            "description": "Ruff check sobre un archivo con import sin usar",
            "why": "Ruff atrapa basura barata (imports muertos, errores F/E) antes del review. La config en pyproject.toml es el contrato del repo, no solo del editor.",
            "code": "# pyproject.toml en la raíz (mínimo):\n# [tool.ruff]\n# line-length = 88\n# target-version = \"py312\"\n# [tool.ruff.lint]\n# select = [\"E\", \"F\", \"I\"]\n\npython -m pip install ruff\n# hello_bad.py con: import sys  (sin usar) + print(\"hola\")\npython -m ruff check hello_bad.py\n# F401 [*] `sys` imported but unused\n\n# Tras quitar el import:\npython -m ruff check hello_bad.py\n# All checks passed!",
            "language": "bash",
            "output": "F401 [*] `sys` imported but unused\nAll checks passed!"
          },
          {
            "demoId": "S01-T4-B-DEMO",
            "subtopicId": "S01-T4-B",
            "environment": "local-python",
            "description": ".gitignore, .env.example y README de instalación",
            "why": ".env se ignora; .env.example se versiona sin secretos. El README cierra el circuito: un clon limpio debe poder instalar y correr el smoke sin adivinar.",
            "code": "# .gitignore mínimo (incluye .venv/ Y venv/)\n# .venv/\n# venv/\n# __pycache__/\n# *.pyc\n# .env\n# .ipynb_checkpoints/\n\necho \"API_TOKEN=supersecreto\" > .env\necho \"API_TOKEN=\" > .env.example\ngit add .gitignore .env.example README.md\ngit status --short\n# .env NO debe aparecer como staged\ngit check-ignore -v .env\n# .gitignore:N:.env    .env\n\n# README debe documentar:\n# python -m venv .venv\n# source .venv/bin/activate\n# python -m pip install -r requirements.txt\n# python scripts/hello_env.py",
            "language": "bash",
            "output": "A  .env.example\nA  .gitignore\nA  README.md\n.gitignore:5:.env    .env"
          }
        ]
      },
      "weDo": {
        "exercises": [
          {
            "id": "S01-T1-A-E1",
            "instruction": "E1 (guiado) — Completa el transcript de una sesión REPL: suma, type() e import de sys. No crees un archivo .py; simula el diálogo en comentarios y luego pruébalo en tu terminal real.",
            "hints": [
              "En el REPL el prompt es >>>. Tras import sys, usa sys.version.split()[0] para la versión corta.",
              "Para salir usa quit() o exit(). Eso no cierra la terminal: vuelves al prompt de bash/PowerShell/zsh."
            ],
            "kind": "guided",
            "tests": "Checklist: (1) 2+2 → 4; (2) type(\"x\") es str; (3) sys.version.split()[0] es string tipo 3.x.y; (4) quit() regresa a la shell.",
            "starterCode": "# Completa las líneas marcadas con ____\n# Esto NO se ejecuta como script: cópialo al REPL línea por línea.\n\n# >>> ____ + ____\n# 4\n\n# >>> type(\"____\")\n# <class 'str'>\n\n# >>> import ____\n# >>> sys.version.____()[0]\n# '3.x.y'\n\n# >>> ____()   # salir del REPL"
          },
          {
            "id": "S01-T1-A-E2",
            "instruction": "E2 (independiente) — Crea `hello_sys.py` que imprima tu nombre sintético y la versión de Python con sys. Debe usar `if __name__ == \"__main__\":` y salir con código 0 al correrse como script (no en el REPL).",
            "hints": [
              "Importa sys. La versión corta es sys.version.split()[0]. Envuelve la lógica en main().",
              "El bloque if __name__ == \"__main__\": llama a main(). Así el archivo es un entrypoint claro cuando haces: python hello_sys.py"
            ],
            "kind": "independent",
            "tests": "python hello_sys.py → exit 0; stdout contiene una versión 3.x y un nombre; no requiere pip install.",
            "starterCode": "import sys\n\ndef main() -> None:\n    nombre = \"____\"  # usa un nombre sintético, no datos reales de terceros\n    version = sys.____.split()[0]\n    print(f\"Hola, soy {nombre}\")\n    print(f\"Python {version}\")\n\n# Completa el entrypoint profesional (no dejes el script sin if __name__)\nif ____ == \"____\":\n    ____()"
          },
          {
            "id": "S01-T1-A-E3",
            "instruction": "E3 (transferencia) — Diagnóstico: en una laptop Windows el usuario escribe `python --version` y obtiene \"no se reconoce como comando\". En macOS, `python` falla pero `python3 --version` muestra 3.12.1. Escribe un procedimiento de 4–6 pasos numerados (como comentarios en un .txt o .md) para dejar un intérprete usable y verificable. No inventes instalaciones pirata ni descargas no oficiales.",
            "hints": [
              "En Windows el culpable típico es el PATH o el alias de la Microsoft Store. En macOS/Linux suele bastar usar python3 de forma consistente.",
              "Cierra y reabre la terminal después de cambiar PATH. Verifica siempre con --version antes de pip install. Fuente oficial: https://www.python.org/downloads/"
            ],
            "kind": "transfer",
            "tests": "Rúbrica: menciona fuente oficial; verifica --version; distingue Windows vs Unix; no recomienda force-push ni desactivar seguridad del SO; 4–6 pasos claros.",
            "starterCode": "# Diagnóstico: intérprete no encontrado\n\n## Contexto\n- Caso A (Windows): `python --version` → no se reconoce\n- Caso B (macOS/Linux): `python` falla; `python3 --version` OK\n\n## Mi procedimiento\n1. ____\n2. ____\n3. ____\n4. ____\n5. ____ (opcional)\n6. ____ (opcional)\n\n## Verificación final\n- Comando: ____\n- Resultado esperado: Python 3.12+ (o 3.x del curso)"
          },
          {
            "id": "S01-T1-B-E1",
            "instruction": "E1 (guiado) — Completa el transcript de terminal: ejecuta un Python que sale con 0, otro con 1, y anota `$?` (bash/zsh) o `$LASTEXITCODE` (PowerShell). No subas secretos ni rutas de usuario reales en la entrega.",
            "hints": [
              "Tras cada comando, imprime el código de salida. En bash: echo $?. En PowerShell: echo $LASTEXITCODE.",
              "python3 -c \"import sys; sys.exit(0)\" debe dejar 0; sys.exit(1) debe dejar 1. El mensaje print y el código son independientes."
            ],
            "kind": "guided",
            "tests": "Checklist: (1) exit 0 documentado; (2) exit 1 documentado; (3) shell nombrada (bash/zsh/PowerShell); (4) sin PII en rutas.",
            "starterCode": "# Completa los ____ y ejecuta en tu shell\n\n# 1) Éxito\npython3 -c \"import sys; print('ok'); sys.exit(____)\"\necho \"codigo_ok=$____\"    # bash/zsh: $?  |  PowerShell: $LASTEXITCODE\n\n# 2) Fallo controlado\npython3 -c \"import sys; print('fail'); sys.exit(____)\"\necho \"codigo_fail=$____\"\n\n# 3) Anota tu shell: bash | zsh | powershell\n# SHELL_USADA=____"
          },
          {
            "id": "S01-T1-B-E2",
            "instruction": "E2 (independiente) — Escribe `check_arg.py`: si recibe exactamente un argumento de línea de comandos, imprime `OK:<arg>` y termina con código 0; si no hay argumentos (o hay más de uno), imprime un mensaje de uso en stderr y termina con código 1. Usa `sys.argv` y `sys.exit`.",
            "hints": [
              "sys.argv[0] es el nombre del script; los argumentos del usuario empiezan en sys.argv[1].",
              "print(..., file=sys.stderr) para el mensaje de error. sys.exit(0) vs sys.exit(1). Prueba: python check_arg.py ok  y  python check_arg.py"
            ],
            "kind": "independent",
            "tests": "python check_arg.py hola → exit 0 y stdout contiene OK:hola; python check_arg.py → exit 1; python check_arg.py a b → exit 1.",
            "starterCode": "import sys\n\ndef main() -> None:\n    # sys.argv: [script, arg1, arg2, ...]\n    args = sys.argv[1:]\n    if ____(args) != ____:\n        print(\"Uso: python check_arg.py <un_valor>\", file=sys.____)\n        sys.exit(____)\n    print(f\"OK:{args[0]}\")\n    sys.exit(____)\n\nif __name__ == \"__main__\":\n    main()"
          },
          {
            "id": "S01-T1-B-E3",
            "instruction": "E3 (transferencia) — Caso: un colega corre `pip install pandas` y ve error. En un escenario el mensaje es \"pip no se reconoce como comando\"; en otro, pip corre pero `python -c \"import pandas\"` falla con ModuleNotFoundError. Clasifica cada escenario (PATH/ejecutable vs intérprete distinto / paquete en otro env) y escribe 3 pasos de verificación por escenario. Entrega en markdown.",
            "hints": [
              "Pregunta siempre: ¿qué ejecutable falló? ¿qué python usa import? Preferir python -m pip para atar pip al mismo intérprete.",
              "Escenario A: el shell no encuentra pip → PATH o nombre de comando. Escenario B: pip y python no son la misma instalación → wrong interpreter / venv no activado."
            ],
            "kind": "transfer",
            "tests": "Rúbrica: clasifica A vs B; propone python -m pip; verifica --version de python y pip; no inventa reinstalls de SO innecesarios; sin secretos.",
            "starterCode": null
          },
          {
            "id": "S01-T2-A-E1",
            "instruction": "E1 (guiado) — En una carpeta de práctica, crea un entorno `.venv` con `python -m venv`, actívalo y verifica que `sys.prefix` apunta a esa carpeta. Luego ejecuta `deactivate`.",
            "hints": [
              "Comando de creación: python3 -m venv .venv. Activación Unix: source .venv/bin/activate. Windows: .venv\\Scripts\\Activate.ps1",
              "Verifica con: python -c \"import sys; print(sys.prefix)\" — debe terminar en .venv. which/where python también ayuda."
            ],
            "kind": "guided",
            "tests": "Checklist: existe carpeta .venv; tras activate, sys.prefix contiene .venv; tras deactivate, el prefijo ya no es el del proyecto (o el prompt pierde (.venv)).",
            "starterCode": "mkdir -p lab_venv_t2a && cd lab_venv_t2a\n\n# 1) Crear\npython3 -m venv ____\n\n# 2) Activar (Unix)\nsource ____/bin/activate\n# Windows: .venv\\Scripts\\Activate.ps1\n\n# 3) Verificar\npython -c \"import sys; print(sys.____)\"\n\n# 4) Salir\n____"
          },
          {
            "id": "S01-T2-A-E2",
            "instruction": "E2 (independiente) — Simula un entorno “roto”: con `.venv` ya creado, borra la carpeta del entorno (o renómbrala) y recrea un `.venv` limpio sin tocar tu código fuente. Documenta los comandos en orden. No uses conda ni reinstales el Python del sistema.",
            "hints": [
              "El código (.py, README) vive fuera de .venv. Borrar .venv no borra tus scripts si están en la raíz del proyecto.",
              "rm -rf .venv  (Unix) o Remove-Item -Recurse -Force .venv (PowerShell), luego python3 -m venv .venv y volver a activar."
            ],
            "kind": "independent",
            "tests": "Tras recrear: activate funciona; sys.prefix apunta al nuevo .venv; archivos .py del proyecto siguen existiendo.",
            "starterCode": "# Supón que ya tienes hello.py en el proyecto y un .venv roto.\n# Completa:\n\n# 0) Asegúrate de no estar usando el venv viejo\n____ 2>/dev/null || true\n\n# 1) Eliminar entorno roto\n____ .venv\n\n# 2) Recrear\npython3 -m venv ____\n\n# 3) Activar y verificar\nsource .venv/bin/activate\npython -c \"import sys; print(sys.prefix)\"\n# hello.py u otros .py NO se borran"
          },
          {
            "id": "S01-T2-A-E3",
            "instruction": "E3 (transferencia) — Un colega propone instalar pandas con `pip install pandas` en el Python global “para no perder tiempo con venv”. Escribe un párrafo (5–8 oraciones) o un markdown corto que: (1) explique el riesgo con dos proyectos que necesitan versiones distintas; (2) proponga el flujo con `.venv`; (3) mencione que `venv` es stdlib y no requiere descarga extra. Sin afirmaciones salariales.",
            "hints": [
              "Usa el escenario: Proyecto A necesita pandas 1.x; Proyecto B necesita 2.x. Un solo site-packages global no puede satisfacer ambos.",
              "Cierra con el flujo: python -m venv .venv → activate → python -m pip install ... por proyecto."
            ],
            "kind": "transfer",
            "tests": "Rúbrica: menciona conflicto de versiones; propone .venv o venv por proyecto; no recomienda install global ni sudo; tono profesional en español.",
            "starterCode": "# ¿Por qué no instalar pandas en el Python global?\n\n## Escenario de conflicto\n____\n\n## Flujo recomendado (este curso)\n1. ____\n2. ____\n3. ____\n\n## Nota sobre la herramienta\n- venv es ____ (stdlib / terceros)\n- Nombre canónico de carpeta: ____"
          },
          {
            "id": "S01-T2-B-E1",
            "instruction": "E1 (guiado) — Con `.venv` activado, instala un paquete de terceros pinneado (ej. `requests==2.32.3`), genera `requirements.txt` con freeze y verifica que el archivo contiene `paquete==versión`. No uses el Python global.",
            "hints": [
              "Siempre: python -m pip install ... y python -m pip freeze > requirements.txt",
              "Si freeze lista paquetes del sistema y no del proyecto, no activaste el venv. Revisa sys.prefix antes de freeze."
            ],
            "kind": "guided",
            "tests": "requirements.txt existe; contiene al menos una línea name==version del paquete instalado; no incluye secretos ni rutas absolutas de usuario.",
            "starterCode": "source .venv/bin/activate   # o Activate.ps1 en Windows\n\npython -m pip install requests==____\npython -m ____ freeze > ____.txt\n\n# Verifica:\ngrep -i \"requests==\" requirements.txt\npython -c \"import requests; print(requests.____)\""
          },
          {
            "id": "S01-T2-B-E2",
            "instruction": "E2 (independiente) — Dado un `requirements.txt` de plantilla (puede ser el de E1), crea un **segundo** entorno limpio (`.venv_replica` o borra y recrea `.venv`), actívalo e instala con `python -m pip install -r requirements.txt`. Confirma con `pip list` o un import que el paquete está presente.",
            "hints": [
              "No copies site-packages a mano. El contrato es el archivo -r.",
              "python3 -m venv .venv_replica && source .venv_replica/bin/activate && python -m pip install -r requirements.txt"
            ],
            "kind": "independent",
            "tests": "En el env limpio: import del paquete pinneado exitoso; versión alineada con requirements.txt.",
            "starterCode": "# Parte de un requirements.txt existente en la raíz\npython3 -m venv .venv_replica\nsource .venv_replica/bin/activate\npython -m pip install -r ____\npython -m pip list | head\npython -c \"import requests; print('ok', requests.__version__)\""
          },
          {
            "id": "S01-T2-B-E3",
            "instruction": "E3 (transferencia) — Forense ModuleNotFoundError: el usuario corre `python -c \"import requests\"` y falla. En un caso, nunca instaló el paquete; en otro, lo instaló con un `pip` de otro Python. Escribe un protocolo de 5 pasos en markdown que use `sys.executable`, `python -m pip`, y distinga stdlib vs terceros. Sin reinstalls de SO innecesarios.",
            "hints": [
              "Primero identifica el intérprete; luego pregunta si el módulo es stdlib; luego instala con python -m pip en ese intérprete/venv.",
              "requests es de terceros; sys/datetime no. Si pip list muestra el paquete pero import falla, casi seguro hay dos Pythons."
            ],
            "kind": "transfer",
            "tests": "Rúbrica: usa sys.executable; recomienda python -m pip; clasifica stdlib vs terceros; contempla wrong interpreter; sin secretos.",
            "starterCode": "# Forense ModuleNotFoundError\n\n## Hipótesis A — nunca instalado\n____\n\n## Hipótesis B — instalado en otro intérprete\n____\n\n## Protocolo (5 pasos)\n1. ____\n2. ____\n3. ____\n4. ____\n5. ____\n\n## ¿stdlib o terceros?\n- requests: ____\n- datetime: ____"
          },
          {
            "id": "S01-T3-A-E1",
            "instruction": "E1 (guiado) — En una carpeta de práctica: `git init`, crea un archivo, haz un commit con mensaje Conventional Commits (`docs:` o `feat:`). Verifica con `git log -1`. No uses mensajes vacíos ni “wip”.",
            "hints": [
              "git add <archivo> && git commit -m \"docs: ...\" — el prefijo va en minúsculas seguido de dos puntos y espacio.",
              "Si Git pide identidad: git config user.email y user.name (en el repo con --local si no quieres global)."
            ],
            "kind": "guided",
            "tests": "git log -1 --pretty=%s coincide con patrón ^(feat|fix|docs|chore|refactor|test): .+",
            "starterCode": "mkdir -p lab_git_t3a && cd lab_git_t3a\ngit init\necho \"# lab\" > README.md\ngit ____ README.md\ngit commit -m \"____: agregar README de practica\"\ngit log -1 --oneline"
          },
          {
            "id": "S01-T3-A-E2",
            "instruction": "E2 (independiente) — Modifica un README ya commiteado (agrega una línea), haz commit, y responde en un markdown corto: (1) qué muestra `git show HEAD` en líneas `+`; (2) diferencia entre archivo nuevo vs modificado; (3) una frase de qué cambió el commit.",
            "hints": [
              "Tras el segundo commit, git show HEAD sin pager: GIT_PAGER=cat git show HEAD",
              "Archivo nuevo: todo el contenido aparece con +. Modificado: solo las líneas tocadas con +/−."
            ],
            "kind": "independent",
            "tests": "Markdown responde las 3 preguntas; menciona +/−; commit existe en log.",
            "starterCode": "# Lectura de diff\n\n## Comandos usados\n____\n\n## 1) ¿Qué líneas aparecen con + en git show HEAD?\n____\n\n## 2) ¿Archivo nuevo o modificado? ¿Cómo se nota?\n____\n\n## 3) Resumen en una frase\n____"
          },
          {
            "id": "S01-T3-A-E3",
            "instruction": "E3 (transferencia) — Te entregan tres mensajes candidatos para el mismo cambio (añadir `scripts/hello_env.py` que imprime la versión de Python): (A) `wip`, (B) `feat: agregar smoke hello_env`, (C) `Actualicé cosas del setup`. Elige el mejor, justifica en 3–5 oraciones y reescribe los otros dos al estilo Conventional Commits si fueran otros cambios plausibles.",
            "hints": [
              "El mejor es claro, con prefijo de tipo y descripción imperativa del *porqué/qué* observable.",
              "wip no es aceptable en main; “Actualicé cosas” no dice qué archivo ni qué valor aporta."
            ],
            "kind": "transfer",
            "tests": "Elige B; justificación menciona legibilidad/historial; reescrituras usan feat/docs/fix/chore válidos.",
            "starterCode": "# Mejor mensaje de commit\n\nCandidatos:\n- A: `wip`\n- B: `feat: agregar smoke hello_env`\n- C: `Actualicé cosas del setup`\n\n## Elección\n____\n\n## Justificación\n____\n\n## Reescritura de A (si fuera un commit temporal local que luego se limpia)\n____\n\n## Reescritura de C\n____"
          },
          {
            "id": "S01-T3-B-E1",
            "instruction": "E1 (guiado) — Desde un repo con al menos un commit en `main`, crea la rama `feat/practica-s01`, añade un archivo pequeño, commit con `feat:` y lista las ramas. No hagas force-push. Si no tienes remote, basta el flujo local.",
            "hints": [
              "git switch -c feat/practica-s01  (equivalente moderno a checkout -b)",
              "git branch debe mostrar * feat/practica-s01. El commit debe vivir en esa rama, no solo en main."
            ],
            "kind": "guided",
            "tests": "branch feat/practica-s01 existe; HEAD en esa rama; log -1 con prefijo feat:",
            "starterCode": "git switch main\ngit switch -c ____/practica-s01\necho \"ok\" > nota.txt\ngit add nota.txt\ngit commit -m \"____: agregar nota de practica\"\ngit branch"
          },
          {
            "id": "S01-T3-B-E2",
            "instruction": "E2 (independiente) — Redacta la descripción de un Pull Request (markdown) para una rama que agrega `scripts/hello_env.py` y actualiza el README de instalación. Incluye: título, resumen (3 bullets), plan de prueba, y checklist de seguridad (sin secretos). No hace falta abrir el PR real si no tienes remote; entrega el archivo.",
            "hints": [
              "Título al estilo Conventional Commits; cuerpo orientado al revisor, no a ti.",
              "Plan de prueba: comandos concretos (venv, install -r, python scripts/hello_env.py)."
            ],
            "kind": "independent",
            "tests": "Archivo con título, ≥3 bullets, pasos de prueba, mención de .env/secretos; sin PII real.",
            "starterCode": "# Título del PR\n____\n\n## Resumen\n- ____\n- ____\n- ____\n\n## Plan de prueba\n1. ____\n2. ____\n3. ____\n\n## Seguridad\n- [ ] No incluye `.env` ni secretos\n- [ ] ____"
          },
          {
            "id": "S01-T3-B-E3",
            "instruction": "E3 (transferencia) — Escenario: editaste `README.md` sin commit y el cambio está mal. Un colega te dice “usa `reset --hard`” y otro “`push --force` a main”. Escribe el procedimiento correcto con `git restore` (y cuándo usarías `stash` en su lugar). Explica por qué force-push a main no es opción. Entrega en markdown.",
            "hints": [
              "restore descarta cambios no deseados en working tree; stash guarda para después. Ninguno reescribe main remoto.",
              "reset --hard borra trabajo no commiteado sin red de seguridad; no es el default de este curso."
            ],
            "kind": "transfer",
            "tests": "Menciona git restore; menciona stash como alternativa de guardado; prohíbe force-push a main; no recomienda reset --hard como primera opción.",
            "starterCode": "# Recuperación no destructiva\n\n## Situación\nREADME.md modificado, sin commit, cambio indeseado.\n\n## Procedimiento con restore\n1. ____\n2. ____\n\n## ¿Cuándo stash en su lugar?\n____\n\n## Por qué NO force-push a main\n____\n\n## Por qué no reset --hard como default\n____"
          },
          {
            "id": "S01-T4-A-E1",
            "instruction": "E1 (guiado) — Completa un `pyproject.toml` mínimo con `[tool.ruff]` (line-length 88, target-version) y `[tool.ruff.lint] select = [\"E\", \"F\", \"I\"]`. No hace falta publicar en PyPI; solo config local.",
            "hints": [
              "TOML usa secciones entre corchetes y listas con corchetes para select.",
              "target-version como \"py312\" (string). line-length es número sin comillas."
            ],
            "kind": "guided",
            "tests": "Archivo contiene [tool.ruff], line-length = 88, select con E F I.",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\n[tool.ruff]\nline-length = 88\ntarget-version = \"py312\"\n\n[tool.ruff.lint]\nselect = [\"E\", \"F\", \"I\"]\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n"
          },
          {
            "id": "S01-T4-A-E2",
            "instruction": "E2 (independiente) — Dado un script con imports sin usar, instala `ruff` en el venv, corre `python -m ruff check` y corrige hasta exit 0. Entrega el script limpio (sin imports muertos).",
            "hints": [
              "F401 = imported but unused. La corrección habitual es borrar el import.",
              "python -m pip install ruff && python -m ruff check archivo.py"
            ],
            "kind": "independent",
            "tests": "ruff check sobre el archivo final exit 0; el script sigue siendo válido Python.",
            "starterCode": "import sys\nimport os\nfrom datetime import datetime\n\ndef main() -> None:\n    print(\"hola\")\n    print(datetime.now().date())\n\nif __name__ == \"__main__\":\n    main()\n\n# Tarea del estudiante: ruff check → quitar imports sin usar (sys, os)"
          },
          {
            "id": "S01-T4-A-E3",
            "instruction": "E3 (transferencia) — Un lead propone `select = [\"ALL\"]` el día 1 en un repo de analytics con notebooks. Justifica por escrito un `select` mínimo (E/F/I u otro set pequeño) para S01: qué ganas, qué ruido evitas, y cuándo ampliarías reglas. 1 página corta en markdown.",
            "hints": [
              "ALL genera cientos de hallazgos; el equipo deja de mirar el linter. Empieza por errores reales (F) y estilo básico (E/I).",
              "Ampliar cuando el check limpio en E/F/I ya es hábito y hay acuerdo de equipo."
            ],
            "kind": "transfer",
            "tests": "Propone set acotado; argumenta ruido vs señal; plan de ampliación; tono profesional.",
            "starterCode": "# Select mínimo para repo de datos (S01)\n\n## Propuesta\n`select = [____]`\n\n## Por qué no ALL el día 1\n____\n\n## Qué cubren E, F, I\n____\n\n## Cuándo ampliar\n____"
          },
          {
            "id": "S01-T4-B-E1",
            "instruction": "E1 (guiado) — Completa un `.gitignore` mínimo de Python/data que incluya `.venv/`, `venv/`, `__pycache__/`, `*.pyc`, `.env` e ipynb checkpoints. Verifica con `git check-ignore -v .env` en un repo de prueba.",
            "hints": [
              "Una entrada por línea. Las barras finales marcan directorios.",
              "Incluye AMBOS: .venv/ y venv/. Si .env ya estaba trackeado, git rm --cached .env tras el ignore."
            ],
            "kind": "guided",
            "tests": "git check-ignore -v .env exit 0; check-ignore aplica a .venv o ruta de entorno.",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\n# Entornos\n.venv/\nvenv/\n\n# Bytecode\n__pycache__/\n*.pyc\n\n# Secretos\n.env\n\n# Jupyter\n.ipynb_checkpoints/\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n"
          },
          {
            "id": "S01-T4-B-E2",
            "instruction": "E2 (independiente) — Crea `.env.example` con al menos 3 claves de un proyecto de intake (ej. `API_URL`, `DB_HOST`, `LOG_LEVEL`) con valores vacíos o ficticios no sensibles. Confirma que `.env` real no se commitea. Nunca copies tokens reales.",
            "hints": [
              "Clave=valor; el valor en example es placeholder. El .env local puede tener secretos pero queda ignorado.",
              "Patrones prohibidos en example: sk-..., passwords reales, connection strings con password."
            ],
            "kind": "independent",
            "tests": "Archivo tiene ≥3 KEY=; sin patrones obvios de secreto; .env en gitignore.",
            "starterCode": "# Copia a .env y completa valores locales (nunca commitees .env)\nAPI_URL=\nDB_HOST=\nLOG_LEVEL=\n# TODO: añade solo placeholders"
          },
          {
            "id": "S01-T4-B-E3",
            "instruction": "E3 (transferencia) — Checklist de “máquina limpia” para el esqueleto CP-N1-A: escribe 5 ítems verificables (clon → venv → install -r → smoke → ignore de .env) que un revisor pueda tildar. Incluye mención a data dictionary + dataset sintético. Markdown.",
            "hints": [
              "Cada ítem debe ser observable (comando + resultado esperado), no “que se vea bonito”.",
              "Incluye: git check-ignore .env; python scripts/hello_env.py exit 0; existencia de data/data_dictionary.md."
            ],
            "kind": "transfer",
            "tests": "≥5 ítems; comandos concretos; menciona datos sintéticos/diccionario; sin secretos.",
            "starterCode": "# Checklist máquina limpia — esqueleto CP-N1-A\n\n- [ ] 1. ____\n- [ ] 2. ____\n- [ ] 3. ____\n- [ ] 4. ____\n- [ ] 5. ____\n\n## Datos\n- [ ] data/clients_synthetic.csv es sintético\n- [ ] data/data_dictionary.md describe columnas\n"
          }
        ]
      },
      "youDo": {
        "title": "Esqueleto CP-N1-A — Reproducible Client Intake Repo",
        "context": "Este You Do es el **primer incremento del capstone CP-N1-A** (Client Intake & Data Quality Script), que se cierra formalmente en S04. En S01 no construyes aún el validador completo: dejas un **repo clonable** con entorno reproducible, higiene Git, calidad mínima (Ruff), datos **sintéticos** y diccionario de datos. S02–S04 montarán el script de intake sobre este esqueleto. El repo puede llamarse `python-ds-journey` o similar; lo importante es la estructura y que un compañero arranque en minutos.",
        "objectives": [
          "Publicar un repo clonable con `.gitignore` (`.venv/` y `venv/`), `.env.example`, `requirements.txt` y `pyproject.toml` (Ruff)",
          "Documentar en README install/run y la frase “esqueleto de CP-N1-A”",
          "Incluir `data/clients_synthetic.csv` (sintético) + `data/data_dictionary.md`",
          "Smoke `scripts/hello_env.py` con exit 0; mínimo 3 commits Conventional Commits y 1 rama feature (merge o PR abierto)"
        ],
        "requirements": [
          "Repo público accesible (GitHub u otro remoto del curso)",
          ".gitignore excluye: .venv/, venv/, __pycache__/, *.pyc, .env, .ipynb_checkpoints/",
          ".env.example trackeado sin secretos; .env nunca en el historial de la entrega",
          "requirements.txt pinneado (python -m pip freeze) usable con install -r",
          "pyproject.toml con [tool.ruff] mínimo; ruff check limpio en scripts/hello_env.py",
          "README: título, descripción, install (venv + pip -r), uso, mención esqueleto CP-N1-A, nota de seguridad",
          "data/clients_synthetic.csv + data/data_dictionary.md (columnas del CSV; PII falsa)",
          "scripts/hello_env.py con if __name__ == \"__main__\" y exit 0",
          "≥3 commits Conventional Commits; 1 rama feat/* con PR o merge documentado"
        ],
        "portfolioNote": "Este repositorio es la base de tu portafolio de Nivel 1. Cuando llegues al gate de S04 (CP-N1-A completo), el revisor valorará que el esqueleto de S01 ya era clonable, sin secretos y con datos sintéticos. Cada sección suma evidencia; no reinicies el repo desde cero sin necesidad.",
        "starterCode": "# Estructura esperada (esqueleto CP-N1-A):\n# python-ds-journey/\n# ├── .gitignore\n# ├── .env.example\n# ├── README.md\n# ├── requirements.txt\n# ├── pyproject.toml\n# ├── data/\n# │   ├── clients_synthetic.csv\n# │   └── data_dictionary.md\n# ├── scripts/\n# │   └── hello_env.py\n# └── section_01/          # opcional: notas de la sección\n#\n# scripts/hello_env.py — smoke del entorno\nimport sys\n\ndef main() -> None:\n    print(f\"Python {sys.version.split()[0]}\")\n    print(\"CP-N1-A skeleton OK\")\n\nif __name__ == \"__main__\":\n    main()\n"
      },
      "selfCheck_stems": [
        {
          "question": "¿Para qué sirve un entorno virtual (venv) en Python?",
          "options": [
            "Para aislar las dependencias (paquetes) por proyecto y evitar conflictos de versiones",
            "Para acelerar la ejecución del código Python",
            "Para conectarse a internet más rápido al instalar paquetes",
            "Para compilar Python a código de máquina más eficiente"
          ]
        },
        {
          "question": "¿Cuál de los siguientes archivos SÍ debería estar en tu .gitignore?",
          "options": [
            "requirements.txt",
            "README.md",
            ".venv/ (o venv/)",
            "hello.py"
          ]
        },
        {
          "question": "¿Cuál es un buen mensaje de commit siguiendo Conventional Commits?",
          "options": [
            "\"cambios\"",
            "\"wip\"",
            "\"arreglé el bug de ayer\"",
            "\"feat: agregar cálculo de churn por segmento\""
          ]
        },
        {
          "question": "¿Qué comando te permite replicar el entorno de otro desarrollador?",
          "options": [
            "pip install pandas numpy",
            "pip install -r requirements.txt",
            "python -m venv venv",
            "git clone https://github.com/usuario/repo.git"
          ]
        },
        {
          "question": "¿Por qué NO debes subir el archivo .env a GitHub?",
          "options": [
            "Porque suele contener credenciales (API keys, passwords, tokens) que son secretos",
            "Porque pesa demasiado y ralentiza el git push",
            "Porque GitHub no soporta archivos sin extensión",
            "Porque entra en conflicto con requirements.txt"
          ]
        }
      ],
      "taught_text_sha": "2cc2d677931f9f55",
      "taught_text_len": 39606
    },
    {
      "file": "src/lib/course/sections/s02-basics.ts",
      "id": "basics",
      "index": 2,
      "title": "Valores, tipos, operadores e I/O",
      "shortTitle": "Basics de Python",
      "tagline": "Literales, nombres, operadores, Decimal e I/O para el parser de intake",
      "jobRelevance": "En onboarding de data en bancos, fintech y retail en Perú, tu primer script “de verdad” no es un loop fancy: es leer campos de un formulario o CSV, saber qué tipo tiene cada valor, convertir texto a número sin crashear, y conservar el original para auditoría. Si confundes 42 con \"42\", usas float para soles o sobrescribes el raw al normalizar, generas bugs de calidad de datos caros. Esta sección construye esa base: valores, nombres, operadores e I/O hacia el parser de intake del capstone CP-N1-A.",
      "learningOutcomes": [
        "Identificar literales y tipos básicos (int, float, str, bool, None) y explicar el tipo de expresiones simples",
        "Inspeccionar con type/isinstance y convertir/validar valores de forma explícita",
        "Asignar nombres con convenciones PEP 8 y distinguir asignación de comparación",
        "Explicar identidad vs igualdad, mutabilidad y conservar copias/valores originales",
        "Evaluar aritmética y comparaciones respetando precedencia",
        "Calcular montos en soles con Decimal y redondeo a 2 decimales",
        "Usar input/print y f-strings para capturar y reportar datos",
        "Parsear un registro sintético de cliente conservando originales y reportando errores accionables"
      ],
      "theory": [
        {
          "subtopicId": "S02-T1-A",
          "heading": "Literales y tipos básicos",
          "paragraphs": [
            "Un **literal** es un valor escrito directamente en el código: `34`, `150.5`, `\"Quispe\"`, `True`, `None`. Python clasifica cada valor en un **tipo**. Los tipos básicos de S02 son: **`int`** (enteros: `0`, `34`, `-7`), **`float`** (punto flotante: `150.5`, `1.0`), **`str`** (texto Unicode: `\"María José\"`, `\"Ñahui\"`), **`bool`** (`True` / `False`) y **`None`** (ausencia de valor; su tipo es **`NoneType`**).",
            "La trampa clásica de intake: el número **`42`** (int) y el texto **`\"42\"`** (str) **no son el mismo valor**. `42 == \"42\"` es `False`. En formularios y CSV **casi todo llega como str**. Si sumas o comparas sin convertir, obtienes `TypeError` o lógica silenciosamente incorrecta. El teléfono **`999000111` debe modelarse como `str`**, no como `int`: no es una cantidad aritmética y puede tener ceros a la izquierda en otros países.",
            "Para ver el tipo usa **`type(x)`** (devuelve la clase) o, en reportes didácticos, `type(x).__name__` (`\"int\"`, `\"str\"`, …). Más adelante preferirás `isinstance` para validar; primero entrenas el ojo con literales. Nota avanzada (no abuses): en Python **`bool` es subtipo de `int`**, así que `isinstance(True, int)` es `True`. Para lógica de negocio, trata `bool` como booleano, no como `0`/`1`, salvo que documentes una conversión explícita."
          ],
          "code": "# Registro sintético — cada literal tiene un tipo\nnombres = \"María José\"       # str\napellido_paterno = \"Quispe\"  # str\nedad = 34                    # int\nmonto_soles = 150.5          # float (¡aún no Decimal!)\nactivo = True                # bool\nreferencia = None            # NoneType\n\nprint(type(nombres).__name__)   # str\nprint(type(edad).__name__)      # int\nprint(type(referencia).__name__)  # NoneType\n\n# 42 vs \"42\": literales distintos\nprint(type(42).__name__)     # int\nprint(type(\"42\").__name__)   # str\nprint(42 == \"42\")            # False",
          "code_output": "str\nint\nNoneType\nint\nstr\nFalse"
        },
        {
          "subtopicId": "S02-T1-B",
          "heading": "Inspección, conversión y validación",
          "paragraphs": [
            "**`type(x)`** responde “¿qué es esto ahora?”. **`isinstance(x, int)`** responde “¿puedo tratarlo como int?” (incluye subtipos). En parsers, `isinstance` suele ser más útil que comparar `type(x) is int`, porque documenta la intención de validación.",
            "La conversión explícita usa constructores: **`int()`**, **`float()`**, **`str()`**. El texto de formularios trae espacios: **`valor.strip()`** antes de convertir. `int(\" 19 \")` funciona; `int(\"19.5\")` o `int(\"abc\")` lanzan **`ValueError`**. **Nunca uses `eval()`** sobre input de usuario: es un riesgo de seguridad y un anti-patrón de calidad de datos.",
            "Validación profesional: capturar el fallo, **nombrar el campo** en el mensaje y **no tragar el error en silencio**. Un patrón útil es devolver una tupla `(ok, valor_o_None, mensaje_o_None)` o acumular errores en una lista. Así un campo inválido no impide reportar los demás, y el raw sigue disponible para depurar."
          ],
          "code": null,
          "code_output": null
        },
        {
          "subtopicId": "S02-T2-A",
          "heading": "Asignación y convenciones de nombres",
          "paragraphs": [
            "**`=` asigna** un nombre a un valor en el namespace actual. **`==` compara** igualdad y devuelve un `bool`. En versiones modernas de Python, `if x = 1:` es **SyntaxError** (el walrus `:=` es otro tema y no es el default de S02). Mezclar `=` y `==` es el bug de novato más citado en code review junior.",
            "PEP 8 (guía de estilo): **`snake_case`** para variables y funciones (`apellido_paterno`, `parse_client`), **`UPPER_CASE`** para constantes (`EDAD_MINIMA`, `IGV_TASA`), **`CapWords`** para clases (más adelante). Evita nombres de una sola letra confusos: **`l` / `O` / `I`** se confunden con `1` y `0`. Prefiere `longitud`, `indice`, `columna`.",
            "En el schema de intake usa nombres estables y en español técnico claro: `nombres`, `apellido_paterno`, `apellido_materno`, `contacto`, `direccion`. No inventes parentesco real a partir de apellidos: son **campos de texto**, no una afirmación genealógica. Si un nombre no existe aún, Python lanza **`NameError`** — señal de typo o de usar antes de asignar."
          ],
          "code": "nombres_cliente = \"Ana\"\napellido_paterno = \"García\"\nEDAD_MINIMA = 18\nedad = 25\n\n# Comparación con == (no uses = aquí)\nif edad == EDAD_MINIMA:\n    print(\"edad mínima exacta\")\nelse:\n    print(f\"edad={edad}, mínima={EDAD_MINIMA}\")\n\n# NameError si descomentas:\n# print(apellido_materno)",
          "code_output": "edad=25, mínima=18"
        },
        {
          "subtopicId": "S02-T2-B",
          "heading": "Identidad, mutabilidad y copias superficiales",
          "paragraphs": [
            "**`==` compara valor**; **`is` / `is not` comparan identidad** (¿mismo objeto en memoria?). El idioma correcto para ausencia es **`x is None`** (no `x == None`, aunque a veces “funcione”). `id(x)` expone un identificador del objeto; úsalo para entender demos, no en lógica de negocio rutinaria.",
            "Los **`str` son inmutables**: `.strip()` o concatenar devuelve **otro** string; el original no cambia. Las **`list` son mutables**: `append` altera el mismo objeto. Si `b = a` y `a` es una lista, **`b` es un alias**: mutar `b` muta `a`. Para independizar: **`a.copy()`** o **`a[:]`** (copia superficial).",
            "Patrón de calidad de datos: guarda **`campo_raw`** (o un dict `raw`) con el texto original y trabaja en **`campo` / `clean`**. Si el parse falla, **el raw sigue ahí** para el mensaje de error y para reintentos. Nunca sobrescribas el original con la versión normalizada en el mismo nombre si necesitas auditoría."
          ],
          "code": null,
          "code_output": null
        },
        {
          "subtopicId": "S02-T3-A",
          "heading": "Operadores y precedencia",
          "paragraphs": [
            "Los operadores aritméticos de S02: **`+`**, **`-`**, **`*`**, **`/`** (división verdadera → `float`), **`//`** (división entera hacia −∞), **`%`** (resto) y **`**`** (potencia). Las **comparaciones** (`==`, `!=`, `<`, `<=`, `>`, `>=`) devuelven `bool` y se combinan con la aritmética en expresiones de negocio (rangos, umbrales).",
            "La **precedencia** importa: `*` y `/` van antes que `+` y `-`; `**` es aún más prioritario y se asocia a la derecha. Trampa clásica: **`-3**2` vale `-9`**, no `9`, porque el unario `-` se aplica al resultado de `3**2`. Usa **`(-3)**2`** si quieres el cuadrado del negativo. Cuando dudes, **paréntesis**: `(a + b) * c` no es lo mismo que `a + b * c`.",
            "En intake peruano, un precio con IGV 18% se escribe mentalmente como *base × (1 + 0.18)*. Si escribes `base + base * 0.18` sin paréntesis extra, la precedencia de `*` ya lo resuelve; si mezclas sumas de líneas y tasas, **paréntesis explícitos** evitan bugs de code review. Para dinero real en soles, **T3-B usa `Decimal`** — aquí entrenas la expresión; allá entrenas la precisión."
          ],
          "code": "a, b, c = 10, 3, 2\nprint(\"10 // 3 =\", a // b)   # 3\nprint(\"10 % 3  =\", a % b)    # 1\nprint(\"3 ** 2  =\", b ** c)   # 9\nprint(\"a + b * c =\", a + b * c)      # 16\nprint(\"(a + b) * c =\", (a + b) * c)  # 26\nprint(\"-3**2 =\", -3**2)              # -9\nprint(\"(-3)**2 =\", (-3)**2)          # 9",
          "code_output": "10 // 3 = 3\n10 % 3  = 1\n3 ** 2  = 9\na + b * c = 16\n(a + b) * c = 26\n-3**2 = -9\n(-3)**2 = 9"
        },
        {
          "subtopicId": "S02-T3-B",
          "heading": "Decimal para dinero y redondeo",
          "paragraphs": [
            "**`float` no es dinero.** `0.1 + 0.2` produce `0.30000000000000004` por representación binaria. En montos en **soles (S/)** de fintech, retail o bancos, usa **`decimal.Decimal`**. Construye desde **`str`**: `Decimal(\"0.1\")`, **nunca** `Decimal(0.1)` (ya arrastras el error del float).",
            "Redondeo a céntimos: **`quantize(Decimal(\"0.01\"))`**. El redondeo bancario por defecto suele ser **`ROUND_HALF_EVEN`** (mitad al par). Importa: `from decimal import Decimal, ROUND_HALF_EVEN`. Patrón: subtotal + IGV 18% → quantize en cada paso monetario que debas mostrar o persistir.",
            "En intake, el campo monto llega como **texto** (`\"150.50\"`). Parseas con `Decimal(texto.strip())`, capturas `InvalidOperation`, y reportas error con nombre de campo. **Convención S02: punto decimal** (`150.50`), no coma; si el CSV trae coma, documenta la normalización antes de Decimal."
          ],
          "code": "from decimal import Decimal, ROUND_HALF_EVEN\n\nprint(\"float:\", 0.1 + 0.2)\nprint(\"Decimal:\", Decimal(\"0.1\") + Decimal(\"0.2\"))\n\nsubtotal = Decimal(\"100.00\")\nigv = (subtotal * Decimal(\"0.18\")).quantize(\n    Decimal(\"0.01\"), rounding=ROUND_HALF_EVEN\n)\ntotal = (subtotal + igv).quantize(Decimal(\"0.01\"), rounding=ROUND_HALF_EVEN)\nprint(\"subtotal\", subtotal, \"IGV\", igv, \"total\", total)",
          "code_output": "float: 0.30000000000000004\nDecimal: 0.3\nsubtotal 100.00 IGV 18.00 total 118.00"
        },
        {
          "subtopicId": "S02-T4-A",
          "heading": "input, print y f-strings",
          "paragraphs": [
            "**`input(prompt)`** siempre devuelve **`str`**, aunque el usuario escriba dígitos. En el browser/Pyodide a menudo **simulas input** con variables o parámetros de función (testeable). **`print(*args, sep=\" \", end=\"\\n\")`** controla separadores y fin de línea.",
            "Las **f-strings** (`f\"...{expr}...\"`) son el formato preferido en S02: legibles, con expresiones cortas y especificadores (`{monto:.2f}`, `{nombre!r}`). Después de T3-B, todo monto de negocio continúa como `Decimal`: formatear con `.2f` no requiere convertirlo a float. Prompts y mensajes de error del intake van en **español claro** (“Ingresa el contacto:”, “ERROR en 'edad': …”).",
            "Patrón profesional: separa **captura** (valores str), **parse** (tipos) y **reporte** (f-strings). Así puedes unit-testear el parse sin depender de la consola. Un resumen de cliente con 4–5 campos es el puente natural al You Do."
          ],
          "code": "from decimal import Decimal\n\n# Simula input (testeable): no llames input() en demos de CI\nnombres = \"María José\"\nmonto = Decimal(\"150.50\")\nprint(f\"Cliente: {nombres} | Monto: S/ {monto:.2f}\")\nprint(\"campos\", \"a\", \"b\", sep=\" | \")",
          "code_output": "Cliente: María José | Monto: S/ 150.50\ncampos | a | b"
        },
        {
          "subtopicId": "S02-T4-B",
          "heading": "Parsing de intake y mensajes de error",
          "paragraphs": [
            "Un **parser de intake** recibe un registro sintético, conserva **`*_raw`**, produce campos limpios (strip) y acumula **`errors: list[str]`** sin tragar excepciones. El raw **siempre** está, incluso si el clean es `None` o el campo está vacío.",
            "Casos mínimos del gate CP-N1-A: **vacío** (mensaje accionable + raw `\"\"`), **Unicode** (García, Ñahui, María — round-trip sin errores ASCII), **número inválido** (`edad=\"abc\"` → error con nombre de campo, raw intacto). Los tests son **asserts** o pytest: no “mirar la consola y ya”.",
            "Mensaje accionable = **qué campo**, **qué valor se recibió** (`!r` / repr), **qué se esperaba**. Evita `except: pass`. No afirmes parentesco real por dos apellidos: son **campos de texto** del schema."
          ],
          "code": "def safe_int(campo: str, valor: str):\n    try:\n        return True, int(valor.strip()), None\n    except ValueError:\n        msg = f\"ERROR en '{campo}': no se pudo convertir {valor!r} a int\"\n        return False, None, msg\n\nok, n, err = safe_int(\"edad\", \"abc\")\nprint(ok, n, err)\nraw = \"  Ñahui  \"\nprint(\"raw\", repr(raw), \"clean\", repr(raw.strip()))",
          "code_output": "False None ERROR en 'edad': no se pudo convertir 'abc' a int\nraw '  Ñahui  ' clean 'Ñahui'"
        }
      ],
      "iDo": {
        "steps": [
          {
            "demoId": "S02-T1-A-DEMO",
            "subtopicId": "S02-T1-A",
            "environment": "browser-pyodide",
            "description": "Literales de un registro de cliente y type() de cada campo",
            "why": "Antes de parsear, el analista debe “ver” el tipo de cada literal. Si el teléfono o un código viaja como int, o si comparas 42 con \"42\", el pipeline de calidad miente. Esta demo fija int/float/str/bool/NoneType y la distinción literal vs texto numérico.",
            "code": "# Cliente sintético (no es persona real)\nnombres = \"María José\"\napellido_paterno = \"Quispe\"\nedad = 34\nmonto_soles = 150.5\nactivo = True\nreferencia = None\n\ncampos = [\n    (\"nombres\", nombres),\n    (\"apellido_paterno\", apellido_paterno),\n    (\"edad\", edad),\n    (\"monto_soles\", monto_soles),\n    (\"activo\", activo),\n    (\"referencia\", referencia),\n]\nfor label, valor in campos:\n    print(f\"{label}: valor={valor!r} type={type(valor).__name__}\")\n\nprint(\"type(42)=\", type(42).__name__)\nprint(\"type('42')=\", type(\"42\").__name__)\nprint(\"42 == '42' →\", 42 == \"42\")",
            "language": "python",
            "output": "nombres: valor='María José' type=str\napellido_paterno: valor='Quispe' type=str\nedad: valor=34 type=int\nmonto_soles: valor=150.5 type=float\nactivo: valor=True type=bool\nreferencia: valor=None type=NoneType\ntype(42)= int\ntype('42')= str\n42 == '42' → False"
          },
          {
            "demoId": "S02-T1-B-DEMO",
            "subtopicId": "S02-T1-B",
            "environment": "browser-pyodide",
            "description": "Convertir campos de texto a número con mensaje de error por campo",
            "why": "strip + int cubre el caso feliz con espacios. El caso \"abc\" no debe tumbar el notebook sin contexto: el mensaje nombra el campo y muestra el valor recibido. isinstance separa “ya es int” de “sigue siendo texto”.",
            "code": "def convertir_edad(campo: str, valor: str):\n    try:\n        n = int(valor.strip())\n        print(f\"OK {campo}={n}\")\n        return n\n    except ValueError:\n        print(f\"ERROR en '{campo}': no se pudo convertir {valor!r} a int\")\n        return None\n\nconvertir_edad(\"edad\", \" 19 \")\nconvertir_edad(\"edad\", \"abc\")\nprint(\"isinstance(19, int) →\", isinstance(19, int))\nprint(\"isinstance('19', int) →\", isinstance(\"19\", int))",
            "language": "python",
            "output": "OK edad=19\nERROR en 'edad': no se pudo convertir 'abc' a int\nisinstance(19, int) → True\nisinstance('19', int) → False"
          },
          {
            "demoId": "S02-T2-A-DEMO",
            "subtopicId": "S02-T2-A",
            "environment": "browser-pyodide",
            "description": "Renombrar a snake_case y usar == en comparaciones",
            "why": "Code review junior en Perú mira nombres y = vs == antes que algoritmos. snake_case + constantes UPPER_CASE + == en el if es el contrato mínimo de legibilidad para el schema de intake.",
            "code": "# Mal estilo (comentado a propósito):\n# NombreCliente = \"Ana\"; AP = \"García\"; l = 1\n\nnombres_cliente = \"Ana\"\napellido_paterno = \"García\"\nEDAD_MINIMA = 18\nedad = 25\n\nif edad == EDAD_MINIMA:\n    print(\"edad mínima exacta\")\nelse:\n    print(f\"edad={edad}, mínima={EDAD_MINIMA}\")\n\nprint(\"nombres_cliente=\", nombres_cliente)\nprint(\"apellido_paterno=\", apellido_paterno)",
            "language": "python",
            "output": "edad=25, mínima=18\nnombres_cliente= Ana\napellido_paterno= García"
          },
          {
            "demoId": "S02-T2-B-DEMO",
            "subtopicId": "S02-T2-B",
            "environment": "browser-pyodide",
            "description": "Alias vs copia y preservar raw tras normalizar",
            "why": "El raw debe sobrevivir al strip. En estructuras mutables, el alias es la forma #1 de corromper el original “sin tocar raw”. copy()/slice y claves *_raw son el hábito que el parser de intake exige.",
            "code": "raw_nombre = \"  José Ñahui  \"\nclean_nombre = raw_nombre.strip()\nprint(\"raw=\", repr(raw_nombre))\nprint(\"clean=\", repr(clean_nombre))\nprint(\"raw is clean?\", raw_nombre is clean_nombre)\n\na = [1, 2, 3]\nb = a\nc = a.copy()\nb.append(4)\nprint(\"a after alias mutate:\", a)\nprint(\"c unchanged:\", c)\nprint(\"a is b?\", a is b)\nprint(\"a is c?\", a is c)\n\nx = None\nprint(\"x is None →\", x is None)",
            "language": "python",
            "output": "raw= '  José Ñahui  '\nclean= 'José Ñahui'\nraw is clean? False\na after alias mutate: [1, 2, 3, 4]\nc unchanged: [1, 2, 3]\na is b? True\na is c? False\nx is None → True"
          },
          {
            "demoId": "S02-T3-A-DEMO",
            "subtopicId": "S02-T3-A",
            "environment": "browser-pyodide",
            "description": "Evaluar // % ** y corregir expresión con precedencia",
            "why": "Antes de confiar en un cálculo de ticket, verificas // % ** y paréntesis. -3**2 es la trampa de precedencia más citada; (a+b)*c vs a+b*c es el bug de descuento/IGV en una línea.",
            "code": "a, b, c = 10, 3, 2\nprint(\"10 // 3 =\", a // b)\nprint(\"10 % 3  =\", a % b)\nprint(\"3 ** 2  =\", b ** c)\nprint(\"a + b * c =\", a + b * c)\nprint(\"(a + b) * c =\", (a + b) * c)\nprint(\"-3**2 =\", -3**2)\nprint(\"(-3)**2 =\", (-3)**2)\n# Precio con IGV 18% (expresión; dinero exacto → Decimal en T3-B)\nbase = 100\ntotal = base * (1 + 0.18)\nprint(\"total con IGV (float demo) =\", total)",
            "language": "python",
            "output": "10 // 3 = 3\n10 % 3  = 1\n3 ** 2  = 9\na + b * c = 16\n(a + b) * c = 26\n-3**2 = -9\n(-3)**2 = 9\ntotal con IGV (float demo) = 118.0"
          },
          {
            "demoId": "S02-T3-B-DEMO",
            "subtopicId": "S02-T3-B",
            "environment": "browser-pyodide",
            "description": "Subtotal + IGV 18% con Decimal y quantize a 2 decimales",
            "why": "En soles, float miente. Decimal desde str + quantize(0.01) es el contrato mínimo de montos en onboarding de data financiera en Perú.",
            "code": "from decimal import Decimal, ROUND_HALF_EVEN\n\nprint(\"float 0.1+0.2 =\", 0.1 + 0.2)\nprint(\"Decimal =\", Decimal(\"0.1\") + Decimal(\"0.2\"))\n\nsubtotal = Decimal(\"100.00\")\nigv = (subtotal * Decimal(\"0.18\")).quantize(\n    Decimal(\"0.01\"), rounding=ROUND_HALF_EVEN\n)\ntotal = (subtotal + igv).quantize(Decimal(\"0.01\"), rounding=ROUND_HALF_EVEN)\nprint(f\"subtotal={subtotal} IGV={igv} total={total}\")",
            "language": "python",
            "output": "float 0.1+0.2 = 0.30000000000000004\nDecimal = 0.3\nsubtotal=100.00 IGV=18.00 total=118.00"
          },
          {
            "demoId": "S02-T4-A-DEMO",
            "subtopicId": "S02-T4-A",
            "environment": "browser-pyodide",
            "description": "Capturar (simulado) y reportar nombre + monto con f-string",
            "why": "input devuelve str; el reporte usa f-strings y :.2f. Separar captura de formato permite testear el parser sin consola interactiva.",
            "code": "from decimal import Decimal\n\n# Simula input() con variables (testeable en Pyodide/CI)\nnombres = \"María José\"\nmonto = Decimal(\"150.50\")\nprint(f\"Cliente: {nombres} | Monto: S/ {monto:.2f}\")\nprint(\"OK\", \"intake\", sep=\" · \")",
            "language": "python",
            "output": "Cliente: María José | Monto: S/ 150.50\nOK · intake"
          },
          {
            "demoId": "S02-T4-B-DEMO",
            "subtopicId": "S02-T4-B",
            "environment": "browser-pyodide",
            "description": "Parser mínimo: raw, clean, errors; 3 casos de prueba",
            "why": "El gate no es “imprimió algo”: son asserts sobre raw, Unicode y errores accionables. Esta demo es el núcleo del You Do CP-N1-A.",
            "code": "def safe_int(campo: str, valor: str):\n    try:\n        return True, int(valor.strip()), None\n    except ValueError:\n        return False, None, f\"ERROR en '{campo}': no se pudo convertir {valor!r} a int\"\n\ndef parse_client(nombres: str, apellido_materno: str, edad=None) -> dict:\n    errors: list[str] = []\n    rec = {\n        \"nombres_raw\": nombres,\n        \"apellido_materno_raw\": apellido_materno,\n        \"edad_raw\": edad,\n        \"nombres\": nombres.strip() if nombres and nombres.strip() else None,\n        \"apellido_materno\": apellido_materno.strip() if apellido_materno and apellido_materno.strip() else None,\n        \"edad\": None,\n        \"errors\": errors,\n    }\n    if not nombres or not nombres.strip():\n        errors.append(f\"ERROR en 'nombres': vacío (raw={nombres!r})\")\n    if edad is not None:\n        ok, n, err = safe_int(\"edad\", edad)\n        if ok:\n            rec[\"edad\"] = n\n        else:\n            errors.append(err)\n    return rec\n\n# 1) Unicode feliz\nr1 = parse_client(\"María\", \"Ñahui\", \"34\")\nassert r1[\"apellido_materno_raw\"] == \"Ñahui\" and r1[\"errors\"] == []\n# 2) Vacío\nr2 = parse_client(\"\", \"Ñahui\")\nassert r2[\"nombres_raw\"] == \"\" and any(\"nombres\" in e for e in r2[\"errors\"])\n# 3) Número inválido\nr3 = parse_client(\"Ana\", \"Díaz\", \"abc\")\nassert r3[\"edad_raw\"] == \"abc\" and any(\"edad\" in e for e in r3[\"errors\"])\nprint(\"3 tests OK\")\nprint(r1)\nprint(r2[\"errors\"])\nprint(r3[\"errors\"])",
            "language": "python",
            "output": "3 tests OK\n{'nombres_raw': 'María', 'apellido_materno_raw': 'Ñahui', 'edad_raw': '34', 'nombres': 'María', 'apellido_materno': 'Ñahui', 'edad': 34, 'errors': []}\n[\"ERROR en 'nombres': vacío (raw='')\"]\n[\"ERROR en 'edad': no se pudo convertir 'abc' a int\"]"
          }
        ]
      },
      "weDo": {
        "exercises": [
          {
            "id": "S02-T1-A-E1",
            "instruction": "E1 (guiado) — Clasifica estos literales imprimiendo `repr(valor)` y `type(valor).__name__` para cada uno: `0`, `3.14`, `\"Lima\"`, `False`, `None`.",
            "hints": [
              "Recorre una lista de literales con un for. Usa type(x).__name__ para un nombre legible (int, float, str, bool, NoneType).",
              "None imprime type NoneType, no \"None\". False es bool, no str. 0 es int, no bool."
            ],
            "kind": "guided",
            "tests": "Checklist: 5 líneas; tipos en orden int, float, str, bool, NoneType.",
            "starterCode": "literales = [0, 3.14, \"Lima\", False, None]\n\nfor lit in literales:\n    # TODO: imprime repr y nombre del tipo\n    print(____, \"→\", ____)"
          },
          {
            "id": "S02-T1-A-E2",
            "instruction": "E2 (independiente) — Demuestra que `42` y `\"42\"` son tipos distintos: imprime ambos `type(...).__name__`, el resultado de `42 == \"42\"`, y el de `str(42) == \"42\"`. Comenta en un print por qué `isinstance(True, int)` es True (subtipo) y por qué no debes abusar de eso en intake.",
            "hints": [
              "No conviertas antes de la primera comparación. str(42) produce el texto \"42\".",
              "bool es subclase de int en Python: isinstance(True, int) → True. En negocio, modela banderas como bool explícito, no como 1/0 accidental."
            ],
            "kind": "independent",
            "tests": "assert types int/str; assert 42 != \"42\"; assert str(42) == \"42\"; mención de isinstance(True, int).",
            "starterCode": "codigo_int = 42\ncodigo_str = \"42\"\n\nprint(\"tipos:\", ____, ____)\nprint(\"igualdad cruda:\", ____)\nprint(\"igualdad tras str():\", ____)\nprint(\"isinstance(True, int) →\", ____)\nprint(\"Nota: ____\")  # una frase sobre no abusar"
          },
          {
            "id": "S02-T1-A-E3",
            "instruction": "E3 (transferencia) — Para el cliente sintético de intake, elige el tipo Python correcto de cada campo y crea literales de ejemplo. Campos: nombres, apellido_paterno, apellido_materno, contacto (teléfono), edad, activo. El teléfono **debe ser str**. Imprime un reporte campo → valor → tipo y verifica con `type(v) is t_esperado`.",
            "hints": [
              "contacto = \"999000111\" (str), no 999000111 (int). Apellidos con ñ/tildes son str Unicode.",
              "Puedes guardar tuplas (valor, tipo_esperado) en un dict y validar type(v) is t en un for."
            ],
            "kind": "transfer",
            "tests": "Rúbrica: 6 campos; contacto str; edad int; activo bool; todos type checks True.",
            "starterCode": "# Completa valor y tipo esperado (int, float, str, bool, type(None) no hace falta aquí)\ncampos = {\n    \"nombres\": (\"____\", str),\n    \"apellido_paterno\": (\"____\", str),\n    \"apellido_materno\": (\"____\", str),\n    \"contacto\": (\"____\", str),  # teléfono\n    \"edad\": (____, int),\n    \"activo\": (____, bool),\n}\n\nfor k, (v, t) in campos.items():\n    ok = type(v) is t\n    print(f\"{k}: {v!r} esperado={t.__name__} ok={ok}\")"
          },
          {
            "id": "S02-T1-B-E1",
            "instruction": "E1 (guiado) — Dado `raw = \" 21 \"`, conviértelo a `int` usando `strip` y muestra el valor y su tipo. Debe quedar `21` de tipo int.",
            "hints": [
              "int(raw.strip()) — strip primero, constructor después.",
              "Si haces int(raw) sin strip, en \" 21 \" también funciona en Python 3, pero el hábito strip es obligatorio para vacíos y mensajes; úsalo siempre en parsers."
            ],
            "kind": "guided",
            "tests": "assert edad == 21 and type(edad) is int",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nraw = \" 21 \"\nedad = int(raw.strip())\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(edad, type(edad).__name__)\n"
          },
          {
            "id": "S02-T1-B-E2",
            "instruction": "E2 (independiente) — Implementa `safe_int(campo, valor) -> tuple` que devuelva `(True, int, None)` si convierte, o `(False, None, mensaje)` si está vacío (tras strip) o no es entero. Prueba con `\" 21 \"`, `\"\"`, `\"abc\"` y `\"  \"`.",
            "hints": [
              "Tras strip, si texto == \"\", error de vacío. try/except ValueError para letras.",
              "Mensaje accionable: incluye el nombre del campo y el valor recibido con !r (repr)."
            ],
            "kind": "independent",
            "tests": "returns (ok, value|None, msg); 4 casos como en la demo de solución.",
            "starterCode": "def safe_int(campo: str, valor: str):\n    texto = valor.strip()\n    # TODO: vacío → (False, None, msg)\n    # TODO: int(texto) → (True, n, None)\n    # TODO: ValueError → (False, None, msg)\n    pass\n\nfor v in [\" 21 \", \"\", \"abc\", \"  \"]:\n    print(repr(v), \"→\", safe_int(\"edad\", v))"
          },
          {
            "id": "S02-T1-B-E3",
            "instruction": "E3 (transferencia) — Pipeline de 2 campos: `edad` (`int`) y `monto` (`Decimal` cuantizado a céntimos) desde texto. Devuelve un dict con `raw`, `clean` y `errors`. Si un campo falla, el otro puede seguir OK; raw siempre conserva los strings originales. Prueba: ambos OK; edad inválida; monto inválido.",
            "hints": [
              "Importa Decimal e InvalidOperation; nunca pases el monto por float.",
              "clean[\"edad\"] = None si falla, pero raw[\"edad\"] sigue siendo el string de entrada."
            ],
            "kind": "transfer",
            "tests": "Contrato exacto: monto válido es Decimal(\"150.50\")/Decimal(\"99.00\"); 3 escenarios; raw intacto; cero llamadas a float().",
            "starterCode": null
          },
          {
            "id": "S02-T2-A-E1",
            "instruction": "E1 (guiado) — Renombra estas 5 variables a PEP 8: `NombreCliente`, `AP`, `x` (índice), `l` (longitud), `EdadMaxima` (constante de negocio). Imprime los cinco nombres buenos con valores sintéticos.",
            "hints": [
              "snake_case para vars; UPPER_CASE para la constante de tope de edad.",
              "Evita l/O/I: usa longitud, indice. apellido_paterno en lugar de AP."
            ],
            "kind": "guided",
            "tests": "style pass: 5 nombres PEP8; sin l/O/I sueltos.",
            "starterCode": "# Antes (malo):\n# NombreCliente = \"Luis\"\n# AP = \"Ramos\"\n# x = 0\n# l = 5\n# EdadMaxima = 120\n\n# Después (bueno):\n____ = \"Luis\"\n____ = \"Ramos\"\n____ = 0\n____ = 5\n____ = 120\n\nprint(nombre_cliente, apellido_paterno, indice, longitud, EDAD_MAXIMA)"
          },
          {
            "id": "S02-T2-A-E2",
            "instruction": "E2 (independiente) — Bug hunt: el siguiente código tiene tres comparaciones rotas con `=` en lugar de `==`. Corrígelas para que imprima tres líneas `ok ...` sin SyntaxError.",
            "hints": [
              "En cada if, cambia = por ==. No uses el operador walrus := en S02.",
              "if flag == True funciona; también puedes escribir if flag: — ambas aceptables aquí si el archivo corre."
            ],
            "kind": "independent",
            "tests": "runs without SyntaxError; tres prints ok.",
            "starterCode": "estado = \"activo\"\ncodigo = 10\nflag = True\n\n# Corrige las tres líneas (hoy están mal a propósito):\nif estado = \"activo\":\n    print(\"ok estado\")\nif codigo = 10:\n    print(\"ok codigo\")\nif flag = True:\n    print(\"ok flag\")"
          },
          {
            "id": "S02-T2-A-E3",
            "instruction": "E3 (transferencia) — Un CSV de intake llega con encabezados feos. Propón nombres PEP 8 (snake_case) para: `Nombres`, `Apellido Paterno`, `Apellido Materno`, `Teléfono / Cel`, `Dirección`, `Edad (años)`. Devuelve un dict `original → snake_case` e imprime el mapeo. Incluye `apellido_paterno` y `apellido_materno`.",
            "hints": [
              "Minúsculas, guiones bajos, sin espacios ni tildes en el identificador Python.",
              "Ejemplos: \"Apellido Paterno\" → apellido_paterno; \"Teléfono / Cel\" → telefono o contacto (elige uno y sé consistente)."
            ],
            "kind": "transfer",
            "tests": "rubric naming: 6 claves; snake_case; incluye apellido_paterno y apellido_materno.",
            "starterCode": "encabezados = [\n    \"Nombres\",\n    \"Apellido Paterno\",\n    \"Apellido Materno\",\n    \"Teléfono / Cel\",\n    \"Dirección\",\n    \"Edad (años)\",\n]\n\n# TODO: dict mapeo original -> snake_case\nmapeo = {\n    # \"Nombres\": \"nombres\",\n}\n\nfor orig in encabezados:\n    print(f\"{orig!r} → {mapeo.get(orig, '???')}\")"
          },
          {
            "id": "S02-T2-B-E1",
            "instruction": "E1 (guiado) — Completa la tabla de predicciones imprimiendo el bool real de: `None is None`, `[] == []`, `[] is []`, `1 == True`, `1 is True`. Luego escribe una línea de comentario: cuándo usar `is` vs `==`.",
            "hints": [
              "Listas nuevas no son el mismo objeto: [] is [] es False. Usa is para None.",
              "1 == True es True (bool subtipo int), pero 1 is True es False: no uses is para igualdad numérica."
            ],
            "kind": "guided",
            "tests": "prediction table: True, True, False, True, False.",
            "starterCode": "print(\"None is None →\", ____)\nprint(\"[] == [] →\", ____)\nprint(\"[] is [] →\", ____)\nprint(\"1 == True →\", ____)\nprint(\"1 is True →\", ____)\n# Comentario: ____"
          },
          {
            "id": "S02-T2-B-E2",
            "instruction": "E2 (independiente) — `original = [\"a\", \"b\"]`. Crea `trabajo` como **copia** (no alias), haz `trabajo.append(\"c\")` y demuestra que `original` sigue siendo `[\"a\", \"b\"]`.",
            "hints": [
              "trabajo = original.copy()  o  trabajo = original[:]",
              "Si haces trabajo = original, append mutará ambos. Verifica con print y con `original is trabajo` → False."
            ],
            "kind": "independent",
            "tests": "assert original == [\"a\", \"b\"] and trabajo == [\"a\", \"b\", \"c\"]",
            "starterCode": "original = [\"a\", \"b\"]\ntrabajo = ____  # debe ser copia, no alias\ntrabajo.append(\"c\")\nprint(\"original:\", original)\nprint(\"trabajo:\", trabajo)\nprint(\"mismo objeto?\", original is trabajo)"
          },
          {
            "id": "S02-T2-B-E3",
            "instruction": "E3 (transferencia) — Diseña `make_record(nombres, contacto)` que devuelva un dict con `nombres_raw`, `contacto_raw`, `nombres` y `contacto` (strip). Luego muta solo el campo limpio a `.upper()` y **assert** que los `*_raw` no cambiaron.",
            "hints": [
              "Guarda el string original en *_raw antes de strip. No reutilices el mismo nombre para ambos.",
              "Tras upper() en clean, assert rec[\"nombres_raw\"] == entrada_original."
            ],
            "kind": "transfer",
            "tests": "assert raw keys; clean puede cambiar; raw idéntico al input.",
            "starterCode": "def make_record(nombres: str, contacto: str) -> dict:\n    # TODO: *_raw + strip en limpios\n    pass\n\nentrada_nombres = \"  María  \"\nentrada_contacto = \" 999 \"\nrec = make_record(entrada_nombres, entrada_contacto)\nrec[\"nombres\"] = rec[\"nombres\"].upper()\nprint(rec)\nassert rec[\"nombres_raw\"] == entrada_nombres\nassert rec[\"contacto_raw\"] == entrada_contacto\nprint(\"raw preserved OK\")"
          },
          {
            "id": "S02-T3-A-E1",
            "instruction": "E1 (guiado) — Con `n = 17` y `d = 5`, imprime `n // d`, `n % d`, `2 ** 4` y `n / d`. Comenta en un print por qué `/` devuelve float.",
            "hints": [
              "Usa los operadores // % ** / directamente. No hace falta import.",
              "17//5 → 3, 17%5 → 2, 2**4 → 16, 17/5 → 3.4 (float)."
            ],
            "kind": "guided",
            "tests": "assert 17//5==3; 17%5==2; 2**4==16; 17/5==3.4",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nn = 17\nd = 5\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(\"//\", n // d)\n"
          },
          {
            "id": "S02-T3-A-E2",
            "instruction": "E2 (independiente) — Demuestra que `-3**2` es `-9` y que `(-3)**2` es `9`. Asigna `cuadrado_neg = (-3)**2` y haz `assert cuadrado_neg == 9`.",
            "hints": [
              "** tiene mayor precedencia que el unario menos. Paréntesis cambian el orden.",
              "print(-3**2) y print((-3)**2); el assert solo pasa con (-3)**2."
            ],
            "kind": "independent",
            "tests": "assert -3**2 == -9; assert (-3)**2 == 9",
            "starterCode": "print(\"sin paréntesis:\", -3**2)\nprint(\"con paréntesis:\", (-3)**2)\ncuadrado_neg = ____  # debe ser 9\nassert cuadrado_neg == 9\nprint(\"assert OK\")"
          },
          {
            "id": "S02-T3-A-E3",
            "instruction": "E3 (transferencia) — Un ticket sintético tiene líneas `50` y `30` (soles, solo para practicar ops). Calcula `subtotal = 50 + 30` y `total = subtotal * (1 + 0.18)` con **paréntesis explícitos** en la tasa IGV 18%. Imprime ambos. (En T3-B reemplazarás float por Decimal.)",
            "hints": [
              "subtotal primero; luego multiplica por (1 + 0.18), no 1 + 0.18 * subtotal sin revisar.",
              "Con base 80, total float es 94.3999… — es la motivación de Decimal. Si usas base 100, total=118.0 limpio."
            ],
            "kind": "transfer",
            "tests": "subtotal==80; total == 80*(1+0.18); expresión usa paréntesis en (1+0.18)",
            "starterCode": "linea_a = 50\nlinea_b = 30\n# TODO: subtotal y total con IGV 18% y paréntesis\nsubtotal = ____\ntotal = ____\nprint(\"subtotal\", subtotal)\nprint(\"total\", total)"
          },
          {
            "id": "S02-T3-B-E1",
            "instruction": "E1 (guiado) — Imprime `0.1 + 0.2` (float) y `Decimal(\"0.1\") + Decimal(\"0.2\")`. Importa desde `decimal`.",
            "hints": [
              "from decimal import Decimal. Construye Decimal desde strings, no desde 0.1 float.",
              "float imprime 0.30000000000000004; Decimal imprime 0.3."
            ],
            "kind": "guided",
            "tests": "assert Decimal(\"0.1\")+Decimal(\"0.2\") == Decimal(\"0.3\")",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nfrom decimal import Decimal\n\nassert Decimal(\"0.1\") + Decimal(\"0.2\") == Decimal(\"0.3\")\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(\"float\", 0.1 + 0.2)\n"
          },
          {
            "id": "S02-T3-B-E2",
            "instruction": "E2 (independiente) — Cuenta de restaurante sintético `Decimal(\"85.50\")`, propina 10%. Calcula propina y total con `quantize(Decimal(\"0.01\"), rounding=ROUND_HALF_EVEN)`. Sin float.",
            "hints": [
              "from decimal import Decimal, ROUND_HALF_EVEN. Multiplica por Decimal(\"0.10\").",
              "propina = (cuenta * Decimal(\"0.10\")).quantize(Decimal(\"0.01\"), rounding=ROUND_HALF_EVEN); total = (cuenta + propina).quantize(...)."
            ],
            "kind": "independent",
            "tests": "propina==Decimal(\"8.55\"); total==Decimal(\"94.05\")",
            "starterCode": "from decimal import Decimal, ROUND_HALF_EVEN\n\ncuenta = Decimal(\"85.50\")\n# TODO: propina 10% y total con quantize\npropina = ____\ntotal = ____\nprint(propina, total)\nassert propina == Decimal(\"8.55\")\nassert total == Decimal(\"94.05\")\nprint(\"OK\")"
          },
          {
            "id": "S02-T3-B-E3",
            "instruction": "E3 (transferencia) — Implementa `parse_monto(texto: str)` que devuelva `(ok, Decimal|None, error|None)`: strip, rechace vacío, parsee con `Decimal`, quantize a `0.01`, error accionable si falla. Prueba `\"150.50\"`, `\"  20.1 \"`, `\"\"`, `\"abc\"`. Convención: **punto** decimal.",
            "hints": [
              "try/except InvalidOperation. raw en el mensaje con !r.",
              "from decimal import Decimal, ROUND_HALF_EVEN, InvalidOperation. Si strip vacío → error antes de Decimal."
            ],
            "kind": "transfer",
            "tests": "OK 150.50; vacío y abc con error; sin float",
            "starterCode": "from decimal import Decimal, ROUND_HALF_EVEN, InvalidOperation\n\ndef parse_monto(texto: str):\n    # TODO: (ok, Decimal|None, error|None)\n    pass\n\nfor s in [\"150.50\", \"  20.1 \", \"\", \"abc\"]:\n    print(repr(s), \"→\", parse_monto(s))"
          },
          {
            "id": "S02-T4-A-E1",
            "instruction": "E1 (guiado) — Con `nombre = \"José\"`, imprime un saludo con f-string: `Hola, José. Bienvenido al intake.` (acento incluido).",
            "hints": [
              "f\"Hola, {nombre}. Bienvenido al intake.\"",
              "No concatenes con + salvo que practiques; f-string es el estilo S02."
            ],
            "kind": "guided",
            "tests": "stdout contiene José y intake",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nnombre = \"José\"\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(f\"Hola, {nombre}. Bienvenido al intake.\")\n"
          },
          {
            "id": "S02-T4-A-E2",
            "instruction": "E2 (independiente) — Con nombres, apellido_paterno, contacto y monto `Decimal(\"99.5\")`, imprime un reporte multi-línea con f-string incluyendo `S/ {monto:.2f}`.",
            "hints": [
              "Usa varios print(f\"...\") o un solo f-string multi-línea. :.2f formatea dos decimales.",
              "Incluye las 4 etiquetas: nombres, apellido_paterno, contacto, monto."
            ],
            "kind": "independent",
            "tests": "salida con 4 campos; monto con 2 decimales (99.50)",
            "starterCode": "from decimal import Decimal\n\nnombres = \"Ana\"\napellido_paterno = \"Ramos\"\ncontacto = \"999000111\"\nmonto = Decimal(\"99.5\")\n# TODO: reporte multi-línea con f-strings\nprint(f\"nombres: {____}\")\nprint(f\"apellido_paterno: {____}\")\nprint(f\"contacto: {____}\")\nprint(f\"monto: S/ {____:.2f}\")"
          },
          {
            "id": "S02-T4-A-E3",
            "instruction": "E3 (transferencia) — Escribe `simular_intake(nombres: str, contacto: str, edad: str) -> dict` que **no** llame a `input()` real: recibe los valores (como si vinieran de prompts), devuelve un dict con esos str y un subdict `types` con `type(...).__name__` de cada uno. Demuestra que todo sigue siendo str.",
            "hints": [
              "No uses input(). Los parámetros ya simulan las respuestas del usuario.",
              "types = {k: type(v).__name__ for k, v in datos.items()}"
            ],
            "kind": "transfer",
            "tests": "function takes values params; all types str",
            "starterCode": "def simular_intake(nombres: str, contacto: str, edad: str) -> dict:\n    # TODO: devolver dict con campos + types\n    pass\n\nr = simular_intake(\"  Ana  \", \"999\", \"34\")\nprint(r)\nassert r[\"types\"][\"edad\"] == \"str\"\nassert r[\"types\"][\"nombres\"] == \"str\"\nprint(\"OK\")"
          },
          {
            "id": "S02-T4-B-E1",
            "instruction": "E1 (guiado) — Completa `parse_nombres(valor: str) -> dict` con claves `nombres_raw`, `nombres` (strip o None si vacío) y `errors` (lista). Si vacío tras strip, agrega error accionable. Prueba con `\"\"`.",
            "hints": [
              "Siempre guarda raw = valor original antes de strip.",
              "errors.append(f\"ERROR en 'nombres': vacío (raw={valor!r})\")"
            ],
            "kind": "guided",
            "tests": "test empty: raw==\"\"; errors no vacío; nombres is None",
            "starterCode": "def parse_nombres(valor: str) -> dict:\n    # TODO: raw + clean + errors\n    pass\n\nr = parse_nombres(\"\")\nprint(r)\nassert r[\"nombres_raw\"] == \"\"\nassert r[\"nombres\"] is None\nassert any(\"nombres\" in e.lower() for e in r[\"errors\"])\nprint(\"OK\")"
          },
          {
            "id": "S02-T4-B-E2",
            "instruction": "E2 (independiente) — Parsea `apellido_materno = \"  Ñahui  \"` conservando raw con espacios y clean strip. Assert de round-trip Unicode: clean == \"Ñahui\" y raw original intacto.",
            "hints": [
              "No encodes a ascii. Python 3 str es Unicode.",
              "raw = original; clean = original.strip(); assert raw == original."
            ],
            "kind": "independent",
            "tests": "test unicode: raw con espacios; clean == \"Ñahui\"",
            "starterCode": "original = \"  Ñahui  \"\n# TODO: raw y clean\nraw = ____\nclean = ____\nprint(repr(raw), \"→\", repr(clean))\nassert raw == original\nassert clean == \"Ñahui\"\nprint(\"Unicode OK\")"
          },
          {
            "id": "S02-T4-B-E3",
            "instruction": "E3 (transferencia) — Implementa `parse_client(nombres, apellido_paterno, apellido_materno, contacto, direccion, edad=None)` con `*_raw`, limpios, `errors`, y `safe_int` para edad. Suite de 3 tests: feliz+Unicode, vacío en nombres, edad `\"abc\"`. Todos deben pasar.",
            "hints": [
              "Reutiliza el patrón del DEMO T4-B. No dejes que ValueError se escape.",
              "assert r[\"edad_raw\"]==\"abc\" y any(\"edad\" in e.lower() for e in errors). raw siempre presente."
            ],
            "kind": "transfer",
            "tests": null,
            "starterCode": null
          }
        ]
      },
      "youDo": {
        "title": "Parser de intake — registro sintético de cliente",
        "context": "Incremento del capstone CP-N1-A: en lugar de una calculadora de presupuesto, construyes el **esqueleto de un parser de intake** para un cliente sintético (nombres, apellido paterno, apellido materno, contacto, dirección). Conservas valores originales, normalizas con strip, validas al menos un campo numérico opcional (edad) con mensaje claro, y reportas con f-strings. Solo datos ficticios — sin PII real.",
        "objectives": [
          "Capturar o recibir nombres, apellido_paterno, apellido_materno, contacto y dirección",
          "Conservar el valor original (raw) de cada campo",
          "Normalizar de forma mínima (strip) sin perder raw",
          "Validar al menos un campo numérico opcional (edad) con mensaje accionable",
          "Cubrir casos: vacío, Unicode (p.ej. Ñahui), número inválido",
          "Imprimir resumen con f-strings"
        ],
        "requirements": [
          "Función parse_client(...) devuelve estructura con *_raw y campos limpios o None",
          "Campo vacío → error accionable; no borrar raw",
          "Unicode (p.ej. José Ñahui) round-trip en raw y clean",
          "Número inválido no lanza traceback no capturado; error listado",
          "Suite de tests (pytest o asserts) documentada y reproducible",
          "Sin PII real; datos sintéticos (example.com si hay email)",
          "main() + if __name__ == \"__main__\""
        ],
        "portfolioNote": "Este esqueleto demuestra tipos, conversión segura, nombres PEP 8, preservación de raw e I/O con f-strings. En entrevistas te pedirán extenderlo (más campos, Decimal para montos, CSV). Si el contrato raw/clean + errors está sólido, esas extensiones son naturales. Súbelo a python-ds-journey sin datos reales.",
        "starterCode": "\"\"\"parse_client_intake.py — incremento CP-N1-A (S02)\nDatos sintéticos únicamente. No uses información real de clientes.\nReferencia ejecutable del parser (raw/clean/errors + f-string resumen).\n\"\"\"\n\nfrom __future__ import annotations\n\n\ndef safe_int(campo: str, valor: str) -> tuple[bool, int | None, str | None]:\n    \"\"\"Convierte a int con strip. Devuelve (ok, valor|None, error|None).\"\"\"\n    try:\n        return True, int(valor.strip()), None\n    except ValueError:\n        return False, None, f\"ERROR en '{campo}': no se pudo convertir {valor!r} a int\"\n\n\ndef parse_client(\n    nombres: str,\n    apellido_paterno: str,\n    apellido_materno: str,\n    contacto: str,\n    direccion: str,\n    edad: str | None = None,\n) -> dict:\n    \"\"\"Parsea un registro de intake: *_raw, limpios, errors.\"\"\"\n    errors: list[str] = []\n    rec: dict = {\n        \"nombres_raw\": nombres,\n        \"apellido_paterno_raw\": apellido_paterno,\n        \"apellido_materno_raw\": apellido_materno,\n        \"contacto_raw\": contacto,\n        \"direccion_raw\": direccion,\n        \"edad_raw\": edad,\n        \"nombres\": None,\n        \"apellido_paterno\": None,\n        \"apellido_materno\": None,\n        \"contacto\": None,\n        \"direccion\": None,\n        \"edad\": None,\n        \"errors\": errors,\n    }\n\n    def clean_required(campo: str, valor: str) -> str | None:\n        if valor is None or str(valor).strip() == \"\":\n            errors.append(f\"ERROR en '{campo}': vacío (raw={valor!r})\")\n            return None\n        return str(valor).strip()\n\n    rec[\"nombres\"] = clean_required(\"nombres\", nombres)\n    rec[\"apellido_paterno\"] = clean_required(\"apellido_paterno\", apellido_paterno)\n    rec[\"apellido_materno\"] = clean_required(\"apellido_materno\", apellido_materno)\n    rec[\"contacto\"] = clean_required(\"contacto\", contacto)\n    rec[\"direccion\"] = clean_required(\"direccion\", direccion)\n\n    if edad is not None:\n        ok, n, err = safe_int(\"edad\", edad)\n        if ok:\n            rec[\"edad\"] = n\n        else:\n            errors.append(err)  # type: ignore[arg-type]\n    return rec\n\n\ndef mostrar_resumen(resultado: dict) -> None:\n    \"\"\"Imprime un resumen legible con f-strings.\"\"\"\n    print(\"=== Resumen intake (sintético) ===\")\n    print(f\"nombres: {resultado.get('nombres')!r} (raw={resultado.get('nombres_raw')!r})\")\n    print(\n        f\"apellidos: {resultado.get('apellido_paterno')!r} / \"\n        f\"{resultado.get('apellido_materno')!r}\"\n    )\n    print(f\"contacto: {resultado.get('contacto')!r}\")\n    print(f\"direccion: {resultado.get('direccion')!r}\")\n    print(f\"edad: {resultado.get('edad')!r} (raw={resultado.get('edad_raw')!r})\")\n    errs = resultado.get(\"errors\") or []\n    if errs:\n        print(f\"errors ({len(errs)}):\")\n        for e in errs:\n            print(f\"  - {e}\")\n    else:\n        print(\"errors: []\")\n\n\n\ndef _run_tests() -> None:\n    # Caso feliz + Unicode\n    r = parse_client(\n        \"María José\",\n        \"Quispe\",\n        \"Ñahui\",\n        \"999000111\",\n        \"Av. Ejemplo 123, Lima\",\n        edad=\"34\",\n    )\n    assert r[\"apellido_materno_raw\"] == \"Ñahui\"\n    assert \"Ñahui\" in (r.get(\"apellido_materno\") or \"\")\n    assert r[\"errors\"] == [] or isinstance(r[\"errors\"], list)\n\n    # Vacío\n    r2 = parse_client(\"\", \"Quispe\", \"Ñahui\", \"999\", \"Lima\")\n    assert any(\"nombres\" in e.lower() or \"vac\" in e.lower() for e in r2[\"errors\"])\n    assert r2[\"nombres_raw\"] == \"\"\n\n    # Número inválido\n    r3 = parse_client(\"Ana\", \"Ramos\", \"Díaz\", \"999\", \"Cusco\", edad=\"abc\")\n    assert r3[\"edad_raw\"] == \"abc\"\n    assert any(\"edad\" in e.lower() for e in r3[\"errors\"])\n\n    print(\"tests OK\")\n\n\ndef main() -> None:\n    _run_tests()\n    demo = parse_client(\n        \"  María José  \",\n        \"Quispe\",\n        \"Ñahui\",\n        \"999000111\",\n        \"Av. Ejemplo 123, Lima\",\n        edad=\" 28 \",\n    )\n    mostrar_resumen(demo)\n\n\nif __name__ == \"__main__\":\n    main()\n"
      },
      "selfCheck_stems": [
        {
          "question": "¿Cuál es el tipo de None en Python?",
          "options": [
            "null",
            "NoneType",
            "void",
            "str vacío"
          ]
        },
        {
          "question": "¿Qué imprime type(\"42\").__name__ y la comparación 42 == \"42\"?",
          "options": [
            "int y True",
            "str y True",
            "int y False",
            "str y False"
          ]
        },
        {
          "question": "¿Por qué el teléfono de un cliente de intake se modela como str?",
          "options": [
            "Porque no es una cantidad aritmética y puede necesitar ceros o formato",
            "Porque int no existe en Python",
            "Porque str es más rápido que int",
            "Porque PEP 8 lo prohíbe como int"
          ]
        },
        {
          "question": "Tras `b = a` con `a = [1, 2]` y `b.append(3)`, ¿qué vale `a`?",
          "options": [
            "[1, 2]",
            "[3]",
            "[1, 2, 3]",
            "Error"
          ]
        },
        {
          "question": "¿Cuál es el idioma correcto para comprobar ausencia de valor?",
          "options": [
            "if x == None:",
            "if x is None:",
            "if x === null:",
            "if not x == None:"
          ]
        }
      ],
      "taught_text_sha": "3d98fb6c190d9d11",
      "taught_text_len": 31291
    },
    {
      "file": "src/lib/course/sections/s03-data-structures.ts",
      "id": "data-structures",
      "index": 3,
      "title": "Decisiones y reglas de validación",
      "shortTitle": "Decisiones & Reglas",
      "tagline": "Booleanos, control de flujo y reglas accept/reject/review sin confundir ausencia con falsy",
      "jobRelevance": "En onboarding de data en bancos, fintech y retail en Perú, el parser de intake no basta con convertir tipos (S02): hay que **decidir** si cada campo se acepta, se rechaza o va a revisión. Si tratas `None` como si fuera `0`, o usas `if monto:` y rechazas un cero válido, generas falsos positivos caros. Esta sección construye el **motor de reglas** del capstone CP-N1-A: comparaciones, truthiness, if/elif/else, guards, allowlists, decision tables y pruebas de ramas con mensajes accionables.",
      "learningOutcomes": [
        "Comparar valores y probar pertenencia con ==, !=, <, >, in/not in de forma predecible",
        "Distinguir truthiness de presencia semántica y predecir short-circuit de and/or",
        "Escribir if/elif/else que clasifiquen un campo en una sola rama dominante",
        "Aplicar guard clauses y detectar ramas inalcanzables por orden de condiciones",
        "Implementar rangos y allowlists combinados para reglas de dominio sintéticas",
        "Leer una decision table y expresar reglas claras con if o match según el caso",
        "Enunciar invariantes de campo con ejemplos accept/reject/review",
        "Redactar mensajes accionables y cubrir cada rama con un caso de prueba"
      ],
      "theory": [
        {
          "subtopicId": "S03-T1-A",
          "heading": "Comparaciones y el operador in",
          "paragraphs": [
            "Un **booleano de negocio** nace de una comparación: `==`, `!=`, `<`, `<=`, `>`, `>=`. En intake, comparas edades, montos, códigos y regiones. Python también permite **encadenar**: `18 <= edad <= 65` equivale a `(18 <= edad) and (edad <= 65)` y se evalúa de forma segura (la expresión del medio se calcula una sola vez en la cadena).",
            "**Pertenencia**: `x in coleccion` / `x not in coleccion` funciona con str, list, set, dict (busca **claves**). Para allowlists de códigos fijos, un **`set` de literales** es ideal: lectura clara y chequeo O(1). Atención a **mayúsculas**: `\"dni\" in {\"DNI\"}` es `False` — normaliza antes o documenta el contrato.",
            "**`is` vs `==`**: usa **`is None` / `is not None`** para ausencia. No uses `is` para comparar números o strings de negocio (`True is 1` es `False` aunque `True == 1` sea `True`). `==` pregunta “¿mismo valor?”; `is` pregunta “¿mismo objeto?”."
          ],
          "code": "region = \"Lima\"\nmonto = 1500\nALLOWED = {\"Lima\", \"Arequipa\", \"Cusco\"}\n\nprint(region == \"Lima\")          # True\nprint(monto >= 1000)             # True\nprint(region in ALLOWED)         # True\nprint(\"Piura\" not in ALLOWED)    # True\nprint(1000 <= monto <= 2000)     # True (encadenado)\n\n# is None para ausencia — no uses == por hábito en validadores\nvalor = None\nprint(valor is None)             # True",
          "code_output": "True\nTrue\nTrue\nTrue\nTrue\nTrue"
        },
        {
          "subtopicId": "S03-T1-B",
          "heading": "Qué es verdadero en un if (y qué no es “ausente”)",
          "paragraphs": [
            "Python evalúa la **truthiness** de un valor en `if`, `while`, `and` y `or`. Son **falsy** (por defecto): `None`, `False`, `0`, `0.0`, `0j`, `\"\"`, `()`, `[]`, `{}`, `set()`, `range(0)`. Casi todo lo demás es **truthy**, incluso `[0]` o `\"False\"`.",
            "El error canónico del gate V3: **`if monto:` trata `0` como “no hay monto”**. En negocio, **cero puede ser válido** y **`None` significa ausente**. Separa políticas: presencia con `is None`, rango con comparaciones numéricas, vacío de texto con `== \"\"` o `not s.strip()` según el contrato.",
            "`and` / `or` hacen **short-circuit** y **devuelven un operando** (no siempre `True`/`False`). `\"\" or \"default\"` → `\"default\"`; `0 and 99` → `0`. `not` sí devuelve booleano. Prioridad: `not` se une más fuerte que `and`, y `and` más que `or`."
          ],
          "code": null,
          "code_output": null
        },
        {
          "subtopicId": "S03-T2-A",
          "heading": "Ramas de decisión con if/elif/else",
          "paragraphs": [
            "La forma canónica de una decisión exclusiva es **`if` / `elif` / `else`**. Se evalúan en orden; **la primera condición verdadera gana** y el resto no se ejecuta. El `else` es el catch-all (útil para `reject` o `review` por defecto).",
            "**Indentación** define el bloque: 4 espacios es el estilo del curso. Un `if` seguido de otro `if` (sin `elif`) **no es excluyente**: ambos pueden dispararse y **sobrescribir** el status. Eso es un bug clásico al clasificar scores.",
            "Para el motor de reglas, un patrón limpio es devolver un **solo status** por campo: `accept`, `review` o `reject`. Fronteras (`score >= 80`) deben estar documentadas en la tabla de ejemplos."
          ],
          "code": "def classify_score(score: int) -> str:\n    if score >= 80:\n        return \"accept\"\n    elif score >= 50:\n        return \"review\"\n    else:\n        return \"reject\"\n\nfor s in [95, 60, 30, 80, 50]:\n    print(s, \"→\", classify_score(s))",
          "code_output": "95 → accept\n60 → review\n30 → reject\n80 → accept\n50 → review"
        },
        {
          "subtopicId": "S03-T2-B",
          "heading": "Salidas tempranas y ramas que nunca se tocan",
          "paragraphs": [
            "Una **guard clause** (salida temprana) valida precondiciones y **retorna de inmediato** con `reject`/`review`, dejando el camino feliz al final sin pirámide de `if` anidados. Mejora legibilidad y reduce bugs de indentación.",
            "Orden típico en validadores: **1) ausencia (`is None`)** → **2) tipo** → **3) rango/allowlist** → **4) accept**. Si comparas `edad < 18` antes de chequear `None`, obtienes `TypeError`.",
            "Una **rama muerta** es código que nunca se ejecuta porque una condición anterior ya la cubre (p. ej. `if x >= 0: ... elif x > 5:` — el `elif` solo vería negativos, nunca `x > 5`). Aprende a leer el orden como un revisor de PRs."
          ],
          "code": "def validate_edad(edad):\n    if edad is None:\n        return {\"status\": \"review\", \"code\": \"MISSING\"}\n    if not isinstance(edad, int):\n        return {\"status\": \"reject\", \"code\": \"BAD_TYPE\"}\n    if edad < 0 or edad > 120:\n        return {\"status\": \"reject\", \"code\": \"OUT_OF_RANGE\"}\n    if edad < 18:\n        return {\"status\": \"review\", \"code\": \"NEEDS_REVIEW\"}\n    return {\"status\": \"accept\", \"code\": \"OK\"}\n\nfor e in [None, \"25\", -1, 15, 30]:\n    print(e, \"→\", validate_edad(e))",
          "code_output": "None → {'status': 'review', 'code': 'MISSING'}\n25 → {'status': 'reject', 'code': 'BAD_TYPE'}\n-1 → {'status': 'reject', 'code': 'OUT_OF_RANGE'}\n15 → {'status': 'review', 'code': 'NEEDS_REVIEW'}\n30 → {'status': 'accept', 'code': 'OK'}"
        },
        {
          "subtopicId": "S03-T3-A",
          "heading": "Reglas de dominio: rangos y listas permitidas",
          "paragraphs": [
            "Una **allowlist** es el conjunto de valores admitidos (`ALLOWED_REGIONES = {\"Lima\", \"Arequipa\", ...}`). Si el valor no está, suele ir a **`review`** (dato desconocido) o **`reject`** (política estricta). Nombra constantes en **`UPPER_CASE`**.",
            "Un **rango** usa comparaciones o encadenamiento: `MIN_EDAD <= edad <= MAX_EDAD`. Combina reglas con **`and`/`or`** de forma explícita; documenta si el fallo de allowlist es distinto del fallo de rango (códigos `NOT_IN_ALLOWLIST` vs `OUT_OF_RANGE`).",
            "Tri-estado en dominio: **accept** (cumple), **reject** (viola hard), **review** (ausente, desconocido u outlier suave). El cero en montos suele ser accept si el invariante lo permite."
          ],
          "code": "ALLOWED_REG = {\"Lima\", \"Arequipa\", \"Cusco\", \"Piura\"}\n\ndef rule_region_edad(region, edad):\n    if region is None or edad is None:\n        return \"review\"\n    if region not in ALLOWED_REG:\n        return \"review\"\n    if not (18 <= edad <= 65):\n        return \"reject\"\n    return \"accept\"\n\nfor r, e in [(\"Lima\", 30), (\"Tacna\", 30), (\"Lima\", 15), (None, 40)]:\n    print(r, e, \"→\", rule_region_edad(r, e))",
          "code_output": "Lima 30 → accept\nTacna 30 → review\nLima 15 → reject\nNone 40 → review"
        },
        {
          "subtopicId": "S03-T3-B",
          "heading": "Tablas de decisión y match/case",
          "paragraphs": [
            "Una **decision table** es una tabla de negocio: filas de condiciones → acción. Primero la escribes en español (o en un dict de ejemplos); después la implementas. Evita inventar ramas en el código que no estén en la tabla.",
            "**`match` / `case`** (Python 3.10+) brilla cuando el sujeto es un **literal o estado finito** (`\"OK\"`, `\"MISSING\"`, códigos de error). Soporta **OR patterns** (`case \"A\" | \"B\":`) y el comodín **`case _:`** (debe ser explícito para defaults). El primer case que matchea gana.",
            "**Cuándo preferir `if`**: rangos numéricos, combinaciones de varios campos, o condiciones que no son patrones de estructura. `match` no depreca `if`; elige por **claridad**."
          ],
          "code": "def status_match(code: str) -> str:\n    match code:\n        case \"OK\":\n            return \"accept\"\n        case \"MISSING\" | \"NEEDS_REVIEW\":\n            return \"review\"\n        case \"OUT_OF_RANGE\" | \"NOT_IN_ALLOWLIST\" | \"BAD_TYPE\":\n            return \"reject\"\n        case _:\n            return \"review\"\n\nfor c in [\"OK\", \"MISSING\", \"OUT_OF_RANGE\", \"FOO\"]:\n    print(c, \"→\", status_match(c))",
          "code_output": "OK → accept\nMISSING → review\nOUT_OF_RANGE → reject\nFOO → review"
        },
        {
          "subtopicId": "S03-T4-A",
          "heading": "Invariantes: promesas que el dato debe cumplir",
          "paragraphs": [
            "Un **invariante** de campo es una promesa en español: “`contacto` es un str de 9 dígitos, o `None` si aún no se capturó”. No es código todavía: es **especificación**. Los **ejemplos canónicos** (accept/reject/review/missing) son la forma más barata de validar que el invariante es usable.",
            "Mínimo profesional: **al menos un ejemplo por estado de decisión** que tu regla produce. Si solo pruebas el camino feliz, el validador miente en producción.",
            "`assert` sirve en desarrollo y tests, pero **no** como única validación de intake en producción (`python -O` desactiva asserts). Usa returns con `status`/`code`/`message` para reglas de negocio."
          ],
          "code": "def validate_contacto(c):\n    if c is None:\n        return \"review\"\n    if not isinstance(c, str) or not c.strip():\n        return \"reject\"\n    digits = c.strip()\n    if not digits.isdigit() or len(digits) != 9:\n        return \"reject\"\n    return \"accept\"\n\nregla = {\n    \"field\": \"contacto\",\n    \"invariant_text\": \"contacto es str de 9 dígitos o None (review)\",\n    \"examples\": [\n        {\"value\": \"999000111\", \"expected\": \"accept\"},\n        {\"value\": \"12345\", \"expected\": \"reject\"},\n        {\"value\": None, \"expected\": \"review\"},\n        {\"value\": \"  \", \"expected\": \"reject\"},\n    ],\n}\nfor ex in regla[\"examples\"]:\n    got = validate_contacto(ex[\"value\"])\n    print(ex[\"value\"], \"→\", got, \"ok=\", got == ex[\"expected\"])",
          "code_output": "999000111 → accept ok= True\n12345 → reject ok= True\nNone → review ok= True\n   → reject ok= True"
        },
        {
          "subtopicId": "S03-T4-B",
          "heading": "Mensajes que se pueden ejecutar y pruebas por rama",
          "paragraphs": [
            "Un mensaje accionable nombra el **campo**, el **problema** y la **acción esperada**: `Campo 'edad'=-5 fuera de rango; usa 0–120.` Evita mensajes vagos como Error o inválido. Códigos estables (`MISSING`, `OUT_OF_RANGE`, `NOT_IN_ALLOWLIST`, `NEEDS_REVIEW`, `OK`) permiten métricas y i18n después.",
            "**Un test por rama** del validador: si tienes 4 caminos (None, tipo mal, rango, OK), necesitas ≥4 casos. El else/default también cuenta.",
            "No loguees secretos ni PII real. En el curso solo datos sintéticos. El ciclo **test rojo → arreglar regla → verde** es la forma de depurar off-by-one en fronteras (`>= 18` vs `> 18`)."
          ],
          "code": "def validate_edad_msg(edad):\n    if edad is None:\n        return {\n            \"status\": \"review\",\n            \"code\": \"MISSING\",\n            \"message\": \"Campo 'edad' ausente: envía un entero 0–120 o marca como desconocido.\",\n        }\n    if not isinstance(edad, int):\n        return {\n            \"status\": \"reject\",\n            \"code\": \"BAD_TYPE\",\n            \"message\": f\"Campo 'edad' recibió {edad!r}; se espera int, no {type(edad).__name__}.\",\n        }\n    if edad < 0 or edad > 120:\n        return {\n            \"status\": \"reject\",\n            \"code\": \"OUT_OF_RANGE\",\n            \"message\": f\"Campo 'edad'={edad} fuera de rango; usa 0–120.\",\n        }\n    return {\"status\": \"accept\", \"code\": \"OK\", \"message\": \"edad OK\"}\n\ntests = [(None, \"MISSING\"), (\"x\", \"BAD_TYPE\"), (-5, \"OUT_OF_RANGE\"), (35, \"OK\")]\nfor val, code in tests:\n    r = validate_edad_msg(val)\n    assert r[\"code\"] == code\n    print(\"PASS\", val, r[\"code\"])",
          "code_output": "PASS None MISSING\nPASS x BAD_TYPE\nPASS -5 OUT_OF_RANGE\nPASS 35 OK"
        }
      ],
      "iDo": {
        "steps": [
          {
            "demoId": "S03-T1-A-DEMO",
            "subtopicId": "S03-T1-A",
            "environment": "browser-pyodide",
            "description": "Comparar región y monto de un registro sintético",
            "why": "Antes de escribir ifs de negocio, el analista predice booleanos sueltos. Cuatro comparaciones + dos membership checks fijan el vocabulario del motor de reglas.",
            "code": "region = \"Lima\"\nmonto = 1500\nALLOWED = {\"Lima\", \"Arequipa\", \"Cusco\"}\n\nprint(\"region == 'Lima' →\", region == \"Lima\")\nprint(\"region != 'Piura' →\", region != \"Piura\")\nprint(\"monto >= 1000 →\", monto >= 1000)\nprint(\"monto < 500 →\", monto < 500)\nprint(\"region in ALLOWED →\", region in ALLOWED)\nprint(\"'Piura' not in ALLOWED →\", \"Piura\" not in ALLOWED)\nprint(\"1000 <= monto <= 2000 →\", 1000 <= monto <= 2000)",
            "language": "python",
            "output": "region == 'Lima' → True\nregion != 'Piura' → True\nmonto >= 1000 → True\nmonto < 500 → False\nregion in ALLOWED → True\n'Piura' not in ALLOWED → True\n1000 <= monto <= 2000 → True"
          },
          {
            "demoId": "S03-T1-B-DEMO",
            "subtopicId": "S03-T1-B",
            "environment": "browser-pyodide",
            "description": "Tres campos: None, 0 y vacío bajo reglas distintas",
            "why": "None, 0 y \"\" son todos falsy, pero la política de monto solo trata None como ausente y acepta cero. Este es el gate crítico del tri-estado.",
            "code": "def decide_monto(m):\n    if m is None:\n        return \"review: ausente\"\n    if m == 0:\n        return \"accept: cero válido\"\n    if m < 0:\n        return \"reject: negativo\"\n    return \"accept: positivo\"\n\ncampos = {\"monto_none\": None, \"monto_cero\": 0, \"nota\": \"\", \"monto_ok\": 150}\nfor k, v in campos.items():\n    print(f\"{k}: valor={v!r} bool={bool(v)}\")\n\nfor v in [None, 0, -5, 150]:\n    print(\"policy\", v, \"→\", decide_monto(v))",
            "language": "python",
            "output": "monto_none: valor=None bool=False\nmonto_cero: valor=0 bool=False\nnota: valor='' bool=False\nmonto_ok: valor=150 bool=True\npolicy None → review: ausente\npolicy 0 → accept: cero válido\npolicy -5 → reject: negativo\npolicy 150 → accept: positivo"
          },
          {
            "demoId": "S03-T2-A-DEMO",
            "subtopicId": "S03-T2-A",
            "environment": "browser-pyodide",
            "description": "Clasificar score de calidad en accept/review/reject",
            "why": "Tres scores, tres ramas. if/elif/else garantiza una sola etiqueta por registro — base del clasificador de calidad de intake.",
            "code": "def classify_score(score: int) -> str:\n    if score >= 80:\n        return \"accept\"\n    elif score >= 50:\n        return \"review\"\n    else:\n        return \"reject\"\n\nfor s in [95, 60, 30]:\n    print(s, \"→\", classify_score(s))",
            "language": "python",
            "output": "95 → accept\n60 → review\n30 → reject"
          },
          {
            "demoId": "S03-T2-B-DEMO",
            "subtopicId": "S03-T2-B",
            "environment": "browser-pyodide",
            "description": "De pirámide a guards en validate_edad",
            "why": "Misma matriz de casos, código lineal. Guards de None y tipo evitan TypeError y dejan el accept al final.",
            "code": "def validate_edad(edad):\n    if edad is None:\n        return {\"status\": \"review\", \"code\": \"MISSING\"}\n    if not isinstance(edad, int):\n        return {\"status\": \"reject\", \"code\": \"BAD_TYPE\"}\n    if edad < 0 or edad > 120:\n        return {\"status\": \"reject\", \"code\": \"OUT_OF_RANGE\"}\n    if edad < 18:\n        return {\"status\": \"review\", \"code\": \"NEEDS_REVIEW\"}\n    return {\"status\": \"accept\", \"code\": \"OK\"}\n\nfor e in [None, \"25\", -1, 15, 30, 200]:\n    print(e, \"→\", validate_edad(e))",
            "language": "python",
            "output": "None → {'status': 'review', 'code': 'MISSING'}\n25 → {'status': 'reject', 'code': 'BAD_TYPE'}\n-1 → {'status': 'reject', 'code': 'OUT_OF_RANGE'}\n15 → {'status': 'review', 'code': 'NEEDS_REVIEW'}\n30 → {'status': 'accept', 'code': 'OK'}\n200 → {'status': 'reject', 'code': 'OUT_OF_RANGE'}"
          },
          {
            "demoId": "S03-T3-A-DEMO",
            "subtopicId": "S03-T3-A",
            "environment": "browser-pyodide",
            "description": "Regla combinada región + edad",
            "why": "Allowlist + rango en una sola función. Región desconocida → review; edad fuera de banda → reject; ausencia → review.",
            "code": "ALLOWED_REG = {\"Lima\", \"Arequipa\", \"Cusco\", \"Piura\"}\n\ndef rule_region_edad(region, edad):\n    if region is None or edad is None:\n        return \"review\"\n    if region not in ALLOWED_REG:\n        return \"review\"\n    if not (18 <= edad <= 65):\n        return \"reject\"\n    return \"accept\"\n\nfor r, e in [(\"Lima\", 30), (\"Tacna\", 30), (\"Lima\", 15), (None, 40)]:\n    print(r, e, \"→\", rule_region_edad(r, e))",
            "language": "python",
            "output": "Lima 30 → accept\nTacna 30 → review\nLima 15 → reject\nNone 40 → review"
          },
          {
            "demoId": "S03-T3-B-DEMO",
            "subtopicId": "S03-T3-B",
            "environment": "browser-pyodide",
            "description": "Misma tabla en if/elif y en match",
            "why": "Ambas implementaciones coinciden; case _ cubre desconocidos. Elige match cuando el sujeto es un código finito.",
            "code": "def status_if(code: str) -> str:\n    if code == \"OK\":\n        return \"accept\"\n    elif code in (\"MISSING\", \"NEEDS_REVIEW\"):\n        return \"review\"\n    elif code in (\"OUT_OF_RANGE\", \"NOT_IN_ALLOWLIST\", \"BAD_TYPE\"):\n        return \"reject\"\n    else:\n        return \"review\"\n\ndef status_match(code: str) -> str:\n    match code:\n        case \"OK\":\n            return \"accept\"\n        case \"MISSING\" | \"NEEDS_REVIEW\":\n            return \"review\"\n        case \"OUT_OF_RANGE\" | \"NOT_IN_ALLOWLIST\" | \"BAD_TYPE\":\n            return \"reject\"\n        case _:\n            return \"review\"\n\nfor c in [\"OK\", \"MISSING\", \"OUT_OF_RANGE\", \"FOO\", \"NEEDS_REVIEW\"]:\n    a, b = status_if(c), status_match(c)\n    print(c, a, b, \"same=\", a == b)",
            "language": "python",
            "output": "OK accept accept same= True\nMISSING review review same= True\nOUT_OF_RANGE reject reject same= True\nFOO review review same= True\nNEEDS_REVIEW review review same= True"
          },
          {
            "demoId": "S03-T4-A-DEMO",
            "subtopicId": "S03-T4-A",
            "environment": "browser-pyodide",
            "description": "Invariante de campo contacto + 4 ejemplos",
            "why": "invariant_text + examples[] ejecutables: la especificación y la prueba viven juntas.",
            "code": "def validate_contacto(c):\n    if c is None:\n        return \"review\"\n    if not isinstance(c, str) or not c.strip():\n        return \"reject\"\n    digits = c.strip()\n    if not digits.isdigit() or len(digits) != 9:\n        return \"reject\"\n    return \"accept\"\n\nregla = {\n    \"field\": \"contacto\",\n    \"invariant_text\": \"contacto es str de 9 dígitos o None (review)\",\n    \"examples\": [\n        {\"value\": \"999000111\", \"expected\": \"accept\"},\n        {\"value\": \"12345\", \"expected\": \"reject\"},\n        {\"value\": None, \"expected\": \"review\"},\n        {\"value\": \"  \", \"expected\": \"reject\"},\n    ],\n}\nprint(regla[\"invariant_text\"])\nfor ex in regla[\"examples\"]:\n    got = validate_contacto(ex[\"value\"])\n    print(ex[\"value\"], \"→\", got, \"ok=\", got == ex[\"expected\"])",
            "language": "python",
            "output": "contacto es str de 9 dígitos o None (review)\n999000111 → accept ok= True\n12345 → reject ok= True\nNone → review ok= True\n   → reject ok= True"
          },
          {
            "demoId": "S03-T4-B-DEMO",
            "subtopicId": "S03-T4-B",
            "environment": "browser-pyodide",
            "description": "Suite mínima de pruebas del motor de reglas",
            "why": "Loop de casos; todos pass; el mensaje de BAD_TYPE muestra campo + valor recibido + tipo esperado.",
            "code": "def validate_edad_msg(edad):\n    if edad is None:\n        return {\n            \"status\": \"review\",\n            \"code\": \"MISSING\",\n            \"message\": \"Campo 'edad' ausente: envía un entero 0–120 o marca como desconocido.\",\n        }\n    if not isinstance(edad, int):\n        return {\n            \"status\": \"reject\",\n            \"code\": \"BAD_TYPE\",\n            \"message\": f\"Campo 'edad' recibió {edad!r}; se espera int, no {type(edad).__name__}.\",\n        }\n    if edad < 0 or edad > 120:\n        return {\n            \"status\": \"reject\",\n            \"code\": \"OUT_OF_RANGE\",\n            \"message\": f\"Campo 'edad'={edad} fuera de rango; usa 0–120.\",\n        }\n    return {\"status\": \"accept\", \"code\": \"OK\", \"message\": \"edad OK\"}\n\ntests = [(None, \"MISSING\"), (\"x\", \"BAD_TYPE\"), (-5, \"OUT_OF_RANGE\"), (35, \"OK\")]\nfor val, code in tests:\n    r = validate_edad_msg(val)\n    assert r[\"code\"] == code\n    print(\"PASS\", val, r[\"code\"])\n    print(\" \", r[\"message\"])",
            "language": "python",
            "output": "PASS None MISSING\n  Campo 'edad' ausente: envía un entero 0–120 o marca como desconocido.\nPASS x BAD_TYPE\n  Campo 'edad' recibió 'x'; se espera int, no str.\nPASS -5 OUT_OF_RANGE\n  Campo 'edad'=-5 fuera de rango; usa 0–120.\nPASS 35 OK\n  edad OK"
          }
        ]
      },
      "weDo": {
        "exercises": [
          {
            "id": "S03-T1-A-E1",
            "instruction": "E1 (guiado) — Con `edad = 25` y `region = \"Cusco\"`, imprime el booleano de: `edad >= 18`, `edad < 65`, `18 <= edad <= 65`, `region == \"Lima\"`, `region != \"Piura\"`.",
            "hints": [
              "Usa print(expresion) directamente; no hace falta if todavía.",
              "El encadenamiento 18 <= edad <= 65 es True para 25. region == \"Lima\" es False."
            ],
            "kind": "guided",
            "tests": "assert expected bools: True, True, True, False, True",
            "starterCode": "edad = 25\nregion = \"Cusco\"\n\nprint(____)  # edad >= 18\nprint(____)  # edad < 65\nprint(____)  # 18 <= edad <= 65\nprint(____)  # region == \"Lima\"\nprint(____)  # region != \"Piura\""
          },
          {
            "id": "S03-T1-A-E2",
            "instruction": "E2 (independiente) — Define `TIPOS_DOC = {\"DNI\", \"CE\", \"PAS\"}`. Para cada valor en `[\"DNI\", \"dni\", \"RUC\"]`, imprime si está en la allowlist (`in`).",
            "hints": [
              "for t in lista: print(t, \"→\", t in TIPOS_DOC)",
              "Case sensitivity: \"dni\" ≠ \"DNI\". RUC no está en el set."
            ],
            "kind": "independent",
            "tests": "3 inputs → True, False, False",
            "starterCode": "TIPOS_DOC = {\"DNI\", \"CE\", \"PAS\"}\nfor t in [\"DNI\", \"dni\", \"RUC\"]:\n    # TODO: imprime pertenencia\n    print(t, \"→\", ____)"
          },
          {
            "id": "S03-T1-A-E3",
            "instruction": "E3 (transferencia) — Diagnóstico: imprime `valor is None` con `valor = None`, luego `True == 1` y `True is 1`. Explica en un print cuándo usar `is` vs `==` en validadores de intake.",
            "hints": [
              "is None para ausencia; == para valores de negocio. No uses is con enteros.",
              "True == 1 es True (bool subtipo int) pero True is 1 es False: identidad ≠ igualdad."
            ],
            "kind": "transfer",
            "tests": "rubric + fixed snippet: True, True, False + nota",
            "starterCode": "valor = None\nprint(\"valor is None →\", ____)\nprint(\"True == 1 →\", ____)\nprint(\"True is 1 →\", ____)\nprint(\"Nota:\", \"____\")"
          },
          {
            "id": "S03-T1-B-E1",
            "instruction": "E1 (guiado) — Tabla truthiness: imprime `bool(v)` para `None`, `False`, `0`, `0.0`, `\"\"`, `[]`, `{}`, `set()`, `range(0)`, `\"x\"`, `1`, `[0]`.",
            "hints": [
              "for v in lista: print(repr(v), \"→\", bool(v))",
              "range(0) es falsy; [0] es truthy (lista no vacía)."
            ],
            "kind": "guided",
            "tests": "checklist: 9 falsy + 3 truthy en el orden dado",
            "starterCode": "vals = [None, False, 0, 0.0, \"\", [], {}, set(), range(0), \"x\", 1, [0]]\nfor v in vals:\n    print(repr(v), \"→\", ____)"
          },
          {
            "id": "S03-T1-B-E2",
            "instruction": "E2 (independiente) — Predice e imprime: `'' or 'default'`, `'Lima' or 'default'`, `0 and 99`, `5 and 99`, `None or 0`.",
            "hints": [
              "and/or devuelven operando, no necesariamente bool. Short-circuit: or se detiene en el primero truthy.",
              "'' or 'default' → 'default'; 0 and 99 → 0; None or 0 → 0."
            ],
            "kind": "independent",
            "tests": "assert results: default, Lima, 0, 99, 0",
            "starterCode": "print(\"'' or 'default' →\", ____)\nprint(\"'Lima' or 'default' →\", ____)\nprint(\"0 and 99 →\", ____)\nprint(\"5 and 99 →\", ____)\nprint(\"None or 0 →\", ____)"
          },
          {
            "id": "S03-T1-B-E3",
            "instruction": "E3 (transferencia) — Bug: un validador hace `if not monto: return \"reject\"`. Reescribe `validate_monto(m)` con `is None` para review, `< 0` reject, y accept para 0 y positivos. Prueba `None`, `0`, `-1`, `100`.",
            "hints": [
              "Nunca uses truthiness para montos. if m is None primero.",
              "0 debe devolver accept; None → review; negativo → reject."
            ],
            "kind": "transfer",
            "tests": "cases accept/review: None review, 0 accept, -1 reject, 100 accept",
            "starterCode": "def validate_monto(m):\n    # BUG legado: if not m: return \"reject\"\n    # TODO: is None → review; < 0 → reject; else accept\n    pass\n\nfor m in [None, 0, -1, 100]:\n    print(m, \"→\", validate_monto(m))"
          },
          {
            "id": "S03-T2-A-E1",
            "instruction": "E1 (guiado) — Completa `classify_score`: ≥80 accept, ≥50 review, else reject. Imprime resultados para 80, 50, 49, 100.",
            "hints": [
              "if score >= 80: ... elif score >= 50: ... else: ...",
              "Fronteras: 80 es accept; 50 es review; 49 es reject."
            ],
            "kind": "guided",
            "tests": "assert status: accept, review, reject, accept",
            "starterCode": "def classify_score(score: int) -> str:\n    # TODO: if/elif/else\n    pass\n\nfor s in [80, 50, 49, 100]:\n    print(s, \"→\", classify_score(s))"
          },
          {
            "id": "S03-T2-A-E2",
            "instruction": "E2 (independiente) — La función `bad` usa ifs independientes y clasifica mal el 95 (queda review). Reescribe `good` con if/elif/else y compara ambos en 95, 60, 30.",
            "hints": [
              "El segundo if score >= 50 pisa el accept. Usa elif para exclusión mutua.",
              "good(95) debe ser accept; bad(95) es review."
            ],
            "kind": "independent",
            "tests": "single status key; good: accept/review/reject",
            "starterCode": "def bad(score):\n    status = None\n    if score >= 80:\n        status = \"accept\"\n    if score >= 50:\n        status = \"review\"\n    if score < 50:\n        status = \"reject\"\n    return status\n\ndef good(score):\n    # TODO: if/elif/else\n    pass\n\nfor s in [95, 60, 30]:\n    print(s, \"bad=\", bad(s), \"good=\", good(s))"
          },
          {
            "id": "S03-T2-A-E3",
            "instruction": "E3 (transferencia) — Traza mentalmente y luego implementa `band(n)`: `>100` alto, `>50` medio, `>0` bajo, else nulo. Verifica 150, 75, 10, 0, -3.",
            "hints": [
              "Orden: primero el umbral más alto. else cubre 0 y negativos.",
              "0 no es > 0 → nulo. 75 no es >100 pero sí >50 → medio."
            ],
            "kind": "transfer",
            "tests": "table match: alto, medio, bajo, nulo, nulo",
            "starterCode": "def band(n):\n    # TODO\n    pass\n\nfor n in [150, 75, 10, 0, -3]:\n    print(n, \"→\", band(n))"
          },
          {
            "id": "S03-T2-B-E1",
            "instruction": "E1 (guiado) — Completa guards en `validate_edad`: None → MISSING/review; no int → BAD_TYPE/reject; luego OUT_OF_RANGE / NEEDS_REVIEW / OK como en la demo. Prueba None, \"25\", 15, 30.",
            "hints": [
              "if edad is None primero; luego isinstance; no compares None con <.",
              "return dicts con status y code; el camino feliz es el último return."
            ],
            "kind": "guided",
            "tests": "no TypeError; codes MISSING, BAD_TYPE, NEEDS_REVIEW, OK",
            "starterCode": "def validate_edad(edad):\n    # TODO: guards\n    pass\n\nfor e in [None, \"25\", 15, 30]:\n    print(e, \"→\", validate_edad(e))"
          },
          {
            "id": "S03-T2-B-E2",
            "instruction": "E2 (independiente) — Refactoriza la pirámide anidada de `validate_monto_nested` a guards lineales con la misma semántica: None review, no int reject, <0 reject, 0–10000 accept, else review (outlier).",
            "hints": [
              "Invierte el anidamiento: un if + return por precondición.",
              "Compara salidas con la función nested en los mismos casos de prueba."
            ],
            "kind": "independent",
            "tests": "same outputs que nested en [None, \"x\", -1, 0, 500, 20000]",
            "starterCode": "def validate_monto_nested(m):\n    if m is not None:\n        if isinstance(m, int):\n            if m >= 0:\n                if m <= 10000:\n                    return \"accept\"\n                else:\n                    return \"review\"\n            else:\n                return \"reject\"\n        else:\n            return \"reject\"\n    else:\n        return \"review\"\n\ndef validate_monto_guards(m):\n    # TODO: misma semántica, sin pirámide\n    pass\n\nfor v in [None, \"x\", -1, 0, 500, 20000]:\n    a, b = validate_monto_nested(v), validate_monto_guards(v)\n    print(v, a, b, \"ok=\", a == b)"
          },
          {
            "id": "S03-T2-B-E3",
            "instruction": "E3 (transferencia) — En el snippet, identifica la rama muerta: imprime cuál `elif` nunca corre y por qué. Luego reordena/corrige para que `x == 0` sea alcanzable y muestre `cero`.",
            "hints": [
              "Mira el orden: cualquier condición ya cubierta por un if anterior es inalcanzable.",
              "Corrige: if x > 0 / elif x < 0 / else (cero), o chequea x == 0 primero."
            ],
            "kind": "transfer",
            "tests": "identify dead branch; after fix x=0 → cero",
            "starterCode": "def etiqueta_bug(x):\n    if x != 0:\n        if x > 0:\n            return \"positivo\"\n        return \"negativo\"\n    elif x == 0:  # ¿se alcanza?\n        return \"cero\"\n    return \"???\"\n\nprint(\"bug 0 →\", etiqueta_bug(0))\nprint(\"bug 3 →\", etiqueta_bug(3))\n\ndef etiqueta_ok(x):\n    # TODO: tres ramas vivas: positivo, negativo, cero\n    pass\n\nprint(\"ok 0 →\", etiqueta_ok(0))\nprint(\"ok 3 →\", etiqueta_ok(3))\nprint(\"ok -2 →\", etiqueta_ok(-2))"
          },
          {
            "id": "S03-T3-A-E1",
            "instruction": "E1 (guiado) — `ALLOWED = {\"Lima\", \"Arequipa\", \"Cusco\", \"Piura\"}`. Escribe `check_region(r)`: None → review; not in ALLOWED → review; else accept. Prueba Lima, Tacna, None.",
            "hints": [
              "if r is None / if r not in ALLOWED / return accept",
              "Región desconocida → review (no reject) en esta política sintética."
            ],
            "kind": "guided",
            "tests": "assert Lima accept, Tacna review, None review",
            "starterCode": "ALLOWED = {\"Lima\", \"Arequipa\", \"Cusco\", \"Piura\"}\n\ndef check_region(r):\n    # TODO\n    pass\n\nfor r in [\"Lima\", \"Tacna\", None]:\n    print(r, \"→\", check_region(r))"
          },
          {
            "id": "S03-T3-A-E2",
            "instruction": "E2 (independiente) — `monto_ingreso(m)`: None → review; <0 → reject; >50000 → review (outlier); else accept (incluye 0). Casos: None, -1, 0, 1200, 60000.",
            "hints": [
              "Orden: ausencia, hard reject, outlier review, accept.",
              "0 es accept; 60000 es review, no reject."
            ],
            "kind": "independent",
            "tests": "table: review, reject, accept, accept, review",
            "starterCode": "def monto_ingreso(m):\n    # TODO\n    pass\n\nfor m in [None, -1, 0, 1200, 60000]:\n    print(m, \"→\", monto_ingreso(m))"
          },
          {
            "id": "S03-T3-A-E3",
            "instruction": "E3 (transferencia) — Combina allowlist de tipo_doc `{\"DNI\",\"CE\",\"PAS\"}` con longitudes DNI=8, CE=9, PAS=9. Devuelve dict status/code: MISSING, NOT_IN_ALLOWLIST, OUT_OF_RANGE, OK. Casos: DNI+8 dígitos, DNI corto, RUC, None.",
            "hints": [
              "Primero None, luego not in allowlist, luego len != esperado.",
              "DOC_LEN = {\"DNI\": 8, \"CE\": 9, \"PAS\": 9}; usa len(str(numero))."
            ],
            "kind": "transfer",
            "tests": "codes MISSING/OUT_OF_RANGE/NOT_IN_ALLOWLIST/OK",
            "starterCode": "ALLOWED_DOC = {\"DNI\", \"CE\", \"PAS\"}\nDOC_LEN = {\"DNI\": 8, \"CE\": 9, \"PAS\": 9}\n\ndef tipo_doc_len(tipo, numero):\n    # TODO\n    pass\n\nfor t, n in [(\"DNI\", \"12345678\"), (\"DNI\", \"123\"), (\"RUC\", \"20123456789\"), (None, \"1\")]:\n    print(t, n, \"→\", tipo_doc_len(t, n))"
          },
          {
            "id": "S03-T3-B-E1",
            "instruction": "E1 (guiado) — Completa la decision table como dict `codigo → status` para OK→accept, MISSING→review, OUT_OF_RANGE→reject, _default→review. Imprime el status de cada clave en la lista de prueba.",
            "hints": [
              "table.get(code, \"review\") si no incluyes _default como clave de negocio.",
              "Fila default cubre códigos desconocidos (FOO)."
            ],
            "kind": "guided",
            "tests": "table complete: 4 filas de negocio + default",
            "starterCode": "TABLE = {\n    \"OK\": \"accept\",\n    \"MISSING\": \"____\",\n    \"OUT_OF_RANGE\": \"____\",\n}\n\ndef apply(code):\n    return TABLE.get(code, \"review\")\n\nfor c in [\"OK\", \"MISSING\", \"OUT_OF_RANGE\", \"FOO\"]:\n    print(c, \"→\", apply(c))"
          },
          {
            "id": "S03-T3-B-E2",
            "instruction": "E2 (independiente) — Implementa la misma tabla con `match/case`, OR patterns para MISSING|NEEDS_REVIEW y OUT_OF_RANGE|NOT_IN_ALLOWLIST|BAD_TYPE, y `case _`.",
            "hints": [
              "case \"A\" | \"B\": en una sola rama. case _: al final.",
              "Requiere Python 3.10+. Si no, usa if/elif equivalente y anótalo."
            ],
            "kind": "independent",
            "tests": "assert statuses en OK, MISSING, OUT_OF_RANGE, FOO, NEEDS_REVIEW",
            "starterCode": "def status_match(code: str) -> str:\n    match code:\n        case \"OK\":\n            return \"accept\"\n        # TODO: MISSING | NEEDS_REVIEW → review\n        # TODO: OUT_OF_RANGE | NOT_IN_ALLOWLIST | BAD_TYPE → reject\n        case _:\n            return \"review\"\n\nfor c in [\"OK\", \"MISSING\", \"OUT_OF_RANGE\", \"FOO\", \"NEEDS_REVIEW\"]:\n    print(c, \"→\", status_match(c))"
          },
          {
            "id": "S03-T3-B-E3",
            "instruction": "E3 (transferencia) — Para (a) códigos de error finitos y (b) rango de edad 18–65, elige if o match, implementa ambos validadores y justifica en prints por qué match no es ideal para el rango numérico.",
            "hints": [
              "match brilla en literales; rangos numéricos son más claros con if y comparaciones.",
              "Puedes match en status codes y if en edad en el mismo módulo."
            ],
            "kind": "transfer",
            "tests": "rubric short answer + code ejecutable",
            "starterCode": "def map_code(code: str) -> str:\n    # TODO: match o if — códigos OK/MISSING/OUT_OF_RANGE\n    pass\n\ndef map_edad(edad: int | None) -> str:\n    # TODO: if — None review, 18–65 accept, else reject\n    pass\n\nprint(map_code(\"OK\"), map_code(\"MISSING\"))\nprint(map_edad(None), map_edad(30), map_edad(10))\nprint(\"Justificación:\", \"____\")"
          },
          {
            "id": "S03-T4-A-E1",
            "instruction": "E1 (guiado) — Completa `examples` para el campo `edad` con expected accept/reject/review/missing (usa value None para missing/review según tu invariante). Ejecuta validate y marca ok.",
            "hints": [
              "Cuatro dicts {value, expected}. None → review; -1 reject; 30 accept; \"x\" reject.",
              "missing key en el record se modela aquí como value None."
            ],
            "kind": "guided",
            "tests": "4 examples present; todos ok=True",
            "starterCode": "def validate_edad(e):\n    if e is None:\n        return \"review\"\n    if not isinstance(e, int):\n        return \"reject\"\n    if e < 0 or e > 120:\n        return \"reject\"\n    return \"accept\"\n\nexamples = [\n    {\"value\": 30, \"expected\": \"____\"},\n    {\"value\": -1, \"expected\": \"____\"},\n    {\"value\": None, \"expected\": \"____\"},\n    {\"value\": \"x\", \"expected\": \"____\"},\n]\nfor ex in examples:\n    got = validate_edad(ex[\"value\"])\n    print(ex[\"value\"], got, got == ex[\"expected\"])"
          },
          {
            "id": "S03-T4-A-E2",
            "instruction": "E2 (independiente) — Escribe invariante multi-campo: `apellido_paterno` y `apellido_materno` no vacíos (tras strip) para accept; si uno falta → review; si ambos vacíos → reject. Incluye texto + 3 examples.",
            "hints": [
              "strip y trata \"\" como vacío. None en uno → review.",
              "invariant_text en español; examples con expected."
            ],
            "kind": "independent",
            "tests": "text + examples ejecutables",
            "starterCode": "def validate_apellidos(ap, am):\n    # TODO\n    pass\n\ninvariant_text = \"____\"\nexamples = [\n    # TODO 3 casos\n]\nprint(invariant_text)\nfor ex in examples:\n    got = validate_apellidos(ex[\"ap\"], ex[\"am\"])\n    print(ex, \"→\", got, got == ex[\"expected\"])"
          },
          {
            "id": "S03-T4-A-E3",
            "instruction": "E3 (transferencia) — El invariante dice “edad siempre entre 18 y 65 inclusive (reject si no)”. Encuentra el contraejemplo que rompe la política real del negocio (menores en review, no reject) y ajusta la función + el texto del invariante.",
            "hints": [
              "edad 15 no debería ser reject duro si la política es review para menores.",
              "None sigue siendo review; >120 o <0 sí reject."
            ],
            "kind": "transfer",
            "tests": "identify broken claim; 15 → review tras fix",
            "starterCode": "def validate_edad_strict(e):\n    \"\"\"Invariante roto: todo lo no 18-65 es reject (incluye None y menores).\"\"\"\n    if isinstance(e, int) and 18 <= e <= 65:\n        return \"accept\"\n    return \"reject\"\n\nprint(\"strict 15 →\", validate_edad_strict(15))\nprint(\"strict None →\", validate_edad_strict(None))\n\ndef validate_edad_fixed(e):\n    # TODO: None review; <0 o >120 reject; <18 review; 18-65 accept; else review\n    pass\n\nprint(\"fixed 15 →\", validate_edad_fixed(15))\nprint(\"fixed None →\", validate_edad_fixed(None))\nprint(\"fixed 30 →\", validate_edad_fixed(30))\nprint(\"Invariante:\", \"____\")"
          },
          {
            "id": "S03-T4-B-E1",
            "instruction": "E1 (guiado) — Reescribe 3 mensajes vagos a plantilla campo+problema+acción: (1) \"Error\", (2) \"inválido\", (3) \"bad age\". Usa el campo `edad` y valores de ejemplo.",
            "hints": [
              "Incluye nombre del campo y qué hacer. No agregues PII real.",
              "Ejemplo: Campo edad=-3 fuera de rango; usa 0–120."
            ],
            "kind": "guided",
            "tests": "rubric keywords: campo, edad, acción en cada mensaje",
            "starterCode": "vagos = [\"Error\", \"inválido\", \"bad age\"]\n# Reescribe en la misma lista accionables\naccionables = [\n    \"____\",\n    \"____\",\n    \"____\",\n]\nfor a in accionables:\n    print(a)"
          },
          {
            "id": "S03-T4-B-E2",
            "instruction": "E2 (independiente) — Dado `classify_score` (accept/review/reject), escribe una lista `cases` con un input por cada rama (incl. frontera) y un loop assert/print PASS.",
            "hints": [
              "Al menos 3 casos: p.ej. 90, 55, 10. Opcional 80 y 50.",
              "for val, expected in cases: assert classify_score(val) == expected"
            ],
            "kind": "independent",
            "tests": "N cases for N branches (mín. 3)",
            "starterCode": "def classify_score(score: int) -> str:\n    if score >= 80:\n        return \"accept\"\n    elif score >= 50:\n        return \"review\"\n    else:\n        return \"reject\"\n\ncases = [\n    # TODO: (valor, expected)\n]\nfor val, expected in cases:\n    got = classify_score(val)\n    assert got == expected, (val, got, expected)\n    print(\"PASS\", val, got)"
          },
          {
            "id": "S03-T4-B-E3",
            "instruction": "E3 (transferencia) — Test rojo: la regla usa `edad > 18` y falla para 18 (espera accept). Arregla a `>= 18` y deja la suite en verde (18 accept, 17 review, None review).",
            "hints": [
              "Off-by-one clásico en frontera inferior inclusiva.",
              "Cambia > por >= en el camino accept; mantén guards de None."
            ],
            "kind": "transfer",
            "tests": "all green after fix",
            "starterCode": "def rango_edad(e):\n    if e is None:\n        return \"review\"\n    if e > 18 and e <= 65:  # BUG: 18 queda fuera\n        return \"accept\"\n    if e < 0 or e > 120:\n        return \"reject\"\n    return \"review\"\n\ncases = [(18, \"accept\"), (17, \"review\"), (None, \"review\"), (30, \"accept\")]\nfor val, expected in cases:\n    got = rango_edad(val)\n    print(val, got, \"ok=\", got == expected)\n    # assert got == expected  # descomenta tras arreglar"
          }
        ]
      },
      "youDo": {
        "title": "Motor de reglas del intake (incremento CP-N1-A)",
        "context": "Reemplaza el legado “Sales Log Parser” como entrega de S03. Construyes el **motor de reglas** sobre el parser de intake de S02: para un registro sintético de cliente, validas ≥3 campos y devuelves **accept | reject | review** con **code** y **message** accionable. Distingues ausencia (`None`/missing) de valores falsy válidos (`0`). Solo datos ficticios.",
        "objectives": [
          "Definir ≥3 reglas de campo sobre registro sintético de cliente",
          "Devolver status accept|reject|review + code + message por campo",
          "Distinguir ausencia (None/missing) de valores falsy válidos (0, y políticas de \"\")",
          "Incluir tabla de ejemplos/pruebas con ≥1 caso por rama crítica",
          "Documentar invariantes en español en README o docstrings"
        ],
        "requirements": [
          "Función o módulo validate_record(record: dict) → dict de resultados por campo",
          "Códigos estables: MISSING, OUT_OF_RANGE, NOT_IN_ALLOWLIST, NEEDS_REVIEW, OK (y BAD_TYPE si aplica)",
          "Sin PII real; dataset sintético embebido o en data/",
          "if __name__ == '__main__' demo reproducible",
          "No usar assert como única validación de negocio (asserts OK en tests)",
          "Preferir guards a pirámides de if anidados"
        ],
        "portfolioNote": "En el README documenta invariantes en español, la decision table de cada campo y por qué no usas `if monto:` para presencia. Incluye la matriz de pruebas. Eso es lo que un lead de datos revisa antes del merge del gate CP-N1-A.",
        "starterCode": "\"\"\"rules_engine_intake.py — incremento CP-N1-A (S03)\nDatos sintéticos únicamente. No uses información real de clientes.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Any\n\n\ndef validate_edad(valor: Any) -> dict:\n    \"\"\"None → MISSING/review; tipo mal → BAD_TYPE; rango; menores → NEEDS_REVIEW.\"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef validate_region(valor: Any) -> dict:\n    \"\"\"Allowlist sintética de regiones del Perú.\"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef validate_monto(valor: Any) -> dict:\n    \"\"\"0 válido; None → review; negativo → reject; outlier opcional → review.\"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef validate_record(record: dict) -> dict:\n    \"\"\"Devuelve {campo: {status, code, message}} para edad, region, monto_ingreso.\"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef _run_tests() -> None:\n    # Caso feliz\n    r = validate_record({\"edad\": 30, \"region\": \"Lima\", \"monto_ingreso\": 0})\n    assert r[\"monto_ingreso\"][\"status\"] == \"accept\"  # cero válido\n    # Ausencia ≠ cero\n    r2 = validate_record({\"edad\": None, \"region\": \"Lima\", \"monto_ingreso\": None})\n    assert r2[\"edad\"][\"code\"] == \"MISSING\"\n    # Más casos por rama...\n    print(\"tests OK\")\n\n\ndef main() -> None:\n    demo = {\n        \"edad\": 17,\n        \"region\": \"Tacna\",\n        \"monto_ingreso\": -5,\n    }\n    print(validate_record(demo))\n    _run_tests()\n\n\nif __name__ == \"__main__\":\n    main()\n"
      },
      "selfCheck_stems": [
        {
          "question": "¿Cuál es la forma correcta de chequear ausencia de un campo opcional en un validador?",
          "options": [
            "if not campo:",
            "if campo == False:",
            "if campo is None:",
            "if campo is 0:"
          ]
        },
        {
          "question": "En una cadena if/elif/else, ¿qué ocurre cuando la primera condición es verdadera?",
          "options": [
            "Se ejecuta esa rama y se omiten las siguientes",
            "Se evalúan todas las ramas y se combinan resultados",
            "Se ejecuta también el else siempre",
            "Python elige la rama más específica automáticamente"
          ]
        },
        {
          "question": "¿Qué devuelve la expresión `\"\" or \"default\"` en Python?",
          "options": [
            "True",
            "\"default\"",
            "False",
            "\"\""
          ]
        },
        {
          "question": "Una allowlist de tipos de documento se implementa mejor como…",
          "options": [
            "Una lista de if anidados por cada letra del código",
            "Un float entre 0 y 1",
            "assert tipo == \"DNI\" como única validación de producción",
            "Un set de literales y el operador in"
          ]
        },
        {
          "question": "¿Cuándo aporta más claridad `match/case` que `if` en un motor de reglas introductorio?",
          "options": [
            "Siempre; match deprecó if en Python 3.12",
            "Solo para rangos numéricos de montos",
            "Cuando el sujeto es un literal/estado finito (códigos) y hay case _",
            "Nunca; match está deprecado"
          ]
        }
      ],
      "taught_text_sha": "4d1e7fdd00196f08",
      "taught_text_len": 29708
    },
    {
      "file": "src/lib/course/sections/s04-functions-modules.ts",
      "id": "functions-modules",
      "index": 4,
      "title": "Iteración y resúmenes transaccionales",
      "shortTitle": "Iteración & Resúmenes",
      "tagline": "for/while, contadores, comprehensions y cierre del Client Intake CP-N1-A",
      "jobRelevance": "En onboarding de data en bancos, fintech y retail en Perú, el motor de reglas (S03) debe correr sobre **lotes**: cientos de filas, no un solo dict de demo. Si no dominas for/while, contadores y tasas con denominador correcto, tu script de calidad miente en el dashboard. Esta sección cierra el gate **CP-N1-A**: procesar múltiples registros, resumir errores, conservar originales y entregar una demo reproducible por stdout.",
      "learningOutcomes": [
        "Recorrer secuencias con for y range sin off-by-one en el stop exclusivo",
        "Usar enumerate y zip (incl. strict) sin desalinear columnas de intake",
        "Escribir while con centinelas y condición de terminación explícita",
        "Aplicar break/continue y guardrails contra loops infinitos",
        "Implementar contadores, acumuladores y búsquedas en un pase O(n)",
        "Escribir comprehensions legibles para filtros simples de resumen",
        "Trazar el estado de un bucle para depurar contadores",
        "Distinguir costo lineal vs cuadrático y corregir off-by-one en índices"
      ],
      "theory": [
        {
          "subtopicId": "S04-T1-A",
          "heading": "for, range y secuencias",
          "paragraphs": [
            "El bucle **`for x in secuencia:`** recorre cada elemento **una vez**, en orden. No necesitas un índice si solo te importa el valor. Las secuencias típicas de intake son: **listas de registros** (dicts), **líneas de texto** y **`range(n)`** cuando quieres un contador 0..n-1.",
            "**`range(stop)`**, **`range(start, stop)`**, **`range(start, stop, step)`** producen enteros sin materializar una lista gigante. El **stop es exclusivo**: `range(3)` → 0,1,2. Eso evita el off-by-one clásico al numerar N filas.",
            "En lotes de clientes sintéticos, el patrón base es `for registro in filas:` y dentro llamar a `validate_record`. No mutes la lista mientras la recorres salvo que sepas lo que haces; acumula resultados en otra lista."
          ],
          "code": null,
          "code_output": null
        },
        {
          "subtopicId": "S04-T1-B",
          "heading": "enumerate y zip sin desalinear",
          "paragraphs": [
            "**`enumerate(seq, start=0)`** te da `(índice, valor)` sin armar el índice a mano. Ideal para reportes “fila 1, fila 2…” (usa `start=1` para humanos) y para localizar el registro que falló en un lote.",
            "**`zip(a, b)`** empareja elementos en paralelo. Se detiene en la **secuencia más corta**. Si `nombres` tiene 3 y `edades` tiene 2, el tercer nombre **desaparece en silencio** — un bug de calidad de datos. En Python 3.10+ existe `zip(..., strict=True)`; en cualquier versión puedes validar `len(a)==len(b)` antes de zipear (helper `zip_strict` en el demo).",
            "Nunca asumas que dos columnas CSV llegaron alineadas solo porque “deberían”. Cuenta longitudes en tests de pipeline."
          ],
          "code": "ids = [\"C001\", \"C002\", \"C003\"]\nregiones = [\"Lima\", \"Cusco\"]  # ¡falta un valor!\n\nfor i, rid in enumerate(ids, start=1):\n    print(f\"fila {i}: {rid}\")\n\nprint(\"zip corto (silencioso):\", list(zip(ids, regiones)))\n\n# Equivalente pedagógico a zip(..., strict=True) — Py 3.10+\ndef zip_strict(a, b):\n    if len(a) != len(b):\n        raise ValueError(f\"desalineado: {len(a)} vs {len(b)}\")\n    return list(zip(a, b))\n\ntry:\n    zip_strict(ids, regiones)\nexcept ValueError as e:\n    print(\"zip strict →\", type(e).__name__ + \":\", e)",
          "code_output": "fila 1: C001\nfila 2: C002\nfila 3: C003\nzip corto (silencioso): [('C001', 'Lima'), ('C002', 'Cusco')]\nzip strict → ValueError: desalineado: 3 vs 2"
        },
        {
          "subtopicId": "S04-T2-A",
          "heading": "while, centinelas y terminación",
          "paragraphs": [
            "**`while condicion:`** repite mientras la condición sea verdadera. Úsalo cuando **no sabes de antemano cuántas** iteraciones habrá: leer hasta línea vacía, reintentar hasta éxito, o procesar un stream.",
            "Un **centinela** es un valor especial que marca el fin (p. ej. `\"\"`, `None`, `\"END\"`). El bucle debe **actualizar el estado** en cada vuelta; si la condición nunca se vuelve falsa, tienes un **loop infinito**.",
            "En demos de browser no usamos `input()` interactivo real; simulamos un buffer de líneas. El patrón es el mismo: leer siguiente, chequear centinela, procesar."
          ],
          "code": "lineas = [\"C001|Lima\", \"C002|Cusco\", \"\", \"C003|Piura\"]  # \"\" = centinela\ni = 0\nleidas = []\nwhile i < len(lineas):\n    linea = lineas[i]\n    i += 1\n    if linea == \"\":\n        break  # fin de lote\n    leidas.append(linea)\nprint(\"procesadas:\", leidas)\nprint(\"restante no leída:\", lineas[i:])",
          "code_output": "procesadas: ['C001|Lima', 'C002|Cusco']\nrestante no leída: ['C003|Piura']"
        },
        {
          "subtopicId": "S04-T2-B",
          "heading": "break, continue y prevención de loops infinitos",
          "paragraphs": [
            "**`break`** sale del bucle actual de inmediato. **`continue`** salta al **siguiente** ciclo sin ejecutar el resto del cuerpo. En intake: `continue` para saltar filas vacías; `break` al encontrar un centinela o un error fatal de configuración.",
            "Prevención de infinito: (1) actualiza la variable de control, (2) pon un **máximo de iteraciones** en prototipos (`MAX = 10_000`), (3) evita `while True` sin break garantizado, (4) no hagas `i = i` por error tipográfico.",
            "Un `while True` con break en el centinela es legítimo si el break es **obvio y testeado**. Documenta la condición de salida."
          ],
          "code": "raw_lines = [\"  \", \"C001|Lima\", \"SKIP\", \"C002|Cusco\", \"END\"]\nMAX = 100\nout = []\nn = 0\nfor line in raw_lines:\n    n += 1\n    if n > MAX:\n        raise RuntimeError(\"guardrail: demasiadas iteraciones\")\n    s = line.strip()\n    if not s:\n        continue\n    if s == \"END\":\n        break\n    if s == \"SKIP\":\n        continue\n    out.append(s)\nprint(out)\nprint(\"iteraciones efectivas del for:\", n)",
          "code_output": "['C001|Lima', 'C002|Cusco']\niteraciones efectivas del for: 5"
        },
        {
          "subtopicId": "S04-T3-A",
          "heading": "Contadores, acumuladores y búsqueda",
          "paragraphs": [
            "Un **contador** suma 1 por evento (`n_reject += 1`). Un **acumulador** suma cantidades (`total_monto += m`). Una **búsqueda** recorre hasta hallar (o no) un elemento y a menudo usa `break` o un flag.",
            "Para **tasas** del gate CP-N1-A: `tasa_error = n_error / n_total` solo si **`n_total > 0`**. El denominador es el número de registros **intentados**, no solo los aceptados. Si no hay filas, reporta `None` o “N/A”, no dividas por cero.",
            "Buscar el primer reject es O(n); contar todos también es O(n). No anides dos bucles sobre el mismo lote “por si acaso” sin necesidad."
          ],
          "code": "statuses = [\"accept\", \"reject\", \"accept\", \"review\", \"reject\", \"accept\"]\nn_total = 0\nn_reject = 0\nn_accept = 0\nn_review = 0\nfirst_reject_idx = None\nfor i, st in enumerate(statuses):\n    n_total += 1\n    if st == \"reject\":\n        n_reject += 1\n        if first_reject_idx is None:\n            first_reject_idx = i\n    elif st == \"accept\":\n        n_accept += 1\n    elif st == \"review\":\n        n_review += 1\ntasa = n_reject / n_total if n_total else None\nprint(\"total\", n_total, \"reject\", n_reject, \"tasa\", round(tasa, 4))\nprint(\"first_reject_idx\", first_reject_idx)",
          "code_output": "total 6 reject 2 tasa 0.3333\nfirst_reject_idx 1"
        },
        {
          "subtopicId": "S04-T3-B",
          "heading": "Comprehensions legibles",
          "paragraphs": [
            "Una **list comprehension** `[expr for x in xs if cond]` construye una lista en una línea. Es idiomática cuando la transformación es **simple**. Si hay validación multi-rama o side effects (prints, I/O), usa un `for` explícito.",
            "También existen **dict** y **set** comprehensions: `{k: v for ...}`, `{x for ...}`. No anides comprehensions de tres niveles “porque cabe”: la legibilidad del revisor manda.",
            "En el resumen de intake, es útil: `rejects = [r for r in results if r['status']=='reject']`. El conteo sigue siendo `len(rejects)` con denominador `len(results)`."
          ],
          "code": "results = [\n    {\"id\": \"C001\", \"status\": \"accept\"},\n    {\"id\": \"C002\", \"status\": \"reject\"},\n    {\"id\": \"C003\", \"status\": \"review\"},\n    {\"id\": \"C004\", \"status\": \"reject\"},\n]\nrejects = [r[\"id\"] for r in results if r[\"status\"] == \"reject\"]\ncodes = {r[\"status\"] for r in results}\nby_id = {r[\"id\"]: r[\"status\"] for r in results}\nprint(\"rejects\", rejects)\nprint(\"codes\", sorted(codes))\nprint(\"by_id C002\", by_id[\"C002\"])",
          "code_output": "rejects ['C002', 'C004']\ncodes ['accept', 'reject', 'review']\nby_id C002 reject"
        },
        {
          "subtopicId": "S04-T4-A",
          "heading": "Trazado de estado",
          "paragraphs": [
            "**Trazar estado** es escribir (o imaginar) una tabla: iteración | variables | salida. Es la herramienta #1 para depurar off-by-one y contadores mal actualizados.",
            "Antes de pedir ayuda, dibuja 3–5 filas de la traza con valores concretos del lote sintético. Si la traza no cuadra con el print, el bug está en la actualización del estado, no en “Python raro”.",
            "En demos usamos `print` de depuración con prefijo `TRACE`. En producción preferirás logging (S09); aquí el objetivo es razonar el bucle."
          ],
          "code": "montos = [10, 0, -5, 20]\ntotal = 0\nn_pos = 0\nprint(\"i | m | total | n_pos\")\nfor i, m in enumerate(montos):\n    if m > 0:\n        total += m\n        n_pos += 1\n    print(f\"{i} | {m} | {total} | {n_pos}\")\nprint(\"final total=\", total, \"n_pos=\", n_pos)",
          "code_output": "i | m | total | n_pos\n0 | 10 | 10 | 1\n1 | 0 | 10 | 1\n2 | -5 | 10 | 1\n3 | 20 | 30 | 2\nfinal total= 30 n_pos= 2"
        },
        {
          "subtopicId": "S04-T4-B",
          "heading": "Costo lineal/cuadrático y off-by-one",
          "paragraphs": [
            "Un solo `for` sobre n filas es **O(n)** (lineal). Dos bucles anidados sobre el mismo lote (`for a in xs: for b in xs:`) es **O(n²)** (cuadrático). Con 10 filas no se nota; con 100_000, el script “se cuelga”.",
            "**Off-by-one**: `range(len(xs))` es correcto para índices 0..n-1; `range(1, len(xs))` se salta el primero; `range(len(xs)+1)` explota con IndexError. Fronteras inclusivas/exclusivas en filtros (`>=` vs `>`) también son off-by-one de negocio.",
            "Para el gate: cuenta registros con un contador O(n); no recalcules la tasa dentro de un doble bucle. Debuggea índices imprimiendo `i` y `len`."
          ],
          "code": "xs = [\"a\", \"b\", \"c\"]\n# Lineal: 3 pasos\nlinear = 0\nfor _ in xs:\n    linear += 1\n# Cuadrático ingenuo: 9 pares\nquad = 0\nfor a in xs:\n    for b in xs:\n        quad += 1\nprint(\"linear\", linear, \"quadratic\", quad)\n\n# Off-by-one: último índice válido\nprint(\"last ok\", xs[len(xs) - 1])\ntry:\n    print(xs[len(xs)])\nexcept IndexError as e:\n    print(\"IndexError en len(xs):\", e)",
          "code_output": "linear 3 quadratic 9\nlast ok c\nIndexError en len(xs): list index out of range"
        }
      ],
      "iDo": {
        "steps": [
          {
            "demoId": "S04-T1-A-DEMO",
            "subtopicId": "S04-T1-A",
            "environment": "browser-pyodide",
            "description": "Recorrer lote de registros sintéticos con for y range",
            "why": "Un for por valor es el esqueleto del procesador por lotes; range(len) solo si necesitas índices.",
            "code": "lote = [\n    {\"id\": \"C001\", \"edad\": 30},\n    {\"id\": \"C002\", \"edad\": 17},\n    {\"id\": \"C003\", \"edad\": 45},\n]\nfor reg in lote:\n    print(reg[\"id\"], \"edad=\", reg[\"edad\"])\nprint(\"n=\", len(lote), \"range →\", list(range(len(lote))))",
            "language": "python",
            "output": "C001 edad= 30\nC002 edad= 17\nC003 edad= 45\nn= 3 range → [0, 1, 2]"
          },
          {
            "demoId": "S04-T1-B-DEMO",
            "subtopicId": "S04-T1-B",
            "environment": "browser-pyodide",
            "description": "enumerate para reportar fila y zip strict para columnas",
            "why": "enumerate numera para humanos; validar len (o zip strict en 3.10+) evita emparejar mal columnas.",
            "code": "ids = [\"C001\", \"C002\", \"C003\"]\nregiones = [\"Lima\", \"Cusco\", \"Arequipa\"]\n\ndef zip_strict(a, b):\n    if len(a) != len(b):\n        raise ValueError(\"desalineado\")\n    return zip(a, b)\n\nfor i, (rid, reg) in enumerate(zip_strict(ids, regiones), start=1):\n    print(f\"fila {i}: {rid} @ {reg}\")\nmal = [\"Lima\", \"Cusco\"]\ntry:\n    list(zip_strict(ids, mal))\nexcept ValueError:\n    print(\"desalineado detectado\")",
            "language": "python",
            "output": "fila 1: C001 @ Lima\nfila 2: C002 @ Cusco\nfila 3: C003 @ Arequipa\ndesalineado detectado"
          },
          {
            "demoId": "S04-T2-A-DEMO",
            "subtopicId": "S04-T2-A",
            "environment": "browser-pyodide",
            "description": "while con centinela END sobre buffer de líneas",
            "why": "El centinela corta el lote; lo posterior no se procesa. i avanza siempre → no hay infinito.",
            "code": "buf = [\"Ana|Lima\", \"Luis|Cusco\", \"END\", \"ignorada\"]\ni = 0\nout = []\nwhile i < len(buf):\n    line = buf[i]\n    i += 1\n    if line == \"END\":\n        break\n    out.append(line)\nprint(out)\nprint(\"indice final\", i)",
            "language": "python",
            "output": "['Ana|Lima', 'Luis|Cusco']\nindice final 3"
          },
          {
            "demoId": "S04-T2-B-DEMO",
            "subtopicId": "S04-T2-B",
            "environment": "browser-pyodide",
            "description": "continue salta vacíos; break corta en ERROR fatal",
            "why": "continue limpia ruido; break detiene el lote ante error de configuración.",
            "code": "lines = [\"\", \"ok:1\", \"\", \"ok:2\", \"ERROR\", \"ok:3\"]\nkept = []\nfor ln in lines:\n    if not ln.strip():\n        continue\n    if ln.startswith(\"ERROR\"):\n        print(\"fatal, stop\")\n        break\n    kept.append(ln)\nprint(\"kept\", kept)",
            "language": "python",
            "output": "fatal, stop\nkept ['ok:1', 'ok:2']"
          },
          {
            "demoId": "S04-T3-A-DEMO",
            "subtopicId": "S04-T3-A",
            "environment": "browser-pyodide",
            "description": "Contadores accept/reject/review y tasa con denominador",
            "why": "Un pase O(n) llena contadores; la tasa usa n total, no solo accepts.",
            "code": "statuses = [\"accept\", \"reject\", \"review\", \"accept\", \"reject\"]\ncounts = {\"accept\": 0, \"reject\": 0, \"review\": 0}\nfor st in statuses:\n    counts[st] = counts.get(st, 0) + 1\nn = len(statuses)\ntasa_err = counts[\"reject\"] / n if n else None\nprint(counts)\nprint(\"n\", n, \"tasa_reject\", round(tasa_err, 2))",
            "language": "python",
            "output": "{'accept': 2, 'reject': 2, 'review': 1}\nn 5 tasa_reject 0.4"
          },
          {
            "demoId": "S04-T3-B-DEMO",
            "subtopicId": "S04-T3-B",
            "environment": "browser-pyodide",
            "description": "Comprehensions para filtrar rejects del resumen",
            "why": "Filtrar con comprehension es legible; el denominador sigue siendo len(rows).",
            "code": "rows = [\n    {\"id\": \"C1\", \"status\": \"accept\"},\n    {\"id\": \"C2\", \"status\": \"reject\"},\n    {\"id\": \"C3\", \"status\": \"reject\"},\n]\nreject_ids = [r[\"id\"] for r in rows if r[\"status\"] == \"reject\"]\nn_rej = len(reject_ids)\nprint(reject_ids, \"tasa\", n_rej / len(rows))",
            "language": "python",
            "output": "['C2', 'C3'] tasa 0.6666666666666666"
          },
          {
            "demoId": "S04-T4-A-DEMO",
            "subtopicId": "S04-T4-A",
            "environment": "browser-pyodide",
            "description": "Tabla TRACE de contador durante el lote",
            "why": "La traza hace visible cuándo sube el contador; base del debugging de resúmenes.",
            "code": "flags = [True, False, True, True]\nn_ok = 0\nprint(\"i flag n_ok\")\nfor i, f in enumerate(flags):\n    if f:\n        n_ok += 1\n    print(i, f, n_ok)\nprint(\"FINAL\", n_ok)",
            "language": "python",
            "output": "i flag n_ok\n0 True 1\n1 False 1\n2 True 2\n3 True 3\nFINAL 3"
          },
          {
            "demoId": "S04-T4-B-DEMO",
            "subtopicId": "S04-T4-B",
            "environment": "browser-pyodide",
            "description": "Detectar O(n²) ingenuo y off-by-one en range",
            "why": "4 vs 16 pasos; range(1,len) omite el primer registro — bug clásico de resúmenes incompletos.",
            "code": "n = 4\nsteps_linear = 0\nfor i in range(n):\n    steps_linear += 1\nsteps_quad = 0\nfor i in range(n):\n    for j in range(n):\n        steps_quad += 1\nprint(\"linear\", steps_linear, \"quad\", steps_quad)\ndata = [10, 20, 30]\n# off-by-one: range(1, len) salta index 0\nskipped = [data[i] for i in range(1, len(data))]\nprint(\"skipped_first\", skipped)",
            "language": "python",
            "output": "linear 4 quad 16\nskipped_first [20, 30]"
          }
        ]
      },
      "weDo": {
        "exercises": [
          {
            "id": "S04-T1-A-E1",
            "instruction": "E1 (guiado) — Dada `regiones = [\"Lima\", \"Cusco\", \"Piura\"]`, imprime cada región en su propia línea con un `for`. Luego imprime `list(range(3))`.",
            "hints": [
              "for r in regiones: print(r)",
              "range(3) produce 0,1,2 — stop exclusivo."
            ],
            "kind": "guided",
            "tests": "3 regiones + [0,1,2]",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nregiones = [\"Lima\", \"Cusco\", \"Piura\"]\nfor r in regiones:\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(r)\n"
          },
          {
            "id": "S04-T1-A-E2",
            "instruction": "E2 (independiente) — `edades = [30, 17, 45, 22]`. Cuenta cuántas son `>= 18` con un for (no uses comprehension todavía). Imprime el contador.",
            "hints": [
              "n = 0; for e in edades: if e >= 18: n += 1",
              "Resultado esperado: 3 (30,45,22)."
            ],
            "kind": "independent",
            "tests": "assert n == 3",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nedades = [30, 17, 45, 22]\nn = 0\nfor e in edades:\n    if e >= 18:\n        n += 1\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(n)\n"
          },
          {
            "id": "S04-T1-A-E3",
            "instruction": "E3 (transferencia) — Simula un mini-lote: lista de dicts con `id` y `monto`. Imprime solo los `id` cuyo monto sea `> 0` usando for. No mutes la lista original.",
            "hints": [
              "for reg in lote: if reg['monto'] > 0: print(reg['id'])",
              "0 no se imprime; negativos tampoco. Conserva lote intacto."
            ],
            "kind": "transfer",
            "tests": "C1 y C4; len 4",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nlote = [{\"id\": \"C1\", \"monto\": 10}, {\"id\": \"C2\", \"monto\": 0}, {\"id\": \"C3\", \"monto\": -2}, {\"id\": \"C4\", \"monto\": 5}]\nfor reg in lote:\n    if reg[\"monto\"] > 0:\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(reg[\"id\"])\n"
          },
          {
            "id": "S04-T1-B-E1",
            "instruction": "E1 (guiado) — Con `ids = [\"A\", \"B\", \"C\"]`, usa `enumerate(..., start=1)` e imprime `fila k: id`.",
            "hints": [
              "for i, x in enumerate(ids, start=1): print(f'fila {i}: {x}')",
              "start=1 es para humanos; el índice interno de la lista sigue siendo 0-based."
            ],
            "kind": "guided",
            "tests": "fila 1..3",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nids = [\"A\", \"B\", \"C\"]\nfor i, x in enumerate(ids, start=1):\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(f\"fila {i}: {x}\")\n"
          },
          {
            "id": "S04-T1-B-E2",
            "instruction": "E2 (independiente) — `nombres` y `edades` de igual longitud. Emparéjalos con `zip` e imprime `nombre=edad`. Luego muestra qué pasa con zip silencioso si acortas edades a 1 elemento (imprime list(zip(...))).",
            "hints": [
              "zip se detiene en la más corta; el resto se pierde.",
              "Compara len antes en código real; aquí solo observa el silencio."
            ],
            "kind": "independent",
            "tests": "3 pares + 1 par en zip corto",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nnombres = [\"Ana\", \"Luis\", \"María\"]\nedades = [30, 25, 40]\nfor n, e in zip(nombres, edades):\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(f\"{n}={e}\")\n"
          },
          {
            "id": "S04-T1-B-E3",
            "instruction": "E3 (transferencia) — Implementa `zip_strict(a,b)` que lance ValueError si `len(a)!=len(b)` (equivalente pedagógico a `zip(..., strict=True)` en Py 3.10+). Prueba desalineado → `DESALINEADO` y alineado → `OK`.",
            "hints": [
              "if len(a) != len(b): raise ValueError(...); return list(zip(a,b))",
              "Dos bloques try/except con prints distintos."
            ],
            "kind": "transfer",
            "tests": "DESALINEADO luego OK",
            "starterCode": "def zip_strict(a, b):\n    # TODO\n    ...\ntry:\n    zip_strict([1, 2, 3], [10, 20])\nexcept ValueError:\n    print(\"DESALINEADO\")\ntry:\n    zip_strict([1, 2], [3, 4])\n    print(\"OK\")\nexcept ValueError:\n    print(\"DESALINEADO\")"
          },
          {
            "id": "S04-T2-A-E1",
            "instruction": "E1 (guiado) — Buffer `lines = [\"r1\", \"r2\", \"\", \"r3\"]`. Con while e índice, acumula hasta el string vacío (sin incluirlo). Imprime la lista.",
            "hints": [
              "while i < len: leer, i+=1, if line=='': break else append",
              "Resultado: ['r1','r2']; r3 queda fuera del lote."
            ],
            "kind": "guided",
            "tests": "['r1','r2']",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nlines = [\"r1\", \"r2\", \"\", \"r3\"]\ni = 0\nout = []\nwhile i < len(lines):\n    line = lines[i]\n    i += 1\n    if line == \"\":\n        break\n    out.append(line)\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(out)\n"
          },
          {
            "id": "S04-T2-A-E2",
            "instruction": "E2 (independiente) — Simula reintentos: `intentos = 0`, `MAX = 3`, `while intentos < MAX`, incrementa e imprime `intento k`. Al salir imprime `done` y el valor final de intentos.",
            "hints": [
              "intentos += 1 dentro del while es la variable de control.",
              "Si olvidas incrementar, loop infinito (no lo hagas)."
            ],
            "kind": "independent",
            "tests": "3 intentos + done 3",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nintentos = 0\nMAX = 3\nwhile intentos < MAX:\n    intentos += 1\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(f\"intento {intentos}\")\n"
          },
          {
            "id": "S04-T2-A-E3",
            "instruction": "E3 (transferencia) — Cola simulada: `cola = [\"job1\", \"job2\", \"job3\"]`. Mientras la cola no esté vacía, saca el primero con `pop(0)`, imprímelo, y si el job es `job2` imprime `PAUSE` y break. Muestra la cola restante.",
            "hints": [
              "while cola: job = cola.pop(0)",
              "Tras break debe quedar ['job3']."
            ],
            "kind": "transfer",
            "tests": "job1 job2 PAUSE rest [job3]",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\ncola = [\"job1\", \"job2\", \"job3\"]\nwhile cola:\n    job = cola.pop(0)\n    if job == \"job2\":\n        break\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(job)\n"
          },
          {
            "id": "S04-T2-B-E1",
            "instruction": "E1 (guiado) — `raw = [\"  \", \"Lima\", \"\", \"Cusco\"]`. Con for, usa continue si `not x.strip()`; imprime las regiones válidas.",
            "hints": [
              "if not x.strip(): continue",
              "Solo Lima y Cusco."
            ],
            "kind": "guided",
            "tests": "Lima\\nCusco",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nraw = [\"  \", \"Lima\", \"\", \"Cusco\"]\nfor x in raw:\n    if not x.strip():\n        continue\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(x)\n"
          },
          {
            "id": "S04-T2-B-E2",
            "instruction": "E2 (independiente) — `codes = [200, 200, 500, 200]`. Recorre; si code >= 500 imprime `STOP` y break; si no, imprime `ok`. Cuenta cuántos ok imprimiste.",
            "hints": [
              "break no procesa el 200 final.",
              "n_ok debe ser 2."
            ],
            "kind": "independent",
            "tests": "ok ok STOP n_ok 2",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\ncodes = [200, 200, 500, 200]\nn_ok = 0\nfor c in codes:\n    if c >= 500:\n        break\n    n_ok += 1\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(\"STOP\")\n"
          },
          {
            "id": "S04-T2-B-E3",
            "instruction": "E3 (transferencia) — Escribe un `while True` que lea de `buf = [\"a\", \"b\", \"END\"]` con índice, break en END, y un guardrail `if i > 10: raise RuntimeError('guard')`. Imprime los valores leídos.",
            "hints": [
              "while True no es pecado si break y guardrail están claros.",
              "No proceses END como dato."
            ],
            "kind": "transfer",
            "tests": "['a','b']",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nbuf = [\"a\", \"b\", \"END\"]\ni = 0\nout = []\nwhile True:\n    if i > 10:\n        raise RuntimeError(\"guard\")\n    item = buf[i]\n    i += 1\n    if item == \"END\":\n        break\n    out.append(item)\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(out)\n"
          },
          {
            "id": "S04-T3-A-E1",
            "instruction": "E1 (guiado) — `sts = [\"accept\", \"reject\", \"accept\"]`. Inicializa contadores y en un for incrementa. Imprime n_accept, n_reject, n_total.",
            "hints": [
              "n_total = len o +=1 por fila",
              "2 accept, 1 reject, total 3"
            ],
            "kind": "guided",
            "tests": "2 1 3",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nsts = [\"accept\", \"reject\", \"accept\"]\nn_accept = n_reject = n_total = 0\nfor s in sts:\n    n_total += 1\n    if s == \"accept\":\n        n_accept += 1\n    elif s == \"reject\":\n        n_reject += 1\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(n_accept, n_reject, n_total)\n"
          },
          {
            "id": "S04-T3-A-E2",
            "instruction": "E2 (independiente) — Calcula `tasa_reject = n_reject / n_total` para sts del E1. Luego calcula tasa para lista vacía sin ZeroDivisionError (imprime None).",
            "hints": [
              "if n_total: tasa = n_rej/n_total else None",
              "Primera tasa ~0.333…; segunda None."
            ],
            "kind": "independent",
            "tests": "0.3333 y None",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\ndef tasa_reject(sts):\n    n_total = len(sts)\n    if n_total == 0:\n        return None\n    n_reject = sum(1 for s in sts if s == \"reject\")\n    return n_reject / n_total\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(round(tasa_reject([\"accept\", \"reject\", \"accept\"]), 4))\n"
          },
          {
            "id": "S04-T3-A-E3",
            "instruction": "E3 (transferencia) — Busca el índice del primer `status==\"review\"` en una lista de dicts. Si no hay, imprime -1. Si hay, imprime el índice y el id.",
            "hints": [
              "first = None; for i,r in enumerate(...): if ...: first=i; break",
              "No uses index() si quieres practicar la búsqueda manual."
            ],
            "kind": "transfer",
            "tests": "1 C2",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nrows = [{\"id\": \"C1\", \"status\": \"accept\"}, {\"id\": \"C2\", \"status\": \"review\"}, {\"id\": \"C3\", \"status\": \"review\"}]\nidx = -1\nfor i, r in enumerate(rows):\n    if r[\"status\"] == \"review\":\n        idx = i\n        break\nif idx == -1:\nelse:\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(-1)\n"
          },
          {
            "id": "S04-T3-B-E1",
            "instruction": "E1 (guiado) — `nums = [1,2,3,4,5]`. Crea con comprehension la lista de cuadrados y la de pares. Imprímelas.",
            "hints": [
              "[x*x for x in nums] y [x for x in nums if x%2==0]",
              "Pares: 2,4"
            ],
            "kind": "guided",
            "tests": "cuadrados y pares",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nnums = [1, 2, 3, 4, 5]\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print([x * x for x in nums])\n"
          },
          {
            "id": "S04-T3-B-E2",
            "instruction": "E2 (independiente) — De `rows` con status, obtén set de statuses distintos ordenados alfabéticamente para el reporte.",
            "hints": [
              "sorted({r['status'] for r in rows})",
              "accept, reject, review"
            ],
            "kind": "independent",
            "tests": "['accept','reject','review']",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nrows = [{\"status\": \"reject\"}, {\"status\": \"accept\"}, {\"status\": \"reject\"}, {\"status\": \"review\"}]\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(sorted({r[\"status\"] for r in rows}))\n"
          },
          {
            "id": "S04-T3-B-E3",
            "instruction": "E3 (transferencia) — Construye dict `id -> status` por comprehension y calcula tasa de reject como len de rejects / len rows usando otra comprehension para rejects.",
            "hints": [
              "by = {r['id']: r['status'] for r in rows}",
              "tasa 0.5 con 2 reject de 4"
            ],
            "kind": "transfer",
            "tests": "reject [C2,C4] 0.5",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nrows = [{\"id\": \"C1\", \"status\": \"accept\"}, {\"id\": \"C2\", \"status\": \"reject\"}, {\"id\": \"C3\", \"status\": \"accept\"}, {\"id\": \"C4\", \"status\": \"reject\"}]\nby = {r[\"id\"]: r[\"status\"] for r in rows}\nrejects = [i for i, st in by.items() if st == \"reject\"]\ntasa = len(rejects) / len(rows)\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(by[\"C2\"], rejects, tasa)\n"
          },
          {
            "id": "S04-T4-A-E1",
            "instruction": "E1 (guiado) — Traza: `vals = [2, -1, 3]`, acumulador `s=0`. En cada paso imprime `i, val, s` después de sumar solo positivos.",
            "hints": [
              "if val > 0: s += val; luego print",
              "Final s=5"
            ],
            "kind": "guided",
            "tests": "traza + final 5",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nvals = [2, -1, 3]\ns = 0\nfor i, val in enumerate(vals):\n    if val > 0:\n        s += val\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(i, val, s)\n"
          },
          {
            "id": "S04-T4-A-E2",
            "instruction": "E2 (independiente) — Hay un bug: el contador `n` se incrementa dos veces por fila. Traza e identifica; imprime n corregido para 3 filas (debe ser 3).",
            "hints": [
              "Busca n += 1 duplicado",
              "Deja un solo incremento."
            ],
            "kind": "independent",
            "tests": "3",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nfilas = [\"a\", \"b\", \"c\"]\nn = 0\nfor f in filas:\n    n += 1\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(n)\n"
          },
          {
            "id": "S04-T4-A-E3",
            "instruction": "E3 (transferencia) — Simula tres registros y un dict de contadores. Imprime una línea TRACE por registro con el estado completo del dict (copy o str).",
            "hints": [
              "counts = dict; al final de cada iter print TRACE",
              "Debe verse el crecimiento paso a paso."
            ],
            "kind": "transfer",
            "tests": "3 TRACE + FINAL",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nregs = [\"accept\", \"reject\", \"accept\"]\ncounts = {\"accept\": 0, \"reject\": 0}\nfor i, st in enumerate(regs):\n    counts[st] = counts.get(st, 0) + 1\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(\"TRACE\", i, st, dict(counts))\n"
          },
          {
            "id": "S04-T4-B-E1",
            "instruction": "E1 (guiado) — Para n=5, cuenta pasos de un for simple y de un doble for anidado. Imprime ambos números.",
            "hints": [
              "linear n, quad n*n",
              "5 y 25"
            ],
            "kind": "guided",
            "tests": "5 25",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nn = 5\nlin = quad = 0\nfor i in range(n):\n    lin += 1\nfor i in range(n):\n    for j in range(n):\n        quad += 1\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(lin, quad)\n"
          },
          {
            "id": "S04-T4-B-E2",
            "instruction": "E2 (independiente) — `data = [\"r0\", \"r1\", \"r2\"]`. El código usa `for i in range(1, len(data)+1)` y hace IndexError. Arréglalo para recorrer todos los índices válidos e imprimir cada elemento.",
            "hints": [
              "range(len(data)) → 0..n-1",
              "No uses range(1, len+1)."
            ],
            "kind": "independent",
            "tests": "r0 r1 r2",
            "starterCode": "data = [\"r0\", \"r1\", \"r2\"]\nfor i in range(1, len(data) + 1):  # BUG intencional — corrige el off-by-one\n    print(data[i])  # TODO: index error hasta que arregles el range"
          },
          {
            "id": "S04-T4-B-E3",
            "instruction": "E3 (transferencia) — Reescribe un conteo de pares reject-reject O(n²) ingenuo a un conteo O(n) de rejects (no necesitas pares). Imprime n_reject y comenta en un print por qué O(n) basta para la tasa.",
            "hints": [
              "No necesitas combinar pares para la tasa de error.",
              "tasa = n_reject/n"
            ],
            "kind": "transfer",
            "tests": "3 0.6 + nota",
            "starterCode": "sts = [\"reject\", \"accept\", \"reject\", \"reject\", \"accept\"]\n# versión ingenua (no la uses en la solución final):\n# pairs = 0\n# for i in range(len(sts)):\n#     for j in range(i+1, len(sts)):\n#         if sts[i]==\"reject\" and sts[j]==\"reject\": pairs += 1\n# TODO O(n)\n"
          }
        ]
      },
      "youDo": {
        "title": "Client Intake & Data Quality Script (cierre CP-N1-A)",
        "context": "Cierra el gate **CP-N1-A**. Sobre el parser (S02) y el motor de reglas (S03), construyes un procesador por **lotes**: múltiples registros sintéticos, un pase O(n), contadores accept/reject/review, **tasa de error con denominador = n_total** (None si vacío), conservación del **raw** por fila y reporte por stdout. La CLI instalable llega en S10.",
        "objectives": [
          "Procesar ≥3 registros sintéticos en un solo pase",
          "Emitir contadores y tasa_reject con denominador correcto",
          "Conservar el original (raw) de cada registro en el resultado",
          "Reutilizar validación tri-estado por campo (S03)",
          "Demo reproducible con if __name__ == '__main__'"
        ],
        "requirements": [
          "process_batch(records) → summary con n_total, contadores, tasa_reject, results[]",
          "Cada result incluye raw intacto + status agregado + detalle de campos",
          "tasa_reject is None cuando n_total == 0 (sin ZeroDivisionError)",
          "Sin PII real; datos sintéticos embebidos",
          "Sin loops O(n²) innecesarios para el resumen",
          "README o docstring con denominador de tasas explicado en español"
        ],
        "portfolioNote": "En el README muestra una tabla de ejemplo (3 filas), el cálculo de tasa y una captura de la demo stdout. Explica por qué el raw se conserva. Eso es evidencia publicable del gate CP-N1-A.",
        "starterCode": "\"\"\"intake_quality_batch.py — cierre CP-N1-A (S04)\nProcesa múltiples registros sintéticos, resume tasas, conserva raw.\nDatos ficticios únicamente. No uses PII real.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Any\n\n\ndef validate_record(record: dict[str, Any]) -> dict[str, Any]:\n    \"\"\"Reutiliza lógica tipo S03: status global + detalle por campo.\n    Mínimo: edad, region, monto_ingreso con accept|reject|review.\n    \"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef process_batch(records: list[dict[str, Any]]) -> dict[str, Any]:\n    \"\"\"Recorre el lote UNA vez (O(n)).\n    Devuelve {\n      \"n_total\", \"n_accept\", \"n_reject\", \"n_review\",\n      \"tasa_reject\",  # None si n_total==0\n      \"results\": [ {\"raw\": ..., \"status\": ..., \"fields\": ...}, ... ]\n    }\n    Conserva cada raw intacto.\n    \"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef format_report(summary: dict[str, Any]) -> str:\n    \"\"\"Texto stdout legible con contadores y tasa.\"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef _run_tests() -> None:\n    batch = [\n        {\"edad\": 30, \"region\": \"Lima\", \"monto_ingreso\": 0, \"raw_line\": \"30|Lima|0\"},\n        {\"edad\": None, \"region\": \"Lima\", \"monto_ingreso\": 10, \"raw_line\": \"|Lima|10\"},\n        {\"edad\": 15, \"region\": \"Tacna\", \"monto_ingreso\": -1, \"raw_line\": \"15|Tacna|-1\"},\n    ]\n    s = process_batch(batch)\n    assert s[\"n_total\"] == 3\n    assert s[\"results\"][0][\"raw\"][\"raw_line\"] == \"30|Lima|0\"\n    assert s[\"tasa_reject\"] is None or 0 <= s[\"tasa_reject\"] <= 1\n    empty = process_batch([])\n    assert empty[\"tasa_reject\"] is None\n    print(\"tests OK\")\n\n\ndef main() -> None:\n    demo = [\n        {\"edad\": 40, \"region\": \"Cusco\", \"monto_ingreso\": 100, \"raw_line\": \"40|Cusco|100\"},\n        {\"edad\": -3, \"region\": \"Lima\", \"monto_ingreso\": 50, \"raw_line\": \"-3|Lima|50\"},\n    ]\n    summary = process_batch(demo)\n    print(format_report(summary))\n    _run_tests()\n\n\nif __name__ == \"__main__\":\n    main()\n"
      },
      "selfCheck_stems": [
        {
          "question": "¿Qué produce list(range(3))?",
          "options": [
            "[1,2,3]",
            "[0,1,2,3]",
            "[3]",
            "[0,1,2]"
          ]
        },
        {
          "question": "zip([1,2,3],[10,20]) sin strict…",
          "options": [
            "Lanza ValueError",
            "Empareja solo (1,10) y (2,20); el 3 se pierde en silencio",
            "Rellena con None el tercero",
            "Empareja en producto cartesiano"
          ]
        },
        {
          "question": "Para la tasa de reject del gate, el denominador debe ser…",
          "options": [
            "Solo n_accept",
            "Siempre 100",
            "n_total de registros procesados (intentados)",
            "n_review únicamente"
          ]
        },
        {
          "question": "¿Qué hace continue en un for de líneas de intake?",
          "options": [
            "Salta al siguiente ciclo del bucle",
            "Termina todo el programa",
            "Borra la lista",
            "Convierte la línea en None"
          ]
        },
        {
          "question": "Un doble for anidado sobre n elementos es aproximadamente…",
          "options": [
            "O(1)",
            "O(n)",
            "O(log n)",
            "O(n²)"
          ]
        }
      ],
      "taught_text_sha": "a9921abcacbce632",
      "taught_text_len": 24999
    },
    {
      "file": "src/lib/course/sections/s05-oop.ts",
      "id": "oop",
      "index": 5,
      "title": "Funciones, contratos y descomposición",
      "shortTitle": "Funciones & Contratos",
      "tagline": "def, defaults seguros, docstrings, pureza e inicio de normalizadores CP-N1-B",
      "jobRelevance": "Tras cerrar CP-N1-A, el siguiente salto de calidad en data engineering junior es **descomponer** la lógica en funciones con contrato: normalizar nombre, email, teléfono y dirección **sin** mezclar lectura de archivos. En bancos y fintech en Perú, un normalizador no idempotente o con default mutable genera basura silenciosa en el ETL (CP-N1-B). Esta sección inicia ese camino — OOP de dominio llega en S11.",
      "learningOutcomes": [
        "Definir funciones con def, llamarlas y retornar valores (no None accidental)",
        "Usar parámetros posicionales, keyword y defaults seguros (sin mutables)",
        "Documentar pre/postcondiciones con docstrings alineados al código",
        "Anotar type hints graduales y modelar errores de dominio",
        "Descomponer lógica en funciones pequeñas y orquestadores delgados",
        "Distinguir pureza de efectos e inyectar I/O en el borde",
        "Explicar LEGB y escribir closures/factories simples",
        "Fijar ejemplos/asserts y refactorizar sin cambiar conducta"
      ],
      "theory": [
        {
          "subtopicId": "S05-T1-A",
          "heading": "Definición, llamada y retorno",
          "paragraphs": [
            "Una función se define con **`def nombre(params):`** y devuelve con **`return`**. Sin `return` explícito, Python devuelve **`None`**. Llamar es `nombre(args)`. El nombre debe ser un **verbo** o acción clara: `normalize_email`, no `email2`.",
            "Las funciones son valores: puedes pasarlas, guardarlas en listas y devolverlas. En S05 nos basta con **definir, llamar y retornar** resultados de normalización.",
            "Un solo `return` temprano por caso de error de dominio es legible; evita funciones de 100 líneas con muchos returns confusos — mejor descomponer (T3)."
          ],
          "code": "def normalize_nombre(raw: str) -> str:\n    return \" \".join(raw.strip().split())\n\nprint(normalize_nombre(\"  María   José  \"))\nprint(normalize_nombre(\"QUISPE\"))\n# sin return → None\ndef noop(x):\n    x + 1\nprint(noop(1))",
          "code_output": "María José\nQUISPE\nNone"
        },
        {
          "subtopicId": "S05-T1-B",
          "heading": "Posicionales, keyword y defaults seguros",
          "paragraphs": [
            "Argumentos **posicionales** se atan por orden; **keyword** por nombre (`fn(x=1)`). Los **defaults** se evalúan **una vez** en la definición: **nunca uses lista/dict mutable como default** (`def f(xs=[])` es un bug clásico). Usa `None` y crea la lista dentro.",
            "Orden recomendado: obligatorios posicionales, luego opcionales con default. En llamadas, los keyword tras posicionales mejoran la lectura en sites de llamada largos.",
            "Para normalizadores: `def normalize_telefono(raw, *, country=\"PE\")` con keyword-only opcional documenta la política regional sin confundir posiciones."
          ],
          "code": null,
          "code_output": null
        },
        {
          "subtopicId": "S05-T2-A",
          "heading": "Pre/postcondiciones y docstrings",
          "paragraphs": [
            "Una **precondición** es lo que debe cumplirse **antes** de llamar (p. ej. `raw` es str). Una **postcondición** es lo que garantiza el return (p. ej. sin espacios extremos, minúsculas en email local-domain policy).",
            "El **docstring** (PEP 257) documenta contrato en español o inglés consistente del proyecto: qué hace, parámetros, retorno, errores. No copies la firma; explica la política de negocio.",
            "En intake sintético: pre = tipo str o None; post = forma canónica o ValueError/resultado de error de dominio según el diseño elegido."
          ],
          "code": "def normalize_email(raw: str) -> str:\n    \"\"\"Normaliza email sintético de intake.\n\n    Pre: raw es str no vacío tras strip.\n    Post: devuelve lowercased sin espacios extremos.\n    Raises: ValueError si falta '@' o queda vacío.\n    \"\"\"\n    s = raw.strip().lower()\n    if not s or \"@\" not in s:\n        raise ValueError(\"email inválido para normalizar\")\n    return s\n\nprint(normalize_email(\"  Ana.Perez@Example.COM \"))\ntry:\n    normalize_email(\"sin-arroba\")\nexcept ValueError as e:\n    print(\"err:\", e)",
          "code_output": "ana.perez@example.com\nerr: email inválido para normalizar"
        },
        {
          "subtopicId": "S05-T2-B",
          "heading": "Type hints graduales y errores de dominio",
          "paragraphs": [
            "Los **type hints** (`def f(x: str) -> str`) no convierten en runtime (salvo herramientas externas). Son documentación verificable con checkers. En S05 usamos hints **graduales**: anota lo público; no atasques con genéricos avanzados.",
            "Un **error de dominio** no es un bug de Python: es un valor de negocio inválido. Opciones: `raise ValueError`, devolver `(ok, value, error)`, o un dict de resultado. Sé consistente en el módulo.",
            "`Optional[str]` / `str | None` documenta ausencia. No uses hints falsos (`-> str` si puedes devolver None)."
          ],
          "code": "from typing import Optional, Tuple\n\ndef parse_edad(raw: str) -> Tuple[bool, Optional[int], Optional[str]]:\n    try:\n        n = int(raw.strip())\n    except ValueError:\n        return False, None, \"no es entero\"\n    if n < 0 or n > 120:\n        return False, None, \"fuera de rango de dominio\"\n    return True, n, None\n\nfor v in [\"34\", \"abc\", \"200\"]:\n    print(v, \"→\", parse_edad(v))",
          "code_output": "34 → (True, 34, None)\nabc → (False, None, 'no es entero')\n200 → (False, None, 'fuera de rango de dominio')"
        },
        {
          "subtopicId": "S05-T3-A",
          "heading": "Funciones pequeñas y composición",
          "paragraphs": [
            "Una función debe hacer **una cosa** en el nivel de abstracción correcto. Si normalizas nombre y además escribes archivo y logueas, sepáralas. **Componer** es llamar funciones pequeñas desde una orquestadora delgada.",
            "Beneficio: tests unitarios fáciles, reuso en CLI (S10) y en ETL (S08). El orquestador `normalize_record` llama a cuatro normalizadores y arma el dict.",
            "Regla práctica: si necesitas un comentario de sección en medio de la función, probablemente es otra función."
          ],
          "code": "def strip_collapse(s: str) -> str:\n    return \" \".join(s.strip().split())\n\ndef title_case_name(s: str) -> str:\n    return strip_collapse(s).title()\n\ndef normalize_nombre(raw: str) -> str:\n    return title_case_name(raw)\n\ndef normalize_record(nombres: str, email: str) -> dict:\n    return {\n        \"nombres\": normalize_nombre(nombres),\n        \"email\": email.strip().lower(),\n    }\n\nprint(normalize_record(\"  maría  josé \", \"  X@Y.COM \"))",
          "code_output": "{'nombres': 'María José', 'email': 'x@y.com'}"
        },
        {
          "subtopicId": "S05-T3-B",
          "heading": "Pureza, efectos e inyección de I/O",
          "paragraphs": [
            "Una función **pura** devuelve el mismo resultado para los mismos argumentos y **no tiene efectos** (no imprime, no lee disco, no muta globales ni los argumentos mutables del caller sin documentarlo).",
            "La **I/O** (stdin, archivos, red) se queda en el borde: `main`, CLI, o funciones `load_*` / `save_*`. Los normalizadores del gate deben ser puros e **idempotentes**: `f(f(x)) == f(x)` para entradas válidas.",
            "**Inyección**: pasar una función `reader` o un path como argumento en vez de hardcodear `open('data.csv')` dentro del core facilita tests con fakes.",
            "**`lambda`**: una función anónima de una expresión. Sintaxis: `lambda args: expresión`. Ejemplo: `normalize_email = lambda s: s.strip().lower()` es equivalente a un `def` de una línea. Úsala para inyectar un normalizador rápido en tests o demos; si la lógica crece, prefiera un `def` con nombre. No es un reemplazo general de funciones: no admite múltiples statements ni es más rápida."
          ],
          "code": "def normalize_telefono(raw: str) -> str:\n    digits = \"\".join(ch for ch in raw if ch.isdigit())\n    return digits\n\n# Idempotencia\nx = \"999-000-111\"\ny = normalize_telefono(x)\nz = normalize_telefono(y)\nprint(y, z, \"idempotent=\", y == z)\n\n# I/O al borde (simulado) + lambda como inyección puntual\ndef process_line(line: str, norm=normalize_telefono) -> str:\n    return norm(line)\n\nprint(process_line(\" (01) 234-5678 \"))\nprint(process_line(\" A@B.COM \", norm=lambda s: s.strip().lower()))",
          "code_output": "999000111 999000111 idempotent= True\n012345678"
        },
        {
          "subtopicId": "S05-T4-A",
          "heading": "LEGB y closures básicos",
          "paragraphs": [
            "**LEGB**: orden de búsqueda de nombres — **L**ocal, **E**nclosing (funciones anidadas), **G**lobal, **B**uiltin. Si Python no halla el nombre, `NameError`.",
            "Un **closure** es una función interna que recuerda variables del enclosing scope. Útil para fabricar normalizadores configurados (`make_stripper(chars)`), sin clases todavía.",
            "`global` y `nonlocal` existen pero en S05 casi no los necesitas: prefiere **return** de valores nuevos. Mutar globales complica tests."
          ],
          "code": null,
          "code_output": null
        },
        {
          "subtopicId": "S05-T4-B",
          "heading": "Pruebas de ejemplo y refactor sin cambiar conducta",
          "paragraphs": [
            "Antes de refactorizar, fija **ejemplos ejecutables**: `assert normalize_email('A@B.COM') == 'a@b.com'`. Luego cambia la forma interna; si los asserts siguen verdes, la conducta se preservó.",
            "El refactor típico de S05: extraer `strip_collapse`, unificar defaults, renombrar. **No** cambies la política de negocio “de paso” sin actualizar tests y docstring.",
            "Idempotencia se prueba con un loop o doble llamada. Fronteras: vacío, solo espacios, Unicode (`Ñ`, tildes), None si el contrato lo admite."
          ],
          "code": "def normalize_email(raw: str) -> str:\n    return raw.strip().lower()\n\ndef _examples() -> None:\n    assert normalize_email(\"  A@B.COM \") == \"a@b.com\"\n    assert normalize_email(normalize_email(\"A@B.COM\")) == \"a@b.com\"\n    print(\"examples OK\")\n\n# refactor interno: misma conducta\ndef normalize_email(raw: str) -> str:\n    s = raw.strip()\n    return s.lower()\n\n_examples()\nprint(normalize_email(\"Ana@Example.COM\"))",
          "code_output": "examples OK\nana@example.com"
        }
      ],
      "iDo": {
        "steps": [
          {
            "demoId": "S05-T1-A-DEMO",
            "subtopicId": "S05-T1-A",
            "environment": "browser-pyodide",
            "description": "def + return de normalize_nombre collapsando espacios",
            "why": "Una función, un return, casos con espacios y Unicode simple.",
            "code": "def normalize_nombre(raw):\n    return \" \".join(raw.strip().split())\n\nfor s in [\"  Ana  \", \"María  José\", \"QUISPE\"]:\n    print(repr(s), \"→\", repr(normalize_nombre(s)))",
            "language": "python",
            "output": "'  Ana  ' → 'Ana'\n'María  José' → 'María José'\n'QUISPE' → 'QUISPE'"
          },
          {
            "demoId": "S05-T1-B-DEMO",
            "subtopicId": "S05-T1-B",
            "environment": "browser-pyodide",
            "description": "Defaults seguros vs default mutable bug",
            "why": "good crea listas nuevas; bad comparte el default entre llamadas.",
            "code": "def good(item, bucket=None):\n    if bucket is None:\n        bucket = []\n    bucket.append(item)\n    return bucket\n\ndef bad(item, bucket=[]):\n    bucket.append(item)\n    return bucket\n\nprint(\"good\", good(1), good(2))\nprint(\"bad \", bad(1), bad(2))",
            "language": "python",
            "output": "good [1] [2]\nbad  [1, 2] [1, 2]"
          },
          {
            "demoId": "S05-T2-A-DEMO",
            "subtopicId": "S05-T2-A",
            "environment": "browser-pyodide",
            "description": "Docstring + pre/post en normalize_email",
            "why": "El contrato está en el docstring y se enforce con raise.",
            "code": "def normalize_email(raw: str) -> str:\n    \"\"\"Pre: str. Post: lower strip con @. Raises ValueError.\"\"\"\n    s = raw.strip().lower()\n    if \"@\" not in s:\n        raise ValueError(\"falta @\")\n    return s\n\nprint(normalize_email(\"  X@Y.COM \"))\ntry:\n    normalize_email(\"x\")\nexcept ValueError as e:\n    print(\"ValueError\", e)",
            "language": "python",
            "output": "x@y.com\nValueError falta @"
          },
          {
            "demoId": "S05-T2-B-DEMO",
            "subtopicId": "S05-T2-B",
            "environment": "browser-pyodide",
            "description": "Type hints + resultado de dominio sin abortar el lote",
            "why": "Hints documentan la tupla; el lote puede seguir tras un False.",
            "code": "from typing import Optional, Tuple\n\ndef norm_tel(raw: str) -> Tuple[bool, Optional[str], Optional[str]]:\n    d = \"\".join(c for c in raw if c.isdigit())\n    if len(d) != 9:\n        return False, None, \"se esperan 9 dígitos\"\n    return True, d, None\n\nfor v in [\"999000111\", \"123\", \"999-000-111\"]:\n    print(v, norm_tel(v))",
            "language": "python",
            "output": "999000111 (True, '999000111', None)\n123 (False, None, 'se esperan 9 dígitos')\n999-000-111 (True, '999000111', None)"
          },
          {
            "demoId": "S05-T3-A-DEMO",
            "subtopicId": "S05-T3-A",
            "environment": "browser-pyodide",
            "description": "Componer strip + lower + orquestador de registro",
            "why": "Orquestador delgado reutiliza piezas pequeñas.",
            "code": "def strip_collapse(s: str) -> str:\n    return \" \".join(s.strip().split())\n\ndef norm_email(s: str) -> str:\n    return s.strip().lower()\n\ndef normalize_pair(nombre: str, email: str) -> dict:\n    return {\"nombre\": strip_collapse(nombre).title(), \"email\": norm_email(email)}\n\nprint(normalize_pair(\"  ana  perez \", \"  Ana@Example.COM \"))",
            "language": "python",
            "output": "{'nombre': 'Ana Perez', 'email': 'ana@example.com'}"
          },
          {
            "demoId": "S05-T3-B-DEMO",
            "subtopicId": "S05-T3-B",
            "environment": "browser-pyodide",
            "description": "Idempotencia de normalize_telefono puro",
            "why": "f(f(x))==f(x) en los tres samples; sin I/O en la función.",
            "code": "def normalize_telefono(raw: str) -> str:\n    return \"\".join(c for c in raw if c.isdigit())\n\nsamples = [\"999-000-111\", \"(999) 000 111\", \"999000111\"]\nfor s in samples:\n    once = normalize_telefono(s)\n    twice = normalize_telefono(once)\n    print(s, \"→\", once, \"idem=\", once == twice)",
            "language": "python",
            "output": "999-000-111 → 999000111 idem= True\n(999) 000 111 → 999000111 idem= True\n999000111 → 999000111 idem= True"
          },
          {
            "demoId": "S05-T4-A-DEMO",
            "subtopicId": "S05-T4-A",
            "environment": "browser-pyodide",
            "description": "Closure factory para prefijo de teléfono",
            "why": "La interna recuerda prefix del enclosing scope sin global.",
            "code": "def make_norm(prefix: str):\n    def norm(raw: str) -> str:\n        d = \"\".join(c for c in raw if c.isdigit())\n        return prefix + d\n    return norm\n\npe = make_norm(\"+51\")\nprint(pe(\"999000111\"))\nprint(pe(\"999-000-111\"))",
            "language": "python",
            "output": "+51999000111\n+51999000111"
          },
          {
            "demoId": "S05-T4-B-DEMO",
            "subtopicId": "S05-T4-B",
            "environment": "browser-pyodide",
            "description": "Ejemplos assert antes y después de micro-refactor",
            "why": "Misma suite de ejemplos en verde tras cambiar la implementación.",
            "code": "def normalize_direccion(raw: str) -> str:\n    return \" \".join(raw.strip().split()).upper()\n\ndef examples(fn):\n    assert fn(\"  av. larco 123 \") == \"AV. LARCO 123\"\n    assert fn(fn(\"Calle 1\")) == fn(\"Calle 1\")\n    return True\n\nassert examples(normalize_direccion)\n\ndef normalize_direccion(raw: str) -> str:\n    parts = raw.strip().split()\n    return \" \".join(parts).upper()\n\nassert examples(normalize_direccion)\nprint(\"refactor OK\", normalize_direccion(\"  jr. unión 5 \"))",
            "language": "python",
            "output": "refactor OK JR. UNIÓN 5"
          }
        ]
      },
      "weDo": {
        "exercises": [
          {
            "id": "S05-T1-A-E1",
            "instruction": "E1 (guiado) — Escribe `def doble(n):` que retorne `n*2`. Imprime doble(21).",
            "hints": [
              "return n * 2",
              "No uses print dentro de doble."
            ],
            "kind": "guided",
            "tests": "42",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\ndef doble(n):\n    return n * 2\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(doble(21))\n"
          },
          {
            "id": "S05-T1-A-E2",
            "instruction": "E2 (independiente) — `normalize_nombre(raw)` colapsa espacios internos y strip. Prueba con `'  Juan   Pérez '`.",
            "hints": [
              "\" \".join(raw.strip().split())",
              "title() es opcional; aquí solo colapsar."
            ],
            "kind": "independent",
            "tests": "Juan Pérez",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\ndef normalize_nombre(raw):\n    return \" \".join(raw.strip().split())\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(normalize_nombre('  Juan   Pérez '))\n"
          },
          {
            "id": "S05-T1-A-E3",
            "instruction": "E3 (transferencia) — Función `saluda(nombre)` que retorne el string; en main imprime el return. Demuestra que sin return print(saluda(...)) muestra None si olvidas return (arregla para que no sea None).",
            "hints": [
              "Si ves None, falta return.",
              "return f'Hola {nombre}'"
            ],
            "kind": "transfer",
            "tests": "Hola Ana",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\ndef saluda(nombre):\n    return f'Hola {nombre}'\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(saluda('Ana'))\n"
          },
          {
            "id": "S05-T1-B-E1",
            "instruction": "E1 (guiado) — `def present(nombre, titulo='Cliente'):` retorna `f'{titulo}: {nombre}'`. Llama posicional y con keyword titulo=.",
            "hints": [
              "default solo se usa si omites el arg",
              "Dos prints distintos"
            ],
            "kind": "guided",
            "tests": "Cliente: / VIP:",
            "starterCode": "def present(nombre, titulo='Cliente'):\n    # TODO\n    ...\nprint(present('Quispe'))\nprint(present('Quispe', titulo='VIP'))"
          },
          {
            "id": "S05-T1-B-E2",
            "instruction": "E2 (independiente) — Reescribe un `bad_add` con default mutable a la versión segura con None. Muestra dos llamadas good(1), good(2) independientes.",
            "hints": [
              "if bucket is None: bucket = []",
              "No deben compartir lista."
            ],
            "kind": "independent",
            "tests": "[1] luego [2]",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\ndef good_add(item, bucket=None):\n    if bucket is None:\n        bucket = []\n    bucket.append(item)\n    return bucket\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(good_add(1))\n"
          },
          {
            "id": "S05-T1-B-E3",
            "instruction": "E3 (transferencia) — `normalize_telefono(raw, *, digits_only=True)` keyword-only. Si digits_only, deja solo dígitos; si False, solo strip. Demuestra ambas llamadas por keyword.",
            "hints": [
              "El * fuerza keyword para digits_only.",
              "normalize_telefono(' 999-1 ', digits_only=False)"
            ],
            "kind": "transfer",
            "tests": "999000 y 999-000",
            "starterCode": "def normalize_telefono(raw, *, digits_only=True):\n    # TODO\n    ...\nprint(normalize_telefono(' 999-000 '))\nprint(normalize_telefono(' 999-000 ', digits_only=False))"
          },
          {
            "id": "S05-T2-A-E1",
            "instruction": "E1 (guiado) — Añade docstring de una línea a `def area(w, h): return w*h` e imprime `area.__doc__`.",
            "hints": [
              "Triple comillas justo bajo def",
              "Docstring no es un comentario #"
            ],
            "kind": "guided",
            "tests": "doc + 12",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\ndef area(w, h):\n    \"\"\"Retorna el área de un rectángulo w×h.\"\"\"\n    return w * h\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(area.__doc__)\n"
          },
          {
            "id": "S05-T2-A-E2",
            "instruction": "E2 (independiente) — `normalize_email` con pre/post en docstring: strip+lower; ValueError si no hay @. Prueba OK y error.",
            "hints": [
              "if '@' not in s: raise ValueError",
              "Mensaje accionable en español."
            ],
            "kind": "independent",
            "tests": "a@b.com + err",
            "starterCode": "def normalize_email(raw: str) -> str:\n    \"\"\"TODO pre/post\"\"\"\n    # TODO\n    ...\nprint(normalize_email('  A@B.COM '))\ntry:\n    normalize_email('x')\nexcept ValueError as e:\n    print('err', e)"
          },
          {
            "id": "S05-T2-A-E3",
            "instruction": "E3 (transferencia) — Documenta y escribe `normalize_nombre` con postcondición: no empiezan/terminan espacios y no hay dobles espacios. Assert de la postcondición en un ejemplo.",
            "hints": [
              "assert result == result.strip() and '  ' not in result",
              "La post se puede chequear en tests."
            ],
            "kind": "transfer",
            "tests": "Ana María + post OK",
            "starterCode": "def normalize_nombre(raw: str) -> str:\n    \"\"\"Post: sin extremos ni dobles espacios.\"\"\"\n    # TODO\n    ...\nr = normalize_nombre('  Ana  María  ')\nprint(r)\nassert r == r.strip() and '  ' not in r\nprint('post OK')"
          },
          {
            "id": "S05-T2-B-E1",
            "instruction": "E1 (guiado) — Anota `def len_safe(s: str) -> int` y retorna len. Imprime el resultado y un comentario mental de que el hint no valida en runtime.",
            "hints": [
              "hints no ejecutan isinstance mágicamente",
              "len_safe(123) fallaría en runtime igual"
            ],
            "kind": "guided",
            "tests": "3 + nota",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\ndef len_safe(s: str) -> int:\n    return len(s)\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(len_safe('abc'))\n"
          },
          {
            "id": "S05-T2-B-E2",
            "instruction": "E2 (independiente) — `parse_monto(raw: str) -> tuple` (ok, value, err). Acepta enteros >=0; error de dominio si negativo o no int.",
            "hints": [
              "try int; if n<0 dominio",
              "0 es válido."
            ],
            "kind": "independent",
            "tests": "4 casos",
            "starterCode": "from typing import Optional, Tuple\n\ndef parse_monto(raw: str) -> Tuple[bool, Optional[int], Optional[str]]:\n    # TODO\n    ...\nfor v in ['0', '10', '-1', 'x']:\n    print(v, parse_monto(v))"
          },
          {
            "id": "S05-T2-B-E3",
            "instruction": "E3 (transferencia) — Elige raise ValueError **o** tupla para `normalize_email` y documenta por qué en un print. Implementa la opción raise y captura en el caller del lote (for de 2 emails).",
            "hints": [
              "Lote: try/except por fila para no abortar todo",
              "Una fila mala no impide la buena"
            ],
            "kind": "transfer",
            "tests": "OK + SKIP",
            "starterCode": "def normalize_email(raw: str) -> str:\n    # TODO raise\n    ...\nfor e in ['ok@ex.com', 'malo']:\n    # TODO try/except print"
          },
          {
            "id": "S05-T3-A-E1",
            "instruction": "E1 (guiado) — Extrae `strip_collapse` y úsalo dentro de `normalize_nombre` que además hace .title().",
            "hints": [
              "def strip_collapse; return strip_collapse(raw).title()",
              "Composición de dos pasos."
            ],
            "kind": "guided",
            "tests": "Ana María",
            "starterCode": "def strip_collapse(s):\n    # TODO\n    ...\ndef normalize_nombre(raw):\n    # TODO\n    ...\nprint(normalize_nombre('  ana  maría '))"
          },
          {
            "id": "S05-T3-A-E2",
            "instruction": "E2 (independiente) — Orquestador `normalize_contact(nombre, email)` devuelve dict usando dos helpers.",
            "hints": [
              "return {'nombre': ..., 'email': ...}",
              "email lower strip"
            ],
            "kind": "independent",
            "tests": "Luis + l@e.com",
            "starterCode": "def norm_n(s):\n    return ' '.join(s.strip().split()).title()\ndef norm_e(s):\n    return s.strip().lower()\ndef normalize_contact(nombre, email):\n    # TODO\n    ...\nprint(normalize_contact('  luis ', '  L@E.COM '))"
          },
          {
            "id": "S05-T3-A-E3",
            "instruction": "E3 (transferencia) — Parte una función monstruo de 10 líneas (nombre+email+tel en un solo def con comentarios) en 3 funciones + orquestador. Misma salida.",
            "hints": [
              "Cada campo = una función",
              "Salida dict con 3 claves"
            ],
            "kind": "transfer",
            "tests": "3 keys normalizadas",
            "starterCode": "# monstruo a reemplazar conceptualmente\ndef normalize_all(n, e, t):\n    # TODO descomponer\n    ...\nprint(normalize_all('  Ana ', 'A@B.COM', '999-1'))"
          },
          {
            "id": "S05-T3-B-E1",
            "instruction": "E1 (guiado) — Escribe `normalize_tel` puro (solo dígitos). Demuestra idempotencia con dos llamadas encadenadas.",
            "hints": [
              "once = f(x); twice = f(once); print once==twice",
              "Sin print dentro de normalize_tel."
            ],
            "kind": "guided",
            "tests": "999000 True",
            "starterCode": "def normalize_tel(raw):\n    # TODO\n    ...\nx = '999-000'\nonce = normalize_tel(x)\nprint(once, normalize_tel(once) == once)"
          },
          {
            "id": "S05-T3-B-E2",
            "instruction": "E2 (independiente) — Separa: `normalize_email` puro + `print_report(email)` con efecto de print. No imprimas dentro del normalizador.",
            "hints": [
              "print solo en print_report",
              "Core testeable sin capturar stdout"
            ],
            "kind": "independent",
            "tests": "email= z@w.com",
            "starterCode": "def normalize_email(raw):\n    # TODO puro\n    ...\ndef print_report(raw):\n    # TODO efecto\n    ...\nprint_report('  Z@W.COM ')"
          },
          {
            "id": "S05-T3-B-E3",
            "instruction": "E3 (transferencia) — Inyecta un normalizador: `process(line, norm=normalize_tel)` usa la fn inyectada. Llama con default y con un norm alternativo que deja todo upper strip.",
            "hints": [
              "norm es parámetro con default",
              "Demuestra dos comportamientos"
            ],
            "kind": "transfer",
            "tests": "999 y 999-A",
            "starterCode": "def normalize_tel(raw):\n    return ''.join(c for c in raw if c.isdigit())\ndef process(line, norm=normalize_tel):\n    # TODO\n    ...\nprint(process(' 999-a '))\nprint(process(' 999-a ', norm=lambda s: s.strip().upper()))"
          },
          {
            "id": "S05-T4-A-E1",
            "instruction": "E1 (guiado) — Predice LEGB: global x=1; dentro de f, x=2 local; print dentro y fuera.",
            "hints": [
              "Local no pisa global sin global keyword",
              "Fuera sigue 1"
            ],
            "kind": "guided",
            "tests": "in 2 out 1",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nx = 1\ndef f():\n    x = 2\nf()\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print('in', x)\n"
          },
          {
            "id": "S05-T4-A-E2",
            "instruction": "E2 (independiente) — `make_multiplier(k)` devuelve función que multiplica por k (closure). Crea *3 y *10.",
            "hints": [
              "def inner(n): return n*k; return inner",
              "print(m3(4), m10(4))"
            ],
            "kind": "independent",
            "tests": "12 40",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\ndef make_multiplier(k):\n    def inner(n):\n        return n * k\n    return inner\nm3 = make_multiplier(3)\nm10 = make_multiplier(10)\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(m3(4), m10(4))\n"
          },
          {
            "id": "S05-T4-A-E3",
            "instruction": "E3 (transferencia) — Factory `make_normalizer(mode)` donde mode 'digits' o 'lower' devuelve la fn adecuada. Úsala en dos prints.",
            "hints": [
              "if mode=='digits': return lambda o def digits...",
              "Sin global mode"
            ],
            "kind": "transfer",
            "tests": "12 hola",
            "starterCode": "def make_normalizer(mode):\n    # TODO\n    ...\nd = make_normalizer('digits')\nlo = make_normalizer('lower')\nprint(d('A-1-B-2'), lo('  Hola '))"
          },
          {
            "id": "S05-T4-B-E1",
            "instruction": "E1 (guiado) — Escribe 2 asserts de ejemplo para `normalize_email = lambda s: s.strip().lower()` e imprime OK.",
            "hints": [
              "assert normalize_email('A@B.COM')=='a@b.com'",
              "Incluye idempotencia"
            ],
            "kind": "guided",
            "tests": "OK",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\ndef normalize_email(s):\n    return s.strip().lower()\nassert normalize_email('  A@B.COM ') == 'a@b.com'\nassert normalize_email(normalize_email('A@B.COM')) == 'a@b.com'\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print('OK')\n"
          },
          {
            "id": "S05-T4-B-E2",
            "instruction": "E2 (independiente) — Refactoriza `normalize_dir` de una línea densa a dos pasos (strip_collapse + upper) sin romper asserts dados.",
            "hints": [
              "Corre asserts antes y después mentalmente",
              "Misma salida"
            ],
            "kind": "independent",
            "tests": "AV 1 / JR 2",
            "starterCode": "def normalize_dir(raw):\n    return ' '.join(raw.strip().split()).upper()\nassert normalize_dir('  av 1 ') == 'AV 1'\nassert normalize_dir(normalize_dir('x')) == normalize_dir('x')\n# TODO reescribir cuerpo\ndef normalize_dir(raw):\n    # TODO\n    ...\nassert normalize_dir('  av 1 ') == 'AV 1'\nprint(normalize_dir(' jr 2 '))"
          },
          {
            "id": "S05-T4-B-E3",
            "instruction": "E3 (transferencia) — Suite mínima: lista de (input, expected) para normalize_nombre; loop assert. Luego cambia implementación a title-case y actualiza expected si la política cambia conscientemente — aquí **mantén** collapsar sin title y deja expected estables.",
            "hints": [
              "No cambies política sin actualizar tests",
              "Todos PASS"
            ],
            "kind": "transfer",
            "tests": "PASS lines",
            "starterCode": "def normalize_nombre(raw):\n    return ' '.join(raw.strip().split())\ncases = [('  a  b ', 'a b'), ('X', 'X')]\n# TODO loop\nprint('all PASS')"
          }
        ]
      },
      "youDo": {
        "title": "Normalizadores puros (inicio CP-N1-B)",
        "context": "Inicias **CP-N1-B** con el núcleo reutilizable: `normalize_nombre`, `normalize_email`, `normalize_telefono`, `normalize_direccion` como funciones **puras**, con docstring, hints graduales e **idempotencia** demostrada. Sin pathlib CSV todavía (S08) y sin clases de dominio (S11). Solo datos sintéticos.",
        "objectives": [
          "Implementar 4 normalizadores puros + orquestador normalize_record",
          "Demostrar idempotencia f(f(x))==f(x) en cada uno",
          "Docstrings con pre/post; ValueError o política explícita en email",
          "Sin I/O ni prints dentro del core",
          "Suite de ejemplos/asserts ejecutable en __main__"
        ],
        "requirements": [
          "normalize_nombre colapsa espacios y aplica title de palabras",
          "normalize_email: strip+lower; error si no hay @",
          "normalize_telefono: solo dígitos (política de demo)",
          "normalize_direccion: colapsa + upper determinista",
          "is_idempotent helper o asserts equivalentes",
          "Datos sintéticos; sin PII real"
        ],
        "portfolioNote": "Documenta en español la política de cada normalizador y pega la salida de la suite. Menciona que la I/O de archivos llegará en S08; aquí solo el core puro.",
        "starterCode": "\"\"\"normalizers_pure.py — inicio CP-N1-B (S05)\nNormalizadores puros de nombre, email, teléfono y dirección.\nIdempotencia demostrada. Sin I/O en el core.\nDatos sintéticos únicamente.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Callable\n\n\ndef normalize_nombre(raw: str) -> str:\n    \"\"\"Colapsa espacios; title-case de palabras.\n    Pre: str. Post: sin extremos ni dobles espacios.\n    \"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef normalize_email(raw: str) -> str:\n    \"\"\"strip + lower. ValueError si falta @.\n    \"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef normalize_telefono(raw: str) -> str:\n    \"\"\"Solo dígitos (política PE sintética de demo).\n    \"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef normalize_direccion(raw: str) -> str:\n    \"\"\"Colapsa espacios; upper para demo determinista.\n    \"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef normalize_record(nombres: str, email: str, telefono: str, direccion: str) -> dict:\n    \"\"\"Orquestador delgado — solo llama normalizadores puros.\"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef is_idempotent(fn: Callable[[str], str], sample: str) -> bool:\n    once = fn(sample)\n    return fn(once) == once\n\n\ndef _run_tests() -> None:\n    assert normalize_nombre(\"  maría  josé \") == \"María José\"\n    assert is_idempotent(normalize_nombre, \"  ana  \")\n    assert normalize_email(\"  A@B.COM \") == \"a@b.com\"\n    assert is_idempotent(normalize_email, \"A@B.COM\")\n    assert normalize_telefono(\"999-000-111\") == \"999000111\"\n    assert is_idempotent(normalize_telefono, \"999-000-111\")\n    assert normalize_direccion(\"  av. larco 100 \") == \"AV. LARCO 100\"\n    r = normalize_record(\"  luis \", \"L@E.COM\", \"(999)111222\", \" jr unión 1 \")\n    assert set(r) >= {\"nombres\", \"email\", \"telefono\", \"direccion\"}\n    print(\"tests OK\")\n\n\ndef main() -> None:\n    print(normalize_record(\n        \"  Ana  Pérez \",\n        \"  Ana.Perez@Example.COM \",\n        \"999-000-111\",\n        \"  Av. Larco 123 \",\n    ))\n    _run_tests()\n\n\nif __name__ == \"__main__\":\n    main()\n"
      },
      "selfCheck_stems": [
        {
          "question": "Si una función no tiene return, ¿qué devuelve la llamada?",
          "options": [
            "None",
            "0",
            "False",
            "Error siempre"
          ]
        },
        {
          "question": "¿Por qué `def f(xs=[])` es peligroso?",
          "options": [
            "Python no permite defaults",
            "Solo falla con type hints",
            "El default mutable se comparte entre llamadas",
            "Convierte xs en tupla"
          ]
        },
        {
          "question": "Una función pura…",
          "options": [
            "Siempre imprime el resultado",
            "Lee un archivo de config global",
            "Solo puede usarse en clases",
            "Mismo input → mismo output, sin efectos colaterales"
          ]
        },
        {
          "question": "LEGB significa…",
          "options": [
            "List, Else, Generator, Break",
            "Local, Enclosing, Global, Builtin",
            "Loop, Eval, Global, Binary",
            "Lambda, Except, Goto, Block"
          ]
        },
        {
          "question": "Idempotencia de un normalizador f significa…",
          "options": [
            "f(f(x)) == f(x) para entradas del dominio",
            "f se ejecuta solo una vez en la vida del proceso",
            "f no puede tener defaults",
            "f siempre lanza ValueError"
          ]
        }
      ],
      "taught_text_sha": "bdc374d0c4f7f2ec",
      "taught_text_len": 21437
    },
    {
      "file": "src/lib/course/sections/s06-numpy.ts",
      "id": "numpy",
      "index": 6,
      "title": "Colecciones y estructuras de datos",
      "shortTitle": "Colecciones",
      "tagline": "listas, dicts, sets y estructuras anidadas para modelo en memoria",
      "jobRelevance": "En pipelines de onboarding y calidad de datos en bancos, fintech y retail en Perú, antes de CSV/JSON necesitas un **modelo tabular en memoria**: clientes, contactos y transacciones como list/dict/set bien elegidos, con deduplicación que **reporta conflictos** y salidas **deterministas**. Esta sección (id de plataforma `numpy` conservado) retematiza a colecciones V3 e inicia el bloque **CP-N1-B** sin NumPy/pandas.",
      "learningOutcomes": [
        "Usar list/tuple y slicing para ventanas de registros sin copiar de más",
        "Desempaquetar secuencias y distinguir alias vs copia superficial/profunda",
        "Modelar registros con dict, get e índices id→fila",
        "Deduplicar con set y reportar conflictos sin borrarlos",
        "Navegar list[dict] anidados cliente→contactos→txs",
        "Acceder campos opcionales sin KeyError; missing vs vacío",
        "Ordenar con sorted(..., key=) de forma estable",
        "Elegir list/dict/set y producir JSON determinista"
      ],
      "theory": [
        {
          "subtopicId": "S06-T1-A",
          "heading": "Listas, tuplas y slicing",
          "paragraphs": [
            "Una **list** es mutable y ordenada: ideal para filas que crecen (`append`, `extend`). Una **tuple** es inmutable: ideal para **claves estables**, headers fijos o “contratos” de columnas que no deben mutarse por accidente.",
            "El **slicing** `seq[i:j:k]` produce una **ventana** sin mutar el original (en listas/tuplas crea una nueva secuencia). `txs[-3:]` son las últimas tres transacciones. El stop es exclusivo, igual que en `range`.",
            "Membership `x in seq` es O(n) en listas: útil para lotes pequeños de demo; para lookups masivos preferirás **set/dict** en T2."
          ],
          "code": "txs = [\n    {\"id\": \"T1\", \"monto\": 10},\n    {\"id\": \"T2\", \"monto\": 25},\n    {\"id\": \"T3\", \"monto\": 7},\n    {\"id\": \"T4\", \"monto\": 40},\n]\nventana = txs[-3:]\nkeys = (\"id\", \"monto\")  # contrato estable\nprint(\"ventana ids:\", [r[\"id\"] for r in ventana])\nprint(\"keys:\", keys)\nprint(\"T2 in slice?\", any(r[\"id\"] == \"T2\" for r in ventana))",
          "code_output": "ventana ids: ['T2', 'T3', 'T4']\nkeys: ('id', 'monto')\nT2 in slice? True"
        },
        {
          "subtopicId": "S06-T1-B",
          "heading": "Unpacking, aliasing y copia",
          "paragraphs": [
            "**Unpacking** `a, b = fila` o `head, *rest = fila` desempaqueta sin índices ruidosos. Falla si el largo no calza: eso es bueno (detecta shape roto).",
            "**Aliasing**: `b = a` no copia; ambas variables apuntan al **mismo** objeto. Si `a` es una lista de dicts y mutas `b[0]['x']`, también cambia `a[0]`. Ese bug aparece al “clonar” clientes en memoria.",
            "`list.copy()` / `seq[:]` hacen **copia superficial**. Para dicts anidados necesitas `copy.deepcopy` o reconstruir. En intake, shallow basta si solo reordenas filas sin mutar campos compartidos."
          ],
          "code": "import copy\nclientes = [{\"id\": \"C001\", \"tags\": [\"vip\"]}]\nalias = clientes\nshallow = clientes.copy()\ndeep = copy.deepcopy(clientes)\nalias[0][\"tags\"].append(\"alias\")\nprint(\"original tras alias:\", clientes)\nshallow[0][\"tags\"].append(\"shallow\")\nprint(\"original tras shallow mut de tags:\", clientes)\ndeep[0][\"tags\"].append(\"solo-deep\")\nprint(\"deep aislado:\", deep)\nprint(\"original final:\", clientes)",
          "code_output": "original tras alias: [{'id': 'C001', 'tags': ['vip', 'alias']}]\noriginal tras shallow mut de tags: [{'id': 'C001', 'tags': ['vip', 'alias', 'shallow']}]\ndeep aislado: [{'id': 'C001', 'tags': ['vip', 'solo-deep']}]\noriginal final: [{'id': 'C001', 'tags': ['vip', 'alias', 'shallow']}]"
        },
        {
          "subtopicId": "S06-T2-A",
          "heading": "Diccionarios y pertenencia",
          "paragraphs": [
            "Un **dict** modela registros y **índices** `id → cliente`. Lookup promedio O(1). Construye índices con `{c['id']: c for c in filas}` cuando harás muchos accesos por clave.",
            "`d.get(k)` o `d.get(k, default)` evita **KeyError** en campos opcionales. `k in d` prueba pertenencia de **clave**, no de valor.",
            "`update` fusiona configs: el segundo dict **pisa** claves del primero. Documenta la precedencia (env > defaults) para no “pisar sin querer” políticas de normalización."
          ],
          "code": "filas = [\n    {\"id\": \"C001\", \"region\": \"Lima\"},\n    {\"id\": \"C002\", \"region\": \"Cusco\"},\n]\nidx = {c[\"id\"]: c for c in filas}\nprint(\"lookup C002:\", idx[\"C002\"][\"region\"])\nprint(\"get missing:\", idx.get(\"C999\", {}).get(\"region\", \"N/A\"))\nbase = {\"timeout\": 30, \"retry\": 1}\noverride = {\"retry\": 3}\nmerged = {**base, **override}\nprint(\"merged:\", merged)",
          "code_output": "lookup C002: Cusco\nget missing: N/A\nmerged: {'timeout': 30, 'retry': 3}"
        },
        {
          "subtopicId": "S06-T2-B",
          "heading": "Deduplicación y operaciones de set",
          "paragraphs": [
            "Un **set** guarda elementos únicos (hashables). Ideal para **ids/emails** deduplicados y para **unión/intersección/diferencia** de cohortes de dos lotes.",
            "Deduplicar **no es borrar a ciegas** cuando hay conflicto de negocio: dos filas con mismo `id` pero montos distintos deben **reportarse**, no silenciarse. El patrón es `unique` + `conflicts`.",
            "Sets no conservan orden de inserción de forma que debas depender para exports estables en todos los contextos pedagógicos: ordena con `sorted(...)` al exportar."
          ],
          "code": "lote_a = {\"C001\", \"C002\", \"C003\"}\nlote_b = {\"C002\", \"C003\", \"C004\"}\nprint(\"intersección:\", sorted(lote_a & lote_b))\nprint(\"solo A:\", sorted(lote_a - lote_b))\nemails = [\"a@ex.com\", \"b@ex.com\", \"a@ex.com\"]\nprint(\"únicos:\", sorted(set(emails)))",
          "code_output": "intersección: ['C002', 'C003']\nsolo A: ['C001']\núnicos: ['a@ex.com', 'b@ex.com']"
        },
        {
          "subtopicId": "S06-T3-A",
          "heading": "Estructuras anidadas y recorridos",
          "paragraphs": [
            "El modelo CP-N1-B anida: `cliente = {id, nombre, contacts: [...], txs: [...]}`. Recorres con `for c in clients: for t in c['txs']:`.",
            "**Aplanar** transacciones a filas densas (con `client_id` denormalizado) prepara el shape de export CSV en S08. **Contar** contactos por cliente valida integridad del grafo en memoria.",
            "Shape inconsistente (falta clave `txs`, o no es lista) se detecta con `isinstance` y se manda a review — no asumas que todo dict llegó bien formado."
          ],
          "code": "clients = [\n    {\n        \"id\": \"C001\",\n        \"contacts\": [{\"tipo\": \"email\", \"valor\": \"a@ex.com\"}],\n        \"txs\": [{\"id\": \"T1\", \"monto\": 10}, {\"id\": \"T2\", \"monto\": 5}],\n    },\n    {\n        \"id\": \"C002\",\n        \"contacts\": [],\n        \"txs\": [{\"id\": \"T3\", \"monto\": 20}],\n    },\n]\nfor c in clients:\n    print(c[\"id\"], \"n_contacts=\", len(c[\"contacts\"]), \"n_txs=\", len(c[\"txs\"]))\nflat = [\n    {\"client_id\": c[\"id\"], \"tx_id\": t[\"id\"], \"monto\": t[\"monto\"]}\n    for c in clients\n    for t in c[\"txs\"]\n]\nprint(\"flat rows:\", flat)",
          "code_output": "C001 n_contacts= 1 n_txs= 2\nC002 n_contacts= 0 n_txs= 1\nflat rows: [{'client_id': 'C001', 'tx_id': 'T1', 'monto': 10}, {'client_id': 'C001', 'tx_id': 'T2', 'monto': 5}, {'client_id': 'C002', 'tx_id': 'T3', 'monto': 20}]"
        },
        {
          "subtopicId": "S06-T3-B",
          "heading": "Acceso seguro y valores faltantes",
          "paragraphs": [
            "Campos opcionales: `contact.get('telefono')` puede devolver `None`. Encadenar `.get` en anidados evita KeyError: `(c.get('profile') or {}).get('phone')`.",
            "Distingue **missing** (`None` / clave ausente) de **vacío falsy** (`''`, `0`, `[]`). Un teléfono `''` no es lo mismo que “no vino el campo”: el reporte de calidad debe etiquetar distinto si la política lo exige.",
            "Helpers `dig(obj, *path)` o `get_nested` centralizan la política y se testean una vez."
          ],
          "code": null,
          "code_output": null
        },
        {
          "subtopicId": "S06-T4-A",
          "heading": "Ordenamiento y key",
          "paragraphs": [
            "`sorted(seq, key=fn)` devuelve **nueva** lista. `list.sort(key=fn)` **muta in-place** y retorna `None` — un bug clásico si haces `x = rows.sort(...)`.",
            "`key` multi-campo: `key=lambda r: (r['region'], r['nombre'])` ordena estable por región y luego nombre. La estabilidad de Timsort preserva el orden relativo de empates.",
            "Para montos, asegúrate de que el tipo sea numérico antes de ordenar; strings `'100' < '20'` rompen el ranking."
          ],
          "code": "clients = [\n    {\"nombre\": \"Zara\", \"region\": \"Lima\", \"monto\": 30},\n    {\"nombre\": \"Ana\", \"region\": \"Lima\", \"monto\": 50},\n    {\"nombre\": \"Luis\", \"region\": \"Cusco\", \"monto\": 20},\n]\nby_region_name = sorted(clients, key=lambda r: (r[\"region\"], r[\"nombre\"]))\nprint([(r[\"region\"], r[\"nombre\"]) for r in by_region_name])\nby_monto = sorted(clients, key=lambda r: r[\"monto\"], reverse=True)\nprint(\"top monto:\", by_monto[0][\"nombre\"], by_monto[0][\"monto\"])",
          "code_output": "[('Cusco', 'Luis'), ('Lima', 'Ana'), ('Lima', 'Zara')]\ntop monto: Ana 50"
        },
        {
          "subtopicId": "S06-T4-B",
          "heading": "Estructura adecuada, complejidad y determinismo",
          "paragraphs": [
            "Elige estructura por **operación dominante**: muchos appends → list; muchos lookups por id → dict; membership de cohortes → set; contrato fijo inmutable → tuple.",
            "Complejidad: membership en list O(n); en set/dict O(1) promedio. No hagas `if x in big_list` dentro de un loop de n si puedes preindexar.",
            "**Determinismo**: `json.dumps(obj, sort_keys=True, ensure_ascii=False)` + `sorted` de ids produce el mismo string en cada corrida. Reproducibilidad es parte del gate de calidad."
          ],
          "code": "import json\npayload = {\"b\": 2, \"a\": 1, \"ids\": [\"C002\", \"C001\"]}\npayload[\"ids\"] = sorted(payload[\"ids\"])\nprint(json.dumps(payload, sort_keys=True, ensure_ascii=False))\nprint(json.dumps(payload, sort_keys=True, ensure_ascii=False))",
          "code_output": "{\"a\": 1, \"b\": 2, \"ids\": [\"C001\", \"C002\"]}\n{\"a\": 1, \"b\": 2, \"ids\": [\"C001\", \"C002\"]}"
        }
      ],
      "iDo": {
        "steps": [
          {
            "demoId": "S06-T1-A-DEMO",
            "subtopicId": "S06-T1-A",
            "environment": "browser-pyodide",
            "description": "Ventana de transacciones con slicing y tupla de keys estables",
            "why": "Slicing da la ventana; la tupla de keys fija el contrato de columnas sin mutación accidental.",
            "code": "txs = [\n    {\"id\": \"T01\", \"monto\": 12.5, \"canal\": \"app\"},\n    {\"id\": \"T02\", \"monto\": 40.0, \"canal\": \"web\"},\n    {\"id\": \"T03\", \"monto\": 8.0, \"canal\": \"app\"},\n    {\"id\": \"T04\", \"monto\": 15.0, \"canal\": \"tienda\"},\n    {\"id\": \"T05\", \"monto\": 22.0, \"canal\": \"app\"},\n]\nKEYS = (\"id\", \"monto\", \"canal\")\nultimas = txs[-3:]\nprint(\"keys contrato:\", KEYS)\nfor row in ultimas:\n    print(tuple(row[k] for k in KEYS))\nprint(\"len ventana:\", len(ultimas))",
            "language": "python",
            "output": "keys contrato: ('id', 'monto', 'canal')\n('T03', 8.0, 'app')\n('T04', 15.0, 'tienda')\n('T05', 22.0, 'app')\nlen ventana: 3"
          },
          {
            "demoId": "S06-T1-B-DEMO",
            "subtopicId": "S06-T1-B",
            "environment": "browser-pyodide",
            "description": "Bug de alias al 'copiar' lista de dicts de clientes",
            "why": "Alias muta el original; dict(c) por fila basta si el nivel 1 es plano; deepcopy para anidados.",
            "code": "import copy\nclientes = [\n    {\"id\": \"C001\", \"score\": 10},\n    {\"id\": \"C002\", \"score\": 20},\n]\nmal = clientes  # alias, no copia\nmal[0][\"score\"] = 99\nprint(\"tras alias mut:\", clientes[0][\"score\"])\n\nbien_shallow = [dict(c) for c in clientes]\nbien_shallow[0][\"score\"] = 1\nprint(\"original tras shallow dict():\", clientes[0][\"score\"])\n\ndeep = copy.deepcopy(clientes)\ndeep[1][\"score\"] = 0\nprint(\"C002 original:\", clientes[1][\"score\"], \"deep:\", deep[1][\"score\"])",
            "language": "python",
            "output": "tras alias mut: 99\noriginal tras shallow dict(): 99\nC002 original: 20 deep: 0"
          },
          {
            "demoId": "S06-T2-A-DEMO",
            "subtopicId": "S06-T2-A",
            "environment": "browser-pyodide",
            "description": "Índice id→cliente y lookup seguro",
            "why": "El índice dict hace lookup O(1); get encadenado evita KeyError en demos de intake.",
            "code": "filas = [\n    {\"id\": \"C001\", \"nombre\": \"Ana Quispe\", \"region\": \"Lima\"},\n    {\"id\": \"C002\", \"nombre\": \"Luis Huamán\", \"region\": \"Arequipa\"},\n]\nidx = {c[\"id\"]: c for c in filas}\ncid = \"C002\"\nprint(\"encontrado:\", idx.get(cid, {}).get(\"nombre\", \"N/A\"))\nprint(\"missing:\", idx.get(\"C999\", {}).get(\"nombre\", \"N/A\"))\nprint(\"keys ordenadas:\", sorted(idx))",
            "language": "python",
            "output": "encontrado: Luis Huamán\nmissing: N/A\nkeys ordenadas: ['C001', 'C002']"
          },
          {
            "demoId": "S06-T2-B-DEMO",
            "subtopicId": "S06-T2-B",
            "environment": "browser-pyodide",
            "description": "ids únicos; intersección de lotes; conflictos = mismo id datos distintos",
            "why": "Dedup preserva primera vista; conflictos se listan sin borrar filas del reporte.",
            "code": "rows = [\n    {\"id\": \"C001\", \"email\": \"a@ex.com\"},\n    {\"id\": \"C002\", \"email\": \"b@ex.com\"},\n    {\"id\": \"C001\", \"email\": \"a@ex.com\"},\n    {\"id\": \"C001\", \"email\": \"otro@ex.com\"},\n]\nseen = {}\nunique = []\nconflicts = []\nfor r in rows:\n    rid = r[\"id\"]\n    if rid not in seen:\n        seen[rid] = r\n        unique.append(r)\n    elif seen[rid] != r:\n        conflicts.append({\"id\": rid, \"a\": seen[rid], \"b\": r})\nprint(\"unique ids:\", sorted(seen))\nprint(\"n_conflicts:\", len(conflicts))\nprint(\"conflict email pair:\", conflicts[0][\"a\"][\"email\"], \"vs\", conflicts[0][\"b\"][\"email\"])\nlote1, lote2 = {\"C001\", \"C002\"}, {\"C002\", \"C003\"}\nprint(\"intersección lotes:\", sorted(lote1 & lote2))",
            "language": "python",
            "output": "unique ids: ['C001', 'C002']\nn_conflicts: 1\nconflict email pair: a@ex.com vs otro@ex.com\nintersección lotes: ['C002']"
          },
          {
            "demoId": "S06-T3-A-DEMO",
            "subtopicId": "S06-T3-A",
            "environment": "browser-pyodide",
            "description": "Modelo en memoria Client con contacts[] y txs[]",
            "why": "El grafo cliente→contactos/txs es el núcleo del modelo tabular CP-N1-B en RAM.",
            "code": "store = [\n    {\n        \"id\": \"C001\",\n        \"nombre\": \"María Quispe\",\n        \"contacts\": [\n            {\"tipo\": \"email\", \"valor\": \"maria@ex.com\"},\n            {\"tipo\": \"tel\", \"valor\": \"999000111\"},\n        ],\n        \"txs\": [\n            {\"id\": \"T1\", \"monto\": 50},\n            {\"id\": \"T2\", \"monto\": 12},\n        ],\n    }\n]\nc = store[0]\nprint(\"cliente\", c[\"id\"], c[\"nombre\"])\nprint(\"contactos:\", len(c[\"contacts\"]))\ntotal = sum(t[\"monto\"] for t in c[\"txs\"])\nprint(\"total txs:\", total)\nflat = [\n    {\"client_id\": c[\"id\"], \"tx_id\": t[\"id\"], \"monto\": t[\"monto\"]}\n    for c in store\n    for t in c[\"txs\"]\n]\nprint(\"flat:\", flat)",
            "language": "python",
            "output": "cliente C001 María Quispe\ncontactos: 2\ntotal txs: 62\nflat: [{'client_id': 'C001', 'tx_id': 'T1', 'monto': 50}, {'client_id': 'C001', 'tx_id': 'T2', 'monto': 12}]"
          },
          {
            "demoId": "S06-T3-B-DEMO",
            "subtopicId": "S06-T3-B",
            "environment": "browser-pyodide",
            "description": "Extraer teléfono opcional de contacto anidado",
            "why": "dig() centraliza acceso seguro; etiquetas missing vs empty alimentan el reporte de calidad.",
            "code": "def dig(d, *path, default=None):\n    cur = d\n    for k in path:\n        if not isinstance(cur, dict) or k not in cur:\n            return default\n        cur = cur[k]\n    return cur\n\nc1 = {\"id\": \"C001\", \"profile\": {\"phone\": \"999111222\"}}\nc2 = {\"id\": \"C002\", \"profile\": {}}\nc3 = {\"id\": \"C003\"}\nfor c in (c1, c2, c3):\n    phone = dig(c, \"profile\", \"phone\", default=\"MISSING\")\n    flag = \"ok\" if phone != \"MISSING\" and phone else (\"empty\" if phone == \"\" else \"missing\")\n    if phone == \"MISSING\":\n        flag = \"missing\"\n    print(c[\"id\"], phone, flag)",
            "language": "python",
            "output": "C001 999111222 ok\nC002 MISSING missing\nC003 MISSING missing"
          },
          {
            "demoId": "S06-T4-A-DEMO",
            "subtopicId": "S06-T4-A",
            "environment": "browser-pyodide",
            "description": "Ordenar clientes por región luego nombre",
            "why": "sorted no muta; key multi-campo da orden estable región→nombre para exports.",
            "code": "clients = [\n    {\"id\": \"C003\", \"nombre\": \"Zara\", \"region\": \"Lima\"},\n    {\"id\": \"C001\", \"nombre\": \"Ana\", \"region\": \"Lima\"},\n    {\"id\": \"C002\", \"nombre\": \"Bruno\", \"region\": \"Cusco\"},\n]\nordered = sorted(clients, key=lambda r: (r[\"region\"], r[\"nombre\"]))\nfor r in ordered:\n    print(r[\"region\"], r[\"nombre\"], r[\"id\"])\nmutated = clients[:]\nmutated.sort(key=lambda r: r[\"id\"])\nprint(\"sort in-place retorna:\", clients[:0].sort(key=lambda r: r[\"id\"]))\nprint(\"ids mutados:\", [r[\"id\"] for r in mutated])",
            "language": "python",
            "output": "Cusco Bruno C002\nLima Ana C001\nLima Zara C003\nsort in-place retorna: None\nids mutados: ['C001', 'C002', 'C003']"
          },
          {
            "demoId": "S06-T4-B-DEMO",
            "subtopicId": "S06-T4-B",
            "environment": "browser-pyodide",
            "description": "Mismo input → mismo JSON dump con sort_keys",
            "why": "sort de filas + sort_keys hace reproducible el artefacto de demo del modelo en memoria.",
            "code": "import json\ndata = {\n    \"clients\": [\n        {\"id\": \"C002\", \"region\": \"Cusco\"},\n        {\"id\": \"C001\", \"region\": \"Lima\"},\n    ],\n    \"generated_by\": \"s06-demo\",\n}\ndata[\"clients\"] = sorted(data[\"clients\"], key=lambda c: c[\"id\"])\na = json.dumps(data, sort_keys=True, ensure_ascii=False)\nb = json.dumps(data, sort_keys=True, ensure_ascii=False)\nprint(a)\nprint(\"determinista:\", a == b)",
            "language": "python",
            "output": "{\"clients\": [{\"id\": \"C001\", \"region\": \"Lima\"}, {\"id\": \"C002\", \"region\": \"Cusco\"}], \"generated_by\": \"s06-demo\"}\ndeterminista: True"
          }
        ]
      },
      "weDo": {
        "exercises": [
          {
            "id": "S06-T1-A-E1",
            "instruction": "E1 (guiado) — Dada `txs` de 5 montos, imprime los **últimos 2** con slicing y la longitud de la ventana. Caso vacío: si `txs=[]`, imprime `ventana=[]` y `len=0`.",
            "hints": [
              "Usa txs[-2:] (funciona también si hay menos de 2).",
              "print la lista y luego len(ventana)."
            ],
            "kind": "guided",
            "tests": "[40, 50] y []",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\ntxs = [10, 20, 30, 40, 50]\nventana = txs[-2:]\nempty = []\nv0 = empty[-2:]\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(ventana)\n"
          },
          {
            "id": "S06-T1-A-E2",
            "instruction": "E2 (independiente) — Convierte la lista de headers `['id','monto']` a **tuple** `KEYS` (motivo: inmutable). Imprime `KEYS` y demuestra que `KEYS + ('canal',)` crea otra tupla sin mutar `KEYS`.",
            "hints": [
              "tuple(lista) o keys = ('id','monto')",
              "KEYS no tiene append; usa + para nueva tupla."
            ],
            "kind": "independent",
            "tests": "KEYS estable + more",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nheaders = ['id', 'monto']\nKEYS = tuple(headers)\nmore = KEYS + ('canal',)\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print('KEYS', KEYS)\n"
          },
          {
            "id": "S06-T1-A-E3",
            "instruction": "E3 (transferencia) — Hay un bug: se trata una tupla de ids como lista y se intenta `.append`. Captura el AttributeError, convierte a list, append 'C003', e imprime el resultado y un mensaje de diagnóstico.",
            "hints": [
              "tuple no tiene append → AttributeError",
              "list(ids) para mutar una copia."
            ],
            "kind": "transfer",
            "tests": "AttributeError + lista mutada",
            "starterCode": "ids = ('C001', 'C002')\ntry:\n    ids.append('C003')  # bug intencional si se ejecuta sobre tuple\nexcept Exception as e:\n    # TODO\n    ..."
          },
          {
            "id": "S06-T1-B-E1",
            "instruction": "E1 (guiado) — Unpacking de fila `('C001', 'Lima', 10)` en `cid, region, monto`. Imprime los tres. Si la fila tuviera 2 valores, el unpack fallaría (no lo fuerces aquí).",
            "hints": [
              "a, b, c = fila",
              "Orden posicional importa."
            ],
            "kind": "guided",
            "tests": "C001 Lima 10",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nfila = ('C001', 'Lima', 10)\ncid, region, monto = fila\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(cid, region, monto)\n"
          },
          {
            "id": "S06-T1-B-E2",
            "instruction": "E2 (independiente) — Demuestra alias vs copy: lista `[1,2]`, `alias = xs`, `copia = xs.copy()`. Mutar alias con append 3; imprimir xs y copia. Luego append 4 a copia e imprimir de nuevo xs.",
            "hints": [
              "alias comparte objeto; copy es superficial de la lista de ints.",
              "ints inmutables: shallow basta."
            ],
            "kind": "independent",
            "tests": "xs crece con alias; no con copia tras divergencia",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nxs = [1, 2]\nalias = xs\ncopia = xs.copy()\nalias.append(3)\ncopia.append(4)\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print('tras alias', xs, copia)\n"
          },
          {
            "id": "S06-T1-B-E3",
            "instruction": "E3 (transferencia) — Lista de dicts anidados `[{'id':'C1','tags':['a']}]`. Muestra que `copy()` de la lista **no** aísla `tags`. Usa `copy.deepcopy`, muta tags del deep, e imprime original vs deep.",
            "hints": [
              "import copy; deepcopy",
              "shallow[0] is original[0] → True"
            ],
            "kind": "transfer",
            "tests": "deep no contamina original en el append 'd'",
            "starterCode": "import copy\nrows = [{'id': 'C1', 'tags': ['a']}]\nshallow = rows.copy()\n# TODO deep + mutaciones\nprint('orig', rows)\nprint('deep', deep)"
          },
          {
            "id": "S06-T2-A-E1",
            "instruction": "E1 (guiado) — Construye un dict desde pares `[('C001','Lima'),('C002','Cusco')]` e imprime el dict y `d['C002']`.",
            "hints": [
              "dict(pares) o comprensión",
              "Claves deben ser hashables (str ok)."
            ],
            "kind": "guided",
            "tests": "Cusco",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\npares = [('C001', 'Lima'), ('C002', 'Cusco')]\nd = dict(pares)\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(d)\n"
          },
          {
            "id": "S06-T2-A-E2",
            "instruction": "E2 (independiente) — Dado `idx` con C001, usa `get` para C001 y C999 (default 'N/A'). Luego muestra un `try/except KeyError` al acceder `idx['C999']` directo.",
            "hints": [
              "idx.get('C999','N/A')",
              "KeyError solo en acceso duro."
            ],
            "kind": "independent",
            "tests": "Ana / N/A / KeyError",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nidx = {'C001': 'Ana'}\ntry:\nexcept KeyError as e:\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(idx.get('C001', 'N/A'))\n"
          },
          {
            "id": "S06-T2-A-E3",
            "instruction": "E3 (transferencia) — Fusiona `defaults={'retry':1,'timeout':30}` con `override={'retry':5}` **sin** perder timeout. Imprime merged. Luego muestra el peligro de `defaults.update(override)` mutando defaults (usa copia).",
            "hints": [
              "{**defaults, **override} o defaults | override (3.9+)",
              "Copia antes de update in-place."
            ],
            "kind": "transfer",
            "tests": "retry 5 timeout 30; defaults original intacto",
            "starterCode": "defaults = {'retry': 1, 'timeout': 30}\noverride = {'retry': 5}\n# TODO merged seguro\nprint('merged', merged)\n# TODO no mutar original defaults al fusionar"
          },
          {
            "id": "S06-T2-B-E1",
            "instruction": "E1 (guiado) — Deduplica `['a@ex.com','b@ex.com','a@ex.com']` con set y devuelve lista **ordenada** de únicos.",
            "hints": [
              "sorted(set(emails))",
              "set no garantiza orden de impresión sin sorted."
            ],
            "kind": "guided",
            "tests": "['a@ex.com', 'b@ex.com']",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nemails = ['a@ex.com', 'b@ex.com', 'a@ex.com']\nunicos = sorted(set(emails))\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(unicos)\n"
          },
          {
            "id": "S06-T2-B-E2",
            "instruction": "E2 (independiente) — Intersección de contactos `lote_a` y `lote_b` (sets de emails). Imprime sorted de la intersección y de la diferencia simétrica.",
            "hints": [
              "a & b ; a ^ b",
              "sorted para determinismo."
            ],
            "kind": "independent",
            "tests": "inter b,c ; symdiff a,d",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\na = {'a@ex.com', 'b@ex.com', 'c@ex.com'}\nb = {'b@ex.com', 'c@ex.com', 'd@ex.com'}\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print('inter', sorted(a & b))\n"
          },
          {
            "id": "S06-T2-B-E3",
            "instruction": "E3 (transferencia) — Implementa `dedup_report(rows, key='id')` que devuelve `{unique: [...], conflicts: [...]}` sin eliminar del reporte los conflictos. Filas sintéticas con C001 repetido distinto.",
            "hints": [
              "seen dict; si key existe y row!=prev → conflict",
              "unique guarda primera ocurrencia."
            ],
            "kind": "transfer",
            "tests": "1 conflict C001",
            "starterCode": "def dedup_report(rows, key='id'):\n    # TODO\n    ...\nrows = [\n    {'id': 'C001', 'v': 1},\n    {'id': 'C002', 'v': 2},\n    {'id': 'C001', 'v': 9},\n]\nprint(dedup_report(rows))"
          },
          {
            "id": "S06-T3-A-E1",
            "instruction": "E1 (guiado) — Cuenta contactos por cliente e imprime `id → n`.",
            "hints": [
              "len(c['contacts'])",
              "for c in clients"
            ],
            "kind": "guided",
            "tests": "C001 → 2 ; C002 → 0",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nclients = [\n    {'id': 'C001', 'contacts': [1, 2]},\n    {'id': 'C002', 'contacts': []},\n]\nfor c in clients:\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(c['id'], '→', len(c['contacts']))\n"
          },
          {
            "id": "S06-T3-A-E2",
            "instruction": "E2 (independiente) — Aplana `txs` anidadas a filas `{client_id, tx_id, monto}` e imprime la lista.",
            "hints": [
              "doble for o comprehension anidada",
              "conserva client_id en cada fila"
            ],
            "kind": "independent",
            "tests": "3 filas flat",
            "starterCode": "clients = [\n    {'id': 'C001', 'txs': [{'id': 'T1', 'monto': 5}]},\n    {'id': 'C002', 'txs': [{'id': 'T2', 'monto': 7}, {'id': 'T3', 'monto': 1}]},\n]\n# TODO flat\nprint(flat)"
          },
          {
            "id": "S06-T3-A-E3",
            "instruction": "E3 (transferencia) — Detecta shape inconsistente: si falta `txs` o no es list, marca `status='review'`. Imprime status por cliente sintético (uno ok, uno malo).",
            "hints": [
              "isinstance(c.get('txs'), list)",
              "No asumas claves siempre presentes."
            ],
            "kind": "transfer",
            "tests": "ok / review / review",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nclients = [\n    {'id': 'C001', 'txs': []},\n    {'id': 'C002'},\n    {'id': 'C003', 'txs': 'oops'},\n]\nfor c in clients:\n    txs = c.get('txs')\n    ok = isinstance(txs, list)\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(c['id'], 'ok' if ok else 'review')\n"
          },
          {
            "id": "S06-T3-B-E1",
            "instruction": "E1 (guiado) — Implementa `get_nested(d, *keys, default=None)` y úsalo para `profile.phone` en un dict sintético.",
            "hints": [
              "Recorre keys; si falta retorna default.",
              "Chequea isinstance dict en cada nivel."
            ],
            "kind": "guided",
            "tests": "999 y N/A",
            "starterCode": "def get_nested(d, *keys, default=None):\n    # TODO\n    ...\nc = {'profile': {'phone': '999'}}\nprint(get_nested(c, 'profile', 'phone'))\nprint(get_nested(c, 'profile', 'email', default='N/A'))"
          },
          {
            "id": "S06-T3-B-E2",
            "instruction": "E2 (independiente) — Para cada cliente, marca en un reporte si `email` es missing (None o ausente) o present. Imprime líneas `id: missing|present`.",
            "hints": [
              "'email' not in c or c['email'] is None → missing",
              "'' puede contarse present vacío según política; aquí '' = present."
            ],
            "kind": "independent",
            "tests": "present/missing/missing",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nclients = [\n    {'id': 'C001', 'email': 'a@ex.com'},\n    {'id': 'C002', 'email': None},\n    {'id': 'C003'},\n]\nfor c in clients:\n    if 'email' not in c or c['email'] is None:\n        flag = 'missing'\n    else:\n        flag = 'present'\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(f\"{c['id']}: {flag}\")\n"
          },
          {
            "id": "S06-T3-B-E3",
            "instruction": "E3 (transferencia) — Construye tabla mental en prints: para valores `None`, `''`, `0`, `[]` indica si son falsy y si representan missing de negocio (solo None = missing aquí).",
            "hints": [
              "bool(x) vs x is None",
              "0 y '' son falsy pero pueden ser datos válidos."
            ],
            "kind": "transfer",
            "tests": "solo None missing=True",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nvals = [None, '', 0, []]\nfor v in vals:\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(repr(v), 'falsy=', not bool(v), 'missing=', v is None)\n"
          },
          {
            "id": "S06-T4-A-E1",
            "instruction": "E1 (guiado) — Ordena por monto ascendente con sorted+key e imprime ids en orden.",
            "hints": [
              "key=lambda r: r['monto']",
              "sorted no muta la lista original."
            ],
            "kind": "guided",
            "tests": "T1 luego T2",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nrows = [{'id': 'T2', 'monto': 30}, {'id': 'T1', 'monto': 10}]\nordered = sorted(rows, key=lambda r: r['monto'])\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print([r['id'] for r in ordered])\n"
          },
          {
            "id": "S06-T4-A-E2",
            "instruction": "E2 (independiente) — key multi-campo: ordena por región y luego nombre. Imprime pares región/nombre.",
            "hints": [
              "key=lambda r: (r['region'], r['nombre'])",
              "Tupla compara lexicográficamente."
            ],
            "kind": "independent",
            "tests": "Cusco Bob; Lima Ana; Lima Zed",
            "starterCode": "rows = [\n    {'nombre': 'Zed', 'region': 'Lima'},\n    {'nombre': 'Ana', 'region': 'Lima'},\n    {'nombre': 'Bob', 'region': 'Cusco'},\n]\n# TODO"
          },
          {
            "id": "S06-T4-A-E3",
            "instruction": "E3 (transferencia) — Demuestra que `rows.sort()` retorna None y muta. Imprime tipo del retorno y la lista mutada. Luego usa sorted para obtener copia ordenada sin tocar otra lista `base`.",
            "hints": [
              "x = lst.sort() → x is None",
              "sorted(base) no muta base."
            ],
            "kind": "transfer",
            "tests": "None + base intacta",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nrows = [3, 1, 2]\nbase = [3, 1, 2]\nret = rows.sort()\ncopy_sorted = sorted(base)\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print('ret', ret)\n"
          },
          {
            "id": "S06-T4-B-E1",
            "instruction": "E1 (guiado) — Para 3 jobs imprime la estructura elegida: (1) cola de llegada (2) lookup por id (3) cohorte de emails únicos.",
            "hints": [
              "list / dict / set",
              "Una línea por job con la elección."
            ],
            "kind": "guided",
            "tests": "list/dict/set",
            "starterCode": "jobs = [\n    'cola de llegada de filas',\n    'lookup frecuente por id',\n    'emails únicos de un lote',\n]\n# TODO print job → estructura"
          },
          {
            "id": "S06-T4-B-E2",
            "instruction": "E2 (independiente) — Haz determinista un export: ordena ids y `json.dumps(..., sort_keys=True)`. Imprime el JSON una vez.",
            "hints": [
              "sorted ids; sort_keys=True",
              "ensure_ascii=False para tildes si hubiera."
            ],
            "kind": "independent",
            "tests": "{\"a\": 2, \"ids\": [\"C001\", \"C002\"], \"z\": 1}",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nimport json\npayload = {'z': 1, 'a': 2, 'ids': ['C002', 'C001']}\npayload['ids'] = sorted(payload['ids'])\ns = json.dumps(payload, sort_keys=True, ensure_ascii=False)\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(s)\n"
          },
          {
            "id": "S06-T4-B-E3",
            "instruction": "E3 (transferencia) — Justifica tradeoff: membership de 1000 ids en list vs set. Simula con prints de complejidad conceptual y un micro-ejemplo n=5 mostrando `x in set` True.",
            "hints": [
              "list O(n) vs set O(1) promedio",
              "No necesitas timeit; razona y demo pequeña."
            ],
            "kind": "transfer",
            "tests": "O(n) vs O(1) + True",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nids = ['C001', 'C002', 'C003', 'C004', 'C005']\ns = set(ids)\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print('list membership: O(n) por chequeo')\n"
          }
        ]
      },
      "youDo": {
        "title": "Modelo tabular en memoria (CP-N1-B)",
        "context": "Inicias el capstone **CP-N1-B**. Representas clientes, contactos y transacciones en estructuras Python puras (sin NumPy/pandas). Deduplicas por clave de negocio **reportando conflictos**, aplanas txs y exportas JSON determinista. En S07–S08 se suma normalización latam e ingesta por archivos. Solo datos sintéticos.",
        "objectives": [
          "Representar cliente/contacto/tx en list[dict] documentado",
          "Implementar dedup_report → unique + conflicts",
          "Aplanar transacciones con client_id",
          "Acceso seguro a faltantes (get_nested)",
          "Export determinista (sorted + sort_keys)"
        ],
        "requirements": [
          "Tipos list[dict] o índices dict documentados",
          "dedup_report(rows, key_fn) sin borrar conflictos",
          "sorted determinista en exports",
          "Datos sintéticos latam (example.com)",
          "Sin NumPy/pandas en este incremento V3"
        ],
        "portfolioNote": "En el README muestra el shape del store, un ejemplo de conflicto de dedup y el JSON determinista de demo. Eso evidencia el incremento en memoria de CP-N1-B.",
        "starterCode": "\"\"\"memory_model.py — Modelo tabular en memoria (CP-N1-B / S06)\nClientes + contactos + transacciones en estructuras Python puras.\nSin NumPy/pandas. Datos sintéticos latam únicamente.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport json\nfrom typing import Any, Callable\n\n\ndef dedup_report(rows: list[dict], key_fn: Callable[[dict], Any]) -> dict:\n    \"\"\"Devuelve {unique, conflicts} sin borrar traza de conflictos.\"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef flatten_txs(clients: list[dict]) -> list[dict]:\n    \"\"\"Aplana txs anidadas a filas con client_id.\"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef get_nested(d: dict, *keys: str, default=None):\n    # TODO\n    raise NotImplementedError\n\n\ndef export_deterministic(clients: list[dict]) -> str:\n    \"\"\"JSON estable: sort por id + sort_keys.\"\"\"\n    # TODO\n    raise NotImplementedError\n\n\ndef build_demo_store() -> list[dict]:\n    return [\n        {\n            \"id\": \"C001\",\n            \"nombre\": \"Ana Quispe\",\n            \"contacts\": [{\"tipo\": \"email\", \"valor\": \"ana@example.com\"}],\n            \"txs\": [{\"id\": \"T1\", \"monto\": 10}, {\"id\": \"T2\", \"monto\": 5}],\n        },\n        {\n            \"id\": \"C002\",\n            \"nombre\": \"Luis Huamán\",\n            \"contacts\": [],\n            \"txs\": [{\"id\": \"T3\", \"monto\": 20}],\n        },\n    ]\n\n\ndef main() -> None:\n    store = build_demo_store()\n    print(\"n_clients\", len(store))\n    print(\"flat\", flatten_txs(store))\n    print(export_deterministic(store))\n    rows = [\n        {\"id\": \"C001\", \"v\": 1},\n        {\"id\": \"C001\", \"v\": 9},\n        {\"id\": \"C002\", \"v\": 2},\n    ]\n    print(dedup_report(rows, key_fn=lambda r: r[\"id\"]))\n\n\nif __name__ == \"__main__\":\n    main()\n"
      },
      "selfCheck_stems": [
        {
          "question": "¿Qué produce xs[-2:] si xs = [1,2,3,4]?",
          "options": [
            "[1,2]",
            "[3,4]",
            "[4]",
            "Error"
          ]
        },
        {
          "question": "b = a (listas) y mutas b.append(1). ¿Qué pasa con a?",
          "options": [
            "a no cambia",
            "se lanza error",
            "a se convierte en tuple",
            "a también ve el append (alias)"
          ]
        },
        {
          "question": "Para reportar dos filas con mismo id y payload distinto debes…",
          "options": [
            "Listar conflicto en conflicts sin silenciar",
            "Borrar ambas",
            "Quedarte con la última sin traza",
            "Convertir a set de dicts"
          ]
        },
        {
          "question": "rows.sort(key=...) retorna…",
          "options": [
            "la lista ordenada",
            "una tuple",
            "None (muta in-place)",
            "un set"
          ]
        },
        {
          "question": "json.dumps(..., sort_keys=True) ayuda a…",
          "options": [
            "comprimir el archivo",
            "salidas deterministas/reproducibles",
            "validar schema JSON Schema",
            "encriptar PII"
          ]
        }
      ],
      "taught_text_sha": "46ea8dc7e855c0e3",
      "taught_text_len": 23990
    },
    {
      "file": "src/lib/course/sections/s07-data-acquisition.ts",
      "id": "data-acquisition",
      "index": 7,
      "title": "Texto, Unicode y expresiones regulares",
      "shortTitle": "Texto & Unicode",
      "tagline": "Unicode latam, strings y regex sin sobrevalidar",
      "jobRelevance": "Los datasets de clientes en Latam rompen normalizadores pensados para ASCII/US: tildes, ñ, dos apellidos y partículas. Si tu matching afirma identidad, parentesco o fraude por un score, creas riesgo de producto y cumplimiento: un score es **evidencia para revisión**, nunca prueba automática. Esta sección (id `data-acquisition` conservado) enseña Unicode, str antes que regex, y evidencia sin overvalidation — tramo central de **CP-N1-B**.",
      "learningOutcomes": [
        "Normalizar Unicode (NFC/NFD) y usar casefold en comparaciones",
        "Modelar nombres latam con dos apellidos y partículas sin forzar formato US",
        "Manipular texto con métodos str idiomáticos antes de regex",
        "Normalizar email/teléfono con reglas modestas sin overvalidation",
        "Escribir patrones con grupos y anchors ^$",
        "Compilar patrones, extraer con findall/finditer y conocer límites",
        "Comparar por igualdad normalizada y Jaccard de tokens",
        "Razonar FP/FN y conservar evidencia sin claims de parentesco"
      ],
      "theory": [
        {
          "subtopicId": "S07-T1-A",
          "heading": "Code points, normalización y casefold",
          "paragraphs": [
            "Python 3 str es Unicode. `ord('ñ')` / `chr(241)` exploran code points. La misma letra puede codificarse de formas distintas: **NFC** (compuesta) vs **NFD** (base + combining mark). En texto y similaridad, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta normalización NFC + evidencia textual (CP-N1-A/B) sin inventar hechos sobre personas reales.",
            "`unicodedata.normalize('NFC', s)` unifica formas antes de comparar. Sin eso, `'José' == 'Jose\\u0301'` puede ser False aunque se vean iguales. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: stdlib str/unicodedata/re (S01–S07); no pandas, no APIs de S08+.",
            "`casefold()` es más agresivo que `lower()` para comparaciones case-insensitive (mejor para matching de nombres/emails normalizados). Caso sintético Perú: nombres sintéticos José/Quispe, emails/teléfonos ficticios, Lima/Arequipa. Documenta decisión, métrica y límite conocido en el memo del subtema «Code points, normalización y casefold»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "import unicodedata\na = \"José\"\nb = \"Jose\\u0301\"\nprint(\"raw equal?\", a == b)\nprint(\"NFC equal?\", unicodedata.normalize(\"NFC\", a) == unicodedata.normalize(\"NFC\", b))\nprint(\"casefold:\", \"MAÑANA\".casefold())\nprint(\"ord ñ:\", ord(\"ñ\"))",
          "code_output": "raw equal? False\nNFC equal? True\ncasefold: mañana\nord ñ: 241"
        },
        {
          "subtopicId": "S07-T1-B",
          "heading": "Tildes, ñ, partículas y apellidos compuestos",
          "paragraphs": [
            "En Perú y Latam es común **nombre(s) + apellido1 + apellido2**. No fuerces el formato US (first/last único). Conserva el **raw** siempre. En texto y similaridad, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta normalización NFC + evidencia textual (CP-N1-A/B) sin inventar hechos sobre personas reales.",
            "Partículas (`de`, `del`, `de la`, `y`) pueden ir en nombres o apellidos (`María del Carmen`, `de la Cruz`). Un parser **suave** tokeniza y aplica heurísticas; si no hay segundo apellido, marca **review** en vez de inventar. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: stdlib str/unicodedata/re (S01–S07); no pandas, no APIs de S08+.",
            "Espacios múltiples se colapsan; tildes y ñ se preservan en la forma normalizada visible (NFC). Caso sintético Perú: nombres sintéticos José/Quispe, emails/teléfonos ficticios, Lima/Arequipa. Documenta decisión, métrica y límite conocido en el memo del subtema «Tildes, ñ, partículas y apellidos compuestos»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "raw = \"  María   del  Carmen  Quispe  Huamán \"\ntokens = raw.split()\nprint(tokens)\n# Heurística demo: últimos 2 tokens = apellidos si len>=3\nif len(tokens) >= 3:\n    ap2, ap1 = tokens[-1], tokens[-2]\n    given = \" \".join(tokens[:-2])\n    print(\"given:\", given)\n    print(\"apellidos:\", ap1, ap2)",
          "code_output": "['María', 'del', 'Carmen', 'Quispe', 'Huamán']\ngiven: María del Carmen\napellidos: Quispe Huamán"
        },
        {
          "subtopicId": "S07-T2-A",
          "heading": "split / join / search / replace",
          "paragraphs": [
            "Antes de regex: `strip`, `split`, `join`, `replace`, `find`, `startswith`. El 80% de limpieza de direcciones y tokens se resuelve así. En texto y similaridad, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta normalización NFC + evidencia textual (CP-N1-A/B) sin inventar hechos sobre personas reales.",
            "`' '.join(s.split())` colapsa espacios. `split(',')` parsea CSV-like simple (sin comillas escapadas: ahí entra el módulo csv en S08). Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: stdlib str/unicodedata/re (S01–S07); no pandas, no APIs de S08+.",
            "`replace` es literal y predecible; úsalo para normalizar guiones o prefijos antes de invocar regex. Caso sintético Perú: nombres sintéticos José/Quispe, emails/teléfonos ficticios, Lima/Arequipa. Documenta decisión, métrica y límite conocido en el memo del subtema «split / join / search / replace»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "dir_raw = \"  Av.  Larco   123  ,  Miraflores \"\nlimpio = \" \".join(dir_raw.strip().split())\nprint(limpio)\nparts = [p.strip() for p in limpio.split(\",\")]\nprint(parts)\nprint(limpio.replace(\"Av.\", \"Avenida\"))",
          "code_output": "Av. Larco 123 , Miraflores\n['Av. Larco 123', 'Miraflores']\nAvenida Larco 123 , Miraflores"
        },
        {
          "subtopicId": "S07-T2-B",
          "heading": "Nombres, emails y teléfonos sin sobrevalidación",
          "paragraphs": [
            "Email: strip + casefold y una comprobación **modesta pero completa**: exactamente un `@`, parte local y dominio no vacíos, y ningún espacio. No confirma que el buzón exista; solo decide `valid` o `review`. Regex hiper-estrictas **rechazan válidos** (plus addressing, Unicode, dominios nuevos).",
            "Teléfono PE sintético de demo: extraer dígitos y conservar el prefijo de país `51` cuando viene como `+51`; opcionalmente revisar 9 dígitos locales — no inventes validación de operadora. El contrato de salida es dígitos (`51999000111`), no conservar el signo `+`.",
            "Nombre: collapse + NFC; title-case es cosmético y puede pelear con partículas (`del` → `Del`). Decide política y documenta. Caso sintético Perú: nombres sintéticos José/Quispe, emails/teléfonos ficticios, Lima/Arequipa. Documenta decisión, métrica y límite conocido en el memo del subtema «Nombres, emails y teléfonos sin sobrevalidación»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "def normalize_email(raw: str) -> str:\n    s = raw.strip().casefold()\n    if s.count(\"@\") != 1 or any(ch.isspace() for ch in s):\n        raise ValueError(\"email requiere un @ y cero espacios\")\n    local, domain = s.split(\"@\")\n    if not local or not domain:\n        raise ValueError(\"email requiere local y dominio\")\n    return s\n\ndef normalize_phone_pe(raw: str) -> str:\n    return \"\".join(c for c in raw if c.isdigit())\n\nprint(normalize_email(\"  Ana+test@Example.COM \"))\nprint(normalize_phone_pe(\"+51 999-000-111\"))",
          "code_output": "ana+test@example.com\n51999000111"
        },
        {
          "subtopicId": "S07-T3-A",
          "heading": "Patrones, grupos y anchors",
          "paragraphs": [
            "Regex cuando el patrón es **regular de verdad**: DNI sintético 8 dígitos, códigos de región, prefijos. Usa `re` con **grupos** `(...)` y anchors `^$` para full match. En texto y similaridad, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta normalización NFC + evidencia textual (CP-N1-A/B) sin inventar hechos sobre personas reales.",
            "`re.fullmatch` ancla inicio y fin. `re.search` encuentra en medio. Confundirlos produce falsos positivos en validación de códigos. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: stdlib str/unicodedata/re (S01–S07); no pandas, no APIs de S08+.",
            "Grupos con nombre `(?P<name>...)` mejoran legibilidad al extraer campos. Caso sintético Perú: nombres sintéticos José/Quispe, emails/teléfonos ficticios, Lima/Arequipa. Documenta decisión, métrica y límite conocido en el memo del subtema «Patrones, grupos y anchors»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "import re\npat = re.compile(r\"^(?P<dni>\\d{8})$\")\nm = pat.fullmatch(\"12345678\")\nprint(m.group(\"dni\") if m else None)\nprint(\"search mid:\", bool(re.search(r\"\\d{8}\", \"DNI 12345678 PE\")))\nprint(\"full mid:\", bool(re.fullmatch(r\"\\d{8}\", \"DNI 12345678 PE\")))",
          "code_output": "12345678\nsearch mid: True\nfull mid: False"
        },
        {
          "subtopicId": "S07-T3-B",
          "heading": "Compilación, extracción y límites",
          "paragraphs": [
            "`re.compile` reutiliza el patrón en loops (claridad + micro-ahorro). `findall` / `finditer` extraen múltiples matches de un log sintético. En texto y similaridad, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta normalización NFC + evidencia textual (CP-N1-A/B) sin inventar hechos sobre personas reales.",
            "Límites: **catastrophic backtracking** con patrones anidados ambiguos; **overfit** de validación que rechaza inputs reales. Prefiere patrones simples. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: stdlib str/unicodedata/re (S01–S07); no pandas, no APIs de S08+.",
            "Si el patrón crece sin control, vuelve a `str` methods o a un parser explícito. Caso sintético Perú: nombres sintéticos José/Quispe, emails/teléfonos ficticios, Lima/Arequipa. Documenta decisión, métrica y límite conocido en el memo del subtema «Compilación, extracción y límites»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "import re\nphone = re.compile(r\"\\b9\\d{8}\\b\")\nlog = \"llamada 999000111 y fallback 988777666 fin\"\nprint(phone.findall(log))\nfor m in phone.finditer(log):\n    print(\"span\", m.span(), m.group())",
          "code_output": "['999000111', '988777666']\nspan (8, 17) 999000111\nspan (29, 38) 988777666"
        },
        {
          "subtopicId": "S07-T4-A",
          "heading": "Exacta y por tokens (Jaccard simple)",
          "paragraphs": [
            "Matching de texto en intake: primero **igualdad normalizada** (NFC + casefold + collapse). Si no, **similitud por tokens** (Jaccard) como señal débil. En texto y similaridad, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta normalización NFC + evidencia textual (CP-N1-A/B) sin inventar hechos sobre personas reales.",
            "Jaccard = |A∩B| / |A∪B| sobre sets de tokens. Score medio → **review**, no auto-merge. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: stdlib str/unicodedata/re (S01–S07); no pandas, no APIs de S08+.",
            "Nunca digas “es la misma persona” ni “parentesco” por un score. Conserva evidencia (score, raw A/B). Caso sintético Perú: nombres sintéticos José/Quispe, emails/teléfonos ficticios, Lima/Arequipa. Documenta decisión, métrica y límite conocido en el memo del subtema «Exacta y por tokens (Jaccard simple)»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "def tokens(s: str) -> set[str]:\n    return set(s.casefold().split())\n\ndef token_jaccard(a: str, b: str) -> float:\n    A, B = tokens(a), tokens(b)\n    if not A and not B:\n        return 1.0\n    if not A or not B:\n        return 0.0\n    return len(A & B) / len(A | B)\n\nprint(round(token_jaccard(\"Juan Perez\", \"Juan P. Perez\"), 3))\nprint(round(token_jaccard(\"Ana Quispe\", \"Luis Huamán\"), 3))",
          "code_output": "0.667\n0.0"
        },
        {
          "subtopicId": "S07-T4-B",
          "heading": "FP/FN y conservación de evidencia",
          "paragraphs": [
            "**FP** (false positive): el sistema dice match y no debería. **FN**: debería matchear y no lo hizo. En nombres latam, tildes y partículas mueven ambos. En texto y similaridad, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta normalización NFC + evidencia textual (CP-N1-A/B) sin inventar hechos sobre personas reales.",
            "Empaqueta evidencia: `{raw_a, raw_b, score, decision, reason}`. La decisión puede ser accept/review/reject de matching — **no** etiqueta familiar. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: stdlib str/unicodedata/re (S01–S07); no pandas, no APIs de S08+.",
            "Documenta por qué no se afirma parentesco: falta de fuente autoritativa, riesgo legal/ético, score insuficiente. Caso sintético Perú: nombres sintéticos José/Quispe, emails/teléfonos ficticios, Lima/Arequipa. Documenta decisión, métrica y límite conocido en el memo del subtema «FP/FN y conservación de evidencia»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "pairs = [\n    (\"José Pérez\", \"Jose Perez\", 0.9, \"review\"),\n    (\"Ana\", \"Ana\", 1.0, \"exact\"),\n    (\"Luis\", \"Carla\", 0.0, \"no_match\"),\n    (\"Juan Perez\", \"Juan P Perez\", 0.67, \"review\"),\n]\nfor a, b, score, dec in pairs:\n    # FP demo: exact sobre homónimos sería riesgo; aquí solo tabula\n    print(f\"{a!r} vs {b!r} score={score} → {dec}\")\nprint(\"nota: sin claims de parentesco ni identidad legal\")",
          "code_output": "'José Pérez' vs 'Jose Perez' score=0.9 → review\n'Ana' vs 'Ana' score=1.0 → exact\n'Luis' vs 'Carla' score=0.0 → no_match\n'Juan Perez' vs 'Juan P Perez' score=0.67 → review\nnota: sin claims de parentesco ni identidad legal"
        }
      ],
      "iDo": {
        "steps": [
          {
            "demoId": "S07-T1-A-DEMO",
            "subtopicId": "S07-T1-A",
            "environment": "browser-pyodide",
            "description": "Comparar 'José' vs 'Jose\\u0301' con y sin normalize",
            "why": "NFC alinea formas visualmente idénticas antes de comparar o indexar nombres.",
            "code": "import unicodedata\na = \"José\"\nb = \"Jose\\u0301\"\nprint(\"sin norm:\", a == b, [hex(ord(c)) for c in a], [hex(ord(c)) for c in b])\nna, nb = unicodedata.normalize(\"NFC\", a), unicodedata.normalize(\"NFC\", b)\nprint(\"NFC:\", na == nb, na)\nprint(\"casefold mañANA:\", \"mañANA\".casefold())",
            "language": "python",
            "output": "sin norm: False ['0x4a', '0x6f', '0x73', '0xe9'] ['0x4a', '0x6f', '0x73', '0x65', '0x301']\nNFC: True José\ncasefold mañANA: mañana"
          },
          {
            "demoId": "S07-T1-B-DEMO",
            "subtopicId": "S07-T1-B",
            "environment": "browser-pyodide",
            "description": "Parse suave de 'María del Carmen Quispe Huamán'",
            "why": "Heurística de dos apellidos finales; given puede incluir partículas del nombre.",
            "code": "import unicodedata\nraw = \"María del Carmen Quispe Huamán\"\nnorm = unicodedata.normalize(\"NFC\", \" \".join(raw.split()))\ntoks = norm.split()\napellidos = toks[-2:]\ngiven = \" \".join(toks[:-2])\nprint(\"raw:\", raw)\nprint(\"given:\", given)\nprint(\"apellido1:\", apellidos[0], \"apellido2:\", apellidos[1])\nprint(\"conserva raw en pipeline: sí\")",
            "language": "python",
            "output": "raw: María del Carmen Quispe Huamán\ngiven: María del Carmen\napellido1: Quispe apellido2: Huamán\nconserva raw en pipeline: sí"
          },
          {
            "demoId": "S07-T2-A-DEMO",
            "subtopicId": "S07-T2-A",
            "environment": "browser-pyodide",
            "description": "Limpiar dirección sintética: strip, colapsar espacios, join tokens",
            "why": "str methods resuelven limpieza de dirección sin regex.",
            "code": "raw = \"  Jr.  de  la  Unión   450  \"\nlimpio = \" \".join(raw.strip().split())\nprint(limpio)\nprint(limpio.replace(\"Jr.\", \"Jiron\"))\nprint(\"find Unión:\", limpio.find(\"Unión\"))",
            "language": "python",
            "output": "Jr. de la Unión 450\nJiron de la Unión 450\nfind Unión: 10"
          },
          {
            "demoId": "S07-T2-B-DEMO",
            "subtopicId": "S07-T2-B",
            "environment": "browser-pyodide",
            "description": "normalize_email y normalize_phone_pe sintético",
            "why": "Validación modesta acepta plus-addressing; regex overfit la rechazaría.",
            "code": "def normalize_email(raw: str) -> str:\n    s = raw.strip().casefold()\n    if s.count(\"@\") != 1 or any(ch.isspace() for ch in s):\n        raise ValueError(\"email requiere un @ y cero espacios\")\n    local, domain = s.split(\"@\")\n    if not local or not domain:\n        raise ValueError(\"email requiere local y dominio\")\n    return s\n\ndef normalize_phone_pe(raw: str) -> str:\n    return \"\".join(ch for ch in raw if ch.isdigit())\n\nprint(normalize_email(\"  User+tag@Example.COM \"))\nprint(normalize_phone_pe(\"(+51) 999-000-111\"))\n# Overvalidation mala (no usar en prod de este curso):\nbad = r\"^[a-z]+@[a-z]+\\.com$\"\nimport re\nprint(\"overfit rejects plus?\", re.fullmatch(bad, \"user+tag@example.com\") is None)",
            "language": "python",
            "output": "user+tag@example.com\n51999000111\noverfit rejects plus? True"
          },
          {
            "demoId": "S07-T3-A-DEMO",
            "subtopicId": "S07-T3-A",
            "environment": "browser-pyodide",
            "description": "Extraer DNI sintético 8 dígitos con grupos",
            "why": "Grupos nombran la captura; fullmatch exige cadena exacta.",
            "code": "import re\npat = re.compile(r\"DNI\\s+(?P<dni>\\d{8})\\b\")\ntext = \"Cliente demo DNI 12345678 activo\"\nm = pat.search(text)\nprint(m.group(\"dni\") if m else None)\nprint(\"fullmatch solo digitos:\", bool(re.fullmatch(r\"\\d{8}\", \"12345678\")))\nprint(\"fullmatch con prefijo:\", bool(re.fullmatch(r\"\\d{8}\", \"DNI 12345678\")))",
            "language": "python",
            "output": "12345678\nfullmatch solo digitos: True\nfullmatch con prefijo: False"
          },
          {
            "demoId": "S07-T3-B-DEMO",
            "subtopicId": "S07-T3-B",
            "environment": "browser-pyodide",
            "description": "compile de patrón teléfono; finditer sobre log sintético",
            "why": "compile + finditer extrae múltiples señales de un log sin overfit de validación.",
            "code": "import re\nphone = re.compile(r\"\\b9\\d{8}\\b\")\nlog = \"ok 999111222 noise 12345 otro 988777666\"\nprint(\"findall:\", phone.findall(log))\nfor m in phone.finditer(log):\n    print(m.group(), \"at\", m.start())\nprint(\"riesgo: evita patrones con cuantificadores anidados ambiguos\")",
            "language": "python",
            "output": "findall: ['999111222', '988777666']\n999111222 at 3\n988777666 at 30\nriesgo: evita patrones con cuantificadores anidados ambiguos"
          },
          {
            "demoId": "S07-T4-A-DEMO",
            "subtopicId": "S07-T4-A",
            "environment": "browser-pyodide",
            "description": "token_jaccard('Juan Perez', 'Juan P. Perez')",
            "why": "Score medio cae en review; no se auto-fusiona ni se afirma identidad.",
            "code": "def token_jaccard(a: str, b: str) -> float:\n    A = set(a.replace(\".\", \" \").casefold().split())\n    B = set(b.replace(\".\", \" \").casefold().split())\n    if not A and not B:\n        return 1.0\n    if not A or not B:\n        return 0.0\n    return len(A & B) / len(A | B)\n\ns = token_jaccard(\"Juan Perez\", \"Juan P. Perez\")\nprint(\"score\", round(s, 3))\nprint(\"decision\", \"review\" if 0.4 <= s < 1.0 else (\"exact\" if s == 1.0 else \"no_match\"))",
            "language": "python",
            "output": "score 0.667\ndecision review"
          },
          {
            "demoId": "S07-T4-B-DEMO",
            "subtopicId": "S07-T4-B",
            "environment": "browser-pyodide",
            "description": "Tabla FP/FN de 4 pares sintéticos",
            "why": "Clasificar FP/FN enseña costo de over/under matching en nombres latam.",
            "code": "# truth: same_entity sintético solo para ejercicio de métricas (no legal)\nrows = [\n    {\"a\": \"Ana\", \"b\": \"Ana\", \"pred\": \"match\", \"truth\": \"match\"},\n    {\"a\": \"José\", \"b\": \"Jose\", \"pred\": \"match\", \"truth\": \"match\"},\n    {\"a\": \"Luis\", \"b\": \"Luisa\", \"pred\": \"match\", \"truth\": \"no\"},\n    {\"a\": \"María del Carmen\", \"b\": \"Maria Carmen\", \"pred\": \"no\", \"truth\": \"match\"},\n]\nfor r in rows:\n    if r[\"pred\"] == \"match\" and r[\"truth\"] == \"no\":\n        tag = \"FP\"\n    elif r[\"pred\"] == \"no\" and r[\"truth\"] == \"match\":\n        tag = \"FN\"\n    elif r[\"pred\"] == \"match\":\n        tag = \"TP\"\n    else:\n        tag = \"TN\"\n    print(r[\"a\"], \"vs\", r[\"b\"], \"→\", tag)\nprint(\"evidencia se conserva; no se afirma parentesco\")",
            "language": "python",
            "output": "Ana vs Ana → TP\nJosé vs Jose → TP\nLuis vs Luisa → FP\nMaría del Carmen vs Maria Carmen → FN\nevidencia se conserva; no se afirma parentesco"
          }
        ]
      },
      "weDo": {
        "exercises": [
          {
            "id": "S07-T1-A-E1",
            "instruction": "E1 (guiado) — Concepto: S07-T1-A (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Normaliza a NFC la lista `['José', 'Jose\\u0301', '']` e imprime cada resultado. El vacío permanece vacío. Salida/pass: `'José' | 'José' | ''`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "unicodedata.normalize('NFC', s)",
              "import unicodedata"
            ],
            "kind": "guided",
            "tests": "NFC iguales visualmente",
            "starterCode": "import unicodedata\nnames = ['José', 'Jose\\u0301', '']\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T1-A-E2",
            "instruction": "E2 (independiente) — Concepto: S07-T1-A (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Usa casefold para decidir si `'MAÑANA'` y `'mañana'` matchean. Imprime True/False. Salida/pass: `True`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "a.casefold() == b.casefold()",
              "lower también funciona en este caso; prefiere casefold en matching."
            ],
            "kind": "independent",
            "tests": "True",
            "starterCode": "a, b = 'MAÑANA', 'mañana'\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T1-A-E3",
            "instruction": "E3 (transferencia) — Diagnostica mismatch: dos strings que se ven iguales pero fallan `==`. Imprime igualdad cruda, igualdad NFC y un mensaje de causa (NFD residual).",
            "hints": [
              "Compara sin/con normalize",
              "Muestra code points con ord si ayuda."
            ],
            "kind": "transfer",
            "tests": "raw False nfc True",
            "starterCode": "import unicodedata\na = 'José'\nb = 'Jose\\u0301'\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T1-B-E1",
            "instruction": "E1 (guiado) — Concepto: S07-T1-B (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: De `'Ana María Quispe Huamán'` extrae given y dos apellidos (últimos dos tokens). Imprímelos. Salida/pass: `Ana María | Quispe Huamán`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "tokens[-2:]",
              "given = join tokens[:-2]"
            ],
            "kind": "guided",
            "tests": "Ana María / Quispe Huamán",
            "starterCode": "raw = 'Ana María Quispe Huamán'\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T1-B-E2",
            "instruction": "E2 (independiente) — Concepto: S07-T1-B (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Preserva partículas en given: `'María del Carmen Quispe Ríos'`. given debe incluir `del Carmen`. Salida/pass: `María del Carmen | Quispe Ríos`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "No borres tokens del medio al cortar apellidos finales.",
              "Misma heurística últimos 2 = apellidos."
            ],
            "kind": "independent",
            "tests": "María del Carmen",
            "starterCode": "raw = 'María del Carmen Quispe Ríos'\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T1-B-E3",
            "instruction": "E3 (transferencia) — Concepto: S07-T1-B (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Si hay menos de 3 tokens, marca `status='review'` y no inventes apellido2. Prueba `'Madonna'` y un nombre completo. Salida/pass: salida exacta del solution output del starter. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de.",
            "hints": [
              "len(toks) < 3 → review",
              "Conserva raw siempre."
            ],
            "kind": "transfer",
            "tests": "review + ok",
            "starterCode": "def parse_nombre(raw):\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T2-A-E1",
            "instruction": "E1 (guiado) — Concepto: S07-T2-A (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Split CSV-like simple `'C001,Ana,Lima'` en lista de campos strippeados e imprime. Salida/pass: `['C001', 'Ana', 'Lima']`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "split(',') + strip por campo",
              "Sin comillas en este ejercicio."
            ],
            "kind": "guided",
            "tests": "['C001', 'Ana', 'Lima']",
            "starterCode": "line = 'C001, Ana , Lima'\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T2-A-E2",
            "instruction": "E2 (independiente) — Concepto: S07-T2-A (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Une tokens `['Jr.', 'Unión', '450']` con espacio estable e imprime. Luego con `'-'`. Salida/pass: `Jr. Unión 450 | Jr.-Unión-450`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "' '.join(tokens)",
              "join no inserta al inicio/final."
            ],
            "kind": "independent",
            "tests": "espacio y guion",
            "starterCode": "toks = ['Jr.', 'Unión', '450']\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T2-A-E3",
            "instruction": "E3 (transferencia) — Normaliza guiones de teléfono con replace encadenado sin regex: `'999.000-111'` → solo stdlib str/unicodedata/re (S01–S07). Documenta el criterio en el memo del ejercicio y no inventes evidencia fuera del fixture sintético.",
            "hints": [
              "replace('.','').replace('-','') o filter isdigit",
              "str primero."
            ],
            "kind": "transfer",
            "tests": "999000111",
            "starterCode": "raw = '999.000-111'\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T2-B-E1",
            "instruction": "E1 (guiado) — Implementa `normalize_email`: strip+casefold, exactamente un `@`, local/dominio no vacíos y cero espacios. Imprime `'  A@B.COM '` y captura errores para `'@b.com'`, `'a@@b.com'` y `'a b@c.com'`.",
            "hints": [
              "s.count('@') == 1; split; local/domain no vacíos; cero espacios",
              "No uses regex ni exijas .com; acepta plus addressing."
            ],
            "kind": "guided",
            "tests": "Contrato exacto: ok a@b.com; tres review_error; user+tag@example.com sigue válido.",
            "starterCode": "def normalize_email(raw):\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T2-B-E2",
            "instruction": "E2 (independiente) — Concepto: S07-T2-B (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Teléfono a dígitos: `'(+51) 999-000-111'` → `'51999000111'`. Salida/pass: `51999000111`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "filter isdigit",
              "No valides operadora."
            ],
            "kind": "independent",
            "tests": "51999000111",
            "starterCode": "raw = '(+51) 999-000-111'\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T2-B-E3",
            "instruction": "E3 (transferencia) — Muestra que la regex `^[a-z]+@[a-z]+\\.com$` rechaza un email válido con `+`. Imprime rejected=True y la política exacta: un @, local/dominio no vacíos y cero espacios; la entregabilidad queda fuera de alcance.",
            "hints": [
              "fullmatch sobre email lower",
              "Print de política en español."
            ],
            "kind": "transfer",
            "tests": "rejected True + política",
            "starterCode": "import re\nemail = 'user+tag@example.com'\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T3-A-E1",
            "instruction": "E1 (guiado) — Concepto: S07-T3-A (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: `fullmatch` de código región `^[A-Z]{3}$` sobre `'LIM'` y `'Lima'`. Imprime bools. Salida/pass: `True | False`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "re.fullmatch",
              "Case sensitive según patrón."
            ],
            "kind": "guided",
            "tests": "True False",
            "starterCode": "import re\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T3-A-E2",
            "instruction": "E2 (independiente) — Concepto: S07-T3-A (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Extrae grupos nombre/apellido de `r'^(?P<nom>\\w+) (?P<ap>\\w+)$'` sobre `'Ana Quispe'`. Salida/pass: `{'nom': 'Ana', 'ap': 'Quispe'}`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "groupdict()",
              "fullmatch"
            ],
            "kind": "independent",
            "tests": "nom Ana ap Quispe",
            "starterCode": "import re\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T3-A-E3",
            "instruction": "E3 (transferencia) — Concepto: S07-T3-A (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Contrasta search vs fullmatch del patrón `\\d{8}` sobre `'DNI 12345678'`. Imprime ambos bools y cuándo usarías cada uno (print corto). Salida/pass: primeros tokens de `search True | fullmatch False | usar search para e…` según solution. Conserva el contrato del.",
            "hints": [
              "search True fullmatch False",
              "Explica en una línea."
            ],
            "kind": "transfer",
            "tests": "True/False + nota",
            "starterCode": "import re\ntext = 'DNI 12345678'\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T3-B-E1",
            "instruction": "E1 (guiado) — Concepto: S07-T3-B (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Compila `\\b9\\d{8}\\b` y reutiliza sobre dos strings; imprime findall de cada uno. Salida/pass: `tel 999000111 → ['999000111'] | no match 123 → []`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "re.compile una vez",
              "reuse en loop"
            ],
            "kind": "guided",
            "tests": "un match / vacío",
            "starterCode": "import re\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T3-B-E2",
            "instruction": "E2 (independiente) — Concepto: S07-T3-B (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: findall de códigos `[A-Z]{3}-\\d{2}` en un log sintético con dos códigos. Salida/pass: `['LIM-01', 'CUS-02']`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "re.findall(pat, log)",
              "Patrón simple."
            ],
            "kind": "independent",
            "tests": "LIM-01 CUS-02",
            "starterCode": "import re\nlog = 'ok LIM-01 y CUS-02 fin'\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T3-B-E3",
            "instruction": "E3 (transferencia) — Explica en prints el riesgo de backtracking con un patrón malo conceptual `(a+)+b` y por qué preferimos `a+b` o str methods. No necesitas colgar el proceso: solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "Cuantificadores anidados ambiguos",
              "Mensaje en español peruano/claro."
            ],
            "kind": "transfer",
            "tests": "mitigación documentada",
            "starterCode": "pass  # fixture vacío — usa solution contract\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T4-A-E1",
            "instruction": "E1 (guiado) — Concepto: S07-T4-A (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Exact match tras normalize: collapse + casefold de `'  Juan  PEREZ '` vs `'juan perez'`. Salida/pass: `True`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "join split + casefold",
              "print bool"
            ],
            "kind": "guided",
            "tests": "True",
            "starterCode": "def norm(s):\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T4-A-E2",
            "instruction": "E2 (independiente) — Concepto: S07-T4-A (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Implementa Jaccard de tokens y calcula score redondeado a 3 decimales para `'Juan Perez'` vs `'Juan P Perez'`. Salida/pass: `0.667`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "|A∩B|/|A∪B|",
              "set(split)"
            ],
            "kind": "independent",
            "tests": "≈0.667",
            "starterCode": "def token_jaccard(a, b):\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T4-A-E3",
            "instruction": "E3 (transferencia) — Concepto: S07-T4-A (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Si score está en [0.4, 1.0) decide `review`; si 1.0 `exact`; si <0.4 `no_match`. Aplica a score 0.67 e imprime decisión + raws. Salida/pass: `review Juan Perez Juan P Perez 0.67`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs.",
            "hints": [
              "Umbrales explícitos",
              "No auto-merge en review."
            ],
            "kind": "transfer",
            "tests": "review",
            "starterCode": "a, b, score = 'Juan Perez', 'Juan P Perez', 0.67\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T4-B-E1",
            "instruction": "E1 (guiado) — Concepto: S07-T4-B (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Clasifica FP/FN: pred match + truth no → FP; pred no + truth match → FN. Dos casos sintéticos. Salida/pass: `FP | FN`. Conserva el contrato del starter (no borres asserts ni datos); no pandas, no APIs de S08+; solo stdlib str/unicodedata/re (S01–S07).",
            "hints": [
              "Tabla de confusión 2x2 simplificada",
              "print tag por caso"
            ],
            "kind": "guided",
            "tests": "FP luego FN",
            "starterCode": "cases = [\n    {'pred': 'match', 'truth': 'no'},\n    {'pred': 'no', 'truth': 'match'},\n]\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T4-B-E2",
            "instruction": "E2 (independiente) — Concepto: S07-T4-B (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Empaqueta evidencia `dict(raw_a, raw_b, score, decision, reason)` e imprímelo para un par sintético. Salida/pass: primeros tokens de `{'raw_a': 'Juan Perez', 'raw_b': 'Juan P Perez', '…` según solution. Conserva el contrato del starter (no borres asserts ni.",
            "hints": [
              "Un dict con 5 claves",
              "reason en español"
            ],
            "kind": "independent",
            "tests": "5 keys",
            "starterCode": "evidence = {\n    'raw_a': 'Juan Perez',\n    'raw_b': 'Juan P Perez',\n    'score': 0.67,\n    'decision': 'review',\n    'reason': 'similitud parcial por tokens; requiere revisión humana',\n}\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S07-T4-B-E3",
            "instruction": "E3 (transferencia) — Concepto: S07-T4-B (Texto, Unicode y regex). Entrada: fixture sintético del starter (`CASO`/ids C00x) en texto y similaridad. Tarea: Redacta en 2-3 prints por qué el pipeline **no** afirma parentesco ni identidad legal a partir de Jaccard. Salida/pass: primeros tokens de `No afirmamos parentesco: el score textual no es pr…` según solution. Conserva el contrato del starter (no borres asserts.",
            "hints": [
              "Falta fuente autoritativa; riesgo ético; score ≠ prueba",
              "Español claro."
            ],
            "kind": "transfer",
            "tests": "3 líneas de política",
            "starterCode": "pass  # fixture vacío — usa solution contract\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          }
        ]
      },
      "youDo": {
        "title": "Normalización latinoamericana (CP-N1-B)",
        "context": "Continúas **CP-N1-B**: un pipeline de texto que conserva **raw**, emite **normalized** y registra **transforms** por campo. Unicode NFC, nombres con dos apellidos, email/tel modestos, regex solo donde aporta. Sin scraping/API. Sin afirmar parentesco ni identidad legal.",
        "objectives": [
          "normalize_record → {raw, normalized, transforms}",
          "NFC + casefold donde corresponda",
          "Dos apellidos / review si incompleto",
          "Regex responsable; str primero",
          "Tests de ejemplos latam sintéticos"
        ],
        "requirements": [
          "Firma normalize_record(raw: dict) documentada",
          "Unicode NFC en campos de nombre",
          "Sin scraping/API en este incremento V3",
          "Datos sintéticos peruanos/latam",
          "Email: un @, local/dominio no vacíos, cero espacios; plus permitido; inválido → review",
          "Teléfono: solo dígitos y prefijo 51 preservado; no inferir operadora",
          "transforms es un dict por campo con nombres ordenados (nfc, collapse_spaces, casefold, digits_only)",
          "Evidencia de match sin claims de parentesco"
        ],
        "portfolioNote": "Muestra en README 3 casos: nombre con partícula, email con +, teléfono con máscara; tabla raw→normalized→transforms. Subraya la política ética de no-parentesco.",
        "starterCode": "\"\"\"latam_normalize.py — Normalización latinoamericana (CP-N1-B / S07)\nConserva raw, produce normalized y lista transforms.\nSin scraping/API. Datos sintéticos.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport unicodedata\nfrom typing import Any\n\n\ndef nfc(s: str) -> str:\n    return unicodedata.normalize(\"NFC\", s)\n\n\ndef normalize_nombre(raw: str) -> tuple[str, list[str]]:\n    \"\"\"Retorna (normalized, transforms).\"\"\"\n    # TODO: collapse, nfc; no inventar apellidos\n    raise NotImplementedError\n\n\ndef normalize_email(raw: str) -> tuple[str, list[str]]:\n    # TODO: strip/casefold; exactamente un @, local+domain, sin espacios\n    raise NotImplementedError\n\n\ndef normalize_phone(raw: str) -> tuple[str, list[str]]:\n    # TODO: solo dígitos; conservar los dígitos 51 del prefijo +51\n    raise NotImplementedError\n\n\ndef normalize_record(raw: dict[str, Any]) -> dict[str, Any]:\n    \"\"\"→ {raw, normalized, transforms}.\"\"\"\n    # TODO: transforms={\"nombre\": [...], \"email\": [...], \"telefono\": [...]}\n    raise NotImplementedError\n\n\ndef main() -> None:\n    sample = {\n        \"nombre\": \"  María del Carmen Quispe Huamán \",\n        \"email\": \"  Ana.Perez+demo@Example.COM \",\n        \"telefono\": \"+51 999-000-111\",\n    }\n    print(normalize_record(sample))\n\n\nif __name__ == \"__main__\":\n    main()\n"
      },
      "selfCheck_stems": [
        {
          "question": "¿Por qué 'José' y 'Jose\\u0301' pueden fallar en == ?",
          "options": [
            "Python no soporta tildes",
            "casefold borra la é",
            "Formas Unicode distintas (NFC vs NFD)",
            "Son de tipos distintos"
          ]
        },
        {
          "question": "En nombres latam, si solo hay un token, la política segura es…",
          "options": [
            "Marcar review y conservar raw",
            "Inventar apellido2 vacío en silencio",
            "Rechazar y borrar el registro",
            "Asumir formato first/last US"
          ]
        },
        {
          "question": "¿Cuándo preferir str.replace/split sobre regex?",
          "options": [
            "Nunca",
            "Cuando la transformación es literal/simple",
            "Solo en emails",
            "Solo si el string es ASCII"
          ]
        },
        {
          "question": "fullmatch(r'\\d{8}', 'DNI 12345678') devuelve…",
          "options": [
            "match del número",
            "True booleano",
            "excepción",
            "None (no es full match)"
          ]
        },
        {
          "question": "Un Jaccard 0.67 entre nombres debe…",
          "options": [
            "Fusionar identidades automáticamente",
            "Afirmar parentesco",
            "Ir a review con evidencia (raw, score)",
            "Borrar ambos registros"
          ]
        }
      ],
      "taught_text_sha": "812cce67125959f6",
      "taught_text_len": 30525
    },
    {
      "file": "src/lib/course/sections/s08-pandas.ts",
      "id": "pandas",
      "index": 8,
      "title": "Archivos, CSV, JSON y contratos de ingesta",
      "shortTitle": "Archivos & ETL",
      "tagline": "pathlib, CSV/JSON, cuarentena y manifest de ingesta",
      "jobRelevance": "El gate **CP-N1-B** se cierra cuando puedes **ingerir archivos reales de negocio** (aunque sean sintéticos en el curso): CSV de clientes, JSON de transacciones, cuarentena con motivo, hashes, backup y **manifest reconciliado**. En junior data/analytics engineering en Perú esto pesa más que un groupby de demo. Id de plataforma `pandas` se conserva; el contenido V3 es stdlib + contratos de ingesta (pandas EDA se difiere al nivel 2).",
      "learningOutcomes": [
        "Abrir archivos con pathlib/Path y with; encoding utf-8 explícito",
        "Manejar newlines y escritura atómica (temp + replace)",
        "Leer/escribir CSV con headers y casts controlados",
        "Enviar filas irregulares a cuarentena con motivo",
        "Serializar/deserializar JSON (array y JSONL)",
        "Validar schema mínimo, nulls y defaults compatibles",
        "Calcular hashes, backup y provenance de inputs",
        "Emitir manifest con conteos reconciliados in=clean+quarantine"
      ],
      "theory": [
        {
          "subtopicId": "S08-T1-A",
          "heading": "pathlib, with, modos y encodings",
          "paragraphs": [
            "`pathlib.Path` unifica rutas. `Path.read_text(encoding='utf-8')` / `write_text` son convenientes; `with path.open(...) as f` da control de modo. En ingesta de archivos, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta contrato de ingesta y manifest CP-N1-B sin inventar hechos sobre personas reales.",
            "Modos: `r` lectura, `w` trunca, `a` append, `x` crea exclusivo. Siempre declara **`encoding='utf-8'`** en texto. `errors=` (`strict` default, `replace`) debe ser decisión documentada. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: pathlib, csv, json, open/with (S01–S08); no pandas de S15, no requests de S12.",
            "`path.exists()` / `is_file()` evitan sorpresas. No asumas el cwd: usa paths relativos al proyecto o `Path(__file__).resolve().parent`. Caso sintético Perú: CSV/JSON sintéticos de clientes C00x y montos PEN ficticios. Documenta decisión, métrica y límite conocido en el memo del subtema «pathlib, with, modos y encodings»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "from pathlib import Path\nimport tempfile, os\ntd = Path(tempfile.mkdtemp())\np = td / \"intake.txt\"\np.write_text(\"línea1\\njosé\\n\", encoding=\"utf-8\")\nprint(p.exists(), p.read_text(encoding=\"utf-8\").splitlines())\nwith p.open(\"a\", encoding=\"utf-8\") as f:\n    f.write(\"extra\\n\")\nprint(p.read_text(encoding=\"utf-8\"))",
          "code_output": "True ['línea1', 'josé']\nlínea1\njosé\nextra\n"
        },
        {
          "subtopicId": "S08-T1-B",
          "heading": "Newlines y escritura atómica",
          "paragraphs": [
            "CSV en Python: abre con `newline=''` para que el módulo csv controle terminadores. Texto universal: prefiere `\\n` en salidas del pipeline. En ingesta de archivos, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta contrato de ingesta y manifest CP-N1-B sin inventar hechos sobre personas reales.",
            "**Escritura atómica**: escribe a un archivo temporal en el mismo directorio y luego `os.replace(tmp, dest)`. Si el proceso muere a medias, no dejas el destino truncado. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: pathlib, csv, json, open/with (S01–S08); no pandas de S15, no requests de S12.",
            "Detectar `\\r\\n` en inputs ayuda a documentar provenance de origen Windows vs Unix. Caso sintético Perú: CSV/JSON sintéticos de clientes C00x y montos PEN ficticios. Documenta decisión, métrica y límite conocido en el memo del subtema «Newlines y escritura atómica»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "from pathlib import Path\nimport os, tempfile\ntd = Path(tempfile.mkdtemp())\ndest = td / \"out.txt\"\n\ndef write_atomic(path: Path, text: str) -> None:\n    path = Path(path)\n    tmp = path.with_suffix(path.suffix + \".tmp\")\n    tmp.write_text(text, encoding=\"utf-8\")\n    os.replace(tmp, path)\n\nwrite_atomic(dest, \"hola\\n\")\nprint(dest.read_text(encoding=\"utf-8\"))\nsample = b\"a\\r\\nb\\n\"\nprint(\"tiene CRLF\", b\"\\r\\n\" in sample)",
          "code_output": "hola\n\ntiene CRLF True"
        },
        {
          "subtopicId": "S08-T2-A",
          "heading": "Dialectos, headers y tipos",
          "paragraphs": [
            "`csv.DictReader` / `DictWriter` trabajan con headers. Declara `fieldnames`. Cast de tipos (`int`, `Decimal`) es **explícito** y fallos van a reject/cuarentena — no silencies con 0 mágico sin traza. El contrato monetario iniciado en S02 continúa: `Decimal` desde texto y cuantizado a `0.01`, nunca `float`.",
            "Fechas pueden quedarse como string ISO en N1-B si no hay parser de calendario aún; lo importante es el **contrato de columnas** documentado. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: pathlib, csv, json, open/with (S01–S08); no pandas de S15, no requests de S12.",
            "Dialectos (delimitador `;` vs `,`) se configuran; no asumas Excel latam sin mirar el archivo. Caso sintético Perú: CSV/JSON sintéticos de clientes C00x y montos PEN ficticios. Documenta decisión, métrica y límite conocido en el memo del subtema «Dialectos, headers y tipos»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "import csv, io\nfrom decimal import Decimal, InvalidOperation\nraw = \"id,nombre,monto\\nC001,Ana,10.5\\nC002,Luis,20\\n\"\nreader = csv.DictReader(io.StringIO(raw))\nfor row in reader:\n    row[\"monto\"] = Decimal(row[\"monto\"]).quantize(Decimal(\"0.01\"))\n    print(row)",
          "code_output": "{'id': 'C001', 'nombre': 'Ana', 'monto': Decimal('10.50')}\n{'id': 'C002', 'nombre': 'Luis', 'monto': Decimal('20.00')}"
        },
        {
          "subtopicId": "S08-T2-B",
          "heading": "Filas irregulares y cuarentena",
          "paragraphs": [
            "Filas con **más/menos columnas** que el header son irregulares. No las “arregles” en silencio: mándalas a `quarantine.csv` con **motivo** y conserva el raw. En ingesta de archivos, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta contrato de ingesta y manifest CP-N1-B sin inventar hechos sobre personas reales.",
            "Resumen de motivos (`contador por reason`) alimenta el manifest y el dashboard de calidad. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: pathlib, csv, json, open/with (S01–S08); no pandas de S15, no requests de S12.",
            "Good path escribe solo filas que pasaron schema + casts + normalización. Caso sintético Perú: CSV/JSON sintéticos de clientes C00x y montos PEN ficticios. Documenta decisión, métrica y límite conocido en el memo del subtema «Filas irregulares y cuarentena»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "import csv, io\ntext = \"id,nombre\\nC001,Ana\\nC002,Luis,EXTRA\\nC003\\n\"\nreader = csv.reader(io.StringIO(text))\nheader = next(reader)\ngood, bad = [], []\nfor row in reader:\n    if len(row) != len(header):\n        bad.append({\"raw\": row, \"reason\": f\"cols {len(row)}!={len(header)}\"})\n    else:\n        good.append(dict(zip(header, row)))\nprint(\"good\", good)\nprint(\"bad\", bad)",
          "code_output": "good [{'id': 'C001', 'nombre': 'Ana'}]\nbad [{'raw': ['C002', 'Luis', 'EXTRA'], 'reason': 'cols 3!=2'}, {'raw': ['C003'], 'reason': 'cols 1!=2'}]"
        },
        {
          "subtopicId": "S08-T3-A",
          "heading": "Objetos/arrays y serialización JSON",
          "paragraphs": [
            "`json.loads` / `dumps` y `load` / `dump` sobre archivos. JSON objects → dict; arrays → list. **JSONL** (un objeto por línea) es útil para streams de txs. JSON no tiene tipo Decimal: en este curso serializamos montos exactos como strings (`\"10.00\"`) y al leerlos reconstruimos `Decimal`; nunca los degradamos a float.",
            "`ensure_ascii=False` preserva tildes legibles. `sort_keys=True` ayuda a determinismo en manifests. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: pathlib, csv, json, open/with (S01–S08); no pandas de S15, no requests de S12.",
            "`datetime` no es serializable por defecto: convierte a `isoformat()` o str para evitar TypeError. Caso sintético Perú: CSV/JSON sintéticos de clientes C00x y montos PEN ficticios. Documenta decisión, métrica y límite conocido en el memo del subtema «Objetos/arrays y serialización JSON»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "import json\nfrom datetime import date\ndata = [{\"id\": \"T1\", \"día\": date(2026, 1, 15).isoformat()}]\ns = json.dumps(data, ensure_ascii=False, sort_keys=True)\nprint(s)\nprint(json.loads(s)[0][\"id\"])",
          "code_output": "[{\"día\": \"2026-01-15\", \"id\": \"T1\"}]\nT1"
        },
        {
          "subtopicId": "S08-T3-B",
          "heading": "Schema, nulls y evolución compatible",
          "paragraphs": [
            "Valida **required keys** antes de normalizar. `null` JSON → `None` en Python. Distingue null explícito de clave ausente si la política lo requiere. En ingesta de archivos, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta contrato de ingesta y manifest CP-N1-B sin inventar hechos sobre personas reales.",
            "Evolución: añadir campo opcional con **default** no rompe lectores viejos. Quitar required sí es breaking. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: pathlib, csv, json, open/with (S01–S08); no pandas de S15, no requests de S12.",
            "`validate_schema(obj, required)` retorna ok/errors para cuarentena. Caso sintético Perú: CSV/JSON sintéticos de clientes C00x y montos PEN ficticios. Documenta decisión, métrica y límite conocido en el memo del subtema «Schema, nulls y evolución compatible»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "def validate_schema(obj, required):\n    missing = [k for k in required if k not in obj]\n    return (len(missing) == 0, missing)\n\nprint(validate_schema({\"id\": \"C1\", \"email\": None}, [\"id\", \"email\"]))\nprint(validate_schema({\"id\": \"C1\"}, [\"id\", \"email\"]))\nobj = {\"id\": \"C1\"}\nobj.setdefault(\"segment\", \"default\")\nprint(obj)",
          "code_output": "(True, [])\n(False, ['email'])\n{'id': 'C1', 'segment': 'default'}"
        },
        {
          "subtopicId": "S08-T4-A",
          "heading": "Backups, hashes y provenance",
          "paragraphs": [
            "`hashlib.sha256` del contenido del input fija un fingerprint en el manifest. Si el CSV cambia, el hash cambia — detectas reprocesos. En ingesta de archivos, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta contrato de ingesta y manifest CP-N1-B sin inventar hechos sobre personas reales.",
            "Backup: copia `input.bak` o a `backups/` **antes** de transformar. No mutes el original in place. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: pathlib, csv, json, open/with (S01–S08); no pandas de S15, no requests de S12.",
            "Provenance mínima: `{path, sha256, bytes, received_at}` por fuente. Caso sintético Perú: CSV/JSON sintéticos de clientes C00x y montos PEN ficticios. Documenta decisión, métrica y límite conocido en el memo del subtema «Backups, hashes y provenance»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "from pathlib import Path\nimport hashlib, tempfile, shutil\ntd = Path(tempfile.mkdtemp())\nsrc = td / \"clients.csv\"\nsrc.write_text(\"id,nombre\\nC001,Ana\\n\", encoding=\"utf-8\")\nh = hashlib.sha256(src.read_bytes()).hexdigest()\nbak = td / \"clients.csv.bak\"\nshutil.copy2(src, bak)\nprint(\"sha256\", h[:16] + \"...\")\nprint(\"bak exists\", bak.exists())\nprint(\"provenance\", {\"path\": str(src.name), \"sha256\": h, \"bytes\": src.stat().st_size})",
          "code_output": "sha256 206bcfbde4f213a5...\nbak exists True\nprovenance {'path': 'clients.csv', 'sha256': '206bcfbde4f213a5b89c4d88b9a63d7b9c436b3b7c13db84e63445d1574f7eba', 'bytes': 19}"
        },
        {
          "subtopicId": "S08-T4-B",
          "heading": "Reconciliación y manifest de corrida",
          "paragraphs": [
            "Manifest JSON de la corrida: timestamps, versión y una entrada por fuente con `name`, hash y conteos `n_in`, `n_clean`, `n_quarantine`. Los totales de corrida se calculan sumando fuentes; no se escriben a mano. En ingesta de archivos, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta contrato de ingesta y manifest CP-N1-B sin inventar hechos sobre personas reales.",
            "**Reconciliación en dos niveles**: cada fuente cumple `n_in == n_clean + n_quarantine` y los totales son la suma exacta de las fuentes. Validar solo el agregado puede ocultar un sobrante en CSV compensado por un faltante en JSON. Si cualquier fuente no cuadra, **falla la corrida** — no publiques clean a medias.",
            "Evidencia del gate CP-N1-B: scripts + fixtures + manifest de demo + tests + README. Caso sintético Perú: CSV/JSON sintéticos de clientes C00x y montos PEN ficticios. Documenta decisión, métrica y límite conocido en el memo del subtema «Reconciliación y manifest de corrida»; nunca PII real ni inferencia automática de parentesco/fraude."
          ],
          "code": "import json\nsources = [\n    {\"name\": \"clients.csv\", \"sha256\": \"abc\", \"n_in\": 6, \"n_clean\": 5, \"n_quarantine\": 1},\n    {\"name\": \"transactions.json\", \"sha256\": \"def\", \"n_in\": 4, \"n_clean\": 3, \"n_quarantine\": 1},\n]\nfor source in sources:\n    source[\"reconcile_ok\"] = source[\"n_in\"] == source[\"n_clean\"] + source[\"n_quarantine\"]\nmanifest = {\n    \"sources\": sources,\n    \"n_in\": sum(s[\"n_in\"] for s in sources),\n    \"n_clean\": sum(s[\"n_clean\"] for s in sources),\n    \"n_quarantine\": sum(s[\"n_quarantine\"] for s in sources),\n}\nmanifest[\"reconcile_ok\"] = all(s[\"reconcile_ok\"] for s in sources)\nprint(\"reconcile_ok\", manifest[\"reconcile_ok\"])\nprint([(s[\"name\"], s[\"reconcile_ok\"]) for s in sources])",
          "code_output": "reconcile_ok True\n[('clients.csv', True), ('transactions.json', True)]"
        }
      ],
      "iDo": {
        "steps": [
          {
            "demoId": "S08-T1-A-DEMO",
            "subtopicId": "S08-T1-A",
            "environment": "local-python",
            "description": "Leer y escribir intake UTF-8 con Path",
            "why": "Path + UTF-8 explícito es la base de toda ingesta local del gate.",
            "code": "from pathlib import Path\nimport tempfile\ntd = Path(tempfile.mkdtemp())\np = td / \"intake.txt\"\np.write_text(\"cliente;José Quispe\\n\", encoding=\"utf-8\")\ntext = p.read_text(encoding=\"utf-8\")\nprint(text.strip())\nprint(\"exists\", p.exists(), \"size\", p.stat().st_size)",
            "language": "python",
            "output": "cliente;José Quispe\nexists True size 21"
          },
          {
            "demoId": "S08-T1-B-DEMO",
            "subtopicId": "S08-T1-B",
            "environment": "local-python",
            "description": "write_atomic(path, text)",
            "why": "os.replace hace el swap atómico del artefacto de salida.",
            "code": "from pathlib import Path\nimport os, tempfile\n\ndef write_atomic(path: Path, text: str) -> None:\n    path = Path(path)\n    tmp = path.with_name(path.name + \".tmp\")\n    tmp.write_text(text, encoding=\"utf-8\")\n    os.replace(tmp, path)\n\ntd = Path(tempfile.mkdtemp())\ndest = td / \"clean.csv\"\nwrite_atomic(dest, \"id,nombre\\nC001,Ana\\n\")\nprint(dest.read_text(encoding=\"utf-8\"))\nprint(\"tmp gone\", not (td / \"clean.csv.tmp\").exists())",
            "language": "python",
            "output": "id,nombre\nC001,Ana\n\ntmp gone True"
          },
          {
            "demoId": "S08-T2-A-DEMO",
            "subtopicId": "S08-T2-A",
            "environment": "local-python",
            "description": "Ingesta CSV con monto Decimal y fecha ISO string",
            "why": "DictReader + Decimal desde texto; fecha como string ISO documentado.",
            "code": "import csv, io\nfrom decimal import Decimal\nraw = \"id,nombre,monto,fecha\\nC001,Ana,10.5,2026-01-10\\nC002,Luis,20,2026-01-11\\n\"\nrows = []\nfor row in csv.DictReader(io.StringIO(raw)):\n    row[\"monto\"] = Decimal(row[\"monto\"]).quantize(Decimal(\"0.01\"))\n    # fecha queda ISO string (contrato N1-B)\n    rows.append(row)\nfor r in rows:\n    print(r[\"id\"], r[\"monto\"], type(r[\"monto\"]).__name__, r[\"fecha\"])",
            "language": "python",
            "output": "C001 10.50 Decimal 2026-01-10\nC002 20.00 Decimal 2026-01-11"
          },
          {
            "demoId": "S08-T2-B-DEMO",
            "subtopicId": "S08-T2-B",
            "environment": "local-python",
            "description": "Separar good.csv vs quarantine.csv",
            "why": "Cuarentena con motivo deja audit trail; good solo tiene filas sanas.",
            "code": "import csv, io\nfrom pathlib import Path\nimport tempfile\n\ntext = \"id,nombre\\nC001,Ana\\nC002,Luis,EXTRA\\nbadonly\\n\"\nreader = csv.reader(io.StringIO(text))\nheader = next(reader)\ngood, quar = [], []\nfor row in reader:\n    if len(row) != len(header):\n        quar.append({\"raw\": \",\".join(row), \"reason\": \"col_count\"})\n    else:\n        good.append(dict(zip(header, row)))\ntd = Path(tempfile.mkdtemp())\nwith (td / \"good.csv\").open(\"w\", newline=\"\", encoding=\"utf-8\") as f:\n    w = csv.DictWriter(f, fieldnames=header)\n    w.writeheader()\n    w.writerows(good)\nwith (td / \"quarantine.csv\").open(\"w\", newline=\"\", encoding=\"utf-8\") as f:\n    w = csv.DictWriter(f, fieldnames=[\"raw\", \"reason\"])\n    w.writeheader()\n    w.writerows(quar)\nprint(\"good\", good)\nprint(\"quarantine\", quar)\nprint((td / \"good.csv\").read_text(encoding=\"utf-8\"))",
            "language": "python",
            "output": "good [{'id': 'C001', 'nombre': 'Ana'}]\nquarantine [{'raw': 'C002,Luis,EXTRA', 'reason': 'col_count'}, {'raw': 'badonly', 'reason': 'col_count'}]\nid,nombre\nC001,Ana\n"
          },
          {
            "demoId": "S08-T3-A-DEMO",
            "subtopicId": "S08-T3-A",
            "environment": "local-python",
            "description": "Exportar results a JSON array + JSONL",
            "why": "Array JSON para batch pequeño; JSONL para append y streaming de txs.",
            "code": "import json\nfrom pathlib import Path\nimport tempfile\nrows = [{\"id\": \"T1\", \"monto\": \"10.00\"}, {\"id\": \"T2\", \"monto\": \"5.00\"}]\ntd = Path(tempfile.mkdtemp())\n(td / \"txs.json\").write_text(\n    json.dumps(rows, ensure_ascii=False, indent=2), encoding=\"utf-8\"\n)\nwith (td / \"txs.jsonl\").open(\"w\", encoding=\"utf-8\") as f:\n    for r in rows:\n        f.write(json.dumps(r, ensure_ascii=False) + \"\\n\")\nprint((td / \"txs.json\").read_text(encoding=\"utf-8\"))\nprint(\"jsonl lines\", (td / \"txs.jsonl\").read_text(encoding=\"utf-8\").splitlines())",
            "language": "python",
            "output": "[\n  {\n    \"id\": \"T1\",\n    \"monto\": \"10.00\"\n  },\n  {\n    \"id\": \"T2\",\n    \"monto\": \"5.00\"\n  }\n]\njsonl lines ['{\"id\": \"T1\", \"monto\": \"10.00\"}', '{\"id\": \"T2\", \"monto\": \"5.00\"}']"
          },
          {
            "demoId": "S08-T3-B-DEMO",
            "subtopicId": "S08-T3-B",
            "environment": "local-python",
            "description": "validate_schema(obj, required) + campo opcional nuevo",
            "why": "Required estricto + defaults opcionales permiten evolucionar el contrato.",
            "code": "def validate_schema(obj, required):\n    missing = [k for k in required if k not in obj]\n    return len(missing) == 0, missing\n\nrequired = [\"id\", \"email\"]\nprint(validate_schema({\"id\": \"C1\", \"email\": None}, required))\nprint(validate_schema({\"id\": \"C1\"}, required))\n# evolución: campo opcional con default\nobj = {\"id\": \"C1\", \"email\": \"a@ex.com\"}\nobj.setdefault(\"segment\", \"standard\")\nprint(obj)",
            "language": "python",
            "output": "(True, [])\n(False, ['email'])\n{'id': 'C1', 'email': 'a@ex.com', 'segment': 'standard'}"
          },
          {
            "demoId": "S08-T4-A-DEMO",
            "subtopicId": "S08-T4-A",
            "environment": "local-python",
            "description": "sha256 de input CSV + backup .bak",
            "why": "Hash + backup del crudo son la provenance mínima del gate.",
            "code": "from pathlib import Path\nimport hashlib, shutil, tempfile\ntd = Path(tempfile.mkdtemp())\nsrc = td / \"clients.csv\"\nsrc.write_text(\"id,nombre\\nC001,Ana\\n\", encoding=\"utf-8\")\ndigest = hashlib.sha256(src.read_bytes()).hexdigest()\nbak = Path(str(src) + \".bak\")\nshutil.copy2(src, bak)\nprint(digest)\nprint(\"backup_ok\", bak.exists() and bak.read_bytes() == src.read_bytes())",
            "language": "python",
            "output": "206bcfbde4f213a5b89c4d88b9a63d7b9c436b3b7c13db84e63445d1574f7eba\nbackup_ok True"
          },
          {
            "demoId": "S08-T4-B-DEMO",
            "subtopicId": "S08-T4-B",
            "environment": "local-python",
            "description": "manifest.json de una corrida ETL",
            "why": "El manifest prueba reconciliación por fuente y agregada; ninguna fuente puede esconder pérdidas detrás de otra.",
            "code": "import json\nfrom pathlib import Path\nimport tempfile\nsources = [\n    {\"name\": \"clients.csv\", \"sha256\": \"deadbeef\", \"n_in\": 3, \"n_clean\": 2, \"n_quarantine\": 1},\n    {\"name\": \"transactions.json\", \"sha256\": \"cafebabe\", \"n_in\": 2, \"n_clean\": 2, \"n_quarantine\": 0},\n]\nfor source in sources:\n    source[\"reconcile_ok\"] = source[\"n_in\"] == source[\"n_clean\"] + source[\"n_quarantine\"]\nmanifest = {\n    \"run_id\": \"demo-001\",\n    \"sources\": sources,\n    \"n_in\": sum(s[\"n_in\"] for s in sources),\n    \"n_clean\": sum(s[\"n_clean\"] for s in sources),\n    \"n_quarantine\": sum(s[\"n_quarantine\"] for s in sources),\n    \"reconcile_ok\": all(s[\"reconcile_ok\"] for s in sources),\n}\ntd = Path(tempfile.mkdtemp())\npath = td / \"manifest.json\"\npath.write_text(json.dumps(manifest, indent=2, sort_keys=True), encoding=\"utf-8\")\nprint(path.read_text(encoding=\"utf-8\"))",
            "language": "python",
            "output": "{\n  \"n_clean\": 4,\n  \"n_in\": 5,\n  \"n_quarantine\": 1,\n  \"reconcile_ok\": true,\n  \"run_id\": \"demo-001\",\n  \"sources\": [\n    {\n      \"n_clean\": 2,\n      \"n_in\": 3,\n      \"n_quarantine\": 1,\n      \"name\": \"clients.csv\",\n      \"reconcile_ok\": true,\n      \"sha256\": \"deadbeef\"\n    },\n    {\n      \"n_clean\": 2,\n      \"n_in\": 2,\n      \"n_quarantine\": 0,\n      \"name\": \"transactions.json\",\n      \"reconcile_ok\": true,\n      \"sha256\": \"cafebabe\"\n    }\n  ]\n}"
          }
        ]
      },
      "weDo": {
        "exercises": [
          {
            "id": "S08-T1-A-E1",
            "instruction": "E1 (guiado) — Concepto: S08-T1-A (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: En un temp dir, crea `Path` a `demo.txt`, escribe 'hola', comprueba `exists` e imprime True/False. Salida/pass: `True`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12; solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "write_text + exists",
              "encoding utf-8"
            ],
            "kind": "guided",
            "tests": "True",
            "starterCode": "from pathlib import Path\nimport tempfile\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T1-A-E2",
            "instruction": "E2 (independiente) — Concepto: S08-T1-A (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Escribe 3 líneas con with open, luego lee con readlines strip e imprime la lista. Salida/pass: `['a', 'b', 'c']`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12; solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "with path.open('w', encoding='utf-8')",
              "newline natural \\n"
            ],
            "kind": "independent",
            "tests": "['a','b','c']",
            "starterCode": "from pathlib import Path\nimport tempfile\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T1-A-E3",
            "instruction": "E3 (transferencia) — Simula UnicodeDecodeError leyendo bytes latinos como utf-8 strict si es latin-1 content… Mejor: escribe bytes no-UTF8 y captura UnicodeDecodeError al read_text utf-8. Imprime tipo de error y sugiere encoding o quarantine.",
            "hints": [
              "path.write_bytes(b'\\xff\\xfe') o latin1 bytes inválidos en utf-8",
              "try/except UnicodeDecodeError"
            ],
            "kind": "transfer",
            "tests": "UnicodeDecodeError",
            "starterCode": "from pathlib import Path\nimport tempfile\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T1-B-E1",
            "instruction": "E1 (guiado) — Concepto: S08-T1-B (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Detecta si un `bytes` sample contiene CRLF `\\r\\n`. Imprime True para sample Windows y False para solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "b'\\r\\n' in data",
              "Dos samples."
            ],
            "kind": "guided",
            "tests": "True False",
            "starterCode": "# fixture\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T1-B-E2",
            "instruction": "E2 (independiente) — Concepto: S08-T1-B (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Implementa `write_atomic` y verifica contenido final. Salida/pass: `ok`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12; solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "tmp + os.replace",
              "mismo directorio que dest"
            ],
            "kind": "independent",
            "tests": "ok",
            "starterCode": "from pathlib import Path\nimport os, tempfile\n\ndef write_atomic(path, text):\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T1-B-E3",
            "instruction": "E3 (transferencia) — Simula fallo mid-write: escribe dest parcial 'PARCIAL' y muestra que un atomic replace posterior deja 'COMPLETO'. Imprime ambos estados.",
            "hints": [
              "Primero write no atómico parcial; luego write_atomic",
              "Contrasta riesgos."
            ],
            "kind": "transfer",
            "tests": "PARCIAL luego COMPLETO",
            "starterCode": "from pathlib import Path\nimport os, tempfile\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T2-A-E1",
            "instruction": "E1 (guiado) — Concepto: S08-T2-A (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: DictReader sobre StringIO con header id,nombre; imprime filas dict. Salida/pass: `{'id': 'C001', 'nombre': 'Ana'}`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12; solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "csv.DictReader",
              "io.StringIO"
            ],
            "kind": "guided",
            "tests": "C001 Ana",
            "starterCode": "import csv, io\nraw = 'id,nombre\\nC001,Ana\\n'\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T2-A-E2",
            "instruction": "E2 (independiente) — Concepto: S08-T2-A (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Escribe CSV con DictWriter (header id,nombre) a StringIO o temp y relee contando filas de datos = 1. Salida/pass: `1 | {'id': 'C001', 'nombre': 'Ana'}`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12;.",
            "hints": [
              "writeheader + writerow",
              "newline='' si usas open file"
            ],
            "kind": "independent",
            "tests": "1 fila",
            "starterCode": "import csv, io\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T2-A-E3",
            "instruction": "E3 (transferencia) — Cast de monto: `Decimal` desde texto + quantize a céntimos; si lanza `InvalidOperation` → reject con motivo. Procesa `['10', 'x', '3.5']` e imprime ok/reject.",
            "hints": [
              "Decimal(v).quantize(Decimal('0.01')); except InvalidOperation",
              "No uses float ni 0 silencioso."
            ],
            "kind": "transfer",
            "tests": "Contrato exacto: ok 10.00; reject x motivo=cast_monto; ok 3.50; sin float().",
            "starterCode": "from decimal import Decimal, InvalidOperation\n\nvals = ['10', 'x', '3.5']\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T2-B-E1",
            "instruction": "E1 (guiado) — Concepto: S08-T2-B (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Detecta fila irregular: header len 2, row `['C1','Ana','x']` → irregular True. Salida/pass: `True`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12; solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "len(row) != len(header)",
              "print bool"
            ],
            "kind": "guided",
            "tests": "True",
            "starterCode": "header = ['id', 'nombre']\nrow = ['C1', 'Ana', 'x']\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T2-B-E2",
            "instruction": "E2 (independiente) — Concepto: S08-T2-B (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Escribe una fila de cuarentena `{raw, reason}` a CSV en temp y relee imprimiendo reason. Salida/pass: `col_count`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12; solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "DictWriter fieldnames raw,reason",
              "newline=''"
            ],
            "kind": "independent",
            "tests": "col_count",
            "starterCode": "from pathlib import Path\nimport csv, tempfile\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T2-B-E3",
            "instruction": "E3 (transferencia) — Concepto: S08-T2-B (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Dada lista de reasons, imprime resumen contador sorted por reason. Salida/pass: `cast_monto 1 | col_count 2 | schema 1`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12; solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "collections.Counter o dict",
              "sorted items"
            ],
            "kind": "transfer",
            "tests": "conteos por reason",
            "starterCode": "from collections import Counter\nreasons = ['col_count', 'cast_monto', 'col_count', 'schema']\nfor k, v in sorted(Counter(reasons).items()):\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T3-A-E1",
            "instruction": "E1 (guiado) — Concepto: S08-T3-A (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: `json.loads` de fixture `'{\"id\":\"C001\"}'` e imprime id. Salida/pass: `C001`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12; solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "json.loads",
              "dict access"
            ],
            "kind": "guided",
            "tests": "C001",
            "starterCode": "import json\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T3-A-E2",
            "instruction": "E2 (independiente) — Concepto: S08-T3-A (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: `dumps` de `{'nombre':'José'}` con ensure_ascii=False e imprime (debe verse José, no \\u). Salida/pass: `{\"nombre\": \"José\"}`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12; solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "ensure_ascii=False",
              "print string"
            ],
            "kind": "independent",
            "tests": "José legible",
            "starterCode": "import json\ns = json.dumps({'nombre': 'José'}, ensure_ascii=False)\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T3-A-E3",
            "instruction": "E3 (transferencia) — Concepto: S08-T3-A (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Intenta dumps de dict con datetime y captura TypeError; reintenta con isoformat str. Salida/pass: `TypeError | {\"ts\": \"2026-01-15T10:00:00\"}`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12 solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "datetime no serializable",
              "convierte a .isoformat()"
            ],
            "kind": "transfer",
            "tests": "TypeError luego ISO",
            "starterCode": "import json\nfrom datetime import datetime\nobj = {'ts': datetime(2026, 1, 15, 10, 0, 0)}\ntry:\n    json.dumps(obj)\nexcept TypeError as e:\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T3-B-E1",
            "instruction": "E1 (guiado) — Concepto: S08-T3-B (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Rechaza missing required: obj sin email con required [id,email] → ok False. Salida/pass: `(False, ['email'])`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12; solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "list comprehension missing",
              "print ok, missing"
            ],
            "kind": "guided",
            "tests": "False, ['email']",
            "starterCode": "def validate_schema(obj, required):\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T3-B-E2",
            "instruction": "E2 (independiente) — Concepto: S08-T3-B (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: null explícito: `{'id':'C1','email': None}` tiene clave email (presente) con valor None. Imprime 'in' y valor. Salida/pass: `True | None`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12; solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "'email' in obj",
              "None is not missing key"
            ],
            "kind": "independent",
            "tests": "True / None",
            "starterCode": "obj = {'id': 'C1', 'email': None}\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T3-B-E3",
            "instruction": "E3 (transferencia) — Concepto: S08-T3-B (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Añade campo opcional `segment` con default `'standard'` vía setdefault sin pisar si ya existe. Dos objs. Salida/pass: primeros tokens de `{'id': 'C1', 'segment': 'standard'} | {'id': 'C2',…` según solution. Conserva el contrato del starter (no borres.",
            "hints": [
              "setdefault",
              "No uses overwrite ciego"
            ],
            "kind": "transfer",
            "tests": "standard vs vip",
            "starterCode": "a = {'id': 'C1'}\nb = {'id': 'C2', 'segment': 'vip'}\na.setdefault('segment', 'standard')\nb.setdefault('segment', 'standard')\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T4-A-E1",
            "instruction": "E1 (guiado) — Concepto: S08-T4-A (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Calcula sha256 hex de un archivo temp con contenido conocido e imprime los primeros 8 chars + len digest 64. Salida/pass: `ba7816bf 64`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12; solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "hashlib.sha256(path.read_bytes()).hexdigest()",
              "temp file"
            ],
            "kind": "guided",
            "tests": "len 64",
            "starterCode": "from pathlib import Path\nimport hashlib, tempfile\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T4-A-E2",
            "instruction": "E2 (independiente) — Concepto: S08-T4-A (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Copia backup con shutil.copy2 y verifica igualdad de bytes. Salida/pass: `True`. Conserva el contrato del starter (no borres asserts ni datos); no pandas de S15, no requests de S12; solo pathlib, csv, json, open/with (S01–S08).",
            "hints": [
              "shutil.copy2",
              "read_bytes compare"
            ],
            "kind": "independent",
            "tests": "True",
            "starterCode": "from pathlib import Path\nimport shutil, tempfile\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T4-A-E3",
            "instruction": "E3 (transferencia) — Concepto: S08-T4-A (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Arma dict de provenance `{path, sha256, bytes}` para un temp file e imprímelo (path solo name). Salida/pass: primeros tokens de `{'path': 'clients.csv', 'sha256': 'b776a3a3926835c…` según solution. Conserva el contrato del starter (no borres asserts ni.",
            "hints": [
              "stat().st_size",
              "sha256 completo ok"
            ],
            "kind": "transfer",
            "tests": "3 keys",
            "starterCode": "from pathlib import Path\nimport hashlib, tempfile\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T4-B-E1",
            "instruction": "E1 (guiado) — Construye un manifest con `run_id` y `sources`: cada fuente incluye name, sha256, n_in, n_clean, n_quarantine y reconcile_ok. Deriva los tres totales con sum; no los hardcodees.",
            "hints": [
              "Calcula reconcile_ok por fuente antes de sumar",
              "sources contiene clients.csv y transactions.json."
            ],
            "kind": "guided",
            "tests": "Contrato exacto: totales 5/4/1; ambas fuentes reconcile_ok=True; manifest reconcile_ok=True.",
            "starterCode": "sources = [\n    {'name': 'clients.csv', 'sha256': 'abc', 'n_in': 3, 'n_clean': 2, 'n_quarantine': 1},\n    {'name': 'transactions.json', 'sha256': 'def', 'n_in': 2, 'n_clean': 2, 'n_quarantine': 0},\n]\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T4-B-E2",
            "instruction": "E2 (independiente) — Concepto: S08-T4-B (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: Implementa `reconcile_sources(sources)`: exige igualdad por cada fuente y también verifica que los totales derivados cuadren. Salida/pass: salida exacta del solution output del starter. Conserva el contrato del starter (no borres asserts ni datos); no.",
            "hints": [
              "all(s['n_in'] == s['n_clean'] + s['n_quarantine'] for s in sources)",
              "Prueba un caso donde el agregado cuadra pero cada fuente no."
            ],
            "kind": "independent",
            "tests": "Contrato exacto: good=True; compensated_bad=False aunque 10 == 9 + 1.",
            "starterCode": "def reconcile_sources(sources):\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          },
          {
            "id": "S08-T4-B-E3",
            "instruction": "E3 (transferencia) — Concepto: S08-T4-B (Archivos, CSV, JSON e ingesta). Entrada: fixture sintético del starter (`CASO`/ids C00x) en ingesta de archivos. Tarea: `run(sources)` publica solo si todas las fuentes reconcilian; el caso compensated_bad debe imprimir la fuente rota y exit_code 1. Salida/pass: salida exacta del solution output del starter. Conserva el contrato del starter (no borres asserts ni datos); no.",
            "hints": [
              "No publiques clean si no cuadra",
              "Reporta los nombres de todas las fuentes rotas."
            ],
            "kind": "transfer",
            "tests": "Contrato exacto: good → OK/0; compensated_bad → ERROR sources=clients.csv,transactions.json/1.",
            "starterCode": "def run(sources):\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
          }
        ]
      },
      "youDo": {
        "title": "Client/Transaction ETL Pipeline (cierre CP-N1-B)",
        "context": "Cierras el gate **CP-N1-B**. Integras normalizadores (S05–S07) y el modelo en memoria (S06) en un ETL **local-python**: `data/clients.csv` + `data/transactions.json` (sintéticos) → `out/clean/`, `out/quarantine/`, `out/manifest.json` con hashes y reconciliación. CLI instalable se difiere a S10. Sin PII real; sin claims de fraude/parentesco.",
        "objectives": [
          "Ingesta CSV y JSON con contratos documentados",
          "Validar/normalizar y cuarentenar rejects con motivo",
          "Manifest por fuente con hash, conteos, reconciliación y totales derivados",
          "Pruebas normal / borde / error",
          "Fail closed si reconcile no cuadra"
        ],
        "requirements": [
          "Entradas sintéticas clients.csv + transactions.json",
          "Salidas out/clean/, out/quarantine/, out/manifest.json",
          "Integrar normalizadores y modelo en memoria",
          "README + demo local-python reproducible",
          "Cada fuente cumple n_in == n_clean + n_quarantine o el proceso termina con exit != 0",
          "Los totales del manifest se derivan de sources; una compensación entre fuentes nunca oculta un error",
          "Montos entran como texto, se convierten con Decimal y se serializan como texto decimal",
          "Empaquetado CLI diferido a S10"
        ],
        "portfolioNote": "Adjunta manifest de demo, 1 fila de cuarentena con motivo, hashes de inputs y un test de reconciliación fallida (exit 1). Esa carpeta es evidencia del gate CP-N1-B.",
        "starterCode": "\"\"\"etl_cp_n1_b.py — Client/Transaction ETL Pipeline (cierre CP-N1-B / S08)\nIngesta CSV+JSON sintéticos → clean/quarantine/manifest.\nstdlib only. Local-python.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport csv\nimport hashlib\nimport json\nimport os\nimport shutil\nfrom decimal import Decimal, InvalidOperation\nfrom datetime import datetime, timezone\nfrom pathlib import Path\nfrom typing import Any\n\n# Estructura esperada:\n# data/clients.csv\n# data/transactions.json\n# out/clean/\n# out/quarantine/\n# out/manifest.json\n\n\ndef sha256_file(path: Path) -> str:\n    # TODO\n    raise NotImplementedError\n\n\ndef write_atomic(path: Path, text: str) -> None:\n    # TODO\n    raise NotImplementedError\n\n\ndef load_clients_csv(path: Path) -> tuple[list[dict], list[dict]]:\n    \"\"\"→ (good_rows, quarantine_rows with reason).\"\"\"\n    # TODO irregular rows + casts\n    raise NotImplementedError\n\n\ndef load_transactions_json(path: Path) -> tuple[list[dict], list[dict]]:\n    # TODO schema mínimo id, client_id, monto; Decimal desde texto + quantize(\"0.01\")\n    raise NotImplementedError\n\n\ndef build_manifest(\n    *,\n    sources: list[dict[str, Any]],\n) -> dict[str, Any]:\n    \"\"\"Deriva totales y reconcile_ok desde cada fuente; no acepta totales agregados.\"\"\"\n    # TODO validar name/sha256/conteos; reconcile por fuente; sumar totales\n    raise NotImplementedError\n\n\ndef run(data_dir: Path, out_dir: Path) -> int:\n    \"\"\"Retorna exit code 0/1.\"\"\"\n    # TODO backup, load, write clean/quar, manifest, fail if not reconcile\n    raise NotImplementedError\n\n\ndef main() -> None:\n    root = Path(__file__).resolve().parent\n    code = run(root / \"data\", root / \"out\")\n    raise SystemExit(code)\n\n\nif __name__ == \"__main__\":\n    main()\n"
      },
      "selfCheck_stems": [
        {
          "question": "¿Por qué declarar encoding='utf-8' al abrir texto?",
          "options": [
            "Es más rápido",
            "Comprime el archivo",
            "Activa pathlib",
            "Evita depender del locale del SO (p. ej. Windows)"
          ]
        },
        {
          "question": "Escritura atómica típica es…",
          "options": [
            "open(dest,'w') y escribir directo siempre",
            "escribir temp y os.replace al destino",
            "solo print al stdout",
            "append eterno al mismo file"
          ]
        },
        {
          "question": "Una fila CSV con columnas de más debe…",
          "options": [
            "Ignorarse en silencio",
            "Rellenarse con None sin traza",
            "Ir a cuarentena con motivo",
            "Pisar el header"
          ]
        },
        {
          "question": "Reconciliación del manifest exige…",
          "options": [
            "n_in == n_clean + n_quarantine",
            "n_clean > n_in",
            "solo n_quarantine == 0",
            "hash del clean == hash del input"
          ]
        },
        {
          "question": "Si reconcile falla, el pipeline debe…",
          "options": [
            "Publicar clean igual",
            "Borrar el manifest",
            "Convertir a pandas automáticamente",
            "Fallar (exit non-zero) / fail closed"
          ]
        }
      ],
      "taught_text_sha": "7cca6ad2df4dee34",
      "taught_text_len": 31988
    },
    {
      "file": "src/lib/course/sections/s09-visualization.ts",
      "id": "visualization",
      "index": 9,
      "title": "Excepciones, debugging y logging seguro",
      "shortTitle": "Excepciones & logs",
      "tagline": "Excepciones específicas, diagnóstico, logging sin PII y resiliencia del pipeline de familiaridad",
      "jobRelevance": "En pipelines de familiaridad y data engineering junior en Perú, un crash opaco o un log con email completo te cuesta incidentes y cumplimiento. Esta sección (id de plataforma `visualization` conservado) retematiza S09 a **excepciones, debugging y logging seguro**: inicio de **CP-N1-C**. matplotlib/seaborn se difieren al tramo de visualización de datos.",
      "learningOutcomes": [
        "Elegir tipos de excepción, raise con contexto y chaining con from",
        "Dibujar fronteras try/except/finally y separar recuperable vs fatal",
        "Leer tracebacks y ubicar el frame útil sin exponer secretos",
        "Reducir fallos a minimal repro con hipótesis y tests de regresión",
        "Configurar logging con niveles y campos estructurados",
        "Propagar correlation_id y redactar email/teléfono/dirección",
        "Decidir fail-fast vs cuarentena según data|config|provider",
        "Reintentar solo errores transitorios con operaciones idempotentes"
      ],
      "theory": [
        {
          "subtopicId": "S09-T1-A",
          "heading": "Tipos específicos, raise y chaining",
          "paragraphs": [
            "Prefiere **tipos concretos**: `ValueError` (valor ilegal), `TypeError` (tipo incorrecto), `KeyError` (clave ausente), `OSError`/`FileNotFoundError` (I/O). `except Exception` genérico oculta la causa y complica el triage.",
            "`raise ValueError('monto no numérico: …')` da contexto accionable. Para montos mantenemos el contrato previo: **`Decimal` desde texto**, `quantize(Decimal('0.01'))` y rechazo de valores no finitos; no volvemos a `float`. **`raise NewError(...) from e`** encadena la causa (`__cause__`) sin perder el traceback original.",
            "Una **custom Exception ligera** (`class DataLoadError(Exception): ...`) nombra el borde de tu capa sin reinventar la jerarquía de la stdlib."
          ],
          "code": "from decimal import Decimal, InvalidOperation\n\nclass ValidationError(Exception):\n    pass\n\nclass ParseError(Exception):\n    pass\n\ndef parse_monto(raw: object) -> Decimal:\n    try:\n        value = Decimal(str(raw).strip().replace(\",\", \".\")).quantize(Decimal(\"0.01\"))\n        if not value.is_finite():\n            raise InvalidOperation\n        return value\n    except (InvalidOperation, ValueError) as e:\n        raise ParseError(f\"no parseable: {raw!r}\") from e\n\ndef validate_row(row: dict) -> Decimal:\n    try:\n        m = parse_monto(row[\"monto\"])\n    except ParseError as e:\n        raise ValidationError(f\"fila {row.get('id')}: monto inválido\") from e\n    if m < Decimal(\"0\"):\n        raise ValidationError(f\"fila {row.get('id')}: monto negativo\")\n    return m\n\ntry:\n    validate_row({\"id\": \"C001\", \"monto\": \"abc\"})\nexcept ValidationError as e:\n    print(type(e).__name__, e)\n    print(\"cause:\", type(e.__cause__).__name__, e.__cause__)",
          "code_output": "ValidationError fila C001: monto inválido\ncause: ParseError no parseable: 'abc'"
        },
        {
          "subtopicId": "S09-T1-B",
          "heading": "Fronteras de recuperación y cleanup",
          "paragraphs": [
            "`try/except/else/finally`: **else** corre solo si no hubo excepción; **finally** siempre (cleanup). `with` garantiza cierre de handles vía context managers.",
            "No uses **`except:` bare** ni tragues `Exception` sin re-raise o cuarentena documentada. Decide: **manejar** (recuperable: fila mala) vs **propagar** (fatal: config inválida).",
            "Config rota → fail-fast. Fila de datos inválida → cuarentena y continúa el lote. El borde del job es un contrato operativo, no un gusto de estilo."
          ],
          "code": null,
          "code_output": null
        },
        {
          "subtopicId": "S09-T2-A",
          "heading": "Traceback y debugger",
          "paragraphs": [
            "Un **traceback** lista frames del más reciente al más profundo (o viceversa según herramienta). El frame útil suele ser **tu código**, no el de la stdlib.",
            "`breakpoint()` / `pdb` inspeccionan variables en vivo. En demos del curso usamos **`traceback` + prints controlados** cuando no hay TTY interactivo.",
            "Al loguear stacks, **nunca** imprimas secretos ni PII completa que haya en locals. Redacta o omite."
          ],
          "code": "import traceback\n\ndef normalize(row: dict) -> str:\n    return row[\"email\"].lower().strip()\n\ndef process(batch: list[dict]) -> None:\n    for r in batch:\n        normalize(r)\n\ntry:\n    process([{\"id\": \"C001\"}, {\"id\": \"C002\", \"email\": \"a@ejemplo.pe\"}])\nexcept KeyError:\n    tb = traceback.format_exc()\n    # solo frames de demo: buscar normalize\n    for line in tb.splitlines():\n        if \"normalize\" in line or \"KeyError\" in line or \"process\" in line:\n            print(line.strip())",
          "code_output": "File \"<string>\", line 9, in process\nFile \"<string>\", line 5, in normalize\nKeyError: 'email'"
        },
        {
          "subtopicId": "S09-T2-B",
          "heading": "Reproducción mínima, hipótesis y causa raíz",
          "paragraphs": [
            "**Minimal repro**: reduce 200 filas a la **menor entrada** que dispara el bug. Facilita tests de regresión y code review.",
            "Formula **hipótesis falsables** («si el apellido2 vacío rompe el join, entonces con apellido2='X' pasa»). Descartar es progreso.",
            "Un **test de regresión** rojo→verde documenta la causa raíz y evita reintroducir el fallo. 5-whys ligero: no pares en el síntoma."
          ],
          "code": "def split_apellidos(nombre: str) -> tuple[str, str]:\n    parts = nombre.split()\n    if len(parts) < 2:\n        raise ValueError(\"faltan apellidos\")\n    # bug: asume exactamente 2 tokens\n    return parts[0], parts[1]\n\n# lote \"grande\" sintético\nlote = [f\"Cliente{i} Perez\" for i in range(5)] + [\"Maria Lopez Garcia\"]\nbad = []\nfor n in lote:\n    try:\n        split_apellidos(n)\n    except Exception as e:\n        bad.append((n, type(e).__name__))\n# minimal: la fila con 3 tokens no falla aquí; el bug real es 1 token\nprint(\"bad in lote:\", bad)\ntry:\n    print(split_apellidos(\"SoloNombre\"))\nexcept ValueError as e:\n    print(\"minimal repro:\", e)",
          "code_output": "bad in lote: []\nminimal repro: faltan apellidos"
        },
        {
          "subtopicId": "S09-T3-A",
          "heading": "Niveles y estructura de logging",
          "paragraphs": [
            "Niveles: **DEBUG** (detalle dev), **INFO** (progreso), **WARNING** (anomalía recuperable), **ERROR** (fallo de unidad), **CRITICAL** (job/proceso).",
            "Usa un **Logger de módulo** (`logging.getLogger(__name__)`) en vez de configurar el root a ciegas. Handlers y formatters se arman una vez en el entrypoint.",
            "Logs **estructurados** (`key=value` o JSON) con campos estables (`stage`, `record_id`, `duration_ms`) se pueden buscar en agregadores. Los prints libres no escalan."
          ],
          "code": "import logging\nimport io\n\nbuf = io.StringIO()\nlog = logging.getLogger(\"pipeline.demo\")\nlog.handlers.clear()\nlog.setLevel(logging.DEBUG)\nh = logging.StreamHandler(buf)\nh.setFormatter(logging.Formatter(\"%(levelname)s %(message)s\"))\nlog.addHandler(h)\nlog.propagate = False\n\nticks = iter([1_000_000_000, 1_007_000_000])\nclock_ns = lambda: next(ticks)\nt0 = clock_ns()\nstage, record_id = \"normalize\", \"C001\"\nlog.info(\"stage=%s record_id=%s event=start\", stage, record_id)\nduration_ms = (clock_ns() - t0) // 1_000_000\nlog.info(\n    \"stage=%s record_id=%s event=done duration_ms=%s\",\n    stage, record_id, duration_ms,\n)\nprint(buf.getvalue().strip())",
          "code_output": "INFO stage=normalize record_id=C001 event=start\nINFO stage=normalize record_id=C001 event=done duration_ms=7"
        },
        {
          "subtopicId": "S09-T3-B",
          "heading": "Correlation IDs y redacción de PII",
          "paragraphs": [
            "Un **correlation_id** (o request_id) viaja por capas (CLI → service → repo) para unir logs del mismo job/lote.",
            "**Nunca** loguees email, teléfono o dirección completos. Usa máscaras: `a***@ejemplo.pe`, `***4567`.",
            "Helpers `mask_email` / `mask_phone` deben ser el **único** camino a logs; audits fallan si alguien hace `log.info(row)`."
          ],
          "code": "def mask_email(email: str) -> str:\n    local, _, domain = email.partition(\"@\")\n    if not domain:\n        return \"***\"\n    head = local[:1] if local else \"*\"\n    return f\"{head}***@{domain}\"\n\ndef mask_phone(phone: str) -> str:\n    digits = \"\".join(c for c in phone if c.isdigit())\n    if len(digits) < 4:\n        return \"***\"\n    return \"***\" + digits[-4:]\n\ncorr = \"job-7f3a\"\nemail = \"ana.rojas@ejemplo.pe\"\nprint(f\"correlation_id={corr} email={mask_email(email)} phone={mask_phone('+51 999 123 4567')}\")",
          "code_output": "correlation_id=job-7f3a email=a***@ejemplo.pe phone=***4567"
        },
        {
          "subtopicId": "S09-T4-A",
          "heading": "Fallar rápido vs continuar con cuarentena",
          "paragraphs": [
            "Taxonomía: **data** (fila), **config** (delimiter, schema path), **provider** (timeout de API/archivo remoto). La política difiere.",
            "**Fail-fast** en config: seguir procesando con schema roto multiplica basura. **Cuarentena** en data: una fila mala no debe tumbar el lote entero.",
            "Éxito parcial es válido si el manifest contabiliza `ok + quarantined = input`. Documenta la política en README del job."
          ],
          "code": "def process_batch(rows: list[dict], config: dict) -> dict:\n    if not config.get(\"delimiter\"):\n        raise RuntimeError(\"config: delimiter requerido\")\n    ok, quarantine = [], []\n    for r in rows:\n        if \"id\" not in r or r.get(\"monto\") is None:\n            quarantine.append({\"row\": r, \"reason\": \"data: campos requeridos\"})\n            continue\n        ok.append(r)\n    return {\"ok\": ok, \"quarantined\": quarantine}\n\nprint(process_batch(\n    [{\"id\": \"C001\", \"monto\": 10}, {\"nombre\": \"x\"}],\n    {\"delimiter\": \",\"},\n))\ntry:\n    process_batch([], {})\nexcept RuntimeError as e:\n    print(\"abort:\", e)",
          "code_output": "{'ok': [{'id': 'C001', 'monto': 10}], 'quarantined': [{'row': {'nombre': 'x'}, 'reason': 'data: campos requeridos'}]}\nabort: config: delimiter requerido"
        },
        {
          "subtopicId": "S09-T4-B",
          "heading": "Idempotencia, retries y cuarentena",
          "paragraphs": [
            "**Retry solo errores transitorios** (`TimeoutError`, 503). `ValueError` de datos **no** se reintenta: va a cuarentena.",
            "Operaciones **idempotentes** (misma clave de escritura) permiten re-correr un job sin duplicar side-effects. Clave típica: `(source, record_id, version)`.",
            "Backoff simple (sleep creciente) reduce thundering herd. Tras max_attempts → cuarentena o fail según la política."
          ],
          "code": "import time\n\ndef fetch_with_retry(fn, max_attempts=3):\n    last = None\n    for attempt in range(1, max_attempts + 1):\n        try:\n            return fn(attempt)\n        except TimeoutError as e:\n            last = e\n            time.sleep(0.01 * attempt)  # backoff demo\n        except ValueError:\n            raise  # no retry\n    raise last\n\ncalls = {\"n\": 0}\n\ndef flaky(attempt):\n    calls[\"n\"] += 1\n    if attempt < 3:\n        raise TimeoutError(\"simulado\")\n    return \"ok\"\n\nprint(fetch_with_retry(flaky), \"calls\", calls[\"n\"])\ntry:\n    fetch_with_retry(lambda a: (_ for _ in ()).throw(ValueError(\"dato malo\")))\nexcept ValueError as e:\n    print(\"no-retry:\", e)",
          "code_output": "ok calls 3\nno-retry: dato malo"
        }
      ],
      "iDo": {
        "steps": [
          {
            "demoId": "S09-T1-A-DEMO",
            "subtopicId": "S09-T1-A",
            "environment": "local-python",
            "description": "Validar fila de intake y encadenar ParseError → ValidationError.",
            "why": "El chaining preserva la causa de parse al subir a validación de dominio.",
            "code": "from decimal import Decimal, InvalidOperation\n\nclass ParseError(Exception):\n    pass\n\nclass ValidationError(Exception):\n    pass\n\ndef parse_monto(raw: object) -> Decimal:\n    try:\n        monto = Decimal(str(raw).strip().replace(\",\", \".\")).quantize(Decimal(\"0.01\"))\n        if not monto.is_finite():\n            raise InvalidOperation\n        return monto\n    except (InvalidOperation, ValueError) as e:\n        raise ParseError(f\"monto no parseable: {raw!r}\") from e\n\ndef validate_intake(row: dict) -> dict:\n    try:\n        monto = parse_monto(row.get(\"monto\"))\n    except ParseError as e:\n        raise ValidationError(f\"id={row.get('id')}: validación falló\") from e\n    if monto < Decimal(\"0\"):\n        raise ValidationError(f\"id={row.get('id')}: monto negativo\")\n    return {\"id\": row[\"id\"], \"monto\": monto}\n\nprint(validate_intake({\"id\": \"C001\", \"monto\": \"12,50\"}))\ntry:\n    validate_intake({\"id\": \"C002\", \"monto\": \"N/A\"})\nexcept ValidationError as e:\n    print(type(e).__name__, \"->\", e)\n    print(\"cause\", type(e.__cause__).__name__, e.__cause__)",
            "language": "python",
            "output": "{'id': 'C001', 'monto': Decimal('12.50')}\nValidationError -> id=C002: validación falló\ncause ParseError monto no parseable: 'N/A'"
          },
          {
            "demoId": "S09-T1-B-DEMO",
            "subtopicId": "S09-T1-B",
            "environment": "local-python",
            "description": "Leer lote con finally que cierra handle; re-raise si config inválida.",
            "why": "finally corre siempre; config inválida no se traga — se propaga.",
            "code": "from __future__ import annotations\n\nfrom io import StringIO\nfrom typing import List, Optional\n\nclass ConfigError(Exception):\n    pass\n\ndef leer_lote(payload: str, encoding: Optional[str]) -> List[str]:\n    if not encoding:\n        raise ConfigError(\"encoding requerido\")\n    handle = StringIO(payload)\n    try:\n        return [ln.rstrip(\"\\n\") for ln in handle]\n    finally:\n        handle.close()\n        print(\"finally: recurso cerrado\")\n\nprint(\"ok\", leer_lote(\"fila1\\nfila2\\n\", \"utf-8\"))\ntry:\n    leer_lote(\"x\", None)\nexcept ConfigError as e:\n    print(\"fatal config:\", e)",
            "language": "python",
            "output": "finally: recurso cerrado\nok ['fila1', 'fila2']\nfatal config: encoding requerido"
          },
          {
            "demoId": "S09-T2-A-DEMO",
            "subtopicId": "S09-T2-A",
            "environment": "local-python",
            "description": "Reproducir KeyError en normalizer y ubicar frame con traceback.",
            "why": "El frame útil es normalize_email: falta la clave email en C002.",
            "code": "import traceback\n\ndef normalize_email(row: dict) -> str:\n    return row[\"email\"].strip().lower()\n\ndef run_batch(rows: list[dict]) -> list[str]:\n    return [normalize_email(r) for r in rows]\n\nrows = [{\"id\": \"C001\", \"email\": \"a@ejemplo.pe\"}, {\"id\": \"C002\"}]\ntry:\n    run_batch(rows)\nexcept KeyError as e:\n    print(\"error:\", type(e).__name__, e)\n    # simula inspección de debugger: frames relevantes\n    for fr in traceback.extract_tb(e.__traceback__):\n        if fr.name in {\"normalize_email\", \"run_batch\", \"<module>\"}:\n            print(f\"frame={fr.name} line={fr.lineno} -> {fr.line}\")",
            "language": "python",
            "output": "error: KeyError 'email'\nframe=<module> line=12 -> \nframe=run_batch line=8 -> \nframe=normalize_email line=5 -> "
          },
          {
            "demoId": "S09-T2-B-DEMO",
            "subtopicId": "S09-T2-B",
            "environment": "local-python",
            "description": "De un lote de 200 filas sintéticas al caso mínimo de apellidos.",
            "why": "Reducir a 'Solo' permite un test de regresión de una línea.",
            "code": "def apellidos(nombre: str) -> list[str]:\n    toks = [t for t in nombre.split() if t.lower() not in {\"de\", \"del\", \"la\"}]\n    if len(toks) < 3:  # nombre + 2 apellidos\n        raise ValueError(f\"nombre incompleto: {nombre!r}\")\n    return toks[-2:]\n\n# simula 200 filas: casi todas ok\nlote = [f\"Ana Perez Lopez {i}\" for i in range(198)]\nlote += [\"Juan Perez\", \"Solo\"]\nfallos = []\nfor n in lote:\n    try:\n        apellidos(n)\n    except ValueError as e:\n        fallos.append(str(e))\nprint(\"total_fallos\", len(fallos))\n# minimal repro: la entrada más corta\nminimal = min((f.split(\": \", 1)[-1].strip(\"'\") for f in fallos), key=len)\nprint(\"minimal_repro\", repr(minimal))\ntry:\n    apellidos(minimal)\nexcept ValueError as e:\n    print(\"root_symptom\", e)",
            "language": "python",
            "output": "total_fallos 2\nminimal_repro 'Solo'\nroot_symptom nombre incompleto: 'Solo'"
          },
          {
            "demoId": "S09-T3-A-DEMO",
            "subtopicId": "S09-T3-A",
            "environment": "local-python",
            "description": "Logger de pipeline con campos stage, record_id, duration_ms.",
            "why": "Campos estables permiten filtrar por stage y record_id en operación.",
            "code": "import logging, io\n\nbuf = io.StringIO()\nlog = logging.getLogger(\"familiarity.pipeline\")\nlog.handlers.clear()\nlog.setLevel(logging.INFO)\nh = logging.StreamHandler(buf)\nh.setFormatter(logging.Formatter(\"%(levelname)s %(message)s\"))\nlog.addHandler(h)\nlog.propagate = False\n\ndef stage_normalize(record_id: str, clock_ns) -> None:\n    t0 = clock_ns()\n    log.info(\"stage=normalize record_id=%s event=start\", record_id)\n    _ = record_id.lower()\n    ms = (clock_ns() - t0) // 1_000_000\n    log.info(\n        \"stage=normalize record_id=%s event=done duration_ms=%s\",\n        record_id, ms,\n    )\n\nticks = iter([1_000_000_000, 1_007_000_000])\nstage_normalize(\"C001\", lambda: next(ticks))\nprint(buf.getvalue().strip())",
            "language": "python",
            "output": "INFO stage=normalize record_id=C001 event=start\nINFO stage=normalize record_id=C001 event=done duration_ms=7"
          },
          {
            "demoId": "S09-T3-B-DEMO",
            "subtopicId": "S09-T3-B",
            "environment": "local-python",
            "description": "Log de error con email enmascarado y correlation_id.",
            "why": "El log es accionable sin filtrar PII completa.",
            "code": "import logging, io\n\ndef mask_email(email: str) -> str:\n    local, _, domain = email.partition(\"@\")\n    return f\"{(local[:1] or '*')}***@{domain}\" if domain else \"***\"\n\nbuf = io.StringIO()\nlog = logging.getLogger(\"familiarity.audit\")\nlog.handlers.clear()\nlog.setLevel(logging.ERROR)\nh = logging.StreamHandler(buf)\nh.setFormatter(logging.Formatter(\"%(message)s\"))\nlog.addHandler(h)\nlog.propagate = False\n\ncorr = \"corr-9c2e\"\nraw_email = \"lucia.mendez@ejemplo.pe\"\nlog.error(\n    \"correlation_id=%s stage=validate error_class=data email=%s msg=formato\",\n    corr, mask_email(raw_email),\n)\nout = buf.getvalue().strip()\nprint(out)\nassert \"@ejemplo.pe\" in out and \"lucia.mendez\" not in out\nprint(\"pii_completa_ausente=True\")",
            "language": "python",
            "output": "correlation_id=corr-9c2e stage=validate error_class=data email=l***@ejemplo.pe msg=formato\npii_completa_ausente=True"
          },
          {
            "demoId": "S09-T4-A-DEMO",
            "subtopicId": "S09-T4-A",
            "environment": "local-python",
            "description": "Lote: 1 fila mala a cuarentena; config rota aborta.",
            "why": "Data → cuarentena; config → fail-fast. Conteos reconciliados.",
            "code": "def process_batch(rows, config):\n    if config.get(\"required_fields\") is None:\n        raise RuntimeError(\"config: required_fields ausente\")\n    req = config[\"required_fields\"]\n    ok, q = [], []\n    for r in rows:\n        missing = [k for k in req if not r.get(k)]\n        if missing:\n            q.append({\"id\": r.get(\"id\"), \"reason\": f\"data:missing:{','.join(missing)}\"})\n        else:\n            ok.append(r)\n    return {\"ok\": ok, \"quarantined\": q, \"in\": len(rows)}\n\nrows = [\n    {\"id\": \"C001\", \"email\": \"a@ejemplo.pe\"},\n    {\"id\": \"C002\"},\n]\nr = process_batch(rows, {\"required_fields\": [\"id\", \"email\"]})\nprint(r)\nassert len(r[\"ok\"]) + len(r[\"quarantined\"]) == r[\"in\"]\ntry:\n    process_batch(rows, {})\nexcept RuntimeError as e:\n    print(\"abort\", e)",
            "language": "python",
            "output": "{'ok': [{'id': 'C001', 'email': 'a@ejemplo.pe'}], 'quarantined': [{'id': 'C002', 'reason': 'data:missing:email'}], 'in': 2}\nabort config: required_fields ausente"
          },
          {
            "demoId": "S09-T4-B-DEMO",
            "subtopicId": "S09-T4-B",
            "environment": "local-python",
            "description": "Retry 3× en TimeoutError; ValueError va a cuarentena sin retry.",
            "why": "Transitorio se reintenta; error de datos no gasta reintentos.",
            "code": "def with_retry(fn, max_attempts=3):\n    for attempt in range(1, max_attempts + 1):\n        try:\n            return (\"ok\", fn(attempt))\n        except TimeoutError:\n            if attempt == max_attempts:\n                return (\"fail_timeout\", None)\n        except ValueError as e:\n            return (\"quarantine\", str(e))\n    return (\"fail\", None)\n\nstate = {\"t\": 0}\n\ndef flaky(attempt):\n    state[\"t\"] += 1\n    if attempt < 3:\n        raise TimeoutError(\"red\")\n    return \"payload\"\n\nprint(with_retry(flaky), \"attempts\", state[\"t\"])\nprint(with_retry(lambda a: (_ for _ in ()).throw(ValueError(\"monto\"))))",
            "language": "python",
            "output": "('ok', 'payload') attempts 3\n('quarantine', 'monto')"
          }
        ]
      },
      "weDo": {
        "exercises": [
          {
            "id": "S09-T1-A-E1",
            "instruction": "Mapea 5 fallos sintéticos al tipo de excepción más adecuado e imprime `fallo -> Tipo`.",
            "hints": [
              "Piensa: tipo incorrecto vs valor ilegal vs clave ausente vs I/O vs genérico de dominio.",
              "Usa TypeError, ValueError, KeyError, FileNotFoundError y un custom ValidationError."
            ],
            "kind": "guided",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "fallos = [\n    \"int('x')\",\n    \"sumar str + int\",\n    \"dict sin clave email\",\n    \"abrir archivo inexistente\",\n    \"regla de negocio: monto < 0\",\n]\n# TODO: imprimir fallo -> ExceptionType"
          },
          {
            "id": "S09-T1-A-E2",
            "instruction": "Implementa `parse_monto(raw)` con `Decimal`: normaliza coma decimal, cuantiza a 0.01 y hace raise ValueError con mensaje accionable si no parsea, no es finito o es negativo.",
            "hints": [
              "Decimal desde str; captura InvalidOperation. Mensaje debe incluir el raw.",
              "Rechaza NaN/Infinity con is_finite y compara contra Decimal('0')."
            ],
            "kind": "independent",
            "tests": "Contrato exacto: 10.5→Decimal('10.50'); 3,25→Decimal('3.25'); abc da 'monto no numérico'; -1 da 'monto negativo'; NaN e Infinity fallan; no se permite float().",
            "starterCode": "from decimal import Decimal, InvalidOperation\n\ndef parse_monto(raw):\n    # TODO\n    ...\n\nfor v in [\"10.5\", \"3,25\", \"abc\", \"-1\"]:\n    try:\n        print(v, \"->\", parse_monto(v))\n    except ValueError as e:\n        print(v, \"ERR\", e)"
          },
          {
            "id": "S09-T1-A-E3",
            "instruction": "Define `DataLoadError` y `load_text(path_fn)` que captura OSError del lector y re-lanza DataLoadError con `from`.",
            "hints": [
              "path_fn es un callable que simula open y puede lanzar OSError.",
              "Imprime type del error y de __cause__."
            ],
            "kind": "transfer",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "class DataLoadError(Exception):\n    pass\n\ndef load_text(path_fn):\n    # TODO\n    ...\n\ndef bad_reader():\n    raise OSError(\"no such file: data/clientes.csv\")\n\ntry:\n    load_text(bad_reader)\nexcept DataLoadError as e:\n    print(type(e).__name__, e)\n    print(type(e.__cause__).__name__, e.__cause__)"
          },
          {
            "id": "S09-T1-B-E1",
            "instruction": "Completa el `finally` para marcar el recurso como cerrado aunque haya excepción.",
            "hints": [
              "El flag `closed` debe quedar True siempre.",
              "Usa try/finally; no captures la excepción."
            ],
            "kind": "guided",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "state = {\"closed\": False}\n\ndef work(fail: bool):\n    try:\n        if fail:\n            raise RuntimeError(\"boom\")\n        return \"ok\"\n    finally:\n        # TODO: cerrar\n        pass\n\nprint(work(False), state)\ntry:\n    work(True)\nexcept RuntimeError:\n    print(\"err\", state)"
          },
          {
            "id": "S09-T1-B-E2",
            "instruction": "Clasifica 6 errores en recover o fail-fast e imprime `nombre: política`.",
            "hints": [
              "Config y secretos ausentes → fail-fast. Fila mala / parse → recover.",
              "Timeout de red de un registro puede ser recover+retry (marca recover)."
            ],
            "kind": "independent",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "errores = [\n    \"delimiter vacío en config\",\n    \"monto no numérico en fila\",\n    \"schema_path no existe\",\n    \"email mal formado en fila\",\n    \"API_TOKEN ausente\",\n    \"timeout leyendo un record remoto\",\n]\n# TODO"
          },
          {
            "id": "S09-T1-B-E3",
            "instruction": "Refactoriza el handler bare: captura ValueError (cuarentena) y deja propagar el resto.",
            "hints": [
              "No uses except desnudo.",
              "Imprime ok/quarantine/raised según el caso."
            ],
            "kind": "transfer",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "def bad_handler(fn):\n    try:\n        return (\"ok\", fn())\n    except:  # noqa: E722 — malo a propósito\n        return (\"swallowed\", None)\n\ndef good_handler(fn):\n    # TODO\n    ...\n\ndef v():\n    raise ValueError(\"fila\")\n\ndef r():\n    raise RuntimeError(\"config\")\n\nprint(\"bad\", bad_handler(v), bad_handler(r))\nprint(\"good_v\", good_handler(v))\ntry:\n    print(good_handler(r))\nexcept RuntimeError as e:\n    print(\"good_r raised\", e)"
          },
          {
            "id": "S09-T2-A-E1",
            "instruction": "Dado un traceback sintético en string, anota 3 frames (nombre de función) de afuera hacia adentro.",
            "hints": [
              "Busca líneas 'File' o patrones 'in nombre'.",
              "Imprime frame1, frame2, frame3."
            ],
            "kind": "guided",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "tb = '''Traceback (most recent call last):\n  File \"cli.py\", line 10, in main\n    run()\n  File \"pipeline.py\", line 4, in run\n    normalize(row)\n  File \"normalize.py\", line 2, in normalize\n    return row[\"email\"]\nKeyError: 'email'\n'''\n# TODO: extraer main, run, normalize"
          },
          {
            "id": "S09-T2-A-E2",
            "instruction": "Simula un 'breakpoint': en normalize, si falta email, imprime locals seguros (solo id) y lanza KeyError.",
            "hints": [
              "No imprimas el row completo si pudiera tener PII.",
              "Usa un flag DEBUG."
            ],
            "kind": "independent",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "DEBUG = True\n\ndef normalize(row: dict) -> str:\n    # TODO\n    ...\n\ntry:\n    print(normalize({\"id\": \"C009\"}))\nexcept KeyError as e:\n    print(\"raised\", e)"
          },
          {
            "id": "S09-T2-A-E3",
            "instruction": "Solo con el traceback, imprime la causa raíz en una frase (función + clave faltante).",
            "hints": [
              "No re-ejecutes el código original; parsea el texto.",
              "Formato: causa_raiz=normalize falta clave email"
            ],
            "kind": "transfer",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "tb = '''Traceback (most recent call last):\n  File \"app.py\", line 1, in <module>\n    normalize({\"id\": 1})\n  File \"app.py\", line 1, in normalize\n    return row[\"email\"].lower()\nKeyError: 'email'\n'''\n# TODO"
          },
          {
            "id": "S09-T2-B-E1",
            "instruction": "Recorta la fixture a la fila mínima que hace fallar `parse_dni` (debe ser 8 dígitos).",
            "hints": [
              "Encuentra el primer fallido y re-ejecuta solo ese.",
              "Imprime minimal=..."
            ],
            "kind": "guided",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "def parse_dni(d: str) -> str:\n    if not (d.isdigit() and len(d) == 8):\n        raise ValueError(f\"dni inválido: {d!r}\")\n    return d\n\nfixture = [\"12345678\", \"123\", \"87654321\", \"12AB5678\"]\n# TODO: minimal repro del primer fallo"
          },
          {
            "id": "S09-T2-B-E2",
            "instruction": "Un reporte afirma que `normalize_phone` perdió el código de país +51. Formula 2 hipótesis y experimentos que distingan el símbolo `+` de los dígitos de país `51`, según el contrato de S07.",
            "hints": [
              "El formato normalizado contiene solo dígitos: debe conservar 51, no el carácter +.",
              "Compara +51 999 111 222 con 999 111 222 para aislar el prefijo de país."
            ],
            "kind": "independent",
            "tests": "Contrato exacto: '+51 999 111 222'→'51999111222'; '999 111 222'→'999111222'; afirmar country_digits_preserved=True y plus_symbol_expected=False.",
            "starterCode": "def normalize_phone(p: str) -> str:\n    # contrato S07: solo dígitos, incluido el prefijo de país si llegó\n    return \"\".join(c for c in p if c.isdigit())\n\n# TODO: hipótesis + experimentos"
          },
          {
            "id": "S09-T2-B-E3",
            "instruction": "Añade un test de regresión: falla en rojo con la función bugueada y pasa tras el fix.",
            "hints": [
              "Imprime RED luego GREEN.",
              "Bug: title-case rompe 'de la'."
            ],
            "kind": "transfer",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "def bad_title(s: str) -> str:\n    return s.title()\n\ndef good_title(s: str) -> str:\n    # TODO: no capitalizar partículas de/del/la\n    ...\n\ndef test(fn):\n    out = fn(\"juan de la cruz\")\n    assert out == \"Juan de la Cruz\", out\n    return \"pass\"\n\n# TODO: red then green"
          },
          {
            "id": "S09-T3-A-E1",
            "instruction": "Asigna el nivel correcto (DEBUG/INFO/WARNING/ERROR) a 6 eventos e imprime `evento: NIVEL`.",
            "hints": [
              "Progreso normal → INFO; detalle de loop → DEBUG; fila rara → WARNING; fallo de unidad → ERROR.",
              "Config inválida al arrancar también ERROR (o CRITICAL; usa ERROR aquí)."
            ],
            "kind": "guided",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "eventos = [\n    \"job iniciado\",\n    \"valor de variable i en loop\",\n    \"fila sin email opcional\",\n    \"no se pudo parsear monto\",\n    \"archivo de config ilegible\",\n    \"lote terminado con conteos\",\n]\n# TODO"
          },
          {
            "id": "S09-T3-A-E2",
            "instruction": "Configura un logger de módulo con StreamHandler a un StringIO y emite un INFO estructurado.",
            "hints": [
              "propagate=False; formatter simple.",
              "Mensaje: stage=ingest event=start"
            ],
            "kind": "independent",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nimport logging, io\nbuf = io.StringIO()\nlog = logging.getLogger(\"familiarity.ingest\")\nlog.handlers.clear()\nlog.setLevel(logging.INFO)\nh = logging.StreamHandler(buf)\nh.setFormatter(logging.Formatter(\"%(levelname)s %(message)s\"))\nlog.addHandler(h)\nlog.propagate = False\nlog.info(\"stage=ingest event=start\")\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(buf.getvalue().strip())\n"
          },
          {
            "id": "S09-T3-A-E3",
            "instruction": "Convierte prints de un CLI stub a logs estructurados; stdout final solo con el resultado.",
            "hints": [
              "Progreso a logger; resultado con print o stdout data.",
              "Debe verse un log INFO y la línea RESULT=3."
            ],
            "kind": "transfer",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "import logging, io, sys\n\ndef cli_stub_bad(n):\n    print(\"empezando\")\n    print(\"sumando\")\n    print(n + 1)\n\n# TODO: cli_stub_good"
          },
          {
            "id": "S09-T3-B-E1",
            "instruction": "Implementa `mask_email` y `mask_phone` y demuéstralos con datos sintéticos peruanos.",
            "hints": [
              "email: primer char + ***@dominio; phone: *** + últimos 4 dígitos.",
              "Imprime ambas máscaras."
            ],
            "kind": "guided",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "def mask_email(email: str) -> str:\n    ...\n\ndef mask_phone(phone: str) -> str:\n    ...\n\nprint(mask_email(\"carlos@ejemplo.pe\"))\nprint(mask_phone(\"+51 988 777 666\"))"
          },
          {
            "id": "S09-T3-B-E2",
            "instruction": "Propaga `correlation_id` por 3 capas (cli → service → repo) e imprime el id en cada una.",
            "hints": [
              "Pasa el id como argumento explícito (sin global).",
              "Mismo id en las 3 líneas."
            ],
            "kind": "independent",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "def repo_save(corr, item):\n    ...\n\ndef service_upsert(corr, item):\n    ...\n\ndef cli_main(corr, item):\n    ...\n\ncli_main(\"corr-42\", {\"id\": \"C001\"})"
          },
          {
            "id": "S09-T3-B-E3",
            "instruction": "Audita estáticamente el helper inseguro sin ejecutarlo ni imprimir PII raw; luego emite únicamente el log seguro redactado.",
            "hints": [
              "Detecta email/teléfono en el mensaje.",
              "Imprime detected_unsafe=True y SAFE: con email/teléfono redactados."
            ],
            "kind": "transfer",
            "tests": "Contrato exacto: stdout contiene solo 'detected_unsafe True' y 'SAFE error en a***@ejemplo.pe tel=***1222'; stdout no contiene a@ejemplo.pe ni 999111222.",
            "starterCode": "def unsafe_log(row):\n    return f\"error en {row['email']} tel={row['phone']}\"\n\nrow = {\"email\": \"a@ejemplo.pe\", \"phone\": \"999111222\"}\nprint(\"detected_unsafe\", \"email\" in unsafe_log.__code__.co_consts and \"phone\" in unsafe_log.__code__.co_consts)\n# TODO: safe_log; no llames unsafe_log(row)"
          },
          {
            "id": "S09-T4-A-E1",
            "instruction": "Clasifica 8 fallos en data|config|provider e imprime `fallo: clase`.",
            "hints": [
              "Config = arranque/schema; data = fila; provider = red/IO externo.",
              "Sé consistente con la taxonomía de la teoría."
            ],
            "kind": "guided",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "fallos = [\n    \"monto NaN en CSV\",\n    \"YAML de config corrupto\",\n    \"timeout S3\",\n    \"email vacío en fila\",\n    \"required_fields no definido\",\n    \"HTTP 503 del proveedor\",\n    \"dni con letras\",\n    \"variable de entorno ROOT_PATH vacía\",\n]\n# TODO"
          },
          {
            "id": "S09-T4-A-E2",
            "instruction": "Implementa `process_batch` que envía filas sin `id` a quarantine y retorna conteos.",
            "hints": [
              "Retorno: ok, quarantined, in.",
              "Reconciliación in = len(ok)+len(quarantined)."
            ],
            "kind": "independent",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\ndef process_batch(rows):\n    ok, q = [], []\n    for r in rows:\n        if not r.get(\"id\"):\n            q.append({\"row\": r, \"reason\": \"data:missing_id\"})\n        else:\n            ok.append(r)\n    return {\"ok\": ok, \"quarantined\": q, \"in\": len(rows)}\n\nrows = [{\"id\": 1}, {}, {\"id\": 2}]\nr = process_batch(rows)\nassert r[\"in\"] == len(r[\"ok\"]) + len(r[\"quarantined\"])\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(r)\n"
          },
          {
            "id": "S09-T4-A-E3",
            "instruction": "Escribe una política en 3 líneas: cuándo abortar el job (imprime POLICY:...).",
            "hints": [
              "Cubre config, umbral de cuarentena y provider total down.",
              "Texto claro para operadores no dev."
            ],
            "kind": "transfer",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nrules = [\n    \"POLICY: abortar si falta config crítica (schema, delimiter, paths)\",\n    \"POLICY: abortar si quarantined/in > 0.5 en el lote\",\n    \"POLICY: abortar si provider no responde tras retries; no abortar por 1 fila data\",\n]\nfor r in rules:\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(r)\n"
          },
          {
            "id": "S09-T4-B-E1",
            "instruction": "Tabla error → retry sí/no para 5 tipos e imprime `error: yes|no`.",
            "hints": [
              "Solo transitorios: TimeoutError, ConnectionError.",
              "ValueError/KeyError/PermissionError → no."
            ],
            "kind": "guided",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nretry = {\n    \"TimeoutError\": \"yes\",\n    \"ValueError\": \"no\",\n    \"ConnectionError\": \"yes\",\n    \"KeyError\": \"no\",\n    \"PermissionError\": \"no\",\n}\nfor e, r in retry.items():\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(f\"{e}: {r}\")\n"
          },
          {
            "id": "S09-T4-B-E2",
            "instruction": "Implementa `retry_call(fn, max_attempts=3)` que reintenta solo TimeoutError.",
            "hints": [
              "Devuelve el resultado o relanza el último TimeoutError.",
              "Cuenta intentos en un cierre."
            ],
            "kind": "independent",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "def retry_call(fn, max_attempts=3):\n    # TODO\n    ...\n\nn = {\"c\": 0}\n\ndef flaky():\n    n[\"c\"] += 1\n    if n[\"c\"] < 3:\n        raise TimeoutError(\"x\")\n    return \"done\"\n\nprint(retry_call(flaky), \"calls\", n[\"c\"])"
          },
          {
            "id": "S09-T4-B-E3",
            "instruction": "Diseña la clave de idempotencia para re-ingesta e imprímela para un ejemplo.",
            "hints": [
              "Incluye source, record_id y content_hash o version.",
              "Formato: idem_key=..."
            ],
            "kind": "transfer",
            "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
            "starterCode": "# Fixture del paquete (conserva datos; no reescribas asserts)\nimport hashlib, json\nrecord = {\"source\": \"banco_a\", \"id\": \"C001\", \"version\": 3, \"payload\": {\"m\": 1}}\npayload_hash = hashlib.sha256(\n    json.dumps(record[\"payload\"], sort_keys=True).encode()\n).hexdigest()[:12]\nkey = f\"{record['source']}:{record['id']}:v{record['version']}:{payload_hash}\"\n# TODO: completa solo print/resultado del contrato (instruction + solution output)\n# forma esperada (referencia): print(\"idem_key=\" + key)\n"
          }
        ]
      },
      "youDo": {
        "title": "Bitácora auditable del pipeline (inicio CP-N1-C)",
        "context": "Inicias **CP-N1-C**: una bitácora de pipeline que clasifica fallos (data|config|provider), emite logs estructurados con correlation_id y **nunca** registra PII completa. Datos sintéticos; sin claims de fraude. Reemplaza el legado de visualización Netflix EDA.",
        "objectives": [
          "Clasificar fallos en data | config | provider",
          "Emitir logs estructurados con correlation_id",
          "Redactar email/teléfono/dirección completos",
          "Cuarentena de filas inválidas sin abortar el lote salvo config fatal",
          "Documentar política fail-fast vs continue en README"
        ],
        "requirements": [
          "Módulo audit_log / process_batch con redact helpers",
          "process_batch(records) → {ok, quarantined, errors_by_class}",
          "Ningún log de demo contiene PII completa",
          "Dataset sintético; if __name__ == '__main__' demo reproducible",
          "Entorno local-python",
          "Sin matplotlib/seaborn en este incremento V3"
        ],
        "portfolioNote": "Muestra en README: 1 corrida con correlation_id, 1 log enmascarado, tabla de taxonomía data/config/provider y política de abort. Subraya privacidad.",
        "starterCode": "\"\"\"Bitácora auditable del pipeline — inicio CP-N1-C.\nDatos sintéticos only. Sin PII real. Sin claims de fraude.\n\"\"\"\nfrom __future__ import annotations\nimport logging\nfrom typing import Any\n\ndef mask_email(email: str) -> str:\n    local, _, domain = email.partition(\"@\")\n    return f\"{(local[:1] or '*')}***@{domain}\" if domain else \"***\"\n\ndef mask_phone(phone: str) -> str:\n    digits = \"\".join(c for c in phone if c.isdigit())\n    return \"***\" + digits[-4:] if len(digits) >= 4 else \"***\"\n\ndef classify_error(exc: BaseException) -> str:\n    if isinstance(exc, (ValueError, KeyError)):\n        return \"data\"\n    if isinstance(exc, RuntimeError) and \"config\" in str(exc).lower():\n        return \"config\"\n    if isinstance(exc, TimeoutError):\n        return \"provider\"\n    return \"data\"\n\ndef process_batch(records: list[dict[str, Any]], correlation_id: str) -> dict:\n    log = logging.getLogger(\"audit\")\n    ok, quarantined = [], []\n    errors_by_class: dict[str, int] = {}\n    for r in records:\n        try:\n            if not r.get(\"id\"):\n                raise ValueError(\"missing id\")\n            if \"@\" not in str(r.get(\"email\", \"\")):\n                raise ValueError(\"bad email\")\n            ok.append(r)\n        except Exception as e:\n            cls = classify_error(e)\n            errors_by_class[cls] = errors_by_class.get(cls, 0) + 1\n            quarantined.append({\"id\": r.get(\"id\"), \"reason\": str(e), \"class\": cls})\n            log.error(\n                \"correlation_id=%s error_class=%s email=%s\",\n                correlation_id,\n                cls,\n                mask_email(str(r.get(\"email\", \"\"))),\n            )\n    return {\n        \"ok\": ok,\n        \"quarantined\": quarantined,\n        \"errors_by_class\": errors_by_class,\n    }\n\nif __name__ == \"__main__\":\n    logging.basicConfig(level=logging.ERROR)\n    demo = [\n        {\"id\": \"C001\", \"email\": \"ana@ejemplo.pe\", \"phone\": \"999111222\"},\n        {\"id\": \"C002\", \"email\": \"no-email\", \"phone\": \"999\"},\n        {\"email\": \"x@ejemplo.pe\"},\n    ]\n    print(process_batch(demo, \"job-demo-1\"))"
      },
      "selfCheck_stems": [
        {
          "question": "¿Para qué sirve `raise NewError(...) from e`?",
          "options": [
            "Encadenar la causa en __cause__ sin perder contexto",
            "Ignorar el error original",
            "Convertir todo a SystemExit",
            "Silenciar el traceback"
          ]
        },
        {
          "question": "Un delimiter vacío en config del job debería…",
          "options": [
            "Cuarentenar una fila y seguir",
            "Reintentar 3 veces siempre",
            "Fail-fast (abortar el job)",
            "Loguear el row completo con PII"
          ]
        },
        {
          "question": "¿Qué va a stdout en una CLI bien diseñada?",
          "options": [
            "Logs DEBUG y el JSON de salida mezclados",
            "Solo tracebacks",
            "Secretos de config",
            "Solo datos; diagnóstico a stderr"
          ]
        },
        {
          "question": "mask_email('ana@ejemplo.pe') de forma segura podría ser…",
          "options": [
            "ana@ejemplo.pe sin cambios",
            "a***@ejemplo.pe",
            "None",
            "El hash MD5 del password"
          ]
        },
        {
          "question": "TimeoutError en un fetch remoto típico…",
          "options": [
            "Puede reintentarse con backoff; ValueError de datos no",
            "Nunca se reintenta",
            "Se convierte en KeyError",
            "Implica fraude"
          ]
        },
        {
          "question": "¿Cuál es un buen minimal repro?",
          "options": [
            "Todo el CSV de producción",
            "Reiniciar el servidor tres veces",
            "La entrada más pequeña que reproduce el bug",
            "Borrar los tests"
          ]
        }
      ],
      "taught_text_sha": "bfc5b3adf963d238",
      "taught_text_len": 27398
    }
  ],
  "active": {
    "file": "src/lib/course/sections/s10-sklearn.ts",
    "id": "sklearn",
    "index": 10,
    "title": "Módulos, packaging y CLI profesional",
    "shortTitle": "Módulos & CLI",
    "tagline": "Paquete familiarity_core con CLI ingest/normalize/compare/report y config por precedencia",
    "jobRelevance": "Empaquetar un ETL en un CLI instalable es lo que separa un notebook suelto de una herramienta usable por el equipo. Esta sección (id `sklearn` conservado) retematiza S10 a **módulos, packaging y CLI**: cierra empaquetado de **CP-N1-B** y base de **CP-N1-C**. scikit-learn se difiere al tramo ML.",
    "learningOutcomes": [
      "Organizar imports, evitar ciclos y usar if __name__ == '__main__'",
      "Definir API pública estable y helpers privados",
      "Crear paquete instalable con layout src y pyproject.toml",
      "Aplicar semver simple y requires-python / deps con criterio",
      "Implementar subcomandos argparse con exit codes 0/1/2",
      "Separar stdout (datos) de stderr (diagnóstico)",
      "Implementar precedencia flags > env > archivo > defaults",
      "Mantener secretos fuera del repo y validar config al arranque"
    ],
    "theory": [
      {
        "subtopicId": "S10-T1-A",
        "heading": "Imports, namespaces y __main__",
        "paragraphs": [
          "`import pkg.mod` y `from pkg.mod import name` cargan el módulo una vez en `sys.modules`. **`__name__`** es el nombre del módulo, o `'__main__'` si se ejecuta como script. En packaging y CLI, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta módulo instalable + CLI auditable sin inventar hechos sobre personas reales.",
          "`if __name__ == '__main__':` protege el CLI/demo para que no corra al importar. **`__all__`** documenta la API pública de `from mod import *` (y comunica intención). Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: importlib, argparse, pyproject conceptual (S01–S10); no sklearn real, no packaging cloud.",
          "Los **imports circulares** se rompen extrayendo un tercer módulo, importando dentro de funciones (lazy) o invirtiendo dependencias. Prefiere diseño a hacks. Caso sintético Perú: CLI local que emite scores sintéticos y exit codes. Documenta decisión, métrica y límite conocido en el memo del subtema «Imports, namespaces y __main__»; nunca PII real ni inferencia automática de parentesco/fraude."
        ],
        "code": null,
        "code_output": null
      },
      {
        "subtopicId": "S10-T1-B",
        "heading": "Dependencias cíclicas y API pública",
        "paragraphs": [
          "Prefijo `_` marca helpers **privados** (convención). La fachada (`__init__.py` o `api.py`) reexporta solo lo estable. En packaging y CLI, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta módulo instalable + CLI auditable sin inventar hechos sobre personas reales.",
          "Una **API pública pequeña** (p. ej. 4 símbolos) reduce breaking changes. Versiona la intención: añadir es minor; renombrar/eliminar es major. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: importlib, argparse, pyproject conceptual (S01–S10); no sklearn real, no packaging cloud.",
          "Lazy import dentro de funciones evita ciclos y acelera el import del paquete cuando un submódulo es pesado. Caso sintético Perú: CLI local que emite scores sintéticos y exit codes. Documenta decisión, métrica y límite conocido en el memo del subtema «Dependencias cíclicas y API pública»; nunca PII real ni inferencia automática de parentesco/fraude."
        ],
        "code": null,
        "code_output": null
      },
      {
        "subtopicId": "S10-T2-A",
        "heading": "Layout src, pyproject.toml y builds",
        "paragraphs": [
          "Layout **src/**: `src/familiarity_core/...` evita importar el paquete desde el repo sin instalar. `pyproject.toml` declara name, version, requires-python y el build backend (setuptools/hatchling). En packaging y CLI, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta módulo instalable + CLI auditable sin inventar hechos sobre personas reales.",
          "`pip install -e .` instala en editable: cambias código y el import refleja al toque. Ideal en desarrollo del CLI. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: importlib, argparse, pyproject conceptual (S01–S10); no sklearn real, no packaging cloud.",
          "Si ves `ModuleNotFoundError` post-install, revisa nombre del paquete, packages discovery y el cwd. Caso sintético Perú: CLI local que emite scores sintéticos y exit codes. Documenta decisión, métrica y límite conocido en el memo del subtema «Layout src, pyproject.toml y builds»; nunca PII real ni inferencia automática de parentesco/fraude."
        ],
        "code": "# fragmento conceptual de pyproject (como dict)\npyproject = {\n    \"project\": {\n        \"name\": \"familiarity-core\",\n        \"version\": \"0.1.0\",\n        \"requires-python\": \">=3.11\",\n        \"dependencies\": [],\n    },\n    \"build-system\": {\n        \"requires\": [\"setuptools>=61\"],\n        \"build-backend\": \"setuptools.build_meta\",\n    },\n}\nprint(pyproject[\"project\"][\"name\"], pyproject[\"project\"][\"version\"])\nprint(\"layout\", \"src/familiarity_core/__init__.py\")",
        "code_output": "familiarity-core 0.1.0\nlayout src/familiarity_core/__init__.py"
      },
      {
        "subtopicId": "S10-T2-B",
        "heading": "Versionado y compatibilidad",
        "paragraphs": [
          "**SemVer** simple: MAJOR.MINOR.PATCH. Breaking → major; feature compatible → minor; fix → patch. En 0.x es más flexible, pero documenta igual. En packaging y CLI, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta módulo instalable + CLI auditable sin inventar hechos sobre personas reales.",
          "`requires-python` y dependencies pinadas con criterio (mínimos, no caos de upper bounds sin razón). Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: importlib, argparse, pyproject conceptual (S01–S10); no sklearn real, no packaging cloud.",
          "Un **CHANGELOG** real, aunque breve (Added/Changed/Fixed con cambios concretos), evita amnesia entre sprints. Breaking de firma pública se anuncia. Caso sintético Perú: CLI local que emite scores sintéticos y exit codes. Documenta decisión, métrica y límite conocido en el memo del subtema «Versionado y compatibilidad»; nunca PII real ni inferencia automática de parentesco/fraude."
        ],
        "code": "def bump(version: str, level: str) -> str:\n    maj, minor, patch = map(int, version.split(\".\"))\n    if level == \"major\":\n        return f\"{maj+1}.0.0\"\n    if level == \"minor\":\n        return f\"{maj}.{minor+1}.0\"\n    if level == \"patch\":\n        return f\"{maj}.{minor}.{patch+1}\"\n    raise ValueError(level)\n\nprint(\"0.1.0 + feature subcomando\", bump(\"0.1.0\", \"minor\"))\nprint(\"0.2.0 + fix help text\", bump(\"0.2.0\", \"patch\"))\nprint(\"1.0.0 + rename API\", bump(\"1.0.0\", \"major\"))",
        "code_output": "0.1.0 + feature subcomando 0.2.0\n0.2.0 + fix help text 0.2.1\n1.0.0 + rename API 2.0.0"
      },
      {
        "subtopicId": "S10-T3-A",
        "heading": "argparse, subcomandos y exit codes",
        "paragraphs": [
          "`argparse.ArgumentParser` + **subparsers** modelan `ingest|normalize|compare|report`. Cada subcomando tiene flags propios. En packaging y CLI, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta módulo instalable + CLI auditable sin inventar hechos sobre personas reales.",
          "Exit codes: **0** éxito, **2** uso/CLI inválido (argparse default), **1** error de runtime/negocio. Scripts y CI dependen de esto. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: importlib, argparse, pyproject conceptual (S01–S10); no sklearn real, no packaging cloud.",
          "Separa el parse de args de la lógica: `main(argv) -> int` retorna el código; el entrypoint hace `sys.exit(main())`. Caso sintético Perú: CLI local que emite scores sintéticos y exit codes. Documenta decisión, métrica y límite conocido en el memo del subtema «argparse, subcomandos y exit codes»; nunca PII real ni inferencia automática de parentesco/fraude."
        ],
        "code": "import argparse\n\ndef build_parser() -> argparse.ArgumentParser:\n    p = argparse.ArgumentParser(prog=\"familiarity\")\n    sub = p.add_subparsers(dest=\"cmd\", required=True)\n    sub.add_parser(\"ingest\", help=\"ingerir archivos\")\n    n = sub.add_parser(\"normalize\", help=\"normalizar registros\")\n    n.add_argument(\"--field\", default=\"name\")\n    sub.add_parser(\"compare\")\n    r = sub.add_parser(\"report\")\n    r.add_argument(\"--format\", choices=[\"text\", \"json\"], default=\"text\")\n    return p\n\nfor argv in [[\"normalize\", \"--field\", \"email\"], [\"report\", \"--format\", \"json\"]]:\n    ns = build_parser().parse_args(argv)\n    print(ns)",
        "code_output": "Namespace(cmd='normalize', field='email')\nNamespace(cmd='report', format='json')"
      },
      {
        "subtopicId": "S10-T3-B",
        "heading": "stdin/stdout/stderr y ayuda",
        "paragraphs": [
          "**stdout** = datos (JSON, CSV). **stderr** = logs y progreso. Así `cmd > out.json` no contamina el archivo. En packaging y CLI, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta módulo instalable + CLI auditable sin inventar hechos sobre personas reales.",
          "Soportar path o **`-`** para stdin habilita pipes: `cat data.json | familiarity normalize`. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: importlib, argparse, pyproject conceptual (S01–S10); no sklearn real, no packaging cloud.",
          "No mezcles `print` de debug en stdout. Progress bars y logs van a stderr. Caso sintético Perú: CLI local que emite scores sintéticos y exit codes. Documenta decisión, métrica y límite conocido en el memo del subtema «stdin/stdout/stderr y ayuda»; nunca PII real ni inferencia automática de parentesco/fraude."
        ],
        "code": "import sys\nfrom io import StringIO\n\ndef normalize_stream(inp: str) -> str:\n    return inp.strip().casefold()\n\n# simula: datos a stdout, log a stderr\ndata_in = \"  Ana Perez  \"\nlog = StringIO()\nlog.write(\"stage=normalize event=start\\n\")\nout = normalize_stream(data_in)\nlog.write(\"stage=normalize event=done\\n\")\nprint(out)  # stdout data\nprint(log.getvalue().strip(), file=sys.stderr)",
        "code_output": "ana perez"
      },
      {
        "subtopicId": "S10-T4-A",
        "heading": "Archivo/env/flags y precedencia",
        "paragraphs": [
          "Precedencia canónica: **flags CLI > variables de entorno > archivo de config > defaults**. En packaging y CLI, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta módulo instalable + CLI auditable sin inventar hechos sobre personas reales.",
          "Documenta la tabla en README. Un flag `--log-level` debe ganar a `FAMILIARITY_LOG_LEVEL`. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: importlib, argparse, pyproject conceptual (S01–S10); no sklearn real, no packaging cloud.",
          "Implementa un `merge_config` puro y testeable: dicts por capa, reduce de menor a mayor prioridad. Caso sintético Perú: CLI local que emite scores sintéticos y exit codes. Documenta decisión, métrica y límite conocido en el memo del subtema «Archivo/env/flags y precedencia»; nunca PII real ni inferencia automática de parentesco/fraude."
        ],
        "code": "def merge_config(defaults, file_cfg, env_cfg, flags):\n    out = {}\n    out.update(defaults)\n    out.update({k: v for k, v in file_cfg.items() if v is not None})\n    out.update({k: v for k, v in env_cfg.items() if v is not None})\n    out.update({k: v for k, v in flags.items() if v is not None})\n    return out\n\ncfg = merge_config(\n    {\"log_level\": \"INFO\", \"jobs\": 1},\n    {\"log_level\": \"WARNING\"},\n    {\"log_level\": \"DEBUG\"},\n    {\"log_level\": \"ERROR\"},\n)\nprint(cfg)",
        "code_output": "{'log_level': 'ERROR', 'jobs': 1}"
      },
      {
        "subtopicId": "S10-T4-B",
        "heading": "Secretos, defaults y validación temprana",
        "paragraphs": [
          "Secretos **fuera del repo**: `.env` en `.gitignore`, nunca en logs. El ETL local de este nivel **no inventa un API token**: solo un adaptador remoto futuro podría exigirlo. Defaults seguros (log level INFO, no debug PII). En packaging y CLI, el *porqué* es operativo: reduce ambigüedad en pipelines locales, deja rastro auditable y alimenta módulo instalable + CLI auditable sin inventar hechos sobre personas reales.",
          "`validate_config()` al arranque reporta qué clave falta y qué subcomando la exige: `input_path` para ingest y `manifest_path` para report. Contrato: entrada explícita → transformación documentada → salida medible; si falta evidencia o el schema no cuadra, falla cerrado (fail-closed) en lugar de rellenar en silencio. Stack permitido: importlib, argparse, pyproject conceptual (S01–S10); no sklearn real, no packaging cloud.",
          "Fail-fast de config evita procesar 10k filas con un path mal tipeado. Caso sintético Perú: CLI local que emite scores sintéticos y exit codes. Documenta decisión, métrica y límite conocido en el memo del subtema «Secretos, defaults y validación temprana»; nunca PII real ni inferencia automática de parentesco/fraude."
        ],
        "code": "def validate_config(cfg: dict, command: str) -> None:\n    required_always = [\"log_level\"]\n    for k in required_always:\n        if not cfg.get(k):\n            raise RuntimeError(f\"config: falta {k}\")\n    required_by_command = {\"ingest\": \"input_path\", \"report\": \"manifest_path\"}\n    required = required_by_command.get(command)\n    if required and not cfg.get(required):\n        raise RuntimeError(f\"config: falta {required} para {command}\")\n\nvalidate_config({\"log_level\": \"INFO\"}, \"normalize\")\nprint(\"normalize ok\")\ntry:\n    validate_config({\"log_level\": \"INFO\"}, \"ingest\")\nexcept RuntimeError as e:\n    print(e)",
        "code_output": "normalize ok\nconfig: falta input_path para ingest"
      }
    ],
    "iDo": {
      "steps": [
        {
          "demoId": "S10-T1-A-DEMO",
          "subtopicId": "S10-T1-A",
          "environment": "local-python",
          "description": "Separar normalize y cli en funciones; demo con guard __main__.",
          "why": "La lógica vive en normalize; el entrypoint solo orquesta.",
          "code": "__all__ = [\"normalize\"]\n\ndef normalize(text: str) -> str:\n    return \" \".join(text.split()).casefold()\n\ndef main(argv=None) -> int:\n    import sys\n    args = argv if argv is not None else [\"  Ana  \"]\n    print(normalize(args[0]))\n    return 0\n\n# al importar no corre main; lo invocamos explícito\nassert normalize(\"X\") == \"x\"\nraise SystemExit(main([\"  José Pérez \"]))",
          "language": "python",
          "output": "josé pérez"
        },
        {
          "demoId": "S10-T1-B-DEMO",
          "subtopicId": "S10-T1-B",
          "environment": "local-python",
          "description": "Fachada que exporta solo 4 símbolos públicos.",
          "why": "Cuatro símbolos estables; helpers con _ fuera de la API.",
          "code": "def _private_token(s: str) -> list[str]:\n    return s.split()\n\ndef normalize(s: str) -> str:\n    return \" \".join(_private_token(s)).casefold()\n\ndef compare(a: str, b: str) -> float:\n    return 1.0 if normalize(a) == normalize(b) else 0.0\n\ndef ingest_row(row: dict) -> dict:\n    return {**row, \"name\": normalize(row.get(\"name\", \"\"))}\n\ndef report(rows: list) -> int:\n    return len(rows)\n\n__all__ = [\"normalize\", \"compare\", \"ingest_row\", \"report\"]\nprint(\"exports\", __all__)\nprint(compare(\"Ana\", \" ana \"))\nprint(report([ingest_row({\"name\": \" Luis \"})]))",
          "language": "python",
          "output": "exports ['normalize', 'compare', 'ingest_row', 'report']\n1.0\n1"
        },
        {
          "demoId": "S10-T2-A-DEMO",
          "subtopicId": "S10-T2-A",
          "environment": "local-python",
          "description": "Modelo mínimo de layout src + metadatos instalables.",
          "why": "El layout src + pyproject es el contrato de packaging del curso.",
          "code": "from pathlib import PurePosixPath\n\nlayout = [\n    \"src/familiarity_core/__init__.py\",\n    \"src/familiarity_core/normalize.py\",\n    \"src/familiarity_core/cli.py\",\n    \"pyproject.toml\",\n    \"README.md\",\n]\nfor p in layout:\n    print(PurePosixPath(p))\nmeta = {\"name\": \"familiarity-core\", \"version\": \"0.1.0\"}\nprint(\"editable_install\", f\"pip install -e .  # {meta}\")",
          "language": "python",
          "output": "src/familiarity_core/__init__.py\nsrc/familiarity_core/normalize.py\nsrc/familiarity_core/cli.py\npyproject.toml\nREADME.md\neditable_install pip install -e .  # {'name': 'familiarity-core', 'version': '0.1.0'}"
        },
        {
          "demoId": "S10-T2-B-DEMO",
          "subtopicId": "S10-T2-B",
          "environment": "local-python",
          "description": "Bump 0.1.0 → 0.2.0 por subcomando nuevo (minor).",
          "why": "Feature compatible sube minor; documenta en CHANGELOG.",
          "code": "def classify_change(description: str) -> str:\n    d = description.lower()\n    if \"breaking\" in d or \"rename api\" in d:\n        return \"major\"\n    if \"add\" in d or \"subcomando\" in d or \"feature\" in d:\n        return \"minor\"\n    return \"patch\"\n\ndef bump(v: str, kind: str) -> str:\n    a, b, c = map(int, v.split(\".\"))\n    return {\n        \"major\": f\"{a+1}.0.0\",\n        \"minor\": f\"{a}.{b+1}.0\",\n        \"patch\": f\"{a}.{b}.{c+1}\",\n    }[kind]\n\nch = \"add subcomando report\"\nkind = classify_change(ch)\nprint(ch, \"->\", kind, bump(\"0.1.0\", kind))",
          "language": "python",
          "output": "add subcomando report -> minor 0.2.0"
        },
        {
          "demoId": "S10-T3-A-DEMO",
          "subtopicId": "S10-T3-A",
          "environment": "local-python",
          "description": "CLI con subcomandos ingest|normalize|compare|report y exit codes.",
          "why": "Subparsers + return codes hacen la CLI operable en scripts.",
          "code": "import argparse\n\ndef main(argv: list[str]) -> int:\n    p = argparse.ArgumentParser(prog=\"familiarity\")\n    sub = p.add_subparsers(dest=\"cmd\", required=True)\n    sub.add_parser(\"ingest\")\n    sub.add_parser(\"normalize\")\n    sub.add_parser(\"compare\")\n    r = sub.add_parser(\"report\")\n    r.add_argument(\"--format\", default=\"text\")\n    try:\n        ns = p.parse_args(argv)\n    except SystemExit as e:\n        # argparse ya usa 2 en usage errors cuando no se intercepta;\n        # aquí re-lanzamos código\n        return int(e.code) if e.code is not None else 2\n    if ns.cmd == \"report\":\n        print(f\"report format={ns.format}\")\n        return 0\n    print(f\"run {ns.cmd}\")\n    return 0\n\nprint(\"code\", main([\"report\", \"--format\", \"json\"]))\nprint(\"code\", main([\"normalize\"]))",
          "language": "python",
          "output": "report format=json\ncode 0\nrun normalize\ncode 0"
        },
        {
          "demoId": "S10-T3-B-DEMO",
          "subtopicId": "S10-T3-B",
          "environment": "local-python",
          "description": "Datos a stdout y diagnóstico a stderr (simula pipe normalize).",
          "why": "El pipe de datos queda limpio; logs viven en stderr.",
          "code": "import sys\nfrom io import StringIO\n\ndef normalize_cmd(raw: str, err: StringIO) -> str:\n    err.write(\"stage=normalize event=start\\n\")\n    out = raw.strip().casefold()\n    err.write(\"stage=normalize event=done\\n\")\n    return out\n\nstderr = StringIO()\ndata = normalize_cmd('  {\"name\": \"Ana\"}  ', stderr)\nprint(data)  # stdout\nprint(\"--- stderr ---\")\nprint(stderr.getvalue().strip())",
          "language": "python",
          "output": "{\"name\": \"ana\"}\n--- stderr ---\nstage=normalize event=start\nstage=normalize event=done"
        },
        {
          "demoId": "S10-T4-A-DEMO",
          "subtopicId": "S10-T4-A",
          "environment": "local-python",
          "description": "FAMILIARITY_LOG_LEVEL vs --log-level: gana el flag.",
          "why": "Precedencia flags > env > default documentada y testeable.",
          "code": "def resolve_log_level(default=\"INFO\", env=None, flag=None) -> str:\n    level = default\n    if env:\n        level = env\n    if flag:\n        level = flag\n    return level\n\nprint(\"solo env\", resolve_log_level(env=\"DEBUG\"))\nprint(\"flag gana\", resolve_log_level(env=\"DEBUG\", flag=\"ERROR\"))\nprint(\"default\", resolve_log_level())",
          "language": "python",
          "output": "solo env DEBUG\nflag gana ERROR\ndefault INFO"
        },
        {
          "demoId": "S10-T4-B-DEMO",
          "subtopicId": "S10-T4-B",
          "environment": "local-python",
          "description": "Abort con mensaje exacto si falta el path requerido por el subcomando local.",
          "why": "Validación temprana y contextual, sin exigir secretos irrelevantes al ETL local.",
          "code": "REQUIRED = {\"ingest\": \"input_path\", \"report\": \"manifest_path\"}\n\ndef validate(command: str, cfg: dict) -> None:\n    key = REQUIRED.get(command)\n    if key and not cfg.get(key):\n        raise SystemExit(f\"config: falta {key} para {command}\")\n\nvalidate(\"normalize\", {})\nprint(\"normalize ok\")\ntry:\n    validate(\"ingest\", {})\nexcept SystemExit as e:\n    print(\"abort\", e)\nvalidate(\"ingest\", {\"input_path\": \"data/clients.csv\"})\nprint(\"ingest ok\")",
          "language": "python",
          "output": "normalize ok\nabort config: falta input_path para ingest\ningest ok"
        }
      ]
    },
    "weDo": {
      "exercises": [
        {
          "id": "S10-T1-A-E1",
          "instruction": "E1 (guiado) — Concepto: S10-T1-A (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Crea un módulo lógico con función pública `clean` y `__all__ = ['clean']`. Salida/pass: `['clean'] | x`. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "Helper privado con _ no va en __all__.",
            "Imprime __all__ y clean('  X ')."
          ],
          "kind": "guided",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "def _ws(s: str) -> str:\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T1-A-E2",
          "instruction": "E2 (independiente) — Concepto: S10-T1-A (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Simula un import circular y arréglalo extrayendo un util compartido. Salida/pass: `hola:a | hola:b | ok`. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "a importa b y b importa a → rompe; mueve la función común a util.",
            "Imprime ok desde ambos lados."
          ],
          "kind": "independent",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "def util_norm(s: str) -> str:\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T1-A-E3",
          "instruction": "E3 (transferencia) — Concepto: S10-T1-A (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Elige import absoluto vs relativo en layout src: imprime la recomendación para 3 casos. Salida/pass: salida exacta del solution output del starter. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "Dentro del paquete: relativo OK; plugins externos: absoluto; scripts: -m.",
            "Formato caso -> recomendación."
          ],
          "kind": "transfer",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "recs = {\n    \"normalize.py importa compare en el mismo paquete\": \"relativo o absoluto del paquete (from . import compare)\",\n    \"plugin externo usa familiarity_core\": \"absoluto (import familiarity_core)\",\n    \"ejecutar el CLI del paquete\": \"python -m familiarity_core\",\n}\nfor c, r in recs.items():\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T1-B-E1",
          "instruction": "E1 (guiado) — Concepto: S10-T1-B (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Marca helpers privados con _ y deja públicas solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "Imprime lista public vs private detectada por nombre.",
            "Usa dir() filtrado o una lista explícita."
          ],
          "kind": "guided",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "def _tokenize(s):\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T1-B-E2",
          "instruction": "E2 (independiente) — Concepto: S10-T1-B (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Implementa una fachada que reexporta normalize y compare e imprime __all__. Salida/pass: `['normalize', 'compare'] | True`. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "Las implementaciones pueden ser locales.",
            "from-style reexport simulado."
          ],
          "kind": "independent",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "def normalize(s: str) -> str:\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T1-B-E3",
          "instruction": "E3 (transferencia) — Concepto: S10-T1-B (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Documenta un breaking change de firma pública: old → new y versión major. Salida/pass: primeros tokens de `BREAKING: compare(a,b)->bool  =>  compare(a,b)->fl…` según solution. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no.",
          "hints": [
            "Imprime BREAKING y NEW_VERSION.",
            "Ejemplo: compare(a,b)->bool se vuelve compare(a,b)->float score."
          ],
          "kind": "transfer",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "pass  # fixture vacío — usa solution contract\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T2-A-E1",
          "instruction": "E1 (guiado) — Concepto: S10-T2-A (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Completa un dict estilo pyproject con name y version e imprímelos. Salida/pass: primeros tokens de `{'name': 'familiarity-core', 'version': '0.1.0', '…` según solution. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud;.",
          "hints": [
            "name=familiarity-core version=0.1.0",
            "Incluye requires-python >=3.11."
          ],
          "kind": "guided",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "project = {\n    \"name\": \"familiarity-core\",\n    \"version\": \"0.1.0\",\n    \"requires-python\": \">=3.11\",\n}\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T2-A-E2",
          "instruction": "E2 (independiente) — Concepto: S10-T2-A (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Lista el layout src mínimo de un paquete importable e imprime cada path. Salida/pass: primeros tokens de `src/familiarity_core/__init__.py | src/familiarity…` según solution. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no.",
          "hints": [
            "Incluye __init__.py, normalize.py, cli.py, pyproject.toml.",
            "Usa prefijo src/familiarity_core/."
          ],
          "kind": "independent",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "paths = [\n    \"src/familiarity_core/__init__.py\",\n    \"src/familiarity_core/normalize.py\",\n    \"src/familiarity_core/cli.py\",\n    \"pyproject.toml\",\n]\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T2-A-E3",
          "instruction": "E3 (transferencia) — Concepto: S10-T2-A (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Diagnostica ModuleNotFoundError: imprime 3 causas probables post-install. Salida/pass: primeros tokens de `cause: paquete no instalado (falta pip install -e …` según solution. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no.",
          "hints": [
            "Piensa en nombre, editable, y cwd.",
            "Formato cause: ..."
          ],
          "kind": "transfer",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "causes = [\n    \"cause: paquete no instalado (falta pip install -e .)\",\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T2-B-E1",
          "instruction": "E1 (guiado) — Concepto: S10-T2-B (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Clasifica 4 cambios en major/minor/patch. Salida/pass: salida exacta del solution output del starter. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "Breaking → major; feature → minor; fix → patch.",
            "Imprime cambio: nivel."
          ],
          "kind": "guided",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "niveles = {\n    \"renombrar normalize a clean_name (API pública)\": \"major\",\n    \"añadir flag --format a report\": \"minor\",\n    \"corregir typo en help\": \"patch\",\n    \"eliminar subcomando compare\": \"major\",\n}\nfor c, n in niveles.items():\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T2-B-E2",
          "instruction": "E2 (independiente) — Concepto: S10-T2-B (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Fija dependencies mínimas del paquete (puede ser lista vacía + note) e imprime requires-python. Salida/pass: primeros tokens de `{'requires-python': '>=3.11', 'dependencies': [], …` según solution. Conserva el contrato del starter (no borres asserts ni datos); no.",
          "hints": [
            "Para N1 stdlib: dependencies=[].",
            "requires-python>=3.11."
          ],
          "kind": "independent",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "deps = {\n    \"requires-python\": \">=3.11\",\n    \"dependencies\": [],\n    \"optional-dependencies\": {\"dev\": [\"pytest\"]},\n}\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T2-B-E3",
          "instruction": "E3 (transferencia) — Concepto: S10-T2-B (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Escribe una política de compatibilidad hacia S11 dominio (3 bullets). Salida/pass: primeros tokens de `POLICY: renombrar ClientRecord es MAJOR; documenta…` según solution. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging.",
          "hints": [
            "Cubrir renames de entidades y versiones.",
            "Imprime POLICY líneas."
          ],
          "kind": "transfer",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "# fixture\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T3-A-E1",
          "instruction": "E1 (guiado) — Concepto: S10-T3-A (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Añade subcomando report con --format text|json y parsea un argv de ejemplo. Salida/pass: `Namespace(cmd='report', format='json')`. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "Usa argparse subparsers.",
            "Imprime el namespace."
          ],
          "kind": "guided",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "import argparse\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T3-A-E2",
          "instruction": "E2 (independiente) — Concepto: S10-T3-A (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Mapea situaciones a exit codes 0/1/2 e imprime `caso: code`. Salida/pass: salida exacta del solution output del starter. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "0 éxito, 1 runtime, 2 usage.",
            "Cinco casos."
          ],
          "kind": "independent",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "codes = {\n    \"normalize ok\": 0,\n    \"archivo de input no existe\": 1,\n    \"flag desconocido\": 2,\n    \"subcomando ausente\": 2,\n    \"validación de config falla al arrancar\": 1,\n}\nfor c, code in codes.items():\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T3-A-E3",
          "instruction": "E3 (transferencia) — Concepto: S10-T3-A (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Escribe 3 líneas de ayuda --help orientadas a operador no dev. Salida/pass: salida exacta del solution output del starter. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "Evita jerga de frameworks.",
            "Imprime HELP: ..."
          ],
          "kind": "transfer",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "# fixture\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T3-B-E1",
          "instruction": "E1 (guiado) — Concepto: S10-T3-B (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Escribe resultado a stdout y un log a stderr (capturado con StringIO en la demo). Salida/pass: `6 | stderr: event=done`. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "Función process(n) retorna n*2; log event=done.",
            "Imprime ambos streams."
          ],
          "kind": "guided",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "from io import StringIO\nimport sys\n\ndef process(n: int, err: StringIO) -> int:\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T3-B-E2",
          "instruction": "E2 (independiente) — Concepto: S10-T3-B (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Implementa `read_input(path_or_dash, stdin_text)` que lee path o '-' como stdin. Salida/pass: salida exacta del solution output del starter. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "Si path=='-', usa stdin_text.",
            "Prueba ambos modos."
          ],
          "kind": "independent",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "def read_input(path_or_dash, stdin_text=\"\", file_text=None):\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T3-B-E3",
          "instruction": "E3 (transferencia) — Concepto: S10-T3-B (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Revisa un CLI que contamina stdout con prints de progreso y propón la salida limpia. Salida/pass: salida exacta del solution output del starter. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "Imprime BAD y GOOD.",
            "GOOD solo JSON final."
          ],
          "kind": "transfer",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "from io import StringIO\n\ndef bad_cli():\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T4-A-E1",
          "instruction": "E1 (guiado) — Concepto: S10-T4-A (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Imprime la tabla de precedencia de 4 capas (1=más baja … 4=más alta). Salida/pass: `1:defaults | 2:file | 3:env | 4:flags`. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "defaults < file < env < flags.",
            "Formato rank:layer"
          ],
          "kind": "guided",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "layers = [\"defaults\", \"file\", \"env\", \"flags\"]\nfor i, name in enumerate(layers, 1):\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T4-A-E2",
          "instruction": "E2 (independiente) — Concepto: S10-T4-A (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Implementa `merge(defaults, file_cfg, env_cfg, flags)` ignorando None en capas altas. Salida/pass: salida exacta del solution output del starter. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "Prueba con log_level en todas las capas.",
            "El flag debe ganar."
          ],
          "kind": "independent",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "def merge(defaults, file_cfg, env_cfg, flags):\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T4-A-E3",
          "instruction": "E3 (transferencia) — Concepto: S10-T4-A (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Caso conflictivo: env=DEBUG y flag=INFO — imprime el resultado esperado y por qué. Salida/pass: primeros tokens de `result=INFO | razon=el flag CLI tiene mayor preced…` según solution. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real,.",
          "hints": [
            "Una línea result= y una razón.",
            "Español claro."
          ],
          "kind": "transfer",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "pass  # fixture vacío — usa solution contract\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T4-B-E1",
          "instruction": "E1 (guiado) — Concepto: S10-T4-B (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Checklist de secretos en .gitignore: imprime 4 entradas típicas. Salida/pass: primeros tokens de `ignore: .env | ignore: .env.* | ignore: *.pem | ig…` según solution. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "Incluye .env, *.pem, credentials.json, .venv opcional no es secreto pero sí local.",
            "Formato ignore: ..."
          ],
          "kind": "guided",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "# fixture\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T4-B-E2",
          "instruction": "E2 (independiente) — Concepto: S10-T4-B (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Implementa `validate_config(cfg)` que exige log_level y data_dir; errores claros. Salida/pass: salida exacta del solution output del starter. Conserva el contrato del starter (no borres asserts ni datos); no sklearn real, no packaging cloud; solo importlib, argparse, pyproject conceptual (S01–S10).",
          "hints": [
            "Raise RuntimeError con nombre de clave.",
            "Prueba ok y fail."
          ],
          "kind": "independent",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "def validate_config(cfg: dict) -> None:\n    pass  # TODO body\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        },
        {
          "id": "S10-T4-B-E3",
          "instruction": "E3 (transferencia) — Concepto: S10-T4-B (Módulos, packaging y CLI). Entrada: fixture sintético del starter (`CASO`/ids C00x) en packaging y CLI. Tarea: Dado un default inseguro (log_level=DEBUG y echo_sql=True), propone defaults seguros. Salida/pass: primeros tokens de `log_level: 'DEBUG' -> 'INFO' | echo_sql: True -> F…` según solution. Conserva el contrato del starter (no borres asserts ni datos); no sklearn.",
          "hints": [
            "Imprime old → new por clave.",
            "Sin secretos en defaults."
          ],
          "kind": "transfer",
          "tests": "Contrato ejecutable: corre exactamente los casos visibles del starter; exit 0 y sin traceback; stdout conserva el orden, etiquetas y valores exigidos por la instrucción, sin líneas extra.",
          "starterCode": "inseguro = {\"log_level\": \"DEBUG\", \"echo_sql\": True, \"api_token\": \"hardcoded\"}\nseguro = {\"log_level\": \"INFO\", \"echo_sql\": False, \"api_token\": None}\n# TODO: completa la operación de dominio; imprime la salida exacta del contrato (no borres el fixture)\n"
        }
      ]
    },
    "youDo": {
      "title": "Paquete familiarity_core + CLI profesional",
      "context": "Conviertes el ETL de familiaridad en **paquete instalable** con subcomandos ingest|normalize|compare|report, config por precedencia y validación temprana. Sin secretos en repo. Reemplaza el legado de churn sklearn.",
      "objectives": [
        "Layout src/ + pyproject.toml instalable en editable",
        "Subcomandos ingest, normalize, compare, report",
        "Lógica de dominio separada de I/O CLI",
        "Config por precedencia y validación temprana",
        "Ayuda --help y exit codes documentados"
      ],
      "requirements": [
        "pip install -e . funciona en entorno limpio (documentado)",
        "python -m familiarity_core --help o entry point console_scripts",
        "Sin secretos en repo; datos sintéticos",
        "Errores de uso vs runtime distinguibles por exit code",
        "Lógica importable sin side-effects",
        "README de precedencia de config",
        "ingest ejecuta ETL CSV real: Decimal desde texto, clean/quarantine y manifest por fuente reconciliado",
        "python -m unittest discover -s tests pasa en un checkout limpio"
      ],
      "portfolioNote": "Incluye captura de --help, tabla de exit codes y ejemplo de pipe `... | normalize > out.json 2> log.txt` con datos sintéticos.",
      "starterCode": "\"\"\"bootstrap_familiarity.py — crea un paquete real, instalable y testeable.\nEjecuta: python bootstrap_familiarity.py; cd familiarity_core_project;\npython -m pip install -e .; python -m unittest discover -s tests.\n\"\"\"\nfrom pathlib import Path\n\nFILES = {\n    \"pyproject.toml\": '''\n[build-system]\nrequires = [\"setuptools>=69\"]\nbuild-backend = \"setuptools.build_meta\"\n\n[project]\nname = \"familiarity-core\"\nversion = \"0.1.0\"\nrequires-python = \">=3.11\"\ndependencies = []\n\n[project.scripts]\nfamiliarity = \"familiarity_core.cli:entrypoint\"\n\n[tool.setuptools.packages.find]\nwhere = [\"src\"]\n''',\n    \"src/familiarity_core/__init__.py\": '''\nfrom .core import compare, normalize, parse_monto, run_ingest\n\n__all__ = [\"compare\", \"normalize\", \"parse_monto\", \"run_ingest\"]\n''',\n    \"src/familiarity_core/config.py\": '''\nDEFAULTS = {\"log_level\": \"INFO\"}\n\ndef merge_config(defaults, file_cfg, env_cfg, flags):\n    result = dict(defaults)\n    for layer in (file_cfg, env_cfg, flags):\n        result.update({key: value for key, value in layer.items() if value is not None})\n    return result\n\ndef validate_config(config, command):\n    if config.get(\"log_level\") not in {\"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\"}:\n        raise ValueError(\"config: log_level inválido\")\n    required = {\"ingest\": \"input_path\", \"report\": \"manifest_path\"}.get(command)\n    if required and not config.get(required):\n        raise ValueError(\"config: falta %s para %s\" % (required, command))\n''',\n    \"src/familiarity_core/core.py\": '''\nfrom __future__ import annotations\n\nimport csv\nimport hashlib\nimport json\nfrom decimal import Decimal, InvalidOperation\nfrom pathlib import Path\n\nCENT = Decimal(\"0.01\")\n\ndef normalize(text: str) -> str:\n    return \" \".join(text.split()).casefold()\n\ndef compare(left: str, right: str) -> Decimal:\n    return Decimal(\"1.00\") if normalize(left) == normalize(right) else Decimal(\"0.00\")\n\ndef parse_monto(raw: object) -> Decimal:\n    try:\n        value = Decimal(str(raw).strip().replace(\",\", \".\")).quantize(CENT)\n        if not value.is_finite():\n            raise InvalidOperation\n    except (InvalidOperation, ValueError):\n        raise ValueError(\"monto inválido: %r\" % (raw,)) from None\n    if value < Decimal(\"0\"):\n        raise ValueError(\"monto negativo: %s\" % value)\n    return value\n\ndef _sha256(path: Path) -> str:\n    return hashlib.sha256(path.read_bytes()).hexdigest()\n\ndef run_ingest(input_path: Path, out_dir: Path) -> dict:\n    clean, quarantine = [], []\n    with input_path.open(encoding=\"utf-8\", newline=\"\") as handle:\n        reader = csv.DictReader(handle)\n        required = {\"id\", \"name\", \"monto\"}\n        if not reader.fieldnames or not required.issubset(reader.fieldnames):\n            raise ValueError(\"CSV requiere columnas id,name,monto\")\n        for line_no, row in enumerate(reader, start=2):\n            try:\n                record_id = row[\"id\"].strip()\n                if not record_id:\n                    raise ValueError(\"id vacío\")\n                clean.append({\n                    \"id\": record_id,\n                    \"name\": normalize(row[\"name\"]),\n                    \"monto\": str(parse_monto(row[\"monto\"])),\n                })\n            except (KeyError, ValueError) as exc:\n                quarantine.append({\"line\": line_no, \"reason\": str(exc)})\n    source = {\n        \"name\": input_path.name,\n        \"sha256\": _sha256(input_path),\n        \"n_in\": len(clean) + len(quarantine),\n        \"n_clean\": len(clean),\n        \"n_quarantine\": len(quarantine),\n    }\n    source[\"reconcile_ok\"] = source[\"n_in\"] == source[\"n_clean\"] + source[\"n_quarantine\"]\n    manifest = {\n        \"sources\": [source],\n        \"n_in\": source[\"n_in\"],\n        \"n_clean\": source[\"n_clean\"],\n        \"n_quarantine\": source[\"n_quarantine\"],\n        \"reconcile_ok\": source[\"reconcile_ok\"],\n    }\n    out_dir.mkdir(parents=True, exist_ok=True)\n    (out_dir / \"clean.json\").write_text(json.dumps(clean, ensure_ascii=False, indent=2), encoding=\"utf-8\")\n    (out_dir / \"quarantine.json\").write_text(json.dumps(quarantine, ensure_ascii=False, indent=2), encoding=\"utf-8\")\n    (out_dir / \"manifest.json\").write_text(json.dumps(manifest, ensure_ascii=False, indent=2), encoding=\"utf-8\")\n    if not manifest[\"reconcile_ok\"]:\n        raise RuntimeError(\"reconciliación falló\")\n    return manifest\n''',\n    \"src/familiarity_core/cli.py\": '''\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport sys\nfrom pathlib import Path\n\nfrom .config import DEFAULTS, merge_config, validate_config\nfrom .core import compare, normalize, run_ingest\n\ndef build_parser():\n    parser = argparse.ArgumentParser(prog=\"familiarity\")\n    parser.add_argument(\"--log-level\", choices=[\"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\"])\n    commands = parser.add_subparsers(dest=\"command\", required=True)\n    ingest = commands.add_parser(\"ingest\")\n    ingest.add_argument(\"--input\", required=True)\n    ingest.add_argument(\"--out-dir\", required=True)\n    normal = commands.add_parser(\"normalize\")\n    normal.add_argument(\"text\")\n    comp = commands.add_parser(\"compare\")\n    comp.add_argument(\"left\")\n    comp.add_argument(\"right\")\n    report = commands.add_parser(\"report\")\n    report.add_argument(\"--manifest\", required=True)\n    return parser\n\ndef main(argv=None):\n    args = build_parser().parse_args(argv)\n    flags = {\"log_level\": args.log_level}\n    command_cfg = {\n        \"input_path\": getattr(args, \"input\", None),\n        \"manifest_path\": getattr(args, \"manifest\", None),\n    }\n    config = merge_config(DEFAULTS, {}, {}, {**flags, **command_cfg})\n    try:\n        validate_config(config, args.command)\n        if args.command == \"normalize\":\n            print(normalize(args.text))\n        elif args.command == \"compare\":\n            print(compare(args.left, args.right))\n        elif args.command == \"ingest\":\n            print(json.dumps(run_ingest(Path(args.input), Path(args.out_dir)), sort_keys=True))\n        elif args.command == \"report\":\n            manifest = json.loads(Path(args.manifest).read_text(encoding=\"utf-8\"))\n            print(json.dumps(manifest, sort_keys=True))\n        return 0\n    except (OSError, ValueError, RuntimeError, json.JSONDecodeError) as exc:\n        print(\"error: %s\" % exc, file=sys.stderr)\n        return 1\n\ndef entrypoint():\n    raise SystemExit(main())\n''',\n    \"src/familiarity_core/__main__.py\": '''\nfrom .cli import entrypoint\n\nentrypoint()\n''',\n    \"tests/test_core.py\": '''\nimport tempfile\nimport unittest\nfrom decimal import Decimal\nfrom pathlib import Path\n\nfrom familiarity_core import compare, parse_monto, run_ingest\n\nclass CoreTests(unittest.TestCase):\n    def test_decimal_and_compare(self):\n        self.assertEqual(parse_monto(\"10,5\"), Decimal(\"10.50\"))\n        self.assertEqual(compare(\" Ana \", \"ana\"), Decimal(\"1.00\"))\n\n    def test_etl_reconciles_and_quarantines(self):\n        with tempfile.TemporaryDirectory() as folder:\n            root = Path(folder)\n            source = root / \"clients.csv\"\n            source.write_text(chr(10).join([\n                \"id,name,monto\", \"C1,Ana,10.50\", \"C2,Luis,abc\",\n            ]) + chr(10), encoding=\"utf-8\")\n            manifest = run_ingest(source, root / \"out\")\n            self.assertEqual((manifest[\"n_in\"], manifest[\"n_clean\"], manifest[\"n_quarantine\"]), (2, 1, 1))\n            self.assertTrue(manifest[\"sources\"][0][\"reconcile_ok\"])\n\nif __name__ == \"__main__\":\n    unittest.main()\n''',\n    \"README.md\": '''\n# familiarity-core\n\n1. python -m pip install -e .\n2. python -m unittest discover -s tests\n3. familiarity ingest --input data/clients.csv --out-dir out\n4. familiarity report --manifest out/manifest.json\n\nPrecedencia: flags > entorno > archivo > defaults. Esta base local no necesita secretos.\nExit 0 = éxito, 1 = runtime/config, 2 = uso inválido de argparse.\n''',\n}\n\nroot = Path(\"familiarity_core_project\")\nfor relative, body in FILES.items():\n    target = root / relative\n    target.parent.mkdir(parents=True, exist_ok=True)\n    target.write_text(body.strip() + chr(10), encoding=\"utf-8\")\nprint(\"created\", len(FILES), \"files in\", root)"
    },
    "selfCheck_stems": [
      {
        "question": "¿Para qué sirve `if __name__ == '__main__'`?",
        "options": [
          "Acelerar el interpreter",
          "Ejecutar el CLI/demo solo al correr el módulo, no al importar",
          "Definir __all__",
          "Instalar dependencias"
        ]
      },
      {
        "question": "Precedencia correcta de config…",
        "options": [
          "defaults > flags > env > file",
          "env > flags > file > defaults",
          "file > flags > env > defaults",
          "flags > env > file > defaults"
        ]
      },
      {
        "question": "Exit code 2 en CLI argparse suele significar…",
        "options": [
          "Error de uso/parseo de argumentos",
          "Éxito",
          "Timeout de red",
          "Fraude detectado"
        ]
      },
      {
        "question": "¿Dónde van los logs de progreso?",
        "options": [
          "stdout con el JSON",
          "en el nombre del archivo",
          "stderr",
          "en __all__"
        ]
      },
      {
        "question": "Añadir un subcomando nuevo compatible es tipicamente…",
        "options": [
          "major",
          "minor",
          "borrar el repo",
          "patch obligatorio siempre"
        ]
      },
      {
        "question": "¿Qué no debe ir al git del paquete?",
        "options": [
          "README.md",
          "pyproject.toml",
          "src/.../__init__.py",
          ".env con API_TOKEN"
        ]
      }
    ],
    "taught_text_sha": "dce9e95d4f7ee31f",
    "taught_text_len": 33966
  },
  "forbidden": "Do not use knowledge outside this packet. You have only basic tech literacy and zero Python except what appears in landing + prior_sections + active section theory/demos/instructions/hints/starters. Solutions and correct answers are not in this packet.",
  "packet_sha": "8b461ffa87af3bfa"
}