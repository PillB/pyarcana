# Parallel lanes

Each subagent writes `LANE-S0N.status.json` here. Orchestrator merges into `checkpoint.json` and `section_ledger.json`.

Do not edit checkpoint/section_ledger/issue_registry from a lane agent.
